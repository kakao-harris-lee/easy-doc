# Kotlin 이미지 빌드 컨텍스트에서 뺄 것. 저장소 루트의 .dockerignore 는 이 컨텍스트
# (./backend-kotlin)에 적용되지 않으므로 여기에 따로 둔다.

# 빌드 산출물 — 이미지 안에서 다시 만든다. 넣으면 호스트 산출물이 섞여 재현성이 깨진다.
build/
*/build/
.gradle/
*/.gradle/

# 로컬 실행 흔적
*.log
.DS_Store
