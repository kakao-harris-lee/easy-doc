# application

인증, 작업 공간, 문서, 변환, 결제와 운영 기능의 유스케이스 및 포트를 정의한다.

## 경계

- `core`의 도메인 타입과 정책만 의존한다.
- 저장소, 암호화, 메일, LLM, 트랜잭션은 포트로 선언하고 `infrastructure`가 구현한다.
- HTTP 요청과 Spring MVC 타입은 `api`에 둔다.
- 유스케이스가 트랜잭션 경계를 정하며, 장시간 외부 호출은 트랜잭션 밖에서 실행한다.
