# 프론트 환경 변수 예시. 실제 값은 .env.local에 두고 커밋하지 않는다.
# VITE_ 접두사가 붙은 값은 빌드 결과물에 그대로 들어간다 — 비밀키를 넣지 말 것.

# API 서버 주소. 비워 두면 http://localhost:8000을 쓴다.
# 파일럿 배포(nginx 단일 호스트)에서는 /api 로 빌드한다.
VITE_API_BASE_URL=http://localhost:8000
