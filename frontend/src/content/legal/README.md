# 법정 고지 문안 원본

이 디렉터리의 두 파일이 **공개되는 이용약관·개인정보처리방침 문안의 단일 원본**이다.
`TermsPage`·`PrivacyPolicyPage`(`../../pages/`)가 Vite `?raw` import로 이 파일을 그대로
읽어 렌더한다 — TSX로 옮긴 사본은 없다.

**버전 문자열 규칙과 개정 절차는 `docs/legal/README.md`에 있다.** 문장을 고치면 그 문서의
규칙대로 버전 문자열도 함께 올려야 한다. 이 파일만 보고 함부로 고치지 말 것.
