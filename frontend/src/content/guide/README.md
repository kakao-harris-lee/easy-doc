# 이용 가이드 원본

이 디렉터리의 `user-guide.md`가 **공개 도움말 화면의 단일 원본**이다.
`GuidePage`가 Vite `?raw` import로 이 파일을 그대로 읽어 렌더한다 — TSX로 옮긴 사본은 없다.

작성 규칙은 `docs/user-guide.md`에 있다. 화면에 보이는 본문을 고치면 그 문서의 작성자용
절과 같은 변경 단위에서 맞춘다.
