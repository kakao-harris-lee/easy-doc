/**
 * 홈 화면.
 *
 * 업로드·변환·검수 에디터·변환 기록은 다음 미션(V)에서 이 자리에 들어온다.
 */
export function HomePage() {
  return (
    <section aria-labelledby="home-heading">
      <h2 id="home-heading">홈</h2>
      <p>로그인했습니다. 문서 업로드와 변환 화면은 준비 중입니다.</p>
    </section>
  )
}
