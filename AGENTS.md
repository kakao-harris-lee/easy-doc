# Easy-Read AI 작업 범위

제품 런타임은 Kotlin/Spring Boot 백엔드와 React/TypeScript 프런트엔드뿐이다. Python 구현, Python 비교 하네스, parity 작업을 만들지 않는다.

## 모델과 비용 상한

- 구현·테스트·디버깅·리팩터링은 Sonnet 5 또는 Codex가 타이핑한다.
- Opus 5는 계획·아키텍처 검토와 **오케스트레이션**에 쓴다. Opus가 파일을 직접 고치는 대신, 범위를 나눠 Sonnet 실행 에이전트에 위임하고 결과를 종합한다.
- 서브에이전트 위임은 허용한다. 서로 파일이 겹치지 않는 조각은 병렬로 위임해도 된다. **재귀 위임**(서브에이전트가 다시 서브에이전트를 띄우는 것), 스킬 체인, 새 검증 하네스는 만들지 않는다.
- 계획 세션이 그대로 구현으로 넘어가도 된다 — 단 타이핑은 위임한다. 계획 모델이 파일을 고치는 형태로 이어지지는 않는다.
- 실제 유료 LLM 호출은 별도 승인 없이는 실행하지 않는다.
- 비대화형 자동 실행은 명시적인 달러 예산 상한 없이 시작하지 않는다.

## Codex 작업 오케스트레이션

`docs/codex-orchestration.md`를 기본 작업 흐름으로 따른다. 한 작업을 설계 → 구현 → 검증 → 리뷰 → 수정 순환으로 진행하고, 완료 조건을 만족하거나 명시적인 중단 사유가 생겼을 때 종료한다.

- 총괄자는 범위·완료 기준·허용 경로·검증 명령·반복 한도를 먼저 정하고, 필요하면 독립된 작업만 실행 에이전트에 위임한다. 위임 에이전트는 다시 위임하지 않는다.
- 설계와 최종 종합은 Opus 5 역할, 파일 수정·테스트·디버깅은 Sonnet 5 또는 Codex 역할로 둔다. 실행 역할이 만든 변경을 총괄자가 검토한다.
- 각 구현 뒤 관련 기존 검증을 실행하고, 실패 로그와 구체적 리뷰 지적을 다음 수정 작업에 전달한다. 설계 결함이면 설계 단계로 돌아간다.
- 코드가 바뀌면 이전 테스트·리뷰 통과를 폐기한다. 현재 최종 diff와 같은 변경에 대한 필수 검증 및 리뷰가 통과해야 완료로 판정한다.
- 반복 횟수 또는 사용자가 명시한 예산에 닿거나, 범위·요구사항·외부 승인 문제로 안전하게 진행할 수 없으면 멈추고 남은 판단과 상태를 보고한다. 유료 모델 호출 승인을 추정하지 않는다.

## 기본 범위

- 요청에 이름이 나온 프로젝트 디렉터리와 직접 연결된 테스트만 먼저 읽는다.
- `backend-kotlin/` 작업은 `frontend/`를 읽거나 고치지 않는다. API 계약이 바뀌는 경우에만 `contracts/`와 필요한 프런트 API 파일까지 범위를 확장한다.
- `frontend/` 작업은 `backend-kotlin/` 내부 구현을 읽거나 고치지 않는다. 서버 동작은 `contracts/easy-doc-v1.yaml`을 기준으로 한다.
- 루트 작업은 Compose, CI, 계약, 공통 문서에 한정한다. 제품 로직을 루트에 추가하지 않는다.
- 계획/문서 요청에서 검증 하네스나 제품 기능을 임의로 구현하지 않는다.

## 계약 변경 단위

외부 HTTP 변경은 `contracts/easy-doc-v1.yaml`, Kotlin 계약 테스트/DTO, `frontend/src/api/`를 한 변경 단위로 맞춘다. 계약이 바뀌지 않으면 다른 프로젝트를 수정하지 않는다.

## 검증

- Backend: `cd backend-kotlin && ./gradlew build`
- Frontend: `cd frontend && npm run check && npm run test -- --run && npm run build`
- Integration: `docker compose config`

각 하위 디렉터리의 `AGENTS.md`가 더 좁은 범위를 정의한다.
