# Easy-Read AI UX 디자인 가이드

> 상태: Lean MVP용 디자인 기준
>
> 대상: `frontend/` React 애플리케이션
>
> 참고: [UI 개인화의 7가지 방법](https://brunch.co.kr/@artneighbor/68)
>
> 최종 갱신: 2026-08-26

## 1. 디자인 목표

Easy-Read AI는 어려운 문서를 “한 번에 바꾸는 AI 도구”가 아니라, 공공기관 담당자가 **원문을 안전하게 접수하고, 변환 상태를 이해하고, AI 초안을 검수해 배포 가능한 문서로 만드는 작업 공간**이다.

새 디자인은 다음 인상을 줘야 한다.

- **차분하다:** 공공 업무 도구답게 장식보다 정보 위계가 먼저 보인다.
- **가볍다:** 흰 캔버스, 넉넉한 여백, 낮은 밀도의 카드로 복잡도를 낮춘다.
- **개인적이다:** 로그인한 사람의 신상보다 현재 작업 공간과 최근 작업 맥락에 반응한다.
- **진행이 보인다:** 접수, 변환, 검수, 저장 상태와 다음 행동이 언제나 분명하다.
- **신뢰할 수 있다:** AI 결과를 완성본처럼 꾸미지 않고, 사람의 검수가 필요한 초안임을 반복해서 알린다.

핵심 문장은 다음과 같다.

> **사용자마다 다른 화면이 아니라, 지금 해야 할 일에 맞는 화면을 보여준다.**

## 2. 범위와 제품 진실성

이 문서는 현재 v0 시안의 제품 구조와 컴포넌트를 버리는 재설계가 아니다. 실제 React 앱의 인증, 작업 공간, 업로드, 변환, 검수, 피드백, 이력 흐름을 유지하면서 시각 언어와 정보 위계를 다시 정하는 기준이다.

현재 Lean MVP에서 이미 구현되어 화면에 실제 기능처럼 표시할 수 있는 범위는 다음과 같다.

- 이메일 가입과 로그인
- 작업 공간 선택, 생성, 이름 변경
- 텍스트 붙여넣기 또는 DOCX·PDF·HWPX 업로드
- 변환 대기·진행·완료·실패 상태
- 원문과 쉬운 글 초안 비교 및 직접 수정
- 검수 저장
- 현재 계약의 DOCX·TXT·HWPX 형식 선택 내보내기
- 변환 기록 조회와 문서 삭제
- 변환 결과에 대한 파일럿 피드백

이번 디자인이 추가로 요구하는 목표 범위는 다음과 같다.

- 입력 형식에 따라 출력 형식을 자동 결정하는 동일 형식 내보내기
- DOCX·HWPX의 원본 문서 서식 유지
- PDF 입력의 PDF 내보내기와 레이아웃 유지 여부 안내

다음 항목은 기획 또는 v0 목업에 있더라도 현재 기능처럼 노출하지 않는다.

- 결제, 크레딧 구매, 구독 변경
- 팀원 초대와 권한 관리
- 운영자 대시보드와 감사 화면
- 입력 형식과 다른 형식으로 변환해서 내보내기
- 문단 단위 재변환, 쉬운 말 추천, 검색
- 변환 완료 이메일 알림
- “발행”, “공유 완료”처럼 현재 API에 없는 상태

미구현 기능을 설명해야 할 때는 실행 버튼 대신 `준비 중` 표식과 짧은 설명을 사용한다. 파일럿의 핵심 흐름 안에서는 준비 중 기능을 주 행동 옆에 배치하지 않는다.

동일 형식 내보내기와 DOCX·HWPX 서식 유지는 이 문서가 정하는 **목표 UX**다. 현재 계약은 DOCX·TXT·HWPX 중 형식을 임의 선택하는 구조이고 PDF 내보내기도 없으므로, 계약·백엔드·프런트 구현이 모두 완료되기 전에는 라이브 기능처럼 표시하지 않는다.

## 3. 참고 이미지에서 가져올 UX

참고 글의 예시는 공통적으로 밝은 배경, 한 가지 강한 포인트 색, 작은 카드의 조합, 명확한 선택 상태, 데스크톱과 모바일의 같은 정보 위계를 사용한다. 이를 화면 외형만 복제하지 않고 Easy-Read AI의 작업 흐름에 맞춰 해석한다.

| 참고 패턴 | Easy-Read AI 적용 | Lean MVP 경계 |
|---|---|---|
| UI 커스텀 | 작업 공간과 입력 방식 선택을 기억하고 현재 선택을 명확히 표시 | 새로운 사용자 설정 API를 만들지 않는다 |
| 적응형 디자인 | 화면 너비뿐 아니라 변환 상태와 원문 제공 여부에 따라 레이아웃을 바꾼다 | 모바일에서 기능을 숨기지 않는다 |
| 개인화된 콘텐츠 | 현재 작업 공간 이름, 최근 문서 상태, 이어 할 작업을 우선 표시한다 | 이메일·개인정보를 장식용으로 반복 노출하지 않는다 |
| 추천 시스템 | “다음 할 일”을 규칙 기반으로 한 개만 제안한다 | 별도 추천 모델이나 유료 LLM 호출을 추가하지 않는다 |
| 개인화 검색 | 현 단계에서는 도입하지 않는다 | 전체 검색 API 없이 일부 페이지만 검색하는 UI를 만들지 않는다 |
| 상황 인식 인터페이스 | 변환 중, 실패, 초안, 미검수, 저장됨, 원문 없음에 맞춰 행동과 안내를 바꾼다 | 서버가 모르는 상태를 추측하지 않는다 |
| 맞춤형 메시징 | 문서 제목·작업 공간·현재 상태를 이용해 구체적으로 안내한다 | 과한 축하, 의인화, 불필요한 팝업을 피한다 |

개인화 우선순위는 다음 순서다.

1. 사용자가 방금 선택한 값
2. 현재 작업 공간
3. 문서와 변환의 서버 상태
4. 검수·저장 여부
5. 최근 문서

성별, 나이, 위치, 브라우징 기록, 추론한 관심사로 화면을 바꾸지 않는다.

## 4. 정보 구조

현재 목적지가 두 개뿐이므로 데스크톱에 영구 사이드바를 두지 않는다. 사이드바는 공간을 차지하고 제품이 실제보다 복잡해 보이게 한다. 목적지가 네 개 이상으로 늘어날 때 다시 검토한다.

```text
상단 앱 바
├─ 로고
├─ 새 변환
├─ 변환 기록
├─ 현재 작업 공간
└─ 계정 / 로그아웃

본문
├─ 화면 제목 + 현재 맥락 + 대표 행동
├─ 핵심 작업 영역
└─ 설명·주의·다음 단계 보조 영역
```

인증 후 기본 흐름은 하나의 연속 작업으로 느껴져야 한다.

```text
원문 입력 → 접수됨 → 쉬운 글 변환 중 → 사람 검수 → 저장·원본 형식 내려받기
    │                                          │
    └────────────── 변환 기록에서 다시 열기 ────┘
```

## 5. 공통 화면 뼈대

### 5.1 상단 앱 바

- 높이 64px, 흰색 또는 95% 불투명 흰색, 아래 1px 경계선을 사용한다.
- 데스크톱에서 로고, 두 개의 주요 메뉴, 작업 공간, 계정 행동을 한 줄에 둔다.
- 작업 공간은 개인화의 핵심 맥락이므로 계정 이메일보다 먼저 보인다.
- 계정 이메일은 항상 펼쳐 놓지 않고 계정 메뉴 안으로 이동해 시각적 소음을 줄인다.
- 모바일에서는 로고, 현재 작업 공간 축약값, 메뉴 버튼만 남긴다.
- 활성 메뉴는 색만 바꾸지 않고 옅은 배경과 굵은 글씨를 함께 사용한다.

### 5.2 본문 컨테이너

- 최대 너비는 1200px로 제한한다. 검수 화면만 1360px까지 확장할 수 있다.
- 데스크톱 좌우 여백 32px, 태블릿 24px, 모바일 16px를 기본으로 한다.
- 화면 제목과 첫 카드 사이 24px, 큰 섹션 사이 32px를 유지한다.
- 카드가 화면을 잘게 쪼개지 않도록 한 화면의 큰 카드 수는 보통 2~3개로 제한한다.

### 5.3 페이지 헤더

모든 업무 화면은 같은 순서로 시작한다.

1. 짧은 맥락 라벨: `새 변환`, `복지정책팀`, `검수 필요`
2. 사용자가 할 일을 나타내는 제목
3. 한 줄 설명
4. 필요한 경우 오른쪽의 대표 행동 한 개

`환영합니다`, `AI로 혁신하세요` 같은 일반 문구보다 `복지정책팀에서 새 문서를 변환합니다`처럼 현재 맥락을 설명한다.

## 6. 화면별 설계

### 6.1 로그인과 가입

- 화면 중앙의 작은 폼만 띄우지 않고, 왼쪽에 제품이 하는 일을 한 문장과 3단계 흐름으로 보여준다.
- 오른쪽 인증 카드는 최대 440px로 제한한다.
- 장식용 대시보드 목업보다 실제 제품 흐름을 설명하는 작은 도식이나 문장을 사용한다.
- 모바일에서는 설명 영역을 한 문장으로 줄이고 폼을 먼저 보여준다.
- 로그인 실패는 폼 상단에 유지되는 오류로 표시하고 토스트만 사용하지 않는다.

### 6.2 새 변환

첫 화면은 “설정 패널”이 아니라 “오늘 처리할 문서 한 건을 시작하는 곳”처럼 보여야 한다.

```text
┌─────────────────────────────────────────────────────────────┐
│ 복지정책팀 · 새 변환                         AI 초안 안내     │
│ 어떤 문서를 쉬운 글로 바꿀까요?                              │
├─────────────────────────────────┬───────────────────────────┤
│ 문서 제목                       │ 이 작업에서 일어나는 일    │
│ [ 글 붙여넣기 ] [ 파일 올리기 ] │ 1 개인정보 2종 가림        │
│                                 │ 2 쉬운 글 초안 생성        │
│ 선택한 입력 영역                │ 3 담당자 직접 검수         │
│                                 │                           │
│ [ 쉬운 글 초안 만들기 ]         │ 지원 형식·크기 안내         │
└─────────────────────────────────┴───────────────────────────┘
```

- 제목, 입력 방식, 입력 영역, 대표 버튼 순으로 한 방향으로 읽히게 한다.
- 텍스트/파일 선택은 탭처럼 보이되 실제 라디오 의미와 키보드 조작을 유지한다.
- 파일을 고른 뒤에는 기본 파일 입력만 남기지 말고 파일명, 크기, 형식, 제거 행동을 하나의 파일 카드로 요약한다.
- 글자 수는 입력창 오른쪽 아래에 `1,240 / 4,000자`로 표시한다. 80% 이상에서만 주의색을 사용한다. **→ 2026-09-03: 상한 20,000자로 바뀌어 `1,240 / 20,000자`로 표시한다.**
- 오른쪽 안내 카드는 제출에 필요한 정보만 보여주며 마케팅 문구를 넣지 않는다.
- 최근 문서가 있다면 페이지 아래에 한 건만 `이어서 검수하기`로 제안할 수 있다. 이는 기존 문서 목록 응답에 완료 상태와 검수 여부가 확인될 때만 표시한다.
- 대표 버튼은 `쉬운 글 초안 만들기`로 통일한다. AI 자체보다 결과와 작업을 말한다.

### 6.3 변환 대기와 진행

빈 화면 중앙의 회전 아이콘 하나로 기다리게 하지 않는다. 같은 URL 안에서 작업 단계가 전개되는 모습이 보여야 한다.

- `문서 접수`, `개인정보 확인`, `쉬운 글 변환`, `검수 준비`의 단계 표시를 둔다.
- 서버가 `pending`과 `processing`만 제공하므로 완료되지 않은 내부 단계를 실제 완료로 단정하지 않는다. 현재 단계와 “다음 단계”만 표현한다.
- 결과 영역의 형태를 닮은 스켈레톤을 사용해 다음 화면을 예고한다.
- 문서 제목과 현재 작업 공간을 표시해 사용자가 올바른 작업을 기다리는지 확인할 수 있게 한다.
- 화면을 닫아도 기록에서 다시 열 수 있다는 안내를 제공한다.
- 오래 걸리는 경우 실패처럼 빨갛게 만들지 않고 `예상보다 오래 걸리고 있습니다`와 기록 이동 행동을 제공한다.
- 실패 시 오류 코드가 아니라 원인, 보존된 항목, 다음 행동을 순서대로 보여준다.

### 6.4 검수 작업 공간

검수는 제품의 중심 화면이다. 참고 이미지의 대시보드처럼 여러 카드를 나열하기보다, 원문과 결과 사이의 비교 작업에 화면을 집중한다.

```text
┌─────────────────────────────────────────────────────────────┐
│ AI 초안 · 사람 검토 필요     저장 안 됨                     │
│ 문서 제목                    [저장] [DOCX로 내려받기]        │
├──────────────────────────────┬──────────────────────────────┤
│ 원문                         │ 쉬운 글 초안                 │
│ 읽기 전용                    │ 직접 편집                    │
│                              │                              │
│                              │                              │
├──────────────────────────────┴──────────────────────────────┤
│ 가린 개인정보 대응표 / 파일럿 피드백                         │
└─────────────────────────────────────────────────────────────┘
```

- 경고 배너는 화면 전체를 지배하지 않게 하되 제목보다 먼저 읽히게 한다.
- 데스크톱에서는 1:1 분할, 1024px 미만에서는 `원문 | 쉬운 글` 탭, 인쇄 또는 넓은 화면에서는 두 패널을 함께 보여준다.
- **원문은 어느 경로로 들어와도 보여준다**(2026-08-27 개정). 파일로 올렸든 붙여넣었든, 변환 기록에서 다시 들어왔든 새로고침했든 왼쪽 패널에는 추출된 원문이 있다. 화면은 `GET /documents/{document_id}/source`로 그것을 가져온다. 검수는 원문과 초안을 견주는 일이라, 비교 대상이 없으면 이 화면이 할 일 자체가 성립하지 않는다.
  - 종전에는 붙여넣기 직후 화면 전환 상태로 넘어온 값 하나뿐이어서 **파일 업로드·기록 재진입·새로고침 셋 다 왼쪽이 비었다.** 그 자리를 메우던 “이 경로에서는 원문을 다시 표시하지 않습니다” 설명 카드는 폐지한다.
  - 원문이 없는 경우는 이제 **불러오는 중**이거나 **불러오지 못했을 때**(파기된 문서·네트워크 실패)뿐이며, 그 둘은 서로 다른 상태로 표현한다(§9). 불러오는 동안 “원문 없음”을 그리지 않는다 — 아직 모르는 것을 없다고 말하는 것이기 때문이다.
  - 원문 패널은 읽기 전용이다. 고치는 것은 오른쪽 결과다.
  - 원문에는 마스킹 **전**의 개인정보가 들어 있다. 화면에 그리는 것 외에 브라우저 저장소·로그·분석 이벤트 어디로도 내보내지 않는다.
- 변환이 끝나기 전(`pending`·`processing`)에도 원문은 이미 확정된 값이므로 함께 보여준다. 기다리는 동안 무엇이 변환되는지 볼 수 있어야 한다(§6.3과 같은 취지).
- **변환에 실패한 화면에도 원문을 둔다.** 그 화면이 시키는 다음 행동이 「문서 다시 올리기」이므로, 추출된 원문이 눈앞에 있으면 그대로 복사해 붙여넣기로 재시도할 수 있다. 감추면 사용자는 원본 파일을 다시 찾아 열어야 한다 — 실패 화면은 원문이 가장 쓸모 있는 자리다. 다만 자리는 실패 사유와 「다음에 할 일」보다 **아래**이고, 이 화면의 유일한 alert는 실패 사유 하나로 유지한다(§11 — 원문 패널이 두 번째 목소리로 끼어들지 않는다). 「남아 있는 것」 문단이 원문을 언급하는 것은 **실제로 불러온 경우에만**이다. 확인하지 못한 것을 남아 있다고 말하지 않는다.
- 좁은 화면에서 `원문 | 쉬운 글` 탭이 **새로 생기는 순간** 어느 패널을 펼칠지는 아래 네 단계를 위에서부터 적용해 정한다. 원문을 뒤늦게 받아오거나 창을 좁히는 것이 편집 중인 사용자의 화면을 가로채면 안 된다는 것이 이 규칙의 목적이다.
  1. **초점이 어느 패널 안에 있으면 그 패널.** 지금 손이 가 있는 곳을 숨기는 것이 막으려는 사고이므로, 앞서 고른 탭보다 강한 신호다.
  2. 초점이 없고 사용자가 **탭을 고른 적이 있으면 그 선택을 그대로 둔다.** 창 크기 조절 한 번에 남의 선택을 되돌리지 않는다.
  3. 그것도 아니고 **고쳐 둔 내용이 있으면(`dirty`) 결과 쪽**을 편든다. 손댄 흔적이 있는 패널을 감추지 않는다.
  4. 아무 신호도 없으면 §11의 읽기 순서대로 **원문**이 먼저다.
  - 판정은 **전이 시점에만** 한다. 매 렌더 판정하면 사용자가 방금 누른 탭을 곧바로 덮어쓴다.
- 결과 패널은 포인트색 경계와 `편집 가능` 라벨로 원문 패널과 구분한다.
- 저장하지 않은 수정이 있으면 상단 상태를 `저장 안 됨`으로 바꾸고 저장 버튼을 강조한다.
- 저장 후에는 성공 토스트만 띄우지 않고 상태 라벨을 `저장됨 · 시각`으로 갱신한다.
- 내려받기 버튼은 원본 형식을 이름에 포함한다: `DOCX로 내려받기`, `HWPX로 내려받기`, `PDF로 내려받기`, `TXT로 내려받기`.
- 다른 파일 형식을 고르는 메뉴는 제공하지 않는다. 사용자가 DOCX를 올렸다면 HWPX·TXT 선택지가 보이지 않는다.
- 가린 개인정보 자리표시자 누락은 내려받기를 막는 이유와 해결 방법을 행동 가까이에 보여준다.
- 피드백은 검수 저장 뒤의 보조 작업이다. 저장 버튼보다 강한 색이나 큰 면적을 사용하지 않는다.

### 6.5 원본 형식 내려받기와 서식 유지

내보내기의 기본 원칙은 **들어온 형식 그대로 나간다**이다. 파일 형식 선택은 사용자가 검수 과정에서 해결해야 할 문제가 아니다.

| 입력 방식 | 내려받기 형식 | 서식 유지 UX |
|---|---|---|
| 텍스트 붙여넣기 | TXT | 적용 대상 아님. UTF-8 텍스트로 내려받는다 |
| DOCX 업로드 | DOCX | 원본 DOCX의 문단·표·이미지 등 유지 상태를 표시한다 |
| HWPX 업로드 | HWPX | 원본 HWPX의 문단·표·이미지 등 유지 상태를 표시한다 |
| PDF 업로드 | PDF | 같은 PDF 형식으로 내려받되, 원본 레이아웃 유지 여부는 별도 상태로 정확히 알린다 |

DOCX와 HWPX에는 검수 화면 안에 `원본 서식 유지` 패널을 추가한다.

```text
┌─────────────────────────────────────────────────────────────┐
│ 원본 서식 유지                                      DOCX    │
│ 원본 문서의 문단, 표, 이미지 위치에 검수한 쉬운 글을 반영합니다. │
│                                                             │
│ 상태  ● 유지 가능                                          │
│       표 3개 · 이미지 2개 · 머리말 있음                     │
│                                                             │
│ [저장되지 않은 수정이 있습니다]       [DOCX로 내려받기]      │
└─────────────────────────────────────────────────────────────┘
```

- `원본 서식 유지`는 DOCX·HWPX에서 기본 동작이다. 별도의 다른 형식 선택을 요구하지 않는다.
- 사용자가 검수한 최신 내용이 저장되어야 내려받을 수 있다. 저장하지 않은 수정이 있으면 먼저 `저장하고 DOCX로 내려받기`처럼 한 번의 명확한 행동을 제공한다.
- 서버가 원본 구조를 분석하는 동안 `서식 확인 중`, 분석이 끝나면 `유지 가능`, 일부 구조가 달라질 수 있으면 `일부 유지`, 결과를 만들 수 없으면 `서식 유지 실패`로 표시한다.
- 상태는 낙관적으로 추측하지 않는다. 서버가 제공한 결과만 표시한다.
- `일부 유지`에서는 영향을 받는 요소를 구체적으로 표시한다. 예: `복잡한 표 1개는 단순 표로 바뀔 수 있습니다.`
- 서식을 유지할 수 없을 때 텍스트 전용 파일로 조용히 대체하지 않는다. 같은 형식으로 다시 만들 수 없는 이유와 다시 시도 행동을 보여준다.
- DOCX·HWPX의 유지 대상은 구현 계약으로 확정된 요소만 나열한다. 문단 스타일, 글꼴, 표, 이미지, 머리말·꼬리말, 쪽 나눔 등을 실제 지원 전에 모두 유지한다고 약속하지 않는다.
- 내려받은 파일명은 원본 제목을 기반으로 하되 `-쉬운글`을 덧붙여 원본 파일과 구분한다. 예: `청년월세안내-쉬운글.docx`.
- 내려받기 직전이나 직후에 형식 선택 모달을 띄우지 않는다. 버튼을 누르면 현재 원본 형식으로 바로 내려받는다.

PDF도 동일 형식 원칙에 따라 PDF로 내려받아야 한다. 다만 DOCX·HWPX와 같은 편집 가능한 문서 구조가 아니므로 `원본 서식 유지`를 자동으로 약속하지 않는다. PDF 레이아웃 유지가 구현되지 않은 단계에서는 업로드 전에 그 제한을 알리고, PDF 다운로드 기능이 준비되기 전까지 다른 형식으로의 우회 다운로드를 기본 행동으로 제공하지 않는다.

**재결정 (2026-09-02, 사용자 결정).** PDF 렌더러는 만들지 않는다 — 위 문단이 금지한 "PDF 다운로드 기능이 준비되기 전까지의 조용한 우회 다운로드"는 여전히 유효하지만, PDF 원본은 이제 그 갈래가 아니다. 사용자가 검수 완료 후 DOCX 또는 HWPX 중 하나를 **명시적으로 골라** 신문서 조립으로 내보낸다. 이것은 위 문단이 막은 *조용한* 대체가 아니다 — 담당자가 두 구조화 형식 사이에서 직접 고르는 행동이고, 아무것도 고르지 않으면 아무것도 내려받지 않는다(§6.5 첫 문장의 "파일 형식 선택은 사용자가 검수 과정에서 해결해야 할 문제가 아니다"는 원본이 이미 그 형식으로 존재하는 DOCX·HWPX·TXT·붙여넣기에 계속 적용되고, PDF처럼 같은 형식으로 내보낼 수단이 아예 없는 원본에는 적용되지 않는다). 검수 화면은 DOCX·HWPX처럼 원본 형식 하나로 내려받기 버튼을 그리는 대신, PDF 원본에서만 두 형식 중 하나를 고르는 선택 UI를 보여준다 — 이때는 위 "내려받기 직전이나 직후에 형식 선택 모달을 띄우지 않는다" 규칙의 예외다(그 규칙은 고를 것이 없는 원본에 적용된다). 계약 필드는 `ConversionResponse.export_format_choices`(계약 2.6.0)다.

#### 필요한 계약 정보

직접 URL로 검수 화면을 다시 열거나 새로고침해도 원본 형식을 잃지 않아야 하므로 화면 전환 상태에만 의존하면 안 된다. 목표 계약에는 다음 정보가 필요하다.

- `ConversionResponse.source_format`: `text | docx | pdf | hwpx`
- `ConversionResponse.export_format`: `txt | docx | pdf | hwpx`
- `ConversionResponse.format_preservation.status`: `checking | available | partial | failed | not_applicable`
- `format_preservation.details`: 사용자에게 표시할 수 있는, 개인정보가 없는 영향 항목 목록

내보내기 엔드포인트는 서버가 원본 형식으로부터 결과 형식을 결정하는 것을 기본으로 한다. 현재처럼 클라이언트가 `format=docx|txt|hwpx`를 임의 선택하게 두지 않는다. 기존 쿼리 파라미터를 유지해야 한다면 원본과 다른 값을 거절하고, 가능한 값은 서버 응답의 `export_format`과 일치해야 한다.

이 변경은 외부 HTTP 계약 변경이므로 다음을 한 변경 단위로 맞춘다.

- `contracts/easy-doc-v1.yaml`
- Kotlin 계약 테스트, DTO, 원본 파일 구조 보존·내보내기 구현
- `frontend/src/api/` 타입과 클라이언트
- 검수 화면과 다운로드 테스트

### 6.6 변환 기록

- 페이지 맥락 라벨에 현재 작업 공간 이름을 표시한다.
- 표의 첫 열은 제목, 두 번째는 사용자가 지금 해야 할 일로 둔다. 내부 처리 상태보다 `변환 중`, `검수 필요`, `검수함`, `실패`가 더 빠르게 읽혀야 한다.
- 데스크톱은 표를 유지하되 행 높이를 56~64px로 넉넉하게 잡는다.
- 제목 아래에 원본 형식과 업로드 날짜를 보조 정보로 둔다. 예: `DOCX · 2026. 8. 26.`
- 대표 행 행동은 제목 링크 하나다. 삭제는 행 끝의 더보기 메뉴 또는 낮은 강조 버튼으로 둔다.
- 빈 상태는 현재 작업 공간 이름과 함께 설명하고 `첫 문서 변환하기` 행동을 제공한다.
- 모바일에서는 표를 가로 스크롤시키지 않고 문서 카드 목록으로 바꾼다.
- 전체 검색 API가 생기기 전에는 검색 상자를 추가하지 않는다. 현재 작업 공간 선택이 유일한 목록 범위 제어다.

### 6.7 작업 공간 선택

- 작업 공간은 단순 설정이 아니라 모든 문서 목록과 새 변환의 범위를 결정하는 현재 맥락이다.
- 데스크톱에서는 상단 앱 바의 작은 선택 메뉴, 모바일에서는 메뉴 상단의 전체 너비 선택 행으로 표시한다.
- 생성과 이름 변경은 브라우저 `prompt` 대신 접근 가능한 작은 대화상자로 옮기는 것을 권장한다. 대화상자는 제목, 설명, 이름 입력, 취소, 확인만 포함한다.
- 작업 공간을 바꾸면 페이지 제목 또는 보조 문구도 함께 바뀌어 선택 결과를 즉시 확인할 수 있어야 한다.

## 7. 규칙 기반 ‘다음 할 일’

개인화는 한 화면에 여러 추천을 늘어놓는 기능이 아니다. 서버 상태로 확실히 판단할 수 있는 가장 중요한 행동 한 개를 보여준다.

| 조건 | 제안 문구 | 행동 |
|---|---|---|
| 문서 없음 | 첫 문서를 쉬운 글로 바꿔 보세요 | 새 변환 |
| `pending` 또는 `processing` | 변환 중인 문서를 확인하세요 | 해당 변환 열기 |
| `done`이고 미검수 | 쉬운 글 초안을 검수해 주세요 | 검수 열기 |
| 검수 저장됨 | 원본 형식으로 내려받을 수 있습니다 | 해당 문서 열기 |
| 최근 변환 실패 | 원문은 그대로 두고 다시 시도해 보세요 | 실패 상세 열기 |

두 조건이 동시에 맞으면 `미검수 완료 문서`를 가장 먼저 보여주고, 그다음 `진행 중`, `실패`, `새 변환` 순으로 정한다. 추천 이유를 추론하거나 감정적 표현을 붙이지 않는다.

## 8. 비주얼 시스템

참고 이미지의 보라색 SaaS 감성을 반영하되, 긴 업무 시간과 접근성을 고려해 채도를 낮춘다. 현재 컴포넌트 구조와 의미 색은 유지하고 토큰을 교체하는 방식으로 구현한다.

### 8.1 색상 토큰

| 토큰 | 값 | 용도 |
|---|---:|---|
| `--background` | `#F7F7FB` | 앱 캔버스 |
| `--foreground` | `#202230` | 기본 글자 |
| `--card` | `#FFFFFF` | 작업 카드 |
| `--muted` | `#F0F0F6` | 선택되지 않은 면, 스켈레톤 |
| `--muted-foreground` | `#626577` | 보조 글자 |
| `--border` | `#DEDEE8` | 일반 경계 |
| `--input` | `#86899F` | 입력 경계 |
| `--primary` | `#5B4BC4` | 대표 행동, 현재 단계 |
| `--primary-hover` | `#493BA5` | 대표 행동 hover |
| `--accent` | `#EFEDFF` | 활성 메뉴, 선택된 옵션 |
| `--accent-foreground` | `#342A7C` | accent 위 글자 |
| `--ring` | `#5B4BC4` | 키보드 포커스 |

- `--input`은 이 표의 초안값 `#C9C9D6`이 아니라 `#86899F`다. §11이 WCAG 2.2 AA를 최소 기준으로 두므로 §8.1의 개별 값이 §11에 양보한다. 초안값은 흰 카드 대비 1.64:1이라 SC 1.4.11(비텍스트 명암비)이 컨트롤 경계에 요구하는 3:1에 못 미쳤고, `#86899F`는 카드(`#FFFFFF`) 대비 3.45:1, 캔버스(`#F7F7FB`) 대비 3.23:1이다.
- `--border`와 `--input`에 요구되는 기준은 다르다. `--border`는 표 구분선 같은 순수 장식 경계라 1.4.11 대상이 아니어서 `#DEDEE8`을 유지하고, `--input`은 컨트롤을 식별하는 유일한 시각 단서이므로 3:1을 지킨다.
- 성공, 경고, 위험, 정보 색은 브랜드 색과 합치지 않고 현재의 의미 색 체계를 유지한다.
- 본문 텍스트와 배경은 WCAG AA 명암비를 만족해야 한다.
- 색은 상태의 유일한 단서가 될 수 없다. 아이콘, 문구, 모양을 함께 쓴다.
- 그라데이션은 로그인 설명 면이나 빈 상태 일러스트 배경에만 매우 약하게 사용한다. 입력·표·검수 본문에는 사용하지 않는다.

### 8.2 타이포그래피

- 기본 글꼴은 Pretendard와 시스템 한글 글꼴 fallback을 유지한다.
- 페이지 제목: 28/36, 800
- 섹션 제목: 20/28, 700
- 카드 제목: 17/24, 700
- 본문: 16/26, 400
- 보조 본문: 14/22, 400 또는 500
- 버튼과 라벨: 14~15/20, 600
- 긴 쉬운 글 편집 영역은 최소 17px, 줄 높이 1.75를 사용한다.
- 대문자 영문, 지나치게 좁은 자간, 회색의 작은 본문을 피한다.

### 8.3 여백, 모서리, 그림자

- 4px 배수 간격 체계를 사용한다: 4, 8, 12, 16, 20, 24, 32, 40, 48.
- 입력과 버튼 모서리는 10px, 큰 카드는 16px, 배지는 완전한 pill 형태를 쓴다.
- 카드 그림자는 `0 8px 28px rgba(35, 31, 70, 0.06)` 이하로 제한한다.
- 모든 카드를 띄우지 않는다. 같은 작업군은 경계선과 배경 차이만으로 묶는다.

### 8.4 아이콘과 이미지

- Lucide 아이콘을 계속 사용한다. 기본 18px, 주요 빈 상태 24~32px를 사용한다.
- 아이콘만 있는 버튼에는 항상 접근 가능한 이름과 툴팁이 필요하다.
- 참고 글의 이미지는 방향 참고 자료이며 제품 자산으로 복사하지 않는다.
- 장식용 인물 사진보다 문서, 비교, 검수 과정을 단순화한 자체 도식을 우선한다.

## 9. 상태와 피드백

모든 비동기 작업은 `시작 전 → 진행 중 → 성공 또는 실패 → 다음 행동`을 같은 자리에서 보여준다.

- 버튼 진행 상태는 문구까지 `저장 중…`, `올리는 중…`으로 바꾼다.
- 성공 토스트는 보조 수단이다. 저장 여부처럼 오래 유지되어야 하는 정보는 화면 안에도 남긴다.
- 오류는 사용자가 고칠 입력 가까이에 표시한다.
- 서버 오류는 기술 용어 대신 일어난 일, 보존된 데이터, 다시 할 수 있는 일을 설명한다.
- 빈 상태, 로딩, 권한 없음, 원문 없음은 서로 다른 상태로 설계한다.
- 삭제 확인에는 삭제되는 대상의 제목과 되돌릴 수 없다는 사실을 함께 쓴다.

메시지 예시는 다음처럼 구체적으로 쓴다.

- `‘청년 월세 지원 안내’ 초안을 저장했습니다.`
- `‘복지정책팀’에는 아직 변환한 문서가 없습니다.`
- `원문은 남아 있습니다. 잠시 후 다시 변환해 주세요.`
- `원문을 찾을 수 없습니다. 보관 기간이 지나 파기됐거나 지워진 문서입니다. 다시 불러와도 결과는 같습니다.` — 다시 시도할 이유가 없는 실패다.
- `원문을 불러오지 못했습니다. 네트워크 상태를 확인한 뒤 다시 불러와 주세요.` — 다시 시도할 수 있는 실패다. 두 문구를 하나로 뭉치지 않는 이유는 **사용자가 다음에 할 일이 다르기 때문**이다. 2026-08-27 이전에 이 자리에 있던 `파일로 올린 문서는 이 화면에서 원문을 다시 표시하지 않습니다.`는 **폐지됐다**(§6.4 — 이제 모든 경로에서 원문을 보여준다).

## 10. 반응형 동작

반응형은 크기를 줄이는 작업이 아니라 우선순위를 다시 배열하는 작업이다.

| 구간 | 동작 |
|---|---|
| 1280px 이상 | 넓은 검수 2열, 새 변환 3:2 분할, 상단 전체 메뉴 |
| 768~1279px | 새 변환 1열, 보조 안내를 아래로 이동, 검수는 필요 시 2열 |
| 767px 이하 | 카드 패딩 16px, 전체 너비 대표 버튼, 기록은 카드 목록, 검수는 탭 전환 |

- 터치 대상은 최소 44×44px로 한다.
- 모바일에서 작업 공간, 현재 상태, 저장 행동이 메뉴 안에 감춰지지 않게 한다.
- 하단 고정 저장 바를 쓸 경우 본문 마지막 요소를 가리지 않도록 안전 여백을 확보한다.
- 가로 스크롤은 검수 텍스트나 기록 표의 기본 해결책으로 사용하지 않는다.

## 11. 접근성 기준

- KWCAG와 WCAG 2.2 AA를 최소 기준으로 한다.
- `header`, `nav`, `main`, 제목 순서와 본문 건너뛰기 링크를 유지한다.
- 모든 입력은 보이는 `label`, 도움말, 오류를 프로그램적으로 연결한다.
- 포커스 링은 제거하지 않고 3px로 충분히 보이게 한다.
- 상태 변경은 적절한 `role=status` 또는 `role=alert`로 알리되 같은 문장을 중복 낭독하지 않는다.
- 모달은 초기 초점, 포커스 가두기, Esc 닫기, 닫힌 뒤 초점 복귀를 지원한다.
- 검수 2열의 읽기 순서는 DOM에서도 원문 다음 결과가 되게 한다.
- 애니메이션은 `prefers-reduced-motion`에서 제거한다.
- 스켈레톤은 실제 텍스트로 오해되지 않게 하나의 로딩 상태 이름을 제공한다.

## 12. 모션

모션은 상태 관계를 설명할 때만 사용한다.

- hover·선택 전환: 120~160ms
- 카드나 상태 패널 전환: 180~220ms
- easing: `ease-out`
- 큰 이동, 무한 반복 장식, 숫자 카운트업은 사용하지 않는다.
- 변환 중 스피너와 스켈레톤을 동시에 과하게 움직이지 않는다. 한 화면의 반복 모션은 하나로 제한한다.

## 13. 구현 순서

### 1단계 — 시각 기반

- 색, 간격, 모서리, 그림자 토큰 변경
- 공통 PageHeader, 상태 패널, 빈 상태, 파일 카드 정리
- 상단 앱 바의 작업 공간·계정 정보 위계 조정
- 새 변환과 기록의 모바일 레이아웃 개선

API 계약 변경 없이 완료할 수 있는 범위다.

### 2단계 — 상황 인식

- 변환 단계 표시와 결과 형태 스켈레톤
- 검수 저장 상태와 sticky 행동 영역
- 원문 없음 전용 상태
- 기존 목록 응답을 이용한 규칙 기반 `다음 할 일` 한 건

새 추천 모델이나 새 검증 하네스를 만들지 않는다.

### 3단계 — 상호작용 완성도

- 작업 공간 생성·이름 변경 대화상자
- 기록 모바일 카드 보기
- 키보드, 낭독기, reduced motion 검증

### 4단계 — 원본 형식 내보내기

- 변환 조회 응답에 원본·출력 형식과 서식 유지 상태 추가
- 텍스트→TXT, DOCX→DOCX, HWPX→HWPX, PDF→PDF 규칙을 서버에서 강제
- DOCX·HWPX 원본 구조 보존과 검수본 반영
- 원본 형식별 단일 내려받기 버튼
- DOCX·HWPX `원본 서식 유지` 상태 패널
- PDF 레이아웃 유지 범위 또는 제한 안내

이 단계는 프런트 UI만 먼저 만들어 완료 처리하지 않는다. 계약, Kotlin 구현, 프런트 소비자와 실제 내려받기 검증이 함께 끝나야 한다.

외부 HTTP 동작이 바뀌는 요구가 생기면 `contracts/easy-doc-v1.yaml`, Kotlin 계약 테스트·DTO, `frontend/src/api/`를 같은 변경 단위로 다룬다.

## 14. 완료 기준

- 처음 보는 사용자가 10초 안에 대표 행동을 찾을 수 있다.
- 새 변환, 진행, 검수, 기록에서 현재 작업 공간과 문서 상태를 알 수 있다.
- 각 화면에는 가장 강한 대표 행동이 한 개만 있다.
- 모바일 320px에서 핵심 기능이 사라지거나 가로 스크롤되지 않는다.
- 키보드만으로 로그인부터 검수 저장과 원본 형식 내려받기까지 이동할 수 있다.
- 상태를 색만으로 구분하지 않는다.
- 로딩, 빈 상태, 실패, 원문 없음, 저장 안 됨이 각각 다르게 표현된다.
- 입력 형식과 다른 교차 형식 내보내기나 아직 구현되지 않은 서식 유지 범위가 라이브 기능처럼 보이지 않는다.
- 텍스트·DOCX·HWPX·PDF 입력이 각각 TXT·DOCX·HWPX·PDF로 내려받아지며 다른 형식 선택을 요구하지 않는다.
- DOCX·HWPX는 원본 서식 유지 상태와 영향을 받는 요소를 내려받기 전에 확인할 수 있다.
- 실제 프런트 검증 명령인 `npm run check`, `npm run test -- --run`, `npm run build`가 통과한다.

## 15. 디자인 판단 체크리스트

새 UI를 추가하기 전에 다음을 확인한다.

1. 이 요소가 사용자의 현재 작업을 더 분명하게 만드는가?
2. 서버 또는 사용자의 명시적 선택으로 근거를 설명할 수 있는가?
3. 화면에 이미 더 강한 대표 행동이 있는가?
4. 미구현 기능을 존재하는 것처럼 보이게 하지는 않는가?
5. 모바일, 키보드, 낭독기에서도 같은 정보와 행동을 제공하는가?
6. AI보다 사람의 검수와 책임을 더 분명하게 보여주는가?

하나라도 답이 불분명하면 더 단순한 설계를 선택한다.
