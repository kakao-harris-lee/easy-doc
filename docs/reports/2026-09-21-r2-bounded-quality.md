# R2 품질 보완: 최대 3회

후속 구현: 사용자 추가 요청에 따라 [근거 무손실 결합 보완과 오프라인 재검증](2026-09-21-r2-r4-r5-implementation.md)을 진행했다. 이 문서의 3회 유료 평가 원본 판정은 유지한다.

2026-09-21 사용자 요청: 개선·평가를 최대 3회까지만 진행해 보완 가능성을 확인한다. 기존 누적 US$50 상한을 유지한다. 한 회는 프롬프트 수정, 승인된 실제 생성, 고정 질문 기반 의미 검수로 구성하며, 성공하면 일찍 멈춘다. 반복은 문서당 2회로 개선 회차와 구분한다.

기준 코드: `055196d80aa8dac492df57596eba3708b5fb1d39`. 기존 20개 결과와 원문·저장 본문은 수정하지 않는다. 실제 모델은 기존과 같은 openai / gpt-6-astra / medium, 출력 8,192토큰, 읽기 90초, 재시도 0, store=false다. [공식 단가](https://developers.openai.com/api/docs/models/gpt-6-astra)는 2026-09-21 재확인한 입력 US$10·출력 US$50 / 1M토큰이다.

개발 6건 `070,087,023,072,077,088` × 2 = 12호출로 시작한다. 구조·인용·제한된 사실 검증과 R0 Q1~Q6 의미 검수를 모두 확인하고, 특히 088의 비급여 포함 범위가 두 반복에서 유지되어야 한다. 통과 시 기존 보류 4건을 회귀 확인한다. 이미 본 표본이므로 새 독립 평가라고 주장하지 않는다. 사람 파일럿·실제 배포의 대체 증거도 아니다.

## 예산 원장

이전 모델 평가: 20호출, 예상 US$2.880730, 보수 예약 US$10.185420. 실패한 시도의 예약도 되돌리지 않으며 실행을 나누어 상한을 초기화하지 않는다.

| 회차 / 단계 | 호출 상한 | 실행 상한 | 사전 예약 | 상태 |
|---|---:|---:|---:|---|
| 1 / 개발 | 12 | US$7 | US$6.239980 | 완료: valid 9 · invalid 2 · timeout 1 |
| 2 / 개발 | 12 | US$7 | US$6.292780 | 완료: valid 9 · invalid 1 · timeout 2 |
| 3 / 개발 | 12 | US$7 | US$6.367540 | 완료: valid 11 · invalid 1 · timeout 0 |

1차 프롬프트 파일 SHA-256: `bfb63f99e0ba49e4139b02ed06c184345c2e56f5f82f39a94c36f21cc8d9cdfd`. 격리된 평가 작업 폴더에서 이 프롬프트를 고정한다. R3 병렬 수정은 이번 R2 평가 코드에 들어가지 않는다.

1차는 원문의 지원·사용 범위와 포함·제외 조건, 괄호·각주·별표·뒤따르는 문장의 조건을 text 또는 cautions에 유지하도록 일반 지침을 보강했다. 088 또는 비급여 문구를 하드코딩하지 않았다. 기존 6개 섹션, 단일 호출과 4,000 코드 포인트 제한을 유지한다.

## 1회차 결과

12회 호출, 재시도 0회. 후보 검증 9/12 통과, 087 run1·088 run1 후보 검증 실패, 023 run1은 90초 시간 초과다. 시간 초과에 대한 추가 호출은 하지 않았다. 이 회차는 수용 기준을 충족하지 못했다. 의미 검수와 구조 실패의 세부 원인을 확인해 2회차 변경 축을 정한다.

성공 응답 11개의 관측 토큰 비용은 US$1.855230이며, 시간 초과 1개의 비용은 미상이다. 이를 이번 실행의 전체 실제 비용으로 보지 않는다. 실패한 호출까지 보수 예약 US$6.239980을 유지하여 **기존 평가 포함 누적 예약 US$16.425400**, 남은 보수 예산은 US$33.574600이다.

[1회차 증거](2026-09-21-r2-bounded-quality-artifacts/round1/manifest.json)에 11개 응답, 실행 조건, 결과 요약과 고정 후보 패치를 보존했다. 023 run1의 응답 파일은 없다. 공개 골든 문서 기반 평가 증거이며 사용자 업로드 문서는 포함하지 않는다.

### 1회차 실패 진단과 다음 후보

[AI 의미 대조](2026-09-21-r2-round1-semantic-review.md)는 응답이 있는 66개 질문 판정에서 누락·변경·추가 0건, 응답 없는 023 run1의 6개는 판정 불가였다. 088 Q5의 두 반복 모두 원문 오타 표기인 `비금여 포함`을 주의문에 보존했다. 사람 검수 결과는 아니다.

087 run1은 본문 `1명당`과 인용 `1인당`의 표기 차이가 제한된 숫자 검사에 걸렸다. 의미가 다른 사실로 판단한 것은 아니다. 088 run1은 exceptions와 steps가 모두 available인데 steps 항목들의 cautions가 전부 비어 있어 기존 연결 규칙에 걸렸다. 두 후보 모두 글자 수·스키마·원문 인용 범위는 통과했다.

2회차의 개선 축은 출력 계약을 지키면서 인용을 간결하게 만드는 것이다. 단위·분모·대상 표기를 원문과 같게 유지하고, 예외를 관련 행동 주의문에 연결하며, 모든 사실을 지지하는 최소한의 정확한 인용을 쓴다. 원문 조건 보존 지침과 기존 validator는 유지한다. 023 시간 초과가 인용 길이 때문이라고 확정하지 않으며, 읽기 90초·출력 8,192토큰·재시도 0 조건도 유지한다.

2회차 고정 프롬프트 파일 SHA-256: `e2b0744c9ac2c07c3f4b093d2840c65bbcfd5e7b178b1c37bbf8fe89e65e9ac0`. 같은 격리 checkout에서 고정했고, preflight 예약 US$6.292780을 확인한 뒤 실행했다. 이번 예약 포함 누적 US$22.718180으로 US$50 이하이다.

## 2회차 결과

12호출·재시도 0회, valid 9·invalid 1(087 run2)·provider timeout 2(023 두 반복)다. 088 두 반복은 기술 검증을 통과했다. 관측 가능한 10응답의 예상 비용 US$1.597370에 시간 초과 2호출의 미상 비용을 별도로 본다. 이번 회차 전체 비용이 US$1.597370이라고 주장하지 않는다. 누적 보수 예약 US$22.718180을 유지한다. [2회차 증거](2026-09-21-r2-bounded-quality-artifacts/round2/manifest.json)를 보존했다. 이 회차 역시 수용 실패다.

2회차 AI 의미 검수는 응답이 있는 60개 판정에서 누락·변경·추가 0건이며, 023 두 반복의 12개 판정은 응답 부재로 미평가다. 088 Q5의 두 반복은 text 또는 cautions에 원문 표기 `비금여 포함`을 보존했다. 실제 독자의 이해도나 일반 출시 승인을 뜻하지 않는다.

2회차 087 run2는 파일명을 쉼표로 연결한 본문과 줄별 파일명을 인용한 근거 사이에서 숫자 추출 방식이 달라져 `2025`, `1호`, `2호`가 불일치로 잡혔다. 스키마·원문 인용·4,000자 상한은 통과했다. 이는 파일명 표현 방식에 따른 validator 오탐이며 실제 파일명 근거 누락이 아니다.

마지막 3회차는 목록·참고자료 표현을 보완한다. 실제 자격·한도·행동·예외를 유지하면서 일반 참고용 비용 항목을 각각 독립 혜택처럼 길게 나열하지 않고, 파일명은 항목별 또는 줄별로 정확히 유지한다. 제출 의무가 확인되지 않은 자료를 필수 서류로 바꾸지 않는다. 원문·고정 질문·검증기는 변경하지 않는다.

3회차 고정 프롬프트 파일 SHA-256: `dc35bdf2f01659db7d2b9f4a9db21b003c141a83d68653dcbfb653f8bfb558c4`. preflight 예약 US$6.367540을 확인했다. **기존 평가와 3개 개선 회차의 누적 보수 예약 US$29.085720**, 잔여 US$20.914280으로 총 US$50 이하이다. 이 회차 뒤에는 네 번째 개선·재시도를 실행하지 않는다.

## 최종 판정: bounded_evaluation_failed

최대 세 번의 개선·평가를 완료하고 종료했다. 3회차는 provider 12/12 응답, 후보 검증 11/12 통과이며 023 run2가 거절됐다. 시간 초과는 없었으나 긴 공고문은 80.56초·88.971초로 90초 제한에 가까웠다. [3회차 증거](2026-09-21-r2-bounded-quality-artifacts/round3/manifest.json)를 보존했다. 의미 질문 72개는 AI 대조에서 보존했지만 자동 검증 12/12 수용 기준에 미달한다. 네 번째 개선이나 추가 유료 생성·선택 회귀는 실행하지 않는다.

기존 20호출과 개선 36호출을 합쳐 **56호출**, 재시도 0회다. 토큰을 관측한 호출의 비용 추정 합계는 **US$8.628340**이며, 이전 개선 회차의 시간 초과 3호출 비용은 미상이다. 확정 청구액으로 표현하지 않는다. 미상 비용을 포함해 모든 호출의 **보수 예약 US$29.085720**, 총 상한 US$50 대비 잔여 US$20.914280을 유지했다.

보완 가능성은 부분적으로 확인됐다. 기존 088 사용 범위 누락은 세 회차 두 반복 모두 보완됐고, 최종 회차의 응답 안정성과 후보 검증 통과 수도 개선됐다. 다만 이 작은 표본으로 일반 품질 향상을 확정할 수 없으며, 잔여 자동 검증 실패와 지연 변동성이 있어 **R2 일반 출시 보류**를 유지한다. 사람 파일럿·운영 배포는 이번 결과로 대체하지 않는다. R3 구현은 별도 기본 OFF 경로로 진행한다.

### 최종 잔여 실패의 정확한 원인

023 run2의 `benefits[1]`는 참고 예산 항목을 한 항목으로 요약하면서 근거 11개를 붙여 **항목당 source_anchors 최대 10개** 제한을 넘었다. 전체 사용자 텍스트는 3,350 코드 포인트로 4,000자 미만이며, 6개 섹션·항목 개수·인용 길이·인용 좌표/원문 일치·제한된 사실 검사·예외 주의문 규칙은 통과했다. 따라서 최종 잔여 실패는 의미 누락이나 숫자 오탐이 아니라 구조 제한 위반이다.

원인 확인은 추가 모델 호출 없이 기존 production validator를 오프라인으로 실행해 수행했다. 임시 진단 테스트는 제거했다. 이번 요청의 최대 3회 제한에 따라 프롬프트를 다시 고치거나 11개 근거를 임의로 잘라 통과시키지 않는다. 향후 다시 승인된 개선에서 항목 분할과 근거 상한 준수를 함께 검증해야 한다.

[1회차 의미 검수](2026-09-21-r2-round1-semantic-review.md) · [2회차 의미 검수](2026-09-21-r2-round2-semantic-review.md) · [3회차 의미 검수](2026-09-21-r2-round3-semantic-review.md).
