# 스프린트 1 구현 계획 — 리포·CI 셋업 + 골든셋 + LLM 벤치마크 + 변환·마스킹 파이프라인

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Lean MVP의 기반이 되는 리포지토리·CI, 쉬운 글 스타일 규칙, 개인정보 마스킹, LLM Provider 추상화, 변환 서비스 프로토타입, 골든셋 20건 + 평가 하네스, LLM 벤치마크 스크립트를 완성한다.

**Architecture:** FastAPI 앱 뼈대 위에 `api → services → repositories` 레이어 규칙을 따르되, 스프린트 1은 DB 없이 `easyread`(스타일 규칙·프롬프트) + `privacy`(마스킹) + `llm`(Provider 추상화) + `services`(변환 오케스트레이션)만 구현한다. 보안 불변식: 문서 텍스트는 반드시 `app/privacy/masking.py`를 통과한 후에만 `LLMProvider`로 전달된다. 스타일 규칙은 `app/easyread/style_rules.py` 단일 정의를 프롬프트 생성과 골든셋 평가가 공유한다.

**Tech Stack:** Python 3.12+ / FastAPI / uv / pytest + pytest-asyncio / ruff / mypy(strict) / GitHub Actions. LLM은 anthropic·openai SDK를 **provider 구현체 안에서만** 사용.

**전제·차단 요소 (실행 중 발견 시 보고):**
- LLM 벤치마크·LLM-as-judge 실행에는 `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` 필요 → 키가 없으면 하네스 구축까지만 하고 실제 벤치마크는 보류.
- 골든셋 문서는 **합성(synthetic) 샘플**로 시작한다. 실제 복지 안내문 수집본으로 교체·보강해야 벤더 확정의 근거가 완성된다 (master-plan 7장 검증 절차 ①).
- 원격(GitHub) 리포는 아직 없음 — CI 워크플로 파일만 커밋해 두고, push 시점부터 동작.

**오케스트레이션 미션 분할 (의존 순서):**

| 미션 | 태스크 | 의존 |
|---|---|---|
| A | Task 1~3 (git·uv·앱 뼈대·CI) | — |
| B | Task 4~5 (예외·스타일 규칙·마스킹) | A |
| C | Task 6~7 (Provider 추상화·구현체) | A (B와 병렬 가능) |
| D | Task 8~9 (프롬프트·변환 서비스) | B, C |
| E | Task 10~12 (골든셋·judge·벤치마크) | D |
| F | Task 13 (전체 게이트 검증) | E |

병렬 미션이 동시에 `git commit` 하다 `index.lock` 충돌이 나면 1~2초 대기 후 재시도한다.

---

### Task 1: git 초기화

**Files:**
- Create: `.gitignore`

**Step 1:** `git init` 실행 (기본 브랜치 `main`).

**Step 2:** `.gitignore` 작성:

```gitignore
# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
dist/

# 환경 변수 (비밀키는 .env로만 관리 — 커밋 금지)
.env

# 에디터/OS
.idea/
.vscode/
.DS_Store

# OMC 상태
.omc/
```

**Step 3:** 커밋:

```bash
git add .gitignore CLAUDE.md docs/
git commit -m "chore: 리포 초기화 (기획 문서 + CLAUDE.md)"
```

### Task 2: uv 프로젝트 스캐폴드 + FastAPI 뼈대

**Files:**
- Create: `pyproject.toml`, `.env.example`, `app/__init__.py`, `app/main.py`, `app/config.py`, `tests/__init__.py`, `tests/test_health.py`

**Step 1:** `pyproject.toml` 작성:

```toml
[project]
name = "easy-doc"
version = "0.1.0"
description = "공공기관용 '쉬운 글' 자동 변환 SaaS (Easy-Read AI)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.8",
    "pydantic-settings>=2.4",
    "anthropic>=0.40",
    "openai>=1.50",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.8",
    "mypy>=1.13",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
strict = true
python_version = "3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "llm: 외부 LLM API를 실제 호출하는 테스트 (기본 실행에서 제외, CI 전체 실행에서만)",
]
addopts = "-m 'not llm'"
```

**Step 2:** `app/config.py` — pydantic-settings 기반, `.env`에서 키 로드:

```python
"""애플리케이션 설정. 비밀키는 .env/환경변수로만 주입한다."""

from pydantic import SecretStr
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """환경변수 기반 설정."""

    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")

    # SecretStr: repr/model_dump에서 자동 마스킹되어 로그 유출을 원천 차단한다.
    anthropic_api_key: SecretStr | None = None
    openai_api_key: SecretStr | None = None
```

**Step 3:** `app/main.py` — FastAPI 앱 + 헬스 체크(Pydantic 응답 모델 필수, dict 반환 금지):

```python
"""FastAPI 애플리케이션 진입점."""

from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI(title="Easy-Read AI")


class HealthResponse(BaseModel):
    """헬스 체크 응답."""

    status: str


@app.get("/health")
async def health() -> HealthResponse:
    """서비스 생존 확인."""
    return HealthResponse(status="ok")
```

**Step 4:** `tests/test_health.py` (fastapi.testclient 사용) 작성 후 `uv sync` → `uv run pytest -v` 로 통과 확인.

**Step 5:** `.env.example` 작성 (`ANTHROPIC_API_KEY=`, `OPENAI_API_KEY=` 두 줄 + 주석). `.python-version` 파일(내용 `3.12`)도 커밋해 로컬·CI 인터프리터를 통일한다. 테스트의 `TestClient`는 `with` 컨텍스트(픽스처)로 사용해 lifespan 실행을 보장한다.

**Step 6:** `uv run ruff check .` / `uv run ruff format .` / `uv run mypy .` 통과 확인 후 커밋:

```bash
git add pyproject.toml uv.lock .env.example app/ tests/
git commit -m "feat: uv 프로젝트 스캐폴드 + FastAPI 헬스 체크"
```

### Task 3: GitHub Actions CI

**Files:**
- Create: `.github/workflows/ci.yml`

**Step 1:** CLAUDE.md의 게이트 순서(ruff → mypy → pytest)를 그대로 강제:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
          enable-cache: true
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy .
      - run: uv run pytest
      # 골든셋 LLM 평가(-m llm)는 API 키 시크릿 등록 후 별도 잡으로 활성화한다.
```

**Step 2:** 커밋: `git add .github && git commit -m "ci: ruff → mypy → pytest 게이트"`

### Task 4: 도메인 예외 + 쉬운 글 스타일 규칙 (TDD)

**Files:**
- Create: `app/exceptions.py`, `app/easyread/__init__.py`, `app/easyread/style_rules.py`
- Test: `tests/easyread/test_style_rules.py` (+ `tests/easyread/__init__.py`)

**Step 1:** `app/exceptions.py`:

```python
"""도메인 예외 정의. 라우터 레벨에서 HTTP 응답으로 변환한다."""


class EasyDocError(Exception):
    """서비스 공통 최상위 예외."""


class LLMProviderError(EasyDocError):
    """LLM 호출 실패."""
```

**Step 2: 실패 테스트 작성** — `tests/easyread/test_style_rules.py`:

```python
"""쉬운 글 스타일 규칙 단위 테스트."""

from app.easyread.style_rules import (
    MAX_SENTENCE_CHARS,
    check_style,
    find_difficult_words,
    split_sentences,
)


def test_split_sentences_기본() -> None:
    text = "신청 기간은 3월 2일까지입니다. 주민센터에 방문하세요."
    assert split_sentences(text) == ["신청 기간은 3월 2일까지입니다.", "주민센터에 방문하세요."]


def test_긴_문장은_이슈로_보고된다() -> None:
    long_sentence = "가" * (MAX_SENTENCE_CHARS + 1) + "."
    result = check_style(long_sentence)
    assert result.issues and "길이" in result.issues[0].reason


def test_이중_피동_검출() -> None:
    result = check_style("이 제도는 개선되어지고 있습니다.")
    assert any("피동" in issue.reason for issue in result.issues)


def test_어려운_한자어_검출() -> None:
    assert "금일" in find_difficult_words("금일 중으로 서류를 제출하십시오.")


def test_쉬운_문장은_통과() -> None:
    result = check_style("오늘 서류를 내세요.")
    assert result.passed
```

**Step 3:** `uv run pytest tests/easyread -v` → ImportError로 FAIL 확인.

**Step 4:** `app/easyread/style_rules.py` 구현:

```python
"""쉬운 글 스타일 규칙 — 단일 정의(SSOT).

프롬프트 생성(app/easyread/prompts.py)과 골든셋 평가(tests/golden)가
반드시 이 모듈의 상수·함수를 공유한다 (CLAUDE.md 아키텍처 규칙 4).
근거: 국립국어원 쉬운 글쓰기 지침, 보건복지부 가이드라인,
서울시 읽기쉬운자료개발센터('알다') 제작 원칙.
"""

import re

from pydantic import BaseModel

MAX_SENTENCE_CHARS = 50
MAX_COMMAS_PER_SENTENCE = 2

# 이중 피동 등 피해야 할 서술 패턴
DOUBLE_PASSIVE_PATTERNS: tuple[str, ...] = ("되어지", "보여지", "쓰여지", "믿겨지", "잊혀지")

# 어려운 한자어·행정 용어 → 쉬운 표현 (시드 목록, 지속 확장)
DIFFICULT_WORD_REPLACEMENTS: dict[str, str] = {
    "금일": "오늘",
    "명일": "내일",
    "익일": "다음 날",
    "익월": "다음 달",
    "당월": "이번 달",
    "동절기": "겨울철",
    "하절기": "여름철",
    "상기": "위",
    "하기": "아래",
    "소정의": "정해진",
    "제반": "여러",
    "구비서류": "준비할 서류",
    "지참": "가지고 오기",
    "송부": "보내기",
    "기재": "적기",
    "수령": "받기",
    "납부": "내기",
    "미납": "내지 않음",
    "잔여": "남은",
    "경감": "줄임",
    "감면": "깎아 줌",
    "부득이한": "어쩔 수 없는",
    "해당자": "해당하는 사람",
    "미제출": "내지 않음",
    "도래": "다가옴",
}

STYLE_PRINCIPLES: tuple[str, ...] = (
    "한 문장에는 정보를 하나만 담는다.",
    f"문장은 {MAX_SENTENCE_CHARS}자를 넘기지 않는다.",
    "어려운 한자어·행정 용어는 쉬운 말로 바꾼다.",
    "능동태로 쓰고 이중 피동(예: '되어지다')을 쓰지 않는다.",
    "날짜·금액·연락처·신청 방법 등 중요한 정보는 빠뜨리지 않는다.",
    "존댓말로 부드럽게 설명한다.",
)

_SENTENCE_SPLIT = re.compile(r"(?<=[.!?])\s+|\n+")


class SentenceIssue(BaseModel):
    """규칙 위반 문장과 사유."""

    sentence: str
    reason: str


class StyleCheckResult(BaseModel):
    """규칙 기반 검사 결과."""

    total_sentences: int
    issues: list[SentenceIssue]

    @property
    def passed(self) -> bool:
        return not self.issues


def split_sentences(text: str) -> list[str]:
    """마침표·물음표·느낌표·줄바꿈 기준의 단순 문장 분리."""
    return [s.strip() for s in _SENTENCE_SPLIT.split(text) if s.strip()]


def find_difficult_words(text: str) -> list[str]:
    """치환 목록에 있는 어려운 표현 중 본문에 남아 있는 것을 찾는다."""
    return [word for word in DIFFICULT_WORD_REPLACEMENTS if word in text]


def check_style(text: str) -> StyleCheckResult:
    """문장 길이·쉼표 수·이중 피동·어려운 표현을 검사한다."""
    sentences = split_sentences(text)
    issues: list[SentenceIssue] = []
    for sentence in sentences:
        if len(sentence) > MAX_SENTENCE_CHARS:
            issues.append(SentenceIssue(sentence=sentence, reason="문장 길이 초과"))
        if sentence.count(",") > MAX_COMMAS_PER_SENTENCE:
            issues.append(
                SentenceIssue(sentence=sentence, reason="쉼표 과다(한 문장 한 정보 위반 의심)")
            )
        for pattern in DOUBLE_PASSIVE_PATTERNS:
            if pattern in sentence:
                issues.append(SentenceIssue(sentence=sentence, reason=f"이중 피동 표현({pattern})"))
    for word in find_difficult_words(text):
        issues.append(SentenceIssue(sentence=word, reason=f"어려운 표현 잔존({word})"))
    return StyleCheckResult(total_sentences=len(sentences), issues=issues)
```

**Step 5:** `uv run pytest tests/easyread -v` PASS 확인 → ruff·mypy 통과 확인 → 커밋 `feat: 쉬운 글 스타일 규칙 SSOT + 도메인 예외`.

### Task 5: 개인정보 마스킹 파이프라인 (TDD)

**Files:**
- Create: `app/privacy/__init__.py`, `app/privacy/masking.py`
- Test: `tests/privacy/test_masking.py` (+ `__init__.py`)

**Step 1: 실패 테스트 작성:**

```python
"""마스킹 파이프라인 단위 테스트. 테스트 데이터는 전부 가짜 개인정보다."""

from app.privacy.masking import MaskCategory, mask_text


def test_주민등록번호_마스킹() -> None:
    result = mask_text("신청자 홍길동(900101-1234567)님께 안내드립니다.")
    assert "900101-1234567" not in result.masked_text
    assert result.items[0].category == MaskCategory.RRN
    assert result.items[0].placeholder in result.masked_text


def test_휴대전화_마스킹() -> None:
    result = mask_text("문의: 010-1234-5678로 연락하세요.")
    assert "010-1234-5678" not in result.masked_text
    assert result.items[0].category == MaskCategory.PHONE


def test_이메일_마스킹() -> None:
    result = mask_text("이메일 hong@korea.kr 로 회신 바랍니다.")
    assert "hong@korea.kr" not in result.masked_text


def test_여러_항목_카테고리별_번호_부여() -> None:
    text = "연락처 010-1111-2222 또는 010-3333-4444"
    result = mask_text(text)
    placeholders = [item.placeholder for item in result.items]
    assert len(placeholders) == len(set(placeholders)) == 2


def test_개인정보_없는_문서는_그대로() -> None:
    text = "3월 2일부터 주민센터에서 신청할 수 있습니다."
    result = mask_text(text)
    assert result.masked_text == text
    assert result.items == []


def test_기관_대표번호도_마스킹된다() -> None:
    # 프로토타입은 과잉 마스킹을 허용한다(누락보다 안전). 문맥 구분은 이후 단계.
    result = mask_text("보건소 02-1234-5678")
    assert result.items[0].category == MaskCategory.PHONE
```

**Step 2:** FAIL 확인 후 `app/privacy/masking.py` 구현:

```python
"""개인정보 마스킹 파이프라인.

보안 불변식 (master-plan 3.2): 사용자 문서 텍스트는 이 모듈의 mask_text()를
통과한 뒤에만 LLMProvider로 전달할 수 있다. 이 순서를 우회하는 코드는 금지.
원문-플레이스홀더 대응은 검수 화면 표시용으로만 쓰고 외부로 내보내지 않는다.
"""

import re
from enum import Enum

from pydantic import BaseModel


class MaskCategory(str, Enum):
    """마스킹 대상 개인정보 분류."""

    RRN = "주민등록번호"
    CARD = "카드번호"
    PHONE = "전화번호"
    EMAIL = "이메일"
    ACCOUNT = "계좌번호"


# 우선순위 순서(먼저 매칭된 구간이 이후 패턴보다 우선).
# EMAIL이 최우선: 지역부의 숫자열(주민번호·카드·전화 패턴)이 부분 마스킹되어
# 도메인이 평문으로 남는 유출을 방지한다. EMAIL은 @를 요구하므로 단독 숫자열을 가로채지 않는다.
# RRN 성별코드는 [1-8]: 5~8은 외국인등록번호(고유식별정보). 구분자 없는 표기도 커버.
_PATTERNS: tuple[tuple[MaskCategory, re.Pattern[str]], ...] = (
    (MaskCategory.EMAIL, re.compile(r"[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}")),
    (MaskCategory.RRN, re.compile(r"(?<!\d)\d{6}[ \t]*-?[ \t]*[1-8]\d{6}(?!\d)")),
    (MaskCategory.CARD, re.compile(r"(?<!\d)\d{4}[- ]?\d{4}[- ]?\d{4}[- ]?\d{4}(?!\d)")),
    (
        MaskCategory.PHONE,
        re.compile(
            r"(?<!\d)(?:01[016789]|02|0[3-6]\d|070|080|050\d)[-.\s]?\d{3,4}[-.\s]?\d{4}(?!\d)"
        ),
    ),
    (MaskCategory.ACCOUNT, re.compile(r"(?<!\d)\d{3,6}-\d{2,6}-\d{4,8}(?!\d)")),
)


class MaskedItem(BaseModel):
    """마스킹된 개별 항목 (검수 화면 표시용)."""

    category: MaskCategory
    placeholder: str
    original: str


class MaskingResult(BaseModel):
    """마스킹 결과."""

    masked_text: str
    items: list[MaskedItem]


def mask_text(text: str) -> MaskingResult:
    """우선순위 패턴 순서로 개인정보를 찾아 플레이스홀더로 치환한다."""
    spans: list[tuple[int, int, MaskCategory]] = []
    for category, pattern in _PATTERNS:
        for match in pattern.finditer(text):
            start, end = match.span()
            if any(start < e and s < end for s, e, _ in spans):
                continue  # 이미 다른 패턴이 차지한 구간
            spans.append((start, end, category))

    spans.sort()
    counters: dict[MaskCategory, int] = {}
    items: list[MaskedItem] = []
    result_parts: list[str] = []
    cursor = 0
    for start, end, category in spans:
        counters[category] = counters.get(category, 0) + 1
        placeholder = f"[[{category.value}{counters[category]}]]"
        items.append(
            MaskedItem(category=category, placeholder=placeholder, original=text[start:end])
        )
        result_parts.append(text[cursor:start])
        result_parts.append(placeholder)
        cursor = end
    result_parts.append(text[cursor:])
    return MaskingResult(masked_text="".join(result_parts), items=items)
```

**Step 3:** PASS 확인 → ruff·mypy → 커밋 `feat: 개인정보 마스킹 파이프라인 (주민번호·전화·이메일·카드·계좌)`.

### Task 6: LLMProvider 추상화 + FakeProvider (TDD)

**Files:**
- Create: `app/llm/__init__.py`, `app/llm/provider.py`, `app/llm/fake.py`
- Test: `tests/llm/test_fake_provider.py` (+ `__init__.py`)

**Step 1: 실패 테스트:**

```python
"""FakeProvider 동작 검증 — 서비스 단위 테스트의 표준 대역."""

import pytest

from app.llm.fake import FakeProvider


async def test_준비된_응답을_순서대로_반환() -> None:
    provider = FakeProvider(responses=["쉬운 글 결과"])
    response = await provider.complete(system="시스템", user="본문")
    assert response.text == "쉬운 글 결과"
    assert provider.calls[0].user == "본문"


async def test_응답_소진_시_예외() -> None:
    provider = FakeProvider(responses=[])
    with pytest.raises(IndexError):
        await provider.complete(system="s", user="u")
```

**Step 2:** `app/llm/provider.py`:

```python
"""LLM Provider 추상화 — 모든 LLM 호출의 유일한 관문 (master-plan 3.1).

서비스 코드는 벤더 SDK를 직접 import하지 않고 이 인터페이스만 사용한다.
모든 구현체는 입력 데이터 학습 미사용(no-training) 계약을 전제로 한다.
"""

from abc import ABC, abstractmethod

from pydantic import BaseModel


class LLMResponse(BaseModel):
    """LLM 완성 응답."""

    text: str
    model: str
    input_tokens: int = 0
    output_tokens: int = 0


class LLMProvider(ABC):
    """LLM 벤더 공통 인터페이스."""

    name: str = "base"

    @abstractmethod
    async def complete(
        self, *, system: str, user: str, max_tokens: int = 4096, temperature: float = 0.2
    ) -> LLMResponse:
        """단일 완성 요청. 실패 시 LLMProviderError를 던진다."""
```

**Step 3:** `app/llm/fake.py` — 호출 기록(`calls: list[FakeCall]`, `FakeCall(system, user)`) + 준비된 응답 큐 반환. `name = "fake"`.

**Step 4:** PASS → ruff·mypy → 커밋 `feat: LLMProvider 추상화 + FakeProvider`.

### Task 7: Anthropic·OpenAI Provider 구현체

**Files:**
- Create: `app/llm/anthropic_provider.py`, `app/llm/openai_provider.py`
- Test: `tests/llm/test_providers.py`

**요구사항:**
- 벤더 SDK(anthropic, openai)는 **이 두 파일 안에서만** import한다.
- 각 파일 모듈 docstring에 no-training 계약 조건 명시 (CLAUDE.md 보안 규칙): "상용 API 입력 데이터 학습 미사용(no-training) 조건 전제. 계약·약관 변경 시 재확인 필수."
- 생성자: `AnthropicProvider(api_key: str, model: str = "claude-sonnet-5")`, `OpenAIProvider(api_key: str, model: str = "gpt-4o")`. `name`은 각각 `"anthropic"`, `"openai"`.
- SDK 예외를 잡아 `LLMProviderError`로 변환해 던진다. **예외 메시지에 문서 본문을 포함하지 않는다** (로그 보안 규칙).
- 단위 테스트는 실제 호출 없이: 생성자 검증 + SDK 클라이언트를 monkeypatch하여 complete()가 system/user를 올바른 페이로드로 전달하고 LLMResponse로 변환하는지 확인. 실제 API 호출 테스트는 `@pytest.mark.llm`으로 1건씩만.

**완료 기준:** pytest(기본, llm 제외) PASS → 커밋 `feat: Anthropic·OpenAI provider 구현체`.

### Task 8: 변환 프롬프트 + 후처리 (TDD)

**Files:**
- Create: `app/easyread/prompts.py`, `app/easyread/postprocess.py`
- Test: `tests/easyread/test_prompts.py`, `tests/easyread/test_postprocess.py`

**요구사항:**
- `build_system_prompt() -> str`: `STYLE_PRINCIPLES` 전체와 `DIFFICULT_WORD_REPLACEMENTS` 치환 목록을 포함한 시스템 프롬프트. 마스킹 플레이스홀더(`[[…]]`)는 **그대로 보존하라**는 지시 포함. "공공기관 안내문을 발달장애인이 이해하기 쉬운 글로 바꾸는 전문가" 역할 정의.
- `build_user_prompt(masked_text: str) -> str`: 마스킹된 원문을 감싸는 사용자 프롬프트. 결과는 변환된 본문만 출력하도록 지시.
- `postprocess(raw: str) -> str`: 앞뒤 공백 제거, 마크다운 코드 펜스 제거, "다음은 변환 결과입니다" 류의 머리말 1줄 제거(정규식).
- 테스트: 시스템 프롬프트에 규칙 문구·치환 예시 포함 확인(`STYLE_PRINCIPLES[0] in build_system_prompt()`), 플레이스홀더 보존 지시 확인, postprocess 케이스 3건.

**완료 기준:** PASS → 커밋 `feat: 쉬운 글 변환 프롬프트 + 후처리`.

### Task 9: 변환 서비스 — 마스킹 선행 불변식 (TDD)

**Files:**
- Create: `app/services/__init__.py`, `app/services/conversion.py`
- Test: `tests/services/test_conversion.py` (+ `__init__.py`)

**Step 1: 실패 테스트 (불변식이 핵심):**

```python
"""변환 서비스 테스트 — 마스킹 선행 보안 불변식 검증이 핵심."""

from app.llm.fake import FakeProvider
from app.services.conversion import ConversionService


async def test_마스킹_후에만_LLM에_전달된다() -> None:
    provider = FakeProvider(responses=["변환 결과"])
    service = ConversionService(provider=provider)
    await service.convert("홍길동 900101-1234567, 연락처 010-1234-5678")
    sent = provider.calls[0].user
    assert "900101-1234567" not in sent
    assert "010-1234-5678" not in sent
    assert "[[주민등록번호1]]" in sent


async def test_변환_결과와_마스킹_항목_반환() -> None:
    provider = FakeProvider(responses=["쉬운 글입니다."])
    service = ConversionService(provider=provider)
    outcome = await service.convert("문의 010-1234-5678")
    assert outcome.easy_text == "쉬운 글입니다."
    assert len(outcome.masked_items) == 1
```

**Step 2:** `app/services/conversion.py` 구현:

```python
"""쉬운 글 변환 서비스.

처리 순서(변경 금지): 마스킹(mask_text) → 프롬프트 생성 → LLMProvider 호출 → 후처리.
마스킹 전 원문을 LLMProvider에 전달하는 코드는 절대 작성하지 않는다 (master-plan 3.2).
"""

from pydantic import BaseModel

from app.easyread.postprocess import postprocess
from app.easyread.prompts import build_system_prompt, build_user_prompt
from app.exceptions import LLMProviderError
from app.llm.provider import LLMProvider
from app.privacy.masking import MaskedItem, mask_text


class ConversionOutcome(BaseModel):
    """변환 결과 + 검수 화면용 마스킹 항목."""

    easy_text: str
    masked_items: list[MaskedItem]
    model: str


class ConversionService:
    """쉬운 글 변환 파이프라인 오케스트레이션."""

    def __init__(self, provider: LLMProvider) -> None:
        self._provider = provider

    async def convert(self, text: str) -> ConversionOutcome:
        """원문을 마스킹한 뒤 쉬운 글로 변환한다."""
        masking = mask_text(text)
        response = await self._provider.complete(
            system=build_system_prompt(),
            user=build_user_prompt(masking.masked_text),
        )
        if response.truncated:
            # 절단 감지는 provider가 사실(truncated)만 보고, 정책(예외/재시도)은 서비스가 결정한다.
            raise LLMProviderError("변환 결과가 토큰 한도에서 잘렸습니다")
        return ConversionOutcome(
            easy_text=postprocess(response.text),
            masked_items=masking.items,
            model=response.model,
        )
```

**Step 3:** PASS → ruff·mypy → 커밋 `feat: 변환 서비스 (마스킹 선행 불변식 포함)`.

### Task 10: 골든셋 구축 (합성 20건) + 규칙 기반 평가

**Files:**
- Create: `tests/golden/__init__.py`, `tests/golden/documents/*.json` (20건), `tests/golden/loader.py`, `tests/golden/test_schema.py`, `tests/golden/test_golden_eval.py`

**골든셋 문서 스키마** (`tests/golden/documents/001-기초연금-신청.json` 형식):

```json
{
  "id": "001",
  "title": "기초연금 신청 안내",
  "category": "복지 안내문",
  "synthetic": true,
  "source_text": "(공공기관 안내문 어투의 원문 500~1500자. 어려운 한자어·긴 문장 포함. 가짜 연락처·담당자 등 합성 개인정보 1~2건 포함)",
  "required_facts": ["3월 2일", "25만 원", "주민센터"]
}
```

**작성 규칙:**
- 20건 주제 분배: 기초연금·독감 예방접종·주민등록 사실조사·에너지바우처·긴급복지지원·장애인활동지원·아동수당·차상위 확인·재난지원금·건강검진·문화누리카드·임대주택 입주·보육료 지원·노인일자리·청년월세지원·의료급여·양곡할인·통합사례관리·전기요금 할인·평생학습 바우처.
- `synthetic: true` 필수 — 실제 수집 문서로 교체 시 false로 변경.
- `required_facts`는 변환 후에도 반드시 남아야 할 리터럴(날짜·금액·기관명·장소 등) 3~6개. **전화번호·이메일·계좌 등 마스킹 대상 패턴은 절대 넣지 않는다** — 마스킹 단계에서 플레이스홀더로 치환되므로 사실 보존 검사가 항상 실패한다.
- 원문에는 `DIFFICULT_WORD_REPLACEMENTS`의 표현을 문서당 2개 이상 자연스럽게 포함(평가 변별력 확보).

**`tests/golden/loader.py`:** Pydantic 모델 `GoldenDocument` + `load_documents() -> list[GoldenDocument]`.

**`tests/golden/test_schema.py`** (기본 실행 포함, LLM 불필요):
- 문서 20건 이상 로드
- 모든 필드 스키마 통과, `required_facts` 비어 있지 않음
- 각 문서 원문에 어려운 표현 2개 이상 포함(`find_difficult_words`)

**`tests/golden/test_golden_eval.py`** (`@pytest.mark.llm`):
- 환경변수 `GOLDEN_PROVIDER`(기본 `anthropic`)로 실제 provider 생성 — 생성은 `app/llm/factory.py`의 `create_provider(name, settings)`를 재사용(키 없으면 None → `pytest.skip`)
- 전 문서를 `ConversionService.convert` → ① `check_style` 통과 ② `required_facts` 전부 잔존 을 문서별 평가
- **집계 기준**: 통과율 ≥ 90% (KPI, master-plan 7장) — 개별 문서가 아닌 집계로 assert

**완료 기준:** `uv run pytest tests/golden -v` (기본 = 스키마 테스트만) PASS → 커밋 `feat: 골든셋 합성 20건 + 평가 하네스`.

### Task 11: LLM-as-judge 채점

**Files:**
- Create: `app/easyread/judge.py`
- Test: `tests/easyread/test_judge.py` (FakeProvider 사용) + `tests/golden/test_golden_eval.py`에 judge 추가

**요구사항:**
- `build_judge_prompt(source: str, converted: str) -> tuple[str, str]` (system, user): 원문 대비 변환문을 ① 충실성(정보 누락·왜곡 없음) ② 이해 용이성 각각 1~5점으로 채점, JSON `{"fidelity": n, "readability": n, "comment": "..."}`만 출력하도록 지시.
- `parse_judge_response(raw: str) -> JudgeScore` (Pydantic): JSON 파싱, 코드 펜스 허용, 실패 시 `LLMProviderError`.
- judge 실행 함수 `judge_conversion(provider, source, converted) -> JudgeScore`.
- 단위 테스트는 FakeProvider로 파싱·프롬프트 구성 검증. 골든셋 judge 평가는 `@pytest.mark.llm`으로 fidelity·readability 평균 ≥ 4.0 집계 assert.

**완료 기준:** 기본 pytest PASS → 커밋 `feat: LLM-as-judge 채점기`.

### Task 12: LLM 벤치마크 스크립트

**Files:**
- Create: `scripts/benchmark.py`

**요구사항:**
- 실행: `uv run python scripts/benchmark.py --providers anthropic,openai --judge anthropic --output docs/benchmarks/`
- 각 provider로 골든셋 전 문서를 변환(재사용: `ConversionService`, `tests/golden/loader.py`의 문서를 `pathlib`로 직접 로드해도 됨 — 단 로직 중복 금지, 로더를 `app/easyread/goldenset.py`로 옮기고 tests에서 재사용하는 쪽을 선택).
- 문서별 측정: 규칙 통과 여부(`check_style`), required_facts 잔존율, judge 점수(옵션), 응답 지연(초), 출력 길이.
- 결과를 `docs/benchmarks/YYYY-MM-DD-llm-benchmark.md`에 provider별 비교 표(markdown)로 저장: 규칙 통과율 / 팩트 잔존율 / judge 평균 / 평균 지연. 마지막에 "벤더 확정 판단 근거" 빈 섹션 포함(사람이 채움).
- provider 생성은 `app/llm/factory.py`의 `create_provider(name, settings)` 재사용. API 키가 없는 provider는 건너뛰고 경고 출력. 사용 후 `aclose()`로 정리. **벤치마크 결과·로그에 문서 본문을 남기지 않는다** (문서 ID·점수만).
- 날짜는 `datetime.now()` 사용 가능(스크립트는 워크플로 밖).

**완료 기준:** `uv run python scripts/benchmark.py --help` 동작 + ruff·mypy 통과 → 커밋 `feat: LLM 벤치마크 스크립트`.

### Task 13: 전체 게이트 검증 + 문서 갱신

**Step 1:** 전체 게이트 순서대로 실행, 출력 확인:

```bash
uv run ruff check . && uv run ruff format --check .
uv run mypy .
uv run pytest -v
```

**Step 2:** `README.md` 작성: 프로젝트 한 줄 소개, 개발 명령어(CLAUDE.md와 동일), 골든셋·벤치마크 실행법, `.env` 설정 안내.

**Step 3:** master-plan과의 정합성 확인 — 어긋난 것이 있으면 문서 갱신을 **제안**(직접 수정 금지, 보고에 포함).

**Step 4:** 최종 커밋 `docs: README + 스프린트 1 마무리`.

---

## 완료 정의 (스프린트 1)

- [ ] git 리포 + CI 워크플로 커밋됨
- [ ] ruff / mypy(strict) / pytest 전부 통과
- [ ] 마스킹 선행 불변식 테스트 존재·통과
- [ ] 골든셋 20건(synthetic) + 스키마 테스트 통과
- [ ] LLM 평가·벤치마크 하네스 완성 (실제 실행은 API 키 확보 후)
- [ ] 로그·예외 메시지에 문서 본문 미포함 확인
- [ ] 남은 사람 결정 사항 보고: ① API 키 제공 ② 실제 안내문 수집 ③ 벤치마크 결과로 벤더 확정
