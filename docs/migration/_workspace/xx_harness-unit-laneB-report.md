# 레인 B 최종 보고 (#2~#7 전부 완료)

> 에이전트 보고 원문 보존(리더 전사, 2026-08-23). 계획: `xx_harness-unit-laneB-plan.md`. #1(§4)은 리더가 `f3398bc` 로 커밋.

## 1. 커밋 표

| SHA | 계획 항목 | 파일 | 초록 근거 |
|---|---|---|---|
| `d7cecfe` | #2 X-3b 출구 | `tests/test_review_coverage_reach.py` | ruff · format --check · mypy . .claude · pytest -q 전체 (1656 passed) |
| `aff00c9` | #3 X-3a 입구(출하 모드) | 같음 | 전체 (1658 passed) |
| `a28ee47` | #4 X-2a 산출물 칸 파싱·어간 결속 | 같음 | 전체 (1657 passed) |
| `df4d60c` | #5 X-2b 역할 구성 · X-2c 시점 결속 | 같음 | 전체 (1657 passed) |
| `0272d3b` | #6 X-4·X-4c·X-11 어간 문법 | `.claude/skills/kotlin-migration/SKILL.md` · `tests/test_harness_scope_reach.py` | 전체 (1659 passed) |
| `329a964` | #7 X-16 주석 예산 축 분리 | `CLAUDE.md`(Kotlin 주석 규약 절) · `tests/test_kotlin_comment_budget.py` | 전체 (1666 passed) |

전 명령 `run_gate.sh` 경유. 매 회 실패는 장부 부트스트랩 1건뿐(리더가 `01d3c48` 로 닫음). `f3398bc` 는 미기재 목록에 없었다 — `60c74cc` 기재가 먹힌 증거.

## 2. 음성 대조 표 (전부 실행 — 읽기 판정 0)

전부 일회용 `git worktree add --detach`. 본 저장소 무변조, `cp`·`git stash` 미사용, worktree remove·prune 완료.

| 항목 | 변이 | 처방 전 | 처방 후 |
|---|---|---|---|
| X-3b | 원장 4행의 `닫힘` 을 `-` 로 위조 | 위조 유무로 결과 동일(무반응) | `b4c2fda`·`6a9ca8b`·`6c970b6`·`4ac13ec` 4행 지목, exit 1 |
| X-3a ⑴ | 출하 모드 ON | — | 미상환 9행 지목, exit 1 |
| X-3a ⑵ | 출하 모드 OFF | — | 통과 + 경고 `… 미상환 대기 9 건` |
| X-3a ⑶ | 합성 장부(저장소 무변조) | — | OFF recorded=(대기,대기,이연) / ON recorded=(이연,). 산문 SHA·회차 빈칸 행은 양쪽 모드 미계상 |
| X-2a ⓑ | 산출물 칸을 남의 회차 실재 파일로 | 유효 범위 4 | 3 (그 행 탈락) |
| X-2a ⓑ' | 커버리지 4행 전부 남의 어간으로 | 4 | 0 → 규칙 4 ⑶ 로 실패 |
| X-2c ⓐ | 없는 파일(`…_없는-증거.md`) | 4 | 3 |
| X-2c ⓒ | `touch` 미커밋 3파일 + 표 한 줄 | **5** — 그 한 줄이 `0d632f9..a28ee47`, 판정 전 구간을 통째로 「리뷰됨」으로 만들었다 | 4 |
| X-2b ⓓ | codex 레인 없이 `migration-reviewer`+`cross` | 4 | 3 |
| X-2c ⓔ | 과거 산출물로 미래 범위 승인(`cbf6e8d..a28ee47`) | 5 | 4 |
| X-4 ⓐ | 문법 위반 산출물 1건 생성 | 42 passed | 1 failed |
| X-4 ⓑ | `{scope}` 정본 표 머리행 삭제 | 42 passed | 2 failed |
| X-4 ⓒ | 핀에서 한 줄 삭제 | — | `['05_scope-reach']` 지목 |
| §5 | 커버리지 강제자 축 2개 제거 | — | 장치 밖(`scope_reach`)에서 둘 다 지목 |
| §5 | 커버리지 강제자 파일 삭제 | — | 장치 밖에서 `FileNotFoundError` 로 실패 (+ `ci.yml:237` 경로 명시) |
| X-16 V1 | 합성 Kotlin | — | 문자열 안 `2026-01-01`·`cafebabe1` 미계상 / 주석의 날짜 2·SHA 1 만 계상. 축 분류 2/2 |
| X-16 V2 | 테스트 `.kt` 주석에 날짜 1건 | 전역 상한이 이미 빨감(§4-3) | 이력 표식 6 > 5, 테스트 축 지목 |
| X-16 V3 | 제품 `.kt` 주석에 SHA 1건 | 〃 | 이력 표식 14 > 13, 제품 축 지목 |
| X-16 V5 | 테스트 축에만 4,500자 추가 | — | `테스트 축 … 61008 > 60000` 지목, 제품 축 무영향 |
| X-16 V4 | 실물 실측 | — | 제품 73,700/76,000(97.0%) · 테스트 56,227/60,000(93.7%) · 이력 표식 13/13 · 5/5 |

출하 모드의 오늘 정상 결과는 빨강이며 통과로 바꾸지 않았다. 지목 9행: `ed3df31`·`7efc7c4`·`3ba7e04`·`e7faccc`·`87457e6`·`73f9f4c`·`0fda906`·`80f6761`·`f3398bc` (리더 기재 후 20행).

## 3. 계획 §6 한계 5건 — 다섯 다 닫히지 않았다

1. 「산출물 내용이 참인가」는 여전히 안 잰다. `xx_harness` 반례(산출물 3건, codex 원문 0줄)는 이 처방으로도 통과한다.
2. 출하 모드의 CI 배선 0. 도달은 「리더가 Phase 종료 판정에서 켠다」 하나. `SHIPPABLE_MODE_ENV` 상수 주석에 적었다.
3. 어간 핀은 은폐형 성분. 정확 일치·축소 전용으로 좁혔을 뿐. 넓히지 않는다는 것을 파일 주석과 실패 메시지 양쪽에 단언으로 고정.
4. X-11 은 문법만이고 강제자 0. 원장 데이터는 리더 소관(→ 리더가 `aff6204` 로 20행을 백틱 어간으로 정리).
5. §4 는 규약뿐이고 탐지자 0. 결함으로 보고한다 — 악용 비용은 요약 한 번.

## 4. 계획과 달라진 점 (넷)

1. **어간 핀은 19종이 아니라 18종.** 표에 더한 `phase-close` 가 `03_phase3-close` 를 합법화해 하나가 빠진다.
2. **X-3a 미상환은 7행이 아니라 9행.** `80f6761`·`f3398bc` 두 줄이 늘었다.
3. **X-16 에서 전역 상한 130,000 을 남기지 않고 축별 상한으로 교체했다.** 남기면 축별 상한이 그보다 먼저 닿을 수 없어 도달 0. 발견: **커밋된 `0272d3b` 시점의 총량이 이미 130,205 > 130,000 으로 빨갰고**, 본 작업 트리가 초록이던 것은 레인 A 의 미커밋 삭제분 덕분이었다. 값: 제품 76,000(실측 73,700) · 테스트 60,000(실측 56,227). 파일별 5,500 은 두 축 공통·불변. 테스트 축 이력 표식 상한은 계획의 6이 아니라 5.
4. **`_LEDGER_BLANK` 를 대시 세 종으로 잡았다**(`_UNRESOLVED_EMPTY` 선례). 양쪽 소비자 모두 「비었다 → 더 요구한다」 방향.

계획 §0 제약 전부 준수 — manifest 0 · 표에 회차 0건(`phase-close` 값 하나) · 개명 0 · `대기`→`이연` 없음 · `00_progress.md` 읽기만 · 레인 A 파일·`codex-reviewer.md` 미접촉 · `80f6761` 미변경.

## 5. 못 끝낸 것

없다. 작업 중 `test_kotlin_comment_budget` 이 두 번 산발적으로 빨갰다가 재실행에서 통과 — 레인 A 가 `.kt` 를 편집하는 중이라 종전 전역 상한(99.9%)을 오갔기 때문. `329a964` 의 축 분리로 해소.
