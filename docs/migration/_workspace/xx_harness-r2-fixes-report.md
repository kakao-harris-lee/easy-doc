# 하네스 조치 레인 최종 보고 — `xx_harness-r2` 차단 4·수정 2·리더 판정 2

> 에이전트 보고 원문 보존(리더 전사, 2026-08-23). 계획: `xx_harness-r2-fixes-plan.md`. HEAD `ecfb2f7`(보고 시점). 푸시 안 함. 본 저장소 무변조(미커밋 `.kt` 4건 외 변화 0).

## 1. 커밋 표

| SHA | 항목 | 파일 | 초록 근거 (파이프 없이 종료 코드 직독) |
|---|---|---|---|
| `b37e034` | 계획 (단독 커밋) | `xx_harness-r2-fixes-plan.md` | ruff 0 · format 0 |
| `de689ff` | **A-5** 환경변수명 정정 · **A-6** 주석 예산 CI 배선 | 레인 A 계획 · `.github/workflows/ci.yml` | ruff 0 · format 0 · mypy 0 · pytest exit 0, 1667 passed |
| `d06f5c5` | **A-3** 중간 축 결손 (codex ⑵) | `tests/test_harness_scope_reach.py` | 대상 파일 exit 0, 45 passed |
| `545f706` | **A-1** 무효 커버리지 행 지목 (C-1) | `tests/test_review_coverage_reach.py` | mypy 0 · pytest 1 failed¹ 1667 passed |
| `60fadff` | **A-2 ⑴⑵⑶** X-3b 분모 (C-2·R-1·R-2) | 같음 | pytest 1 failed¹ 1668 passed |
| `3b7ba16` | **A-2 ⑵ 보강** — 열 단언을 자리로 | 같음 | pytest 1 failed¹ 1668 passed |
| `fb7d01c` | **A-4** `이연` 상환 요구 (codex ⑴) + 축 9종 등재 | 위 두 파일 | pytest 1 failed¹ 1669 passed · 출하 모드에서 새 축 통과 |
| `06ea048` | **P-1** X-11 강제자 + SKILL.md 문면 교체 | `tests/test_harness_scope_reach.py` · SKILL.md | pytest 1 failed¹ 1670 passed |
| `a304cc1` | **P-3** 하한 라쳇 사유 분리 | `tests/test_kotlin_gate_reach.py` | 대상 파일 exit 0, 304 passed |
| `d20447f` | 계획에 실행 중 갈린 점 6 · 한계 3 (사후 기재) | 계획 문서 | — |
| `ecfb2f7` | 음성 대조 A1-e (리더 통지 실사건) 기재 | 계획 문서 | docs 전용 |

¹ 부트스트랩 빨강 — 지목 대상은 이 레인의 비-docs 8건뿐(리더가 `103f565` 로 닫음).

최종 전건(`d20447f`): ruff 0 · format 0 · mypy 0 (145 files) · pytest 1 failed¹ 1670 passed.

## 2. 음성 대조 표 (저장소 밖 일회용 worktree · 판정 = 게이트 exit code)

| ID | 변이 | 처방 전 | 처방 후 — 지목 |
|---|---|---|---|
| A3-a | 두 스킬에서 축⑤ 함께 삭제, 번호 유지 | exit 0, 45 passed | exit 1 — 두 파일 경로 + 빠진 축 이름 |
| A1-a | 없는 산출물 경로 | exit 0, 4 passed | exit 1 — `산출물이 HEAD 에 커밋돼 있지 않다: [...ghost-reviewer.md]` + 행 원문 |
| A1-b | 다른 회차 어간의 실재 산출물 | exit 0 | exit 1 — `산출물 칸이 규약을 어겼다 — reviews/xx_harness_<리뷰어>.md 여야 한다` |
| A1-c | codex 산출물 없는 역할 구성 | exit 0 | exit 1 — `필수 역할이 선언되지 않았다: ['codex-reviewer']` |
| A1-d | 과거 산출물로 미래 범위 승인 | exit 0 | exit 1 — `시작 커밋 01d3c48 에 이미 있었다` + 3경로 |
| **A1-e** | **리더 실증 재현** — 이 회차 `cross` 산출물이 HEAD 에 없는 트리 | 평시 exit 0(신호 0) / 출하 모드 exit 1 인데 메시지는 `리뷰도 장부도 없는 커밋 28 건`뿐 | exit 1 — `산출물이 HEAD 에 커밋돼 있지 않다: ['reviews/xx_harness-r2_cross.md']` |
| A2-a | 리뷰된 `대기` 행 상태 칸 `**대기**(미리뷰)` | exit 0, 5 passed | exit 1 — 탈락 24행 + `리뷰 범위에 든 대기 행이 0 건` |
| A2-b | 머리행 `닫힘` → `닫은 회차` 개명 | exit 0 | exit 1 — `자리 5: 기대 닫힘 · 실제 닫은 회차` |
| A2-d | `리뷰할 회차`↔`닫힘` 통째 맞바꾸기 | exit 0 | exit 1 — 자리 4·5 양쪽 지목 |
| A4-a | 비면제 미리뷰 6커밋을 `이연`·`닫힘` 공란 + 출하 모드 | exit 0, 6 passed | exit 1 — 6행 전부 SHA |
| A6-a | `tests/test_kotlin_comment_budget.py` 삭제 | 형제 5개 스텝 exit 0 | 신설 스텝 exit 4 (`no tests ran`) |
| P1-a | 칸을 산문 `xx_harness 2회차` | exit 0, 45 passed | exit 1 — 20행, `백틱 어간 하나가 아니다` |
| P1-b | 백틱 없이 `xx_harness-r2` | exit 0 | exit 1 |
| P1-c | 문법 이전 핀 어간 `` `16_gate15-fixes` `` | exit 0 | exit 1 — 핀이 이 칸으로 새지 않음 |
| P3-a | 이력에 없는 새 하한 상수 | exit 1, `판정 불가` 덩어리 | exit 1 — `미커밋 신규 상수 1 건` + 이름·값 |
| P3-b | 기존 핀 이름 드리프트 | exit 1, `현재 파일에서 못 찾음` | 불변 — 두 사유가 갈림 |

A1-e 가 가장 무거운 근거 — C-1 의 오독 경로가 합성이 아니라 실제로 일어났고 평시 실행은 신호 0 이었다.

## 3. 층수·장치 분류

| 항목 | 장치 | 층수 |
|---|---|---|
| A-1 | 탐지형 | 2 |
| A-2 ⑴⑵⑶ | 탐지형 | 2 (⑵⑶ 기존 층 안) |
| A-3 | 탐지형 | 0 신설(단언 1) |
| A-4 | 탐지형 | 1 |
| A-6 | 강제(배선) | 0 신설 |
| P-1 | 탐지형 | 2 |
| P-3 | 강제·표현형(강도 불변) | 0 신설 |

4층 없음. 은폐형 신설 0. `PRE_GRAMMAR_STEMS` 를 P-1 소비자로 넓히지 않았다(P1-c 가 음성 대조).

## 4. 계획과 달라진 점

1. **A-2 ⑵ 는 교차 문면 그대로는 도달 0** — 기존 단언이 절 본문 전체의 부분문자열 검사라 데이터 칸이 늘 참. 종전 세 열도 같은 형태로 도달 0. `_header_drift()` 로 머리행 자리마다 대조(`3b7ba16`).
2. 커밋 순서 변경 — A-2 ⑴ 의 `_table_data_lines()` 를 A-1 이 필요로 한다.
3. 커버리지 표에는 행 수 대조를 두지 않았다 — A-1 이 같은 것을 행·사유까지 지목(규칙 7).
4. A-4 를 필터 조이기가 아니라 직접 단언으로 — 필터를 조이면 엉뚱한 커밋 이름으로 실패한다.
5. P-1 에서 핀 19종 비적용.
6. 착수 실측이 심판문 시점과 달랐다 — `37f2962` 로 미상환 0, 커버리지 5행, 출하 모드 이미 초록. 세 거짓 초록 경로가 처음 하중을 받는 시점이었다.
7. A1-e 추가(`ecfb2f7`).

## 5. 한계 (닫지 않음)

1. 「산출물 내용이 참인가」 미측정 — `xx_harness` 반례는 여전히 통과.
2. 보안 불변식 축 미검토.
3. A-4 의 오늘 하중 0(`이연` 행 0건). 도달은 첫 `이연` 행 기재 시점.
4. A-2 ⑶·P-1 은 「오늘의 실물이 참」에 기대어 켜졌다 — 전제가 정직하게 무너지는 날 거짓 빨강이 되고 그때 조치는 사유를 리뷰에 올리는 것.
5. `_header_drift` 는 절의 첫 표 머리행만 본다.
6. P-3 은 스칼라 하한 축 하나. 표 라쳇 축의 `assert history` 는 미접촉.
7. P-1 은 문법만 재고 회차 실재는 안 잰다(SKILL.md `:174` 명시).
8. **원장 장부 표가 빈 줄로 두 조각** — `parse_tables` 로 읽으면 뒤 13행이 조용히 빠진다 → 리더가 같은 날 빈 줄 제거로 닫음(이 보고와 같은 커밋).

## 6. 못 끝낸 것

원장 기재(리더, `103f565` 로 닫힘) · 지시대로 안 한 것: A-7(M-3) · P-2 · §7.2①(리더 백로그) · 푸시 안 함.
