# 게이트 16 · 3단계 교차 종합 — `16_gate15-fixes`

작성 2026-08-18 · `migration-reviewer` **2회차(교차 종합 전용)** · 대상 `614afed..1cb7bdf` (6 커밋)

> **이 회차는 대조만 한다.** 두 산출물에 없던 새 지적을 만들지 않는다. 종합 중 눈에 띈 것은
> §9 「종합 중 발견 — 미교차」에 분리했고, 다음 게이트 범위로 제안한다.
> **1차 산출물의 심각도를 재판단해 올리거나 내리지 않았다** — 양쪽 심각도가 갈리는 자리는
> 삭제하지 않고 §5에 병기했다.
>
> **리더에게 올리는 정본은 이 문서다.** 1차 산출물만으로 Phase 종료·착수를 판정하지 않는다.

---

## 1. 대조한 두 산출물

| 회차 | 레인 | 경로 | 크기 | 결론 |
|---|---|---|---|---|
| 1단계 | `codex-reviewer` | `docs/migration/_workspace/reviews/16_gate15-fixes_codex-reviewer.md` | 27,892 B | `needs-attention` · **high 3건**(K16-1·K16-2·K16-3) · exit 0 |
| 2단계 | `migration-reviewer` 1회차 | `docs/migration/_workspace/reviews/16_gate15-fixes_migration-reviewer.md` | 42,396 B | **차단② 1**(T-E) · 수정 필요 4 · 권고 10 · 이월 6 |

**어간 정합**: 세 파일 모두 `16_gate15-fixes`. 리더 지정값을 그대로 썼고 새로 만들지 않았다.

**독립성**: 1차 두 레인은 서로를 보지 않고 병렬 작성됐다. codex 레인은 로그 20행에서 과거 게이트
지침을 메모리로 참조했다고 **스스로 밝혔고**(codex §4.3-8) 같은 문장에서 "과거 결론은 재사용하지
않는다"고 적었다 — 완전한 맥락 무지가 아니었다는 사실을 기록으로 남긴다. 이 회차의 판정에서
codex 지적은 전부 **현재 배치의 코드 근거로 재대조**했으므로(§4) 메모리 참조가 판정을 오염시킨
자리는 확인되지 않았다.

### 1.1 관점 수 표기 — 이 게이트의 가장 중요한 한계

| 검증 | Claude | codex | **관점 수** |
|---|---|---|---|
| 음성 대조 ⓐ 잠금 제거 → `FlywayBaselineGuardTest` 빨강 | **재현** (3회 반복, 10/2 failures) | **미수행** (샌드박스 EPERM) | **단일 관점** |
| 음성 대조 ⓑ parity 본류 호출부 제거 → 빨강 | **재현** (`:1984`→2 failed · `:2282`→1 failed) | **미수행** (동일) | **단일 관점** |
| 음성 대조 ⓒ 구판 생성기 복원 → 4 failed | **재현** (4 failed / 1 passed) | **미수행** (동일) | **단일 관점** |
| **T-E** (러너 빈 문자열 → exit 0) | **실측 판정** | 안 봄 | **단일 관점 (차단②)** |

**세 음성 대조의 재현 결과 자체는 유효하다** — 일회용 worktree에서 실행됐고 절차가 기록됐다
(1차 §5.3). 그러나 **어느 것도 두 관점의 확인을 받지 못했다.** codex는 이 미수행을 스스로
"커밋 메시지의 재현 주장은 독립 검증으로 인정하지 않았다"로 적었고 `Next steps` 첫 항목으로
쓰기 가능한 환경에서의 재실행을 요구했다.

**T-E(차단②)도 단일 관점 판정이다.** 게이트 15의 X3와 정확히 같은 상황이며, 단일 관점이라는
사실이 심각도를 낮추는 근거가 아니다.

---

## 2. 요약 — 합의 / 충돌 / 단독

| 상태 | 건수 | 항목 |
|---|---|---|
| **합의** | **8** | A · E · G · H · J · K · L · W |
| **충돌** | **1** | F (floor 정본의 생성기 내부 배치) |
| **codex 단독** | **2** | B · I |
| **Claude 단독** | **12** | C · D · M · N · O · P · Q · R · S · T · U · V |
| 합계 | **23** | — |

**차이가 값을 낸 자리**: codex 단독 2건 중 **B(러너 밖 외부 파이프)**는 Claude가 "검토함 — 지적
없음"으로 닫은 구획(R-2)을 **좁혔고**, **I(F3 kdoc의 과대 선언)**는 Claude가 행 번호만 대조하고
문면은 대조하지 않은 자리를 짚었다. 게이트 14 교차 종합에서 관찰된 것과 같은 형태다 —
**완전 수령의 효과는 「합의 확인」이 아니라 Claude의 통과 판정을 되여는 것이다.**

---

## 3. 교차 대조표

> 심각도는 **양쪽이 부여한 값을 그대로 병기**한다. 이 회차에서 새로 매기지 않았다.
> 「도달 범위」 축에서 걸린 지적은 어느 축에서 걸렸는지 괄호로 덧붙였다.

| # | 항목 | 근거 위치 | 축 | Claude | codex | **상태** | 심각도 (Claude / codex) | 마감 | 관련 종료 조건 |
|---|---|---|---|---|---|---|---|---|---|
| **A** | 러너에 소비자·회귀 테스트가 0건 — 삭제해도 아무것도 빨개지지 않는다 | `run_gate.sh` 전역 · `ci.yml` 참조 0 | 도달 범위(테스트) | **T-G** + R-9 | **K16-1 후단** | **합의** | 수정 필요 / high 1.00 | 러너를 게이트 근거로 쓰기 전 | §6 규칙 6 |
| **B** | 러너를 경유해도 **러너 밖 외부 파이프**가 실패를 다시 삼킨다 | `run_gate.sh:18-21`(선언) · 셸 우선순위 | 도달 범위(테스트) | **안 함** (R-2에서 표기 정직성만 검토) | **K16-1 전단** | **codex 단독** | — / high 1.00 | 러너를 게이트 근거로 쓰기 전 | §6 규칙 5 규약 |
| **C** | 빈/공백 문자열 인자 → 아무것도 실행하지 않고 `exit 0` | `run_gate.sh:30`·`:35` | 도달 범위(테스트) | **T-E** | 안 함 | **Claude 단독** | **차단②** / — | **즉시** (러너를 게이트 근거로 쓰기 전) | §5 Phase 7 즉시중단 기준(장치) |
| **D** | `cmd="$*"` 재조립에서 인용 경계·연속 공백 소실 | `run_gate.sh:35-37` | 도달 범위(테스트) | **T-F** | 안 함 | **Claude 단독** | 수정 필요 / — | C와 같음 | §6 규칙 5 |
| **E** | `PROMPT_CASE_FLOOR` **빈 선언이 통과한다** (섹션 단위 자기 축소) | `dump_python_snapshots.py:280` | parity | **P-B⑴** (postprocess 29→1 실측) | **K16-2** (system_prompts 6→1 프로브) | **합의 — 독립 재현 2건(서로 다른 섹션)** | 수정 필요 / high 0.99 | **`app/**` 삭제 전** | §6 X1 · CLAUDE.md 규칙 4 |
| **F** | floor 정본을 **검사 대상 생성기 안 상수**로 둔 결정 | `dump_python_snapshots.py:113` | 도달 범위(parity) | **P-A⑶ — 정당하다** | **K16-2/④c — 자기채점이다** | **⚠ 충돌** (§5.1) | 지적 없음 / high 0.99 | **리더 판정** | §6 X1 · 규칙 4 ⑶ |
| **G** | 하한 미등재 케이스를 아무도 탐지하지 않는다 (케이스 정체성 미고정) | `test_python_snapshot_guard.py:161-175` | parity | **P-B⑵** | **K16-2** ("독립 정체성은 고정하지 않는다") | **합의** | 수정 필요 / high 0.99 | E와 같음 | §6 X1 |
| **H** | **F3 다섯 필드의 실행 가능한 강제자가 0이다** | `FrameworkErrorContractTest.kt:130-182` | 계약 준수 | **§4.1 X6 「부분 해소 — (a) 강제자 0이 열려 있다」** + R-10 | **K16-3** | **합의** | 부분 해소(권고 R-10) / high 0.99 | **Phase 3 해당 DTO 구현 커밋** | §5 Phase 3 · §6 Contract |
| **I** | F3 kdoc이 다섯 필드를 **모두 "정규화 후"**라 적는데 정본은 2/5가 원시 | `FrameworkErrorContractTest.kt:137` vs `easy-doc-v1.yaml:395`·`:400` | 도달 범위(계약 준수) | **안 함** (C-F는 행 번호만 대조) | **K16-3 부속** | **codex 단독** | — / high 0.99(모항목) | H와 같음 | §6 Contract |
| **J** | X-F11~F13이 실행 소스에 0건 — Markdown 계획뿐 | test-plan · preflight (2파일) | 테스트 적정성 | **R-9**(`안 돎`) + **R-10**(계획을 강제자로 소개) | **K16-3** ("Markdown 항목을 강제자라고 분류하지 마라") | **합의** | 권고 / high 0.99(모항목) | H와 같음 | §6 Contract 게이트 |
| **K** | CI 새 스텝은 **3개**다 (4개 아님) | `ci.yml` · `04ced00`(2) + `4cba492`(1) | 도달 범위(계약 준수) | **R-1 표는 3 신규**(헤딩만 "4개") | **정정함** (3개, 전부 `quality` 잡) | **합의(데이터)** — **리더 전제 오류** | 지적 없음 / summary 정정 | 즉시(기록 정정) | §6 게이트 배선 |
| **L** | X3 진단 자체는 옳다 | 게이트 15 X3 | 도달 범위(테스트) | 전제로 수용 | **"진단 자체는 맞지만 대응은 닫히지 않았다"** | **합의** | — / — | — | 게이트 15 X3 |
| **M** | `type: "missing"` 고정의 출처가 계약이 아니라 구현이다 | `FrameworkErrorContractTest.kt:159` · `GlobalExceptionHandler.kt:392` | 계약 준수 | **C-B** | 안 함 | **Claude 단독** | 권고 / — | Phase 3 계약 테스트 | §6 Contract |
| **N** | 「전사 금지」 규약이 자기가 다스리는 세 행에 도달하지 않는다 | test-plan `:137`·`:139`·`:82` · preflight `:166`·`:168` | 도달 범위(계약 준수) | **C-C** | 안 함 | **Claude 단독** | 수정 필요 / — | **Phase 3 첫 계약 테스트 작성 전** | §6 · 규칙 4(범위 선언형) |
| **O** | `internal companion object`가 근거(상수 1개)보다 넓게 4개를 연다 | `FlywayBaselineGuard.kt:197`·`:204-214` | Kotlin 관용성 | **K-B** | **안 함**(축 ④a 미판정) | **Claude 단독** | 권고 / — | Phase 3 내 | §3.2 |
| **P** | 탐침의 "**모두** 이 자리를 지난다" 선언이 실제 도달보다 넓다 (구멍 2) | `MigrationStatementTracer.kt:132-133` | 도달 범위(Kotlin) | **K-C** | **안 함**(축 ②c 미판정) | **Claude 단독** | 권고 / — | Phase 3 내 | §4.2 |
| **Q** | 본류 호출부가 한 곳뿐인 게이트 helper **8자리 중 2자리만** 회귀를 얻었다 | `compare_parity.py` 8 호출선 | 테스트 적정성 | **T-D⑵** (판정 요청) | 안 함 | **Claude 단독** — 단 **게이트 14 R-4⑴이 1자리를 변이 실측**(§6.1) | 권고+판정 필요 / — | **리더 판정** | §6 X2 · 규칙 4 |
| **R** | Phase 배치 표의 이중 등재 (X-F10~13이 Phase 3·4 동시) | test-plan `:293`·`:294`·`:311-318` | 계약 준수 | **C-D** | 안 함 | **Claude 단독** | 권고 / — | Phase 4 착수 전 | §5 |
| **S** | 스냅샷의 두 번째 소비자가 함께 축소된다 | `dump_parity_fixtures.py:1423` | parity | **P-C** | 안 함 | **Claude 단독** | 권고 / — | E와 같음 | §6 X1 |
| **T** | 러너가 실행 명령 전문을 stdout에 찍는다 (비밀값 전제 미기재) | `run_gate.sh:36` | 보안 불변식 | **S-B** | 안 함 | **Claude 단독** | 권고 / — | 러너 실사용 전 | §2.3 |
| **U** | `MIN_CRITICAL_STATEMENTS = 6`은 전체 사망만 가른다 | `FlywayBaselineGuardTest.kt:42` | 테스트 적정성 | **K-D** | **안 함**(축 ③a 미판정) | **Claude 단독** | 권고 / — | Phase 3 내 | §6 |
| **V** | TOCTOU 음성 대조의 750ms 시간 의존이 조용히 힘을 잃을 수 있다 | `FlywayBaselineGuardTest.kt:52` | 테스트 적정성 | **K-E** | 안 함 | **Claude 단독** | 권고 / — | Phase 3 내 | §6 |
| **W** | preflight가 **계획을 「강제자」로 소개한다** (상태 열 없음) | preflight `:161`·`:163-170` | 도달 범위(계약 준수) | **R-10** | **K16-3** (같은 취지) | **합의** | 권고 / high 0.99(모항목) | Phase 3 착수 시 | §6 |

**각주 — 표현이 달라 한 행으로 묶은 자리**

- **A**: Claude는 "러너 자신이 어떤 검사 대상에도 없다(셸 린트 0·계약 테스트 0·참조 2건)"로, codex는
  "전체 검색에서도 SKILL 문장과 스크립트 자체 외 소비자·회귀 테스트가 없었다 … 이 파일을 삭제해도
  아무것도 빨개지지 않아"로 적었다. **같은 대상·같은 결론.**
- **E**: Claude는 `postprocess` 하한을 `()`로 비워 29→1 축소가 `--check exit 0`으로 통과함을 실측했고,
  codex는 `system_prompts` 하한을 빈 튜플로 바꿔 6→1 축소 후 재생성 결과가 **바이트 동일**함을
  메모리 프로브로 확인했다. **서로 다른 섹션에서 같은 기제가 재현됐다** — 이 게이트에서 두 관점이
  독립적으로 확인한 유일한 실측이다.
- **H·J·W**: codex는 셋을 K16-3 한 건에 묶었고, Claude는 §4.1 X6 행 · R-9 · R-10 셋으로 나눴다.
  **같은 결함의 서로 다른 절단면**이라 세 행으로 펼치되 codex 심각도(high)를 각 행에 병기했다.

---

## 4. codex §4.3 「전제 확인 필요」 8건 — 코드 대조 판정

> **기존 지적의 근거 검증이며 새 지적이 아니다**(리더 규칙 3). 대조는 `1cb7bdf` 기준.
> 실행이 필요한 항목은 **일회용 worktree**(`git worktree add --detach … 1cb7bdf`)에서 돌리고
> `git worktree remove --force`로 폐기했다. `cp` 복원 없음. 본 저장소 무변조(§10).

| # | codex 전제 | **판정** | 대조 근거 |
|---|---|---|---|
| **1** | `run_gate.sh:18-21`이 "CI 배선 0 · 비경유 탐지 불가" 자기 선언 구간이다 | **참** | `:18` "자기 도달(정직하게): local" / `:19` "CI 배선 0. 이 러너는 경유하지 않은 파이프 명령을 탐지하지 못한다" / `:20-21` "규약 자체의 강제자가 아니라 … 정직하게 만드는 장치까지만이다". **행 인용 정확** |
| **2** | 비인용 외부 파이프가 bash·zsh 모두 outer status 0 | **참 — 독립 재현** | worktree 실측: 인용 `run_gate.sh 'false \| tail -1'` → **outer 1** / 비인용 `run_gate.sh false \| tail -1` → **bash 0 · zsh 0** / 대조군(러너 없이) → 0 / 러너 직접(파이프 없음) → 1. **codex 수치와 완전 일치.** 사용 형태의 현실성은 §5.2 |
| **3** | `PROMPT_CASE_FLOOR` 프로브 + "X1의 45건" | **참 (양쪽)** | ⑴ `_floor_checked_cases:280` `missing = [n for n in PROMPT_CASE_FLOOR[section] if n not in present]` — 하한이 `()`면 `missing=[]`이라 통과. codex의 "키는 있고 선언만 빈 상태는 통과"·"빈 dict·키 누락은 KeyError" **둘 다 코드와 일치**(`[section]` 인덱싱). ⑵ AST 파싱 실측 하한 총계 = **45**(6+5+5+29) — codex의 "45건"과 Claude P-A⑵ 표가 **같은 수치**를 가리킨다 |
| **4** | `FrameworkErrorContractTest.kt:145-180` 행 인용 | **부분 참 — 실질 주장은 참** | 인용 구간이 두 테스트에 걸친다: kdoc 꼬리 `:145-148` + `Bean Validation 실패는 422 배열이다` `:149-164` + `서비스 층 길이 위반은 422 문자열이다` `:166-182`. codex 문면 "합성 `InvalidInputException` **한 건**"은 **둘째 테스트만** 서술해 첫째(합성 `probe` Bean Validation)를 빠뜨렸다. 그러나 **실질 주장(실제 F3 DTO·경계값을 거치지 않는다)은 두 테스트 모두에 참**이다 — 경로가 `/__probe/bean-validation`·`/__probe/domain/invalid-input`으로 **둘 다 합성 프로브**다 |
| **5** | F3 주석이 "모두 정규화 후"라 적는데 정본은 2/5가 원시 | **참 — 대상 특정 완료** | **대상은 `FrameworkErrorContractTest.kt:137`**: "다섯은 **서비스 층에서 정규화한 뒤** 판정하고". 정본 `easy-doc-v1.yaml`: `:390` email=정규화 후 · **`:395` password="원시 값 — 정규화하지 않는다"** · **`:400` text="원시 값 — 정규화하지 않는다"** · `:405` edited_text=정규화 후 · `:410` name=정규화 후. **3 정규화 / 2 원시** — codex 서술과 정확히 일치. 부속 주장(test-plan이 `text`의 원시 축을 미커버로 남김)도 **참**: test-plan `:175` `\| [2] DocumentTextRequest.text \| X-F1 \| X-F1 \| — (원시 · X13 미결) \|` |
| **6** | X-F11~F13이 실행 소스 0건, Markdown 계획뿐 | **참 — 그리고 정본 어휘가 맞다** | `git grep -E 'X-F1[123]'` 전 저장소 → **2파일뿐**: `00_contract-keeper_test-plan.md` · `03_kotlin-implementer_phase3-preflight.md`. Kotlin·Python 실행 소스 **0건**. 식별자는 test-plan의 X-F* 체계(X-F1·X-F9·X-F10과 동일 계열) — **이 하네스의 정본 어휘가 맞다** |
| **7** | CI 새 스텝은 4개가 아니라 3개 | **참 — 리더 전제 오류 확정** | 커밋별 `ci.yml` 추가 `run:` 실측: `65a7eb6` 0 · `526bfeb` 0 · `f9d78e0` 0 · **`04ced00` 2**(`dump_python_snapshots.py --check` · `pytest tests/test_parity_ci_gate.py`) · **`4cba492` 1**(`pytest tests/test_python_snapshot_guard.py`) · `1cb7bdf` 0. **합계 3.** 전부 `quality` 잡 — codex의 잡 귀속도 참. §7 참조 |
| **8** | codex가 메모리 레지스트리를 참조했다 | **참 — 기록만** | codex 자신이 로그 20행에 적었다. 판정 오염은 확인되지 않았다(§1) — codex 지적 3건이 전부 이 회차의 코드 대조로 독립 확인됐다(#1·#2·#3·#4·#5) |

**판정 요약: 8건 중 7건 참 · 1건 부분 참(#4 — 문면이 두 테스트 중 하나만 서술하나 실질 주장은 참).
codex 지적 K16-1·K16-2·K16-3 중 전제 오류로 무효화되는 것은 없다.**

### 4.1 ⚠ 리더 전제 오류 — 「새 CI 스텝 4개」

리더 프롬프트가 codex 레인에 전달한 "이 배치가 넣은 새 스텝 **4개**"는 **틀렸다.** 실제 신규는
**3개**(`04ced00` 2 + `4cba492` 1)이며 codex가 정정한 값이 맞다.

**게이트 15 별건 1과 같은 형태다** — 검증하지 않은 값이 프롬프트를 타고 하위 레인으로 내려갔다.
codex 레인은 §1.3에서 "프롬프트의 4개는 리더 지시문에서 온 값이며 내가 검증하지 않고 전달했다"고
**전달 경로를 정직하게 기록했고**, codex는 그 전제를 받지 않고 스스로 세어 정정했다. 두 방어가
모두 작동했다.

**Claude 1차 산출물의 상태**: R-1 **헤딩**은 리더 전제를 그대로 옮겨 "새 CI 스텝 4개"라 적었으나,
**같은 절의 표는 신규를 3개(idx 7·11·12)로, 나머지 2개(idx 9·10)를 「기존」으로 정확히 구분**한다.
즉 **데이터는 맞고 헤딩만 전제를 물려받았다.** 그래서 이 항목은 실질 합의(표 K)로 분류하되,
헤딩 문구 정정을 §8 조치에 넣는다.

---

## 5. 충돌 — 양쪽 근거 병기 · **리더 판단 요청** · 어느 쪽도 삭제하지 않는다

### 5.1 충돌 F — `PROMPT_CASE_FLOOR`를 **생성기 안 상수**로 둔 결정

> **주의: 두 레인은 증상에 대해서는 합의했다**(표 E·G — 빈 선언 통과 / 미등재 미탐지).
> 충돌하는 것은 **근본 진단과 처방의 위치**다.

**Claude 근거 (1차 P-A⑶ — 실측)**

> "케이스 이름 6종을 `app/`·`tests/`에서 grep한 결과 **새 가드 테스트 1건 외 0건**이다. 따라서
> **열거의 정본을 생성기 안 고정 상수로 둔 결정은 정당하다** — `STYLE_CONSTANTS`와 같은 층이라는
> 설명도 맞다. 재계산할 상류가 실제로 없는데 상류를 요구하는 것은 **근거 없는 요구다.**"

**codex 근거 (K16-2 / 축 ④c 원문)**

> "검사는 `PROMPT_CASE_FLOOR[section]`에 열거된 이름만 요구하므로 **범위 선언과 판정기가 같은
> 파일에 있다.** … 현 회귀는 섹션 키 집합과 스냅샷의 비어 있지 않음만 보며 system/user/repair의
> **독립 정체성은 고정하지 않는다.** … 따라서 X1의 45건 범위를 소수만 남기고도 게이트와 회귀를
> 함께 초록으로 유지할 수 있다."
> **처방**: "**생성기 밖의 독립 정본**에 전체 케이스 정체성을 고정하고 각 섹션의 비어 있지 않은
> 하한과 정확한 필수 ID를 대조하라."

**제3 근거 (이 회차 코드 대조 — 어느 쪽도 지우지 않고 사실만 더한다)**

1. **생성기 밖 독립 정본이 이미 존재하지만 「그룹 키」 입도까지만이다.**
   `tests/test_python_snapshot_guard.py:37` `_SECTIONS = ("system_prompts","user_prompts","repair_prompts","postprocess")`는
   생성기 밖의 복제이고, `:161-175`가 `set(generator.PROMPT_CASE_FLOOR) == set(_SECTIONS)` 및
   스냅샷 그룹과의 동등성을 고정한다. **그러나 케이스 이름 입도의 독립 정본은 없다.**
   → codex의 "독립 정체성 미고정"은 참이고, Claude가 "의도된 이중화"라 부른 것도 참이다.
   **둘은 입도가 달라 서로를 반증하지 않는다.**
2. **Claude의 「상류 없음」 실측은 재현된다.** 6종 중 5종은 `app/`·`tests/` 히트 0,
   `double_preamble_both_removed` 1건은 **새 가드 테스트 자신**이다. 상류가 실제로 없다.
3. **두 처방의 효과 차이**: Claude 처방(가드 테스트에 `set(floor[sec]) == {c["name"] for c in doc[sec]}`
   추가)은 **⑴ 빈 선언·⑵ 미등재를 둘 다 닫는다** — 생성기 밖에서 대조하되 정본을 새로 만들지
   않는다. codex 처방(생성기 밖 독립 정본 신설)은 같은 두 증상을 닫으면서 **상류가 없는 값을 위한
   제2 정본을 만든다** — 이 저장소가 반복해 겪은 **이중 기재 드리프트**를 새로 여는 방향이다.

**리더 판단 요청.** 쟁점은 "빈 선언이 통과하는가"(합의됨 — 통과한다)가 아니라
**"생성기 안 상수 배치 자체가 규칙 4 ⑶ 위반인가, 아니면 상류 부재로 정당화되는가"**다.
두 처방 중 하나를 고르는 판정이며, 어느 쪽을 골라도 표 E·G의 증상은 닫아야 한다.

### 5.2 겹침 판정 — K16-1이 「표기된 한계」를 넘는가 (리더 규칙 5)

**표기 원문** (`run_gate.sh:19-21`)

> "CI 배선 0. 이 러너는 **경유하지 않은 파이프 명령을 탐지하지 못한다** — 즉 「게이트 명령은 러너
> 경유 또는 파이프 금지」 규약 자체의 강제자가 아니라, **경유한 명령의 종료 코드를 정직하게
> 만드는 장치까지만**이다."

**Claude R-2 판정**: "검토함 — 지적 없음. 실측 대조: `ci.yml`에 `run_gate` 0건 → **선언과 실제가
일치한다.** 이 배치에서 가장 정확하게 처리된 자리다." (단, "이 정직성은 T-E·T-F·T-G를 상쇄하지
않는다"고 병기)

**이 회차 판정 — 부분적으로 넘는다.**

표기는 상태를 **둘**로 가른다: ⑴ 러너를 경유하지 않은 명령(못 잡는다) / ⑵ 경유한 명령(종료 코드를
정직하게 만든다). **실측된 세 번째 상태가 있다** — **⑶ 명령은 경유했는데 파이프가 러너 밖에서
구성된 경우**(`run_gate.sh false | tail`). 이 경우 사용자는 규약을 지켰다고 믿지만 outer status는
0이다(§4 #2 실측: bash 0 · zsh 0).

- 표기는 ⑶을 **이름 붙이지 않는다.** "경유하지 않은 파이프 명령"은 문면상 ⑴을 가리키고,
  ⑶에서 명령은 분명히 경유했다.
- 사용례 `:24` `run_gate.sh uv run pytest tests/x`(argv 나열형)가 **⑶으로 가는 자연스러운 확장을
  가르친다** — 출력을 줄이려면 `run_gate.sh uv run pytest … | tail -20`을 쓰게 된다. 규약을 지키려는
  사람이 정확히 ⑶에 빠진다.
- 반면 `:25`의 인용형 사용례는 안전하다(§4 #2: outer 1).

**결론**: R-2의 "선언과 실제가 일치한다"는 **CI 도달 축에서는 참**이고, K16-1은 **표기가 이름
붙이지 않은 제3 상태**를 짚었다. **모순이 아니라 R-2가 좁아진 것이다.** K16-1은 표기의 재진술이
아니라 **표기 밖의 결함**으로 분류한다.

**러너 결함 3종의 기제는 서로 다르다** — 하나로 뭉치지 않는다.

| 항목 | 기제 | 발견 |
|---|---|---|
| **C (T-E)** | 가드가 인자 **개수**(`$# -eq 0`)만 보고 **내용**을 안 본다 → 0건 실행 후 exit 0 | Claude 단독 |
| **D (T-F)** | `cmd="$*"` 결합 후 `bash -c` **재파싱** → 인용 경계 소실 | Claude 단독 |
| **B (K16-1)** | 파이프를 **호출 측 셸이 러너보다 먼저 구성** → 러너가 구조적으로 볼 수 없다 | codex 단독 |

**A(T-G / K16-1 후단)는 셋 모두의 공통 원인이다** — 러너의 계약을 고정하는 테스트가 0건이라
세 결함 중 어느 것도 자동으로 잡히지 않았다. Claude가 T-G에 적은 "**T-E는 3줄짜리 테스트로 잡혔을
결함이다**"는 B에도 그대로 적용된다.

### 5.3 겹침 판정 — K16-2와 P-B는 같은 결함인가

**같은 결함이다** (표 E·G). 두 레인이 **서로 다른 섹션**(Claude `postprocess` / codex
`system_prompts`)에서 **같은 기제**를 재현했고, 근거 위치도 같다(`_floor_checked_cases:280`).
이 게이트에서 **두 관점이 독립적으로 확인한 유일한 실측**이므로 §1.1의 단일 관점 한계가 이 항목에는
적용되지 않는다.

**갈리는 것은 처방과 심각도뿐이다** — 처방은 §5.1 충돌 F, 심각도는 Claude 수정 필요 / codex high.
**심각도 차이를 이 회차에서 병합하지 않는다**(§8에 **판정 필요**로 올린다).

### 5.4 겹침 판정 — K16-3과 X6 「해소」 표기 (**원장이 과대 표기했다**)

**세 판정이 같은 방향으로 모인다.**

| 출처 | X6 상태 판정 |
|---|---|
| 원장 `00_progress.md:458` | **「해소」** |
| Claude 1차 §4.1 | **「부분 해소 — (a) 강제자 0이 열려 있다」** |
| codex K16-3 | **"X6의 '강제자 0'과 X13 측정축 공백은 닫히지 않았다"** (high) |
| **이 회차 코드 대조** | **강제자 0은 열려 있다** — §4 #6: X-F11~F13은 Markdown 2파일에만 존재, 실행 소스 0건 |

**판정: 원장의 「해소」는 과대 표기다.** 근거는 원장 자신의 두 행이 서로 어긋난다는 것이다.

- X6의 **항목명**은 「**F3 금지에 강제자 0** + 정반대 경로가 정상으로 단언돼 있다」 — **두 축**이다.
- `f9d78e0`·`526bfeb`가 닫은 것은 **둘째 축**(정반대 경로 단언 제거 — Claude C-A가 계약 대조로 확인)과
  전파 축이다.
- **첫째 축(강제자 0)을 들고 있는 것은 X5 행**이고, 그 행의 실행 경로는 **`안 돎`**이며 원장이
  같은 칸에 "고친 것은 계획이고 계약 테스트는 아직 없다"고 **정직하게 적었다.**
- 즉 **정보 자체는 원장에 있으나, 항목명이 두 축을 담은 X6가 「해소」로 닫히면서 첫째 축의 미해소가
  상태 열에서 사라졌다.** 표를 훑는 사람은 X6를 닫힌 것으로 읽는다.

**대조 — X3 행은 같은 함정을 피했다.** `:454`는 「수정 완료 · **강제자 0 은 열린 채**」로 상태 열
자체에 미해소를 병기했다. **X6도 같은 형태로 적을 수 있었다.**

**권고(조치 §8-6)**: X6 상태를 「부분 해소 — (a) 강제자 0은 X5(`안 돎`)가 들고 있다」로 정정하고,
X6의 마감(Phase 3 해당 DTO 구현 커밋)을 상태 열에 살린다. **조항·계약 변경은 필요 없다** — 표기만
바로잡는 일이다.

---

## 6. 리더 판정 요청 2건 — codex 관점 대조로 근거 보강 (리더 규칙 6)

### 6.1 T-D⑵ — 본류 회귀를 「두 자리」로 둘까 「종류」로 닫을까

**codex 관점**: 이 자리에 **지적 없음**(축 ③c 미판정). 따라서 codex 대조로는 보강되지 않는다.
**그러나 게이트 14 교차 종합이 이 판정에 직접 쓰이는 실측을 이미 갖고 있다.**

**게이트 14 `14_floor-hardening_cross.md` §R.6.2 — R-4⑴ 변이 실측 원문**

> "⑴ `full_gate_floor_problems`의 **유일한 본류 호출선**(`compare_parity.py:2274`)을 어떤 테스트도
> 검증하지 않는다 — **미해소 — 변이로 증명**. **Y-7**: 그 한 줄을 지워도 `test_parity_ci_gate.py`
> **25 passed**, 전체 스위트 **1191 passed**. 저장소 테스트 6건이 전부
> `comparer.full_gate_floor_problems(...)`를 **직접 호출**한다(`:295·309·319·335·346·353`)."

**T-D⑵의 8자리 목록과 대조 — 정확히 겹친다.**

| # | helper | 유일 본류 호출부 | 본류 회귀 (T-D⑵) | 게이트 14 R-4⑴ |
|---|---|---|---|---|
| 1 | `runtime_problem` | `compare_parity.py:1984` | **있음(신설)** | — |
| 2 | `provenance_problems` | `:2282` | **있음(신설)** | — |
| 3 | `spec_shape_problems` | `:1207` | 없음 | — |
| 4 | `case_floor_problems` | `:2269` | 없음 | — |
| **5** | **`full_gate_floor_problems`** | **`:2274`** | **없음** | ⬅ **변이 실측 완료 — 지워도 25 passed / 1191 passed** |
| 6 | `stale_ledger_problems` | `:2329` | 없음 | — |
| 7 | `ledger_write_problems` | `:2344` | 없음 | — |
| 8 | `case_floor_additions` | `:2409` | 없음 | — |

**보강되는 판정 근거**

1. **T-D⑵의 잔여 6자리는 「구조적 의심」이 아니다 — 그중 1자리는 이미 변이로 증명된 실사 결함이다.**
   CLAUDE.md 규칙 4의 판정 기준은 "겪은 **횟수**가 아니라 **결함의 구조**"이고, 부기준은
   "빈자리를 **열거할 수 없더라도 종류로** 댈 수 있는가"다. 여기서는 **종류로 댈 수 있고**(표 8행)
   **그 종류의 한 자리가 실측으로 비어 있음이 확인됐다.**
2. **이 배치가 게이트 14 R-4의 세 갈래 중 ⑵만 닫았다.** `04ced00`이 `ci.yml` 경로 명시로 R-4⑵를
   닫았고(게이트 14 cross가 그렇게 기록), **R-4⑴은 여전히 미해소**다. 즉 **같은 결함이 두 게이트를
   연속으로 통과했다.**
3. **장치 분류상 「탐지형」이므로 규칙 4가 확대를 막지 않는다.** 은폐형(무시 패턴·면제 조항)이
   아니라 회귀 테스트 추가이므로 "은폐형은 넓히지 않는다 — 탐지형으로 갈아탄다"에 걸리지 않는다.

**따라서 이 회차의 종합 권고는 「종류로 닫는다」 쪽이다** — 다만 **판정 권한은 리더에게 있고**,
Claude 1차가 부여한 심각도(권고)를 이 회차에서 올리지 않는다. 게이트 14 cross가 R-4에 붙인 마감
표기("**판정 필요 — 게이트가 지금 매 push 마다 돈다**")를 함께 올린다.

### 6.2 X6 「해소」 판정

§5.4에서 판정했다. **codex 관점이 Claude 1차와 같은 방향으로 보강한다** — codex K16-3이
독립적으로 "X6의 강제자 0은 닫히지 않았다"에 도달했고(high), 이 회차의 코드 대조(§4 #6)가
X-F11~F13의 실행 소스 0건을 확인했다. **Claude 1차의 「부분 해소」 판정이 세 관점 중 둘 + 코드
대조의 지지를 받는다.** 원장의 「해소」만 홀로 서 있다.

**X6의 마감은 살아 있다** — Claude 1차가 적은 "Phase 3 해당 DTO 구현 커밋"이 그대로 유효하고,
codex도 같은 시점을 가리킨다("F3 다섯 필드의 실제 구현 테스트가 생기기 전에는 **Phase 3 게이트를
닫지 않는다**"). **두 레인이 마감에 대해 합의한다.**

---

## 7. 양쪽 검토 통과 / 한쪽만 검토 (리더 규칙 8)

> **"codex 지적 없음"은 "문제 없음"이 아니다.** codex 레인 §5.2가 명시했다 — "지적 없음"은
> "codex가 이 회차에 그 축에서 지적을 내지 않았다"는 **사실의 기록**이다. 따라서 아래 5개 축은
> **양쪽 검토 통과로 셀 수 없다.**

| 축 (리더가 codex에 준 항목) | codex | Claude | **판정** |
|---|---|---|---|
| ②c `MigrationStatementTracer`가 Flyway SQL을 실제로 가로채는가 | **미판정** — 계획(로그 46행)했으나 결과를 적지 않음 | **검토함**: K-A 회차 C(`record()` 본문 비움 → **1 failure**, `Expected size: 3 but was: 0`)로 **가로챈다**를 실측 + **K-C 권고**(선언 "모두"의 구멍 2) | **한쪽만 검토 (Claude)** |
| ③a `hasSize(3)`·질의 수 하한이 추적 사망을 잡는가 | **미판정** | **검토함**: 회차 C가 **잡는다**를 실측(창 개수 선단언 `:216-218`) + **K-D 권고**(`MIN_CRITICAL_STATEMENTS=6`은 전체 사망만 가른다) | **한쪽만 검토 (Claude)** |
| ③c 본류 회귀 4건의 문구 단언 설계 | **미판정** | **검토함**: **T-D 권고** ⑴문구 두 벌 ⑵같은 종류 6자리(§6.1) | **한쪽만 검토 (Claude)** |
| ④a `MIGRATION_LOCK_KEY` private→internal | **미판정** | **검토함**: **K-B 권고** — `internal companion object`가 4개를 열어 kdoc "이유는 하나뿐"이 참이 아니게 됨 | **한쪽만 검토 (Claude)** |
| ④b `1cb7bdf` 주석 `//:`→`//` 내용 보존 | **미판정** | **검토함 — 지적 없음**: R-7 `difflib` 전수 대조, 비어 있지 않은 주석 **71줄 전건 동일**, 손실 0 | **한쪽만 검토 (Claude)** |
| ⑤ 은폐형(무시 패턴·면제·억제) 신설·확대 | **답함** — "새 은폐형 지적 없음" + "떼면 무엇이 빨개지는가"에 답함(K16-1) | **검토함 — 지적 없음**: R-4 신설 0건, `.gitignore`·`pyproject` 무변경, ktlint 수동 해소(`@Suppress` 부착 아님) | **✅ 양쪽 검토 통과** |

**집계: 양쪽 검토 통과 1축 · 한쪽만 검토(Claude) 5축.**

**리더 계산에 남길 사실**: codex 프롬프트가 준 축이 5개 상한을 넘어(리뷰 축 5 × 하위 항목 14)
codex가 **하위 항목 14개 중 5개에 답하지 않았다.** codex 레인이 §5.2에서 "축 개수가 5개 상한을
넘어 얕아졌을 가능성"을 스스로 제기했다. **다음 게이트의 codex 프롬프트는 축을 좁히는 편이 낫다** —
이번에 답이 안 온 5개가 전부 Claude 단독으로 남았고, 그 5개 중 3개(K-B·K-C·K-D)에서 Claude가
실제 지적을 냈으므로 **교차 검증을 받지 못한 지적이 3건 생겼다.**

---

## 8. 조치 목록 — 심각도 순 · 마감 병기 · **착수 차단 여부는 별개 축**

> **심각도와 「Phase 3 착수를 막는가」는 다른 축이다.** 아래는 각 항목의 **마감(그 게이트가 처음
> 실제로 쓰이는 시점)**을 적고, 착수 차단 여부의 **판정은 리더에게 넘긴다.** 심각도를 "아직 안 쓰이는
> 게이트니까"로 스스로 낮추지 않았다.

### 8.1 Phase 3 **착수**를 막는 후보 — 러너 결함군 (표 A·B·C·D)

**한 장치에 결함 4건이 모였고, 그 장치의 존재 이유가 「게이트 증거를 정직하게 만드는 것」이다.**

| # | 항목 | 심각도 | 마감 | 착수 차단 판정 근거 |
|---|---|---|---|---|
| **1** | **C (T-E)** 빈/공백 인자 → 0건 실행 후 `exit 0` | **차단②** (Claude · **단일 관점**) | **즉시** — 러너를 게이트 근거로 처음 쓰기 전 | 척도의 차단② 예시("검사 대상 0건인데 성공으로 끝나는 스크립트") **그 자체** |
| **2** | **B (K16-1)** 러너 밖 외부 파이프가 실패 재삼킴 | **high** (codex) | 동일 | 러너가 닫으려던 X3 결함이 **사용례가 가르치는 형태로 되살아난다**(§5.2) |
| **3** | **A (T-G / K16-1 후단)** 러너 계약을 고정하는 테스트 0 · 셸 린트 0 | 수정 필요 / high | 동일 | **1·2를 자동으로 잡았을 장치가 없다.** 규칙 6의 정확한 대상 |
| **4** | **D (T-F)** `cmd="$*"` 인용 경계 소실 | 수정 필요 | 동일 | 게이트 명령이 의도와 다른 명령으로 바뀐다 |

**완화 사실(삭제하지 않고 병기)**: 러너의 현재 도달은 **`local` · CI 0 · 실사용 0**이다. 이 배치의
6커밋 중 러너를 실제로 경유한 검증은 **0건**이며, 원장 X3 행이 "강제자 0은 열린 채"를 상태 열에
병기했다.

**→ 리더 판정 요청**: 실사용 0이므로 「착수 자체는 가능하되 러너를 게이트 근거로 쓰는 것을 금지」와
「네 건을 닫고 착수」 중 어느 쪽인가. **4건은 한 커밋으로 닫힌다**(가드를 `$#`에서 `"$*"` 내용 검사로
바꾸고, `$# -eq 1`이면 셸 문자열·아니면 argv 직접 실행, 출력 절단 옵션 추가, 계약 테스트 3~5건 신설).

### 8.2 Phase 3 **착수**는 막지 않으나 Phase 3 **종료 전** 닫아야 하는 것

| # | 항목 | 심각도 | 마감 | 비고 |
|---|---|---|---|---|
| **5** | **H·I·J·W (X6(a) / K16-3)** F3 다섯 필드 강제자 0 + kdoc 과대 선언 + 계획을 강제자로 소개 | **high**(codex) / 부분 해소·권고(Claude) | **Phase 3 해당 DTO 구현 커밋** | **양 레인이 마감에 합의**(§6.2). codex: "실제 구현 테스트가 생기기 전에는 Phase 3 게이트를 **닫지 않는다**" — **착수가 아니라 종료를 막는다** |
| **6** | **§5.4** 원장 X6 상태 「해소」 → 「부분 해소」 정정 | 수정 필요(표기) | **즉시** | 표기만 바로잡는 일. X3 행(`:454`)이 이미 올바른 형태를 보여 준다 |
| **7** | **N (C-C)** 전사 금지 규약이 자기 세 행에 도달 못 함 | 수정 필요 | **Phase 3 첫 계약 테스트 작성 전** | 범위 선언형이 빈 채로 통과 |
| **8** | **§4.1** Claude 1차 R-1 헤딩 "새 CI 스텝 4개" → **3개** 정정 | 권고(기록) | 즉시 | 표 데이터는 이미 정확 |
| **9** | **O·P·U·V (K-B·K-C·K-D·K-E)** | 권고 ×4 | Phase 3 내 | **교차 검증 미수령**(§7) — codex 축 ④a·②c·③a 미판정 |
| **10** | **M (C-B)** `type:"missing"` 계약 근거 없음 | 권고 | Phase 3 계약 테스트 | — |
| **11** | **T (S-B)** 러너가 cmd 전문 stdout | 권고 | 러너 실사용 전 | 8.1과 같은 커밋에서 처리 가능 |

### 8.3 Phase 3 이후 — 마감이 뒤에 있는 것

| # | 항목 | 심각도 | 마감 |
|---|---|---|---|
| **12** | **E·G (P-B / K16-2)** floor 빈 선언 통과 + 미등재 미탐지 | **판정 필요** (Claude 수정 필요 / codex high — §8.4) | **`app/**` 삭제 전** |
| **13** | **F** floor 정본 배치 — **충돌**(§5.1) | **리더 판정** | 12과 같은 커밋 |
| **14** | **Q (T-D⑵)** 본류 회귀 8자리 중 6자리 — **리더 판정**(§6.1) | 권고 + 판정 필요 | **게이트 14 R-4 마감 계승 — "게이트가 지금 매 push 마다 돈다"** |
| **15** | **S (P-C)** 스냅샷 2차 소비자 | 권고 | 12와 같음 |
| **16** | **R (C-D)** Phase 배치 이중 등재 | 권고 | Phase 4 착수 전 |
| **17** | **K-F 이월 6건** X8·X9·X10(부분)·X12·X16·X17 | 이월 | 게이트 15 cross §5.2 (나) — Phase 3 내 |

### 8.4 심각도가 갈린 항목 — **판정 필요**

| 항목 | Claude | codex | 처리 |
|---|---|---|---|
| **E·G** floor 빈 선언·미등재 | **수정 필요** (마감 `app/**` 삭제 전) | **high 0.99** | **판정 필요로 올린다.** 1차 심각도를 이 회차에서 올리지 않았다 — 두 값을 병기해 리더가 정한다. 판정 재료: 이 결함이 여는 창은 **큐레이션 45건의 영구 손실**(CLAUDE.md "재개발의 유일한 영구 손실 위험")이고, **되돌릴 수 없다** |
| **H** F3 강제자 0 | 부분 해소 + 권고(R-10) | **high 0.99** | **판정 필요.** 마감에는 양 레인이 합의(§6.2)하므로 쟁점은 심각도뿐이다 |

---

## 9. 대조하지 못한 범위 · 「종합 중 발견 — 미교차」

### 9.1 codex 미수행으로 대조하지 못한 범위

| 범위 | 사유 | 결과 |
|---|---|---|
| 음성 대조 3건(ⓐⓑⓒ) | codex 샌드박스 **read-only EPERM** — `mktemp … Operation not permitted` | **단일 관점 재현으로 남는다**(§1.1). codex `Next steps` 첫 항목이 재실행을 요구한다. codex 레인은 "내가 대신 재현하지 않았다"고 적었고 그 판단은 옳다 — 대신 재현했다면 독립 관측이 사라진다 |
| 축 ②c·③a·③c·④a·④b | codex 미판정 | **한쪽만 검토 5축**(§7). Claude 지적 3건(K-B·K-C·K-D)이 교차 검증을 받지 못했다 |
| **T-E(차단②)** | codex가 러너의 빈 인자 축을 보지 않음 | **단일 관점 차단 판정**. 게이트 15 X3와 같은 상황 |

**codex 리뷰 자체는 누락되지 않았다** — exit 0, findings 3건, 잘림 없음. 위는 **축 단위 미도달**이다.

### 9.2 종합 중 발견 — **미교차** (다음 게이트 범위로 제안)

> 이 회차에서 눈에 띈 것이나 **두 산출물 어느 쪽도 교차 검증하지 않았다.** 정본 판정에 쓰지 않는다.

1. **원장 X3 행(`:454`)의 음성 대조 증거가 인용형만 덮는다.** 원장은 "실패 `pytest | tail`이 러너
   없이 zsh·bash 모두 exit 0(결함 재현), **러너 경유 시 exit 4 전파**"라 적는다. §4 #2 실측상
   "러너 경유"가 인용형이면 참이지만 **비인용 외부 파이프(⑶ 상태)에서는 outer 0**이다. 원장의
   증거 문장이 K16-1이 짚은 상태를 포함하는지 문면상 갈린다. **원장 표기 축이므로 §8-6과 함께
   다음 회차에서 다루기를 제안한다.**
2. **Claude 1차 R-1 헤딩과 표의 불일치**(§4.1) — 산출물 내부 문서 결함이며 코드 결함이 아니다.
   §8-8에 기록 정정으로 넣었다.

### 9.3 이 회차가 하지 않은 것

- **새 지적을 만들지 않았다.** §4의 8건은 codex 기존 지적의 **전제 검증**이고, §5·§6은 두 산출물에
  이미 있는 항목의 **겹침·충돌 판정**이다.
- **1차 산출물의 심각도를 올리거나 내리지 않았다.** 갈린 자리는 §8.4에 **판정 필요**로 병기했다.
- **어느 쪽 지적도 삭제하지 않았다.** 충돌 F는 양쪽 근거를 전문으로 병기했다(§5.1).
- **Phase 종료·착수를 판정하지 않았다.** 마감만 적었고 판정은 리더 몫이다.

---

## 10. 절차 고지 (규칙 5 준수)

- 실행 검증은 **일회용 `git worktree`**(`--detach 1cb7bdf`)에서만 했고
  `git worktree remove --force`로 폐기했다. `worktree list`에서 사라진 것을 확인했다.
- **`cp` 복원 없음.** 이 회차는 파일 변조 실험을 하지 않았다 — 정적 대조와 러너 호출뿐이다.
- 본 저장소 작업 트리 **무변조**: `git status --short`가 이 회차 전후로 리뷰 산출물 3종과
  기존 untracked 항목만 보인다. 추적 파일 변경 0.
- 종료 코드는 전부 **파이프 없이** 확인했다. 파이프가 필요한 자리는 서브셸 안에서 종료 코드를
  먼저 받고 그 값을 출력했다.
- **커밋하지 않았다.** `00_progress.md`는 **읽기만** 했다(§5.4의 X3·X5·X6 행 인용).
- 인용 행 번호는 전부 **`1cb7bdf` 기준**이며 `git show`로 그 커밋의 판을 직접 읽어 대조했다.

---

## 11. 리더에게 (2회차 종결)

1. **정본 경로** — `docs/migration/_workspace/reviews/16_gate15-fixes_cross.md`
2. **집계** — 합의 **8** · 충돌 **1** · codex 단독 **2** · Claude 단독 **12** (23항목)
3. **Phase 3 착수 차단 후보** — **러너 결함군 4건**(C·B·A·D). 한 커밋으로 닫힌다.
   완화 사실은 실사용 0. **판정은 리더 몫으로 넘긴다.**
4. **Phase 3 종료 차단** — H·I·J·W(F3 강제자 0). **양 레인이 마감에 합의**한 유일한 차단급 항목.
5. **리더 판정 필요 4건** — ⑴ 충돌 F(floor 정본 배치) ⑵ T-D⑵(두 자리 vs 종류 — **게이트 14
   R-4⑴ 변이 실측이 「종류」 쪽을 보강**) ⑶ E·G 심각도 ⑷ H 심각도.
6. **리더 전제 오류 1건 확정** — 「새 CI 스텝 4개」는 **3개**다. 게이트 15 별건 1과 같은 형태이며
   codex 정정이 맞다.
7. **원장 정정 1건** — X6 「해소」 → 「부분 해소」(§5.4). 세 관점 + 코드 대조가 같은 방향.
8. **관점 수 경고** — 음성 대조 3건과 T-E(차단②)가 전부 **단일 관점**이다. codex 축 5개 미판정으로
   Claude 지적 3건도 교차 검증을 받지 못했다. **다음 게이트는 codex 프롬프트의 축을 좁히기를
   권고한다** — 이번 14개 하위 항목 중 5개에 답이 오지 않았다.
