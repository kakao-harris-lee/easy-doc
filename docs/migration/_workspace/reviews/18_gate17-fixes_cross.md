# 게이트 18 — 교차 종합 (2회차 · 정본) · `18_gate17-fixes`

> **이 회차는 대조만 한다.** 두 산출물에 없던 새 지적을 만들지 않았다. 종합 중 눈에 띈 것은
> §10.1 「종합 중 발견 — 미교차」에 분리했고, 다음 회차 범위로 제안한다.
> 1차 산출물의 심각도를 임의로 올리거나 내리지 않았다 — 갈린 자리는 **병기**하고 판정을 넘긴다.

---

## 1. 대조한 산출물과 회차

| 회차 | 레인 | 경로 | 크기 | 결론 |
|---|---|---|---|---|
| **1단계** | `codex-reviewer` | `docs/migration/_workspace/reviews/18_gate17-fixes_codex-reviewer.md` | 36,266 B | verdict **`needs-attention`** · findings **high 4** · exit 0 · job `review-msyth3iy-r2mefm` · "No-ship: four independent false-green/scope-integrity defects remain" |
| **2단계** | `migration-reviewer` 1회차 | `docs/migration/_workspace/reviews/18_gate17-fixes_migration-reviewer.md` | 35,063 B | **차단 0** · 수정 필요 **5** · 권고 **6** · 검토함(지적 없음) **10** · 미검토 **1** |
| **3단계** | 이 파일 | — | 2026-08-19 · **교차 종합 전용** |

- **어간**: `18_gate17-fixes` — 리더 지정값. 세 산출물이 같은 어간이다(대조 완료).
- **대상**: `36b5ed4..318069b` — 커밋 2개(`107c8a5` 러너 zero-work · `318069b` 완전성/중복/갈래), 6파일 · +492/−60 · 새 파일 0.
- **독립성**: codex 산출물 §5.4 가 "Claude 리뷰를 수행하지 않았다"를 자기 등재하고, Claude 1차 머리가
  "codex 부재는 이 회차의 정상 상태"를 적는다. **양쪽에서 확인된다.**
- **범위 드리프트 없음**: 착수·종료 모두 `HEAD` = `318069b`(codex §1.6 · 이 대조 시점 `git rev-parse HEAD` 동일).
- **참조 정본**: `.claude/skills/kotlin-migration/SKILL.md` 「선언한 범위와 실제 도달을 대조한다」 ·
  CLAUDE.md 규칙 4·5 · 계획 §5 Phase 7 · §6 (X1·X2·X3) · `reviews/17_gate16-fixes_cross.md` ·
  `docs/migration/_workspace/00_progress.md`(`e577838` 판, **읽기만 했다**).

### 1.1 이 회차가 **추가로 실행한 것** (제3 근거 — 지적이 아니다)

에이전트 규약상 2차는 대조만 하지만 **상충 항목과 codex §4.5 전제 확인은 제3 근거로 보강할 수 있다**
(에러 핸들링 3항 · 리더 규칙 4). 아래는 그 목적의 실행이며 **새 지적을 만들지 않았다** —
전부 기존 지적의 재현/전제 검증이다.

| # | 확인 대상 | 방법 | 결과 |
|---|---|---|---|
| ⑴ | **X18-1** `_MAINLINE_PHRASES = ()` | 일회용 worktree 파일 변이 → 파일 전체 + `test_harness_scope_reach.py` | **재현 — 36 passed · 37 passed(전부 초록)** |
| ⑵ | **X18-2** 외부 모듈 helper 를 `main` 에 배선 | `_x18_probe_ext.py` 신설 + `compare_parity.py` import·호출 삽입 | **재현 — 36 passed.** `external_problems.__module__ = _x18_probe_ext ≠ comparer.__name__` 이라 `discovered` 에서 탈락 |
| ⑶ | **X18-3** 마커 행렬 15종 | 저장소 **실물 러너**를 변조 없이 직접 호출 | **재현 — codex 7종·Claude 4종 전건 일치**(§5 ⓒ 표) |
| ⑷ | **X18-4** `RUN_GATE_PATH` 활성 | 추적 실물을 **망가뜨린 채** 온전한 사본을 손잡이로 지정 | **false-green 성립 — 손잡이 없으면 2 failed / 있으면 23 passed** |
| ⑸ | codex §4.5-1⑴ 새 판 `23 passed` | `.venv/bin/pytest tests/test_run_gate.py` | **23 passed** — codex 미확인분 보완 |
| ⑹ | codex §4.5-1⑵ here-doc 마커 | 실물 러너 2종 | `cat <<EOF` rc 0 · `: <<EOF` rc 0 — **새 갈래 아님**(`:` 갈래와 동일) |
| ⑺ | codex §4.5-1⑶ `BASH_ENV` 마커 선점 | 쓰기 가능 FS 에서 완주 | **우회 성립 — 주석 전용 입력이 rc 2 → rc 0** (§6 ①) |
| ⑻ | codex §4.5-3 `_MAINLINE_ROOTS=()` | 변이 후 실패 단언 식별 | **`outside`·`callers[0] in …` 가 빨강, `in_roots` 는 공허** — 양쪽 진술 동시 참 |
| ⑼ | codex §4.5-4 "러너 소비자 없다" | 저장소 전수 `grep -rn run_gate` | **참** — 소비자 0(자기 테스트·SKILL.md 지목·자기 주석뿐) |
| ⑽ | codex §4.5-6 `_MAINLINE_PHRASES` 소비처 | 저장소 전수 grep | **참** — 정의 `:624` + 소비 `:720`·`:740` **3건이 전부** |

**절차 고지 (규칙 5)** — 변이는 전부 일회용 `git worktree`(`…/scratchpad/wt-x18`, detached `318069b`)에서 했고
리뷰 종료 시 `git worktree remove --force` 로 제거했다. **`cp` 복원 없음** — 전건 `git checkout -- <path>` +
**매 회 sha256 대조**로 되돌렸다. 사본 추출은 `git show` 로 스크래치패드에만 했다.
본 저장소 최종 상태: `HEAD` = `318069b` · 추적 파일 변경 **0** ·
`test_parity_ci_gate.py` `9f775c76…` · `test_run_gate.py` `0dfd29cb…` · `run_gate.sh` `86df9e74…` ·
`compare_parity.py` `984ed9e4…` — **전건 기준선 일치**. 종료 코드는 전부 파이프 없이 받았다.
원장 `00_progress.md` 는 **읽기만** 했다. 커밋하지 않았다.

---

## 2. 요약 — 합의 / 충돌 / 단독

| 상태 | 건수 | 항목 |
|---|---|---|
| **합의** | **4** | X3(마커 과대 선언) · X2b(동일 모듈 별칭류는 빨강) · T4(대조표 불완전) · T6-f(함수 정의 전용 rc 2 — 사실) |
| **충돌** | **4** | **X1**(빈 `_MAINLINE_PHRASES`) · **X4**(`RUN_GATE_PATH` 가림) · **R7**(nounset) · **S2**(SKILL.md 축약) |
| **codex 단독** | **3** | **X2a**(외부 모듈·동적 임포트) · X5(`BASH_ENV` 마커 선점) · X6(마커 truncate → 거짓 rc 2) |
| **Claude 단독** | **8** | T1 · T2 · T3 · T5 · T6 · R3 · R10(shellcheck 이월) · P1(게이트 17 ⑥⑦ 해소 판정) |
| 합계 | **19** | — |

**차이가 값을 낸 자리 — 이번에는 방향이 갈렸다.**

- codex 가 **Claude 의 통과 판정 두 개를 되열었다.** X1 은 Claude 가 R18-4 3행에서
  「도달 가능한 상태가 아니다」로 닫은 자리이고, X4 는 Claude 가 §2.1 에서
  「`delenv` 가 실제로 동작한다는 뜻」이라며 게이트 17 ⑭ 를 **해소**로 판정한 바로 그 관측이다.
  게이트 16·17 cross 가 두 회차 연속 적은 문장 — 「**완전 수령의 효과는 「합의 확인」이 아니라
  Claude 의 통과 판정을 되여는 것**」 — 이 **세 회차 연속** 재현됐다.
- 반대 방향도 있다. **Claude 단독 8건 중 5건(T1·T2·T3·T5·R3)은 codex focus 3축이 겨누지 않은 자리**이고,
  T1(문구 결속이 문자열 grep) · T2(CI 배선 탐지기가 `- run:`·블록 스칼라 미도달)는 **codex 축②·축③ 이
  물었어야 할 형태인데 codex 최종 출력에 없다.**
- **codex 가 답하지 않은 구획이 있다** — `dump_python_snapshots.py`·`test_python_snapshot_guard.py`
  (게이트 17 ⑥·⑦). "지적 없음"이 **아니라 답이 오지 않았다**(codex §5.3 자기 등재).
  따라서 **P1(⑥⑦ 해소)은 단일 관점 판정이다** — §7.3.

---

## 3. 교차 대조표

지적 하나가 한 행이다. 같은 대상을 가리키면 표현이 달라도 한 행으로 묶고 각주로 양쪽 표현을 남겼다.
**심각도 열은 두 리뷰의 값을 병기**한다(`Claude / codex`) — 어느 한쪽으로 통일하지 않았다.
`도달 범위` 축은 다섯 축 중 어디서 걸렸는지를 괄호로 덧붙인다.

### 3.1 차단 후보 / high

| # | 항목 | 근거 위치 | 축 | Claude | codex | 상태 | 심각도 (C / X) | 마감 | 관련 종료 조건 |
|---|---|---|---|---|---|---|---|---|---|
| **X1** | `_MAINLINE_PHRASES` 가 비면 **완전성 테스트와 대조군 2건이 전부 공허 통과** — 이 상수에는 어떤 단언도 붙어 있지 않다 | `test_parity_ci_gate.py:624-632` (소비처 `:720`·`:740` — **유일**) | 도달 범위(테스트 적정성) | **반대 견해** — R18-4 3행 "표가 비어도 5개는 남는다 … **도달 가능한 상태가 아니다**" | **지적함** (high) | **충돌** | 지적 없음 / **high · no-ship** | **Phase 3 착수 전**(§6 X2 게이트의 첫 실사용) | §6 X2 · SKILL.md 규칙 3(빈 선언 통과 금지) |
| **X2a** | **외부 모듈·동적 임포트 helper 가 완전성 게이트에 보이지 않는다** — 발견이 `member.__module__ == comparer.__name__` 로 제한된다 | `test_parity_ci_gate.py:979-1035`(`_call_sites` · `discovered`) | 도달 범위(테스트 적정성) | **안 함** — R18-5 의 전수 조사 목록에 **외부 모듈·동적 임포트 축이 없다**(별칭·`getattr`·`partial`·모듈 레벨·클래스 메서드만) | **지적함** (high) | **codex 단독** | — / **high · no-ship** | **Phase 3 착수 전** | §6 X2 · 규칙 1(범위는 근거를 넘지 않는다) |
| **X3** | **DEBUG 마커가 의미상 no-op 를 「실행된 작업」으로 분류**하고, 머리 주석·`:104` 오류 문구가 실제 탐지 범위보다 넓다 | `run_gate.sh:27-34`·`:90-104` · 대조표 `test_run_gate.py:22-23` | 도달 범위(테스트 적정성) | **지적함** — R18-1(「(실측)」 명령 치환 주장 절반 미재현) + R18-2(잔여 1종 선언인데 4종 이상 미탐지) | **지적함** (high) | **합의 — 증거 상보** | **수정 필요 ×2** / **high · no-ship** | **러너를 게이트 근거로 처음 쓰기 전 = Phase 3 착수 전** | §6 규칙 4·6 |
| **X4** | **기본 대상 테스트가 활성 `RUN_GATE_PATH` 를 가린다** — 그 테스트만 스스로 손잡이를 지우고, 나머지 22건은 우회 경로를 계속 쓴다 | `test_run_gate.py:94-116`(`monkeypatch.delenv`) · `_runner()` `:65-67` | 도달 범위(테스트 적정성) | **반대 견해** — §2.1 "`RUN_GATE_PATH` 를 옛 판으로 돌려도 이 테스트가 통과한다 — **⑭ 의 `delenv` 가 실제로 동작한다는 뜻**" → 게이트 17 ⑭ **해소** 판정 | **지적함** (high) | **충돌** | (⑭ 해소 근거로 사용) / **high · no-ship** | **러너 실사용 전 = Phase 3 착수 전** | §6 규칙 6(자기 검사) |

### 3.2 수정 필요 / 권고

| # | 항목 | 근거 위치 | 축 | Claude | codex | 상태 | 심각도 (C / X) | 마감 | 관련 종료 조건 |
|---|---|---|---|---|---|---|---|---|---|
| **T1** | **④ 문구 결속이 문자열 grep 이라 「주석 한 줄」로 충족된다** — 같은 커밋이 ③에서 버린 방법 | `test_parity_ci_gate.py:1090`·`:1094` (↔ `_call_sites` docstring `:979-981`) | 테스트 적정성 | **지적함** (실측 — 단언을 주석 처리해도 **36 passed**) | **안 함** — 축② 프로브는 「결속 단언 **제거** → 드리프트 허용」만 확인 | **Claude 단독** | **수정 필요** / — | **Phase 3 착수 전**(게이트 17 ④ 마감 승계) | §6 X2 |
| **T2** | **새 CI 배선 탐지기가 `ci.yml` 세 스텝 형식 중 하나만 본다** — 「배선 0」 선언의 강제자가 CI 의 3분의 1을 못 본다 | `test_run_gate.py:295` (`startswith("run:")`) | 도달 범위(테스트 적정성) | **지적함** (실측 — 블록 스칼라 안 삽입 시 **1 passed**, 들여쓴 `run:` 형태는 1 failed) | **안 함** | **Claude 단독** | **수정 필요** / — | **Phase 3 착수 전** | §6 규칙 6 · 규칙 4 근거 5번과 동형 |
| **R3** | **원장 `00_progress.md:548` 이 「9자리·음성 대조 9/9」인 채다** — 코드는 `EXPECTED_MAINLINE_HELPERS = 10` | `00_progress.md:548` ↔ `test_parity_ci_gate.py:617` | 도달 범위(테스트 적정성) | **지적함** (강제자 0 — 상수와 원장을 잇는 검사 없음) | **안 함** — 원장은 codex 대상 범위 밖 | **Claude 단독** | **수정 필요** / — | **리더의 게이트 18 후속 원장 기재** | §6 X2 |
| **T3** | **러너 계약 대조표 자신에게 강제자가 0** — 이름을 댄 테스트가 지워져도 어디서도 빨개지지 않는다 | `test_run_gate.py:12-39` (산문) ↔ `test_parity_ci_gate.py:1084-1101`(저쪽엔 실재·유일성·결속 단언 있음) | 도달 범위(테스트 적정성) | **지적함** (권고) | **안 함 — 그러나 codex 가 그 표를 수기로 검증했다**("all test names listed in the docstring exist") | **Claude 단독**(codex 실측이 근거를 보강) | 권고 / — | **러너 실사용 전** | §6 규칙 6 |
| **T4** | **러너 계약 대조표가 머리 주석 문장을 전부 덮지 않는다** | `run_gate.sh:10-12`·`:27-28`·`:43-44`·`:47-48` ↔ `test_run_gate.py:12-39` | 도달 범위(테스트 적정성) | **지적함 — 빠진 문장 3개**(자기 도달 ⑴ · 장치 분류 「면제 목록 없음」 · zsh 안전) | **지적함 — 빠진 문장 1개**("The missing substantive correspondence is **command substitution**") | **합의(사실) · 항목 상보** | 권고 / (X18-3 본문에 포함) | **러너 실사용 전** | §6 규칙 6 |
| **T5** | **「zsh 에서 불러도 안전하다」의 도달 0** — 22개 계약 테스트가 전부 러너를 `bash` 로만 부른다 | 선언 `run_gate.sh:10-12` ↔ `test_run_gate.py:75` | 도달 범위(테스트 적정성) | **지적함** (권고 · 수동 실측으로 선언이 참임은 확인) | **안 함** | **Claude 단독** | 권고 / — | **러너 실사용 전** | 게이트 17 ⑬ 잔여 |
| **R7** | **`-o nounset` 계약 변경의 명시가 머리 주석 한 곳뿐** / 그 변경이 실제로 깨뜨리는 형태 | `run_gate.sh:35-38` ⒞ · `SKILL.md:238` | 계약 준수 + 도달 범위 | **지적함(권고)** — "명시는 한 곳, **오탐 실측 11종 전부 무영향** … 지적은 **문서 도달**이지 기능이 아니다" | **지적 없음** — "nounset is explicitly documented and tested" · 단 **깨지는 형태 1종 실측**: `if [ -n "$OPTIONAL_GATE_FLAG" ]; then …; fi` 가 옛 rc 0 → 새 자식 **rc 127** | **충돌 + 증거 상보** | 권고 / 지적 없음 | **러너 실사용 전** | §6 규칙 4 |
| **S2** | **SKILL.md 축약으로 사용 지점의 두 경고(파이프 함정 · 비밀값)가 사라졌고, 대체 강제자는 산문 한 문장(도달 0)** | `SKILL.md:238` ↔ `run_gate.sh:52-55`·`:60-61` | 보안 불변식 + 계약 준수 | **지적함(권고 · 손실 기록)** — 처방 선택은 리더에게 | **지적 없음** — "rule 5 now points to the runner header without copying its values, and that canonical header exists. **No issue found there**" | **충돌(축이 다르다)** | 권고 / 지적 없음 | **리더 판단(처방 선택)** | 게이트 17 ⑨ 리더 판정(R17-3)의 부수 결과 |
| **T6** | 커밋 `107c8a5` 메시지의 「**함수** … (테스트 고정)」이 과대 — 대조표·테스트에 함수 항목이 없다 | `107c8a5` 본문 ↔ `test_run_gate.py:12-39` | 테스트 적정성 | **지적함**(권고) | **안 함** — 다만 **부수 사실은 양쪽 실측 일치**: 함수 정의 전용 입력 rc 2(**T6-f 합의**) | **Claude 단독**(사실은 합의) | 권고 / — | 이월(등재만) | — |
| **X5** | **`BASH_ENV` 가 preamble 보다 먼저 실행되며 마커 경로가 보인다** — 쓰기 가능 FS 에서 마커 선점·무력화 가능 | `run_gate.sh:97-98`(preamble·`RUN_GATE_MARKER` 전달) | 보안 불변식 + 도달 범위 | **안 봄** — S18-1 은 마커의 **비밀값 노출**만 점검(명령 문자열 미포함·잔존 0) | **지적함(미완주)** — Next steps 에만, 최종 findings 4건에는 **미포함** | **codex 단독** | — / (심각도 미부여) | **러너 실사용 전** | §6 규칙 6 |
| **X6** | 마커를 명령 안에서 **truncate 하면 거짓 rc 2** — 첫 DEBUG 기록은 못 막는다 | `run_gate.sh:102-106` | 도달 범위(테스트 적정성) | **안 봄** | **지적함(사실 기록)** — 안전 방향(오탐) | **codex 단독** | — / (심각도 미부여) | 이월(등재만) | — |
| **R10** | **shellcheck 도달 0** — 러너는 `bash -n` 까지만 | `ci.yml` 전역 · `test_run_gate.py:84-90` | 도달 범위(Kotlin/Spring 관용성 밖 — 하네스) | **이월 등재**(게이트 17 ⑮ 그대로) | **안 함** | **Claude 단독(이월)** | 사실 등재 / — | (등재만) | 게이트 17 ⑮ |

### 3.3 양쪽이 「지적 없음」/무변경으로 합의한 자리

| 항목 | 근거 | Claude | codex |
|---|---|---|---|
| **X2b** 동일 모듈 별칭·`getattr`·딕셔너리 디스패치·`partial` | `_call_sites` 가 `ast.Name` 호출만 세므로 **0-호출 실패(빨강)** 방향 | R18-5 "새로 생기면 `callers` 가 비어 **빨강**(안전 방향)" | "same-module alias/getattr/dict/partial calls **generally become zero-call failures**" |
| **`_MAINLINE_ROOTS = ()` 는 빨강** | `outside` 단언과 `callers[0] in …` 가 잡는다. `in_roots` 는 공허해진다 | R18-4 1행 (실측 A) | 축② "`_MAINLINE_ROOTS=()` failed, although its `not in_roots` check becomes vacuous" |
| **`EXPECTED_MAINLINE_HELPERS` 정확 일치는 빈 표를 잡는다** | ⑤a·⑤b · `{}` 변이 | §2.3 ⑤a·⑤b 실측 | 축① ⒜ · 축② `EXPECTED_MAINLINE_HELPERS=0` → failed |
| **본류 호출선 제거 → 대응 본류 테스트 빨강** | `compare_parity.py:2072` 등 10자리 | §2.4 **10/10** (파일 변이 + sha256 복원) | 축① ⒞ 재현 (메모리 변이) |
| **Kotlin·계약·프론트 diff 0줄** | `backend-kotlin/`·`contracts/`·`frontend/`·`app/`·`parity/` | K18-1 · C18-1 "검토함 — 지적 없음" | 대상 범위에 Kotlin 없음(focus §대상) |
| **골든셋 무영향** | 프롬프트·스타일 규칙·LLM 설정 무변경 | §8 "대상 아님" | 언급 없음(대상 밖) |

---

## 4. 충돌 항목 — 양쪽 근거 전문 병기 (**리더 판단 요청** · 어느 쪽도 삭제하지 않는다)

### 충돌 X1 — 빈 `_MAINLINE_PHRASES`: "도달 불가한 상태" ↔ "빈 선언에서 통과하는 장치"

**Claude 근거 (1차 R18-4, 3행 전문)**

> | 빈 표에서 **대조군 부정 단언**은 무엇을 부정하는가 | `_MAINLINE_PHRASES`(`:624-632`)는 표에서 유도한 10개 + **하드코딩 5개**로 이뤄져 있어, 표가 비어도
> 5개는 남는다 — **완전히 공허해지지는 않지만 절반 이하로 줄어든다.** 다만 그 상태 자체가 ⑤a/⑤b 로
> 먼저 빨개지므로 **도달 가능한 상태가 아니다** |

**codex 근거 (원문 §3.1 finding 1 전문)**

> - [high] Empty `_MAINLINE_PHRASES` makes both controls vacuously pass (tests/test_parity_ci_gate.py:624-632)
>   Reproduction: an in-memory probe set `_MAINLINE_PHRASES=()` and invoked the completeness test plus both
>   control-test bodies. Observed: completeness PASS; both controls also PASS even when their supplied output
>   contained `원장이 낡았다`. The only consumers are the loops at lines 720 and 740, so an empty declaration
>   measures zero phrases. Impact: helper failures can leak into normal/control executions without invalidating
>   their claimed attribution. Violates conditions 3 and 5.

**갈림의 성질** — 두 진술은 **다른 변이 경로**를 가정한다. Claude 는 「**표(`_MAINLINE_HELPERS`)가 비는** 경로」를
따라가 그 경로가 ⑤a/⑤b 로 먼저 막힘을 보였고, codex 는 「**`:624` 자체를 편집하는** 경로」를 따라갔다.
**후자에 대해 Claude 의 완화 근거(⑤a/⑤b)는 작동하지 않는다** — 표가 온전한 채 파생 상수만 빈다.

**제3 근거 (이 회차 실행 · §1.1 ⑴)** — codex 경로가 성립함을 확인했다.

```
_MAINLINE_PHRASES: tuple[str, ...] = ()          # :624-632 를 직접 치환
→ pytest tests/test_parity_ci_gate.py            36 passed
→ pytest tests/test_harness_scope_reach.py       37 passed
```

**저장소 전수 grep 결과 소비처는 `:720`·`:740` 둘뿐이고**(codex §4.5-6 전제 확인 완료), 이 상수를
검사하는 단언은 어디에도 없다. 같은 파일이 `EXPECTED_MAINLINE_HELPERS` 에는 정확 일치를 붙였다.

> **리더 판단 요청** — Claude 의 「도달 가능한 상태가 아니다」는 **표-경유 경로에 한해 참**이고,
> codex 의 「빈 선언에서 통과」는 **직접 편집 경로에서 참**이다. 두 진술 모두 삭제하지 않는다.
> 심각도는 **판정 필요**로 올린다(저평가된 지적은 게이트를 그냥 통과한다).

### 충돌 X4 — `RUN_GATE_PATH`: "delenv 가 동작한다" ↔ "기본 대상 테스트가 override 를 가린다"

**Claude 근거 (1차 §2.1 전문)**

> 레인 보고(3 failed / 23 passed)와 **일치**. `RUN_GATE_PATH` 를 옛 판으로 돌려도
> `test_default_target_is_the_tracked_repo_file` 이 통과한다 — ⑭ 의 `monkeypatch.delenv` 가
> **실제로 동작한다는 뜻**이다.

(이 관측이 게이트 17 ⑭ 를 **「해소」**로 판정한 근거다 — 1차 §5 표 ⑭행.)

**codex 근거 (원문 §3.1 finding 4 전문)**

> - [high] Default-target test hides an active `RUN_GATE_PATH` override (tests/test_run_gate.py:94-116)
>   Reproduction: set `RUN_GATE_PATH=/redirected/not-the-repo-runner.sh`, invoked
>   `test_default_target_is_the_tracked_repo_file` with its normal MonkeyPatch, then restored it. Observed:
>   the test PASSed because it deletes the override itself; afterward `_runner()` again returned the redirected
>   path used by subsequent tests. In the old-runner probe this produced the claimed 20 pass/3 fail count while
>   this one test alone inspected the current repository runner. Impact: a stale or injected CI/local environment
>   can test a different script while the suite still claims the tracked default was tested.
>   Violates conditions 4 and 5.

**갈림의 성질** — **관측은 동일하고 해석이 반대다.** 같은 사실(`delenv` 가 그 테스트만 손잡이를 지운다)을
Claude 는 **⑭ 수정이 작동하는 증거**로, codex 는 **나머지 22건이 다른 파일을 재는데도 초록이 되는 통로**로 읽었다.

**제3 근거 (이 회차 실행 · §1.1 ⑷)** — codex 가 시사한 false-green 을 끝까지 밀어 확인했다.

| 조건 | 결과 |
|---|---|
| 추적 실물의 zero-work 판정 블록을 `if false` 로 무력화, **손잡이 없음** | **2 failed** / 21 passed — 탐지됨 |
| 같은 파손 상태 + `RUN_GATE_PATH` = **온전한 사본**(저장소 밖, 바이트 동일 `86df9e74…`) | **23 passed** — `test_default_target_is_the_tracked_repo_file` **포함 전건 초록** |
| 파손 없음 + `RUN_GATE_PATH` = 온전한 사본 | 23 passed (22건이 사본을 쟀다) |

즉 **추적 실물이 망가진 채로도 스위트 전건이 초록**이 된다. 그 테스트의 sha256 대조는
`target.read_bytes()` 와 `(REPO / _RUNNER_REL).read_bytes()` 를 비교하는데, 손잡이를 지운 뒤에는
**둘이 같은 경로**라 심볼릭 링크만 잡고 「다른 22건이 무엇을 쟀는가」는 재지 않는다.

> **리더 판단 요청** — Claude 의 「`delenv` 실동작」과 codex 의 「false-green 통로」는 **동시에 참**이다.
> 게이트 17 ⑭ 의 「해소」 판정을 유지할지, codex 축을 반영해 재판정할지는 리더 몫이다.
> 이 회차는 **1차의 ⑭ 판정을 고쳐 쓰지 않았다**(2차 재판정 금지 규약).

### 충돌 R7 — `-o nounset`: "문서 도달 문제일 뿐" ↔ "명시·테스트 충분"

**Claude 근거 (1차 R18-7 전문 요지)**

> 자식 셸 계약이 바뀌었는데(`$1`·미설정 변수 참조 → 비-0) 명시는 `run_gate.sh:35-38` ⒞ 한 곳이고,
> SKILL.md 는 값을 지우면서 산문 강제자만 남겼다(S18-2). **오탐 실측 — 현실 게이트 명령 11종 전부 무영향**
> … **`uv run pytest …`·`./gradlew …` 형태에서 오탐 위험은 낮다.** 지적은 **문서 도달**이지 기능이 아니다.

**codex 근거 (원문 Next steps 축③ 전문)**

> - Axis 3 — nounset is explicitly documented and tested. It is a real compatibility change:
>   `if [ -n "$OPTIONAL_GATE_FLAG" ]; then ...; fi` returned old rc 0 and new-child rc 127 when unset.
>   No current repository consumer of `run_gate.sh` was found, so no present gate break was established.

**갈림의 성질** — codex 는 **문서·테스트 축**에서 무지적을 냈고 Claude 는 **문서 도달 축**에서 권고를 냈다.
그런데 **완화 근거는 반대로 codex 가 좁혔다**: Claude 의 11종 목록에 `[ -n "$UNSET" ]` 형태가 없었고,
codex 가 그 한 종에서 rc 127 을 실측했다. 두 리뷰가 **서로의 결론을 각각 좁힌다.**
「현재 소비자 0」은 이 회차가 저장소 전수 grep 으로 **확인**했다(§6 ④) — 양쪽이 여기서는 합의한다.

### 충돌 S2 — SKILL.md 축약: "정본이 실재하니 문제 없음" ↔ "사용 지점의 경고가 사라졌다"

**Claude 근거 (1차 S18-2 전문 요지)**

> 리더 판정(R17-3 채택)에 따른 **의도된** 결과이므로 지적이 아니라 **손실 기록**이다. `SKILL.md:238`
> 에서 함께 사라진 것: ⑴ 「러너 호출 자체를 파이프에 태우면 무효다」 — **게이트 16 B 로 실제 사고가 났던 그 함정**,
> ⑵ 「인자에 비밀값을 넣지 마라」 — 보안 전제. 대체 강제자는 산문 한 문장(「쓰기 전에 머리 주석을 읽어라」)뿐이고
> **도달 0**이다. … 처방은 **리더 판단**으로 넘긴다.

**codex 근거 (원문 Next steps 축③ 전문)**

> - Axis 3 — SKILL.md rule 5 now points to the runner header without copying its values, and that canonical
>   header exists. **No issue found there.**

**갈림의 성질** — **같은 대상의 다른 축**이다. codex 가 답한 질문은 focus 축③-4 의
「지목만 하는가 / 지목 대상 정본이 실재하는가」이고, 둘 다 참이다. Claude 가 제기한 것은
「**사용 지점에서 함정 경고가 사라졌고 대체 강제자의 도달이 0**」이라는 별개 축이며, **codex 는 그 축을 묻지 않았다.**
codex 의 무지적이 Claude 의 축을 덮지 않는다.

---

## 5. 겹침 판정 (리더 규칙 3)

### ⓐ X18-1 ↔ Claude 의 자기 감사(R18-4) · T18-* 의 `_MAINLINE_PHRASES` 언급

- **Claude 는 놓치지 않았다 — 같은 대상을 검토하고 반대 결론을 냈다.** `_MAINLINE_PHRASES` 는
  Claude 1차에서 **R18-4 3행**(도달 범위 구획)과 **P18-3**(귀속 고정 근거)에 등장한다.
- **다만 T18-* 계열에는 나오지 않는다.** T18-1 이 다루는 것은 `_MAINLINE_HELPERS` 의 **문구 결속 단언**
  (`:1094` `f'"{phrase}" in output'`)이고, X18-1 이 다루는 것은 **파생 상수 `_MAINLINE_PHRASES`**
  (`:624`, 대조군 부정 루프의 원료)다. **두 「문구」는 다른 객체다** — 한 행으로 묶지 않았다.
- 따라서 겹침은 **R18-4 ↔ X18-1 한 자리**이고, 상태는 **충돌**(§4).

### ⓑ X18-3 ↔ R18-1(명령 치환 안 갈림) · R18-2(4종 미탐지) — **같은 결함군이다**

세 지적 모두 **「러너 머리 주석·`:104` 오류 문구가 실제 마커 기제보다 넓게 적혀 있다」**는 하나의 사실을 가리킨다.
겨누는 문장이 다를 뿐이다.

| 지적 | 겨누는 문장 | 주장 |
|---|---|---|
| **R18-1** | `:27-28` "functrace 가 없으면 서브셸·**명령 치환** 안의 명령이 trap 에 안 잡혀 오탐이 난다(**실측**)" | 명령 치환 쪽은 functrace 유무로 **갈리지 않는다** → 실측 주장 절반 미재현 |
| **X18-3** | 같은 `:28` + `:104` "`빈 확장 등`" | **빈 명령 치환이 rc 0** 이라 그 문장이 약속한 탐지가 없다 |
| **R18-2** | `:29-34` "**못 잡는 잔여(정직하게)**: … 빈 문자열로 설정된 변수만" | 잔여가 **닫힌 열거인데 실제로는 더 넓다** |

**제3 근거 — 마커 행렬 15종 (저장소 실물 러너, 변조 없음)**

| 입력 | rc | 출처 |
|---|---|---|
| `$(echo)` · `$()` · `` `` `` · `eval ''` · `:` · `if false; then echo x; fi` · `trap - DEBUG` · `set +u` | **0** | codex 7종 재현 |
| `X=1` · `bash -c ""` | **0** | Claude 2종 재현 |
| `true` · `(echo sub)` (대조군 — 실제 작업) | 0 | 양쪽 |
| `# comment` · `for x in ; do echo $x; done` · `f() { echo x; }` | **2** | 양쪽(T6-f 합의) |

**codex 목록과 Claude 목록이 겹치는 것은 `:`·eval 계열뿐이고 나머지는 상보**다.
결합하면 **잔여는 최소 9종**이며, `run_gate.sh:29-34` 는 그것을 **1종으로 못박는다.**
`:104` 가 열거하는 「빈 확장 등」은 `$()`·`` `` `` 에서 **실제로 탐지되지 않는다** — codex 의 문면 지적이 참이다.

**한계 구획도 양쪽이 같은 자리를 짚었다**: 대조표(`test_run_gate.py:22-23`)는 「functrace — **서브셸** 안 명령」만
매기고 **명령 치환에는 테스트가 없다**(Claude R18-1 후단 = codex "The contract table has no
command-substitution test despite line 28 naming that form").

### ⓒ X18-4 ↔ T18-6 · 권고 중 `RUN_GATE_PATH` 관련 — **겹치는 권고는 없다**

- Claude 1차의 권고 6건(T18-3·T18-4·T18-5·T18-6·S18-2·R18-7) 중 **`RUN_GATE_PATH` 를 다루는 것은 0건**이다.
- `RUN_GATE_PATH` 는 Claude 1차에서 **§2.1(음성 대조 절차)** 과 **§5 표 ⑭행(해소 판정 근거)** 에만 나온다.
  즉 **지적이 아니라 「해소의 근거」로 쓰였다.**
- **T18-6 은 무관하다** — 커밋 메시지의 「(테스트 고정)」이 함수 경로에 대해 과대하다는 지적이고,
  손잡이와 접점이 없다. 겹침 없음.
- 결론: **X18-4 는 Claude 권고 어느 것과도 겹치지 않으며, Claude 의 ⑭ 해소 판정과 정면으로 갈린다**(§4 충돌 X4).

### ⓓ X18-2 ↔ Claude 의 AST 대조 한계 검토(R18-5) — **부분 겹침, 축이 다르다**

**Claude R18-5 가 실제로 조사한 목록**(전수 AST 조사, helper 11종):
비호출 `Name` 참조 0 · `Attribute` 호출 0 · `getattr`/`partial` 호출 0 · 모듈 레벨 호출 0 · 클래스 메서드 안 호출 0.
그리고 **이론적 false-green 을 하나 명시**했다 — 「직접 호출 1곳 **+** 별칭으로 본류에서 1곳」 혼합.

**codex X18-2 가 든 목록**: 외부 모듈 · 동적 임포트 · 중첩 함수 · callable 을 돌려주는 데코레이터.

| 축 | Claude | codex | 상태 |
|---|---|---|---|
| 동일 모듈 별칭·`getattr`·딕셔너리·`partial` | 조사함 — "새로 생기면 **빨강**(안전 방향)" | "generally become **zero-call failures**" | **합의(무지적)** — §3.3 X2b |
| **외부 모듈·동적 임포트** | **조사 목록에 없다** | **지적함(high)** | **codex 단독** |

**제3 근거 (§1.1 ⑵)** — 외부 모듈 helper 를 `main` 의 본류 루프에 배선하고 완전성 테스트를 돌렸다.

```
_x18_probe_ext.py: def external_problems(payload) -> list[str]
compare_parity.py: from _x18_probe_ext import external_problems
                   main(): _ext = external_problems(None)   # 본류 호출
→ pytest tests/test_parity_ci_gate.py            36 passed
→ pytest tests/test_harness_scope_reach.py       37 passed
→ external_problems.__module__ = '_x18_probe_ext'  ≠  comparer.__name__
```

`discovered` 의 필터(`member.__module__ == comparer.__name__`)에서 탈락하므로 `_call_sites` 에 이름이
전달되지 않고, 표에도 없고, 어떤 단언도 이 helper 를 보지 못한다. **codex 진술이 코드로 참이다.**
Claude 의 「안전 방향」 판정은 **동일 모듈 축에 한해 참**이며, 그 판정이 외부 모듈 축을 덮지 않는다.

### ⓔ T18-1(문구 결속 문자열 grep) ↔ codex — **codex 는 보지 않았다**

- codex 축②-4 는 「그 단언을 **지우면** 어떤 변이가 통과하는가」를 물었고, 결속 단언에 대해
  "removing phrase binding allowed a table phrase to drift to another comparer literal" 로 답했다.
  이는 **단언의 유효성**을 확인한 것이지, **그 단언이 재는 대상이 「단언」이 아니라 「텍스트」**라는
  T18-1 의 축을 다루지 않는다.
- codex 최종 출력 전문에 `주석`/`comment`로 결속을 우회하는 서술은 **없다**.
- Claude 실측: `test_본류가_빈_fixture를_막는다:765-768` 의 `assert "빈 fixture" in output, (…)` 을
  **주석 + `pass`** 로 바꿔도 **36 passed**. 표는 「그 문구를 단언한다」고 계속 판정한다.
- 결론: **Claude 단독.** 같은 커밋이 ③에서 「문자열 grep 이 아니다」를 명시적으로 버렸는데
  ④는 그 방법으로 구현됐다는 관찰도 Claude 쪽에만 있다.

---

## 6. codex §4.5 「전제 확인 필요」 6건 + 부속 — 코드 대조 판정 (리더 규칙 4)

> codex 레인이 판정을 이쪽 몫으로 미룬 항목이다. **실제로 코드를 열고 실행해 대조했다.**
> 새 지적이 아니라 기존 지적의 근거 검증이며, 결과에 따라 §3 표의 「상태」가 달라진다.

| # | codex 전제 | **대조 판정** | 근거 |
|---|---|---|---|
| **①** | codex 샌드박스가 읽기 전용 — ⑴ 새 판 `23 passed` 미확인 · ⑵ here-doc 미확정 · ⑶ `BASH_ENV` 우회 미완주 | **전부 보완 — 그리고 ⑶ 은 우회가 성립한다** | ⑴ `.venv/bin/pytest tests/test_run_gate.py` → **23 passed**(Claude 1차 §1.2 와 일치, 두 관점). ⑵ `cat <<EOF…` rc 0 · `: <<EOF…` rc 0 — **새 갈래 아님**, `:` 갈래와 같다. ⑶ **완주 확인**: BASH_ENV 파일이 `$RUN_GATE_MARKER` 를 보고(진단 A) 선점 기록하면 주석 전용 입력이 **rc 2 → rc 0**(대조군 rc 2). 마커 13바이트 vs BASH_ENV 없을 때 0바이트 |
| **②** | X18-4 와 축① 첫 항목의 관계 — `RUN_GATE_PATH` 주입 방식이 커밋 메시지의 음성 대조 방식과 같은가 | **같다 — 같은 손잡이다** | Claude §2.1 이 "`RUN_GATE_PATH` 를 옛 판으로 돌려도"라 적었고, codex 도 같은 환경변수를 썼다(codex: "subprocess path injection"). 이 회차 재현도 동일 손잡이. **따라서 「20 passed / 3 failed」는 두 관점 일치이고, 동시에 그 수치가 X18-4 경로로 산출됐다는 codex 단서도 참이다** |
| **③** | `_MAINLINE_ROOTS=()` 가 failed 인데 "공허해진다"는 부기 — 어느 단언이 실패를 냈고 어느 검사가 공허해지는가 | **분리 확인 — 두 진술 동시 참** | 공허해지는 것: `assert not in_roots`(`:1054-1058`). 빨강을 내는 것: `assert not outside`(`:1059-1063`) 와 `assert callers[0] in _MAINLINE_ROOTS or callers[0] in table`(`:1074`). **Claude R18-4 1행과 codex 부기가 같은 내용이다** |
| **④** | "저장소에 `run_gate.sh` 소비자가 없다"가 전수 조사 결과인가 | **참 — 전수 확인** | `grep -rn "run_gate"` 전체(리뷰 산출물 제외): 히트는 `tests/test_run_gate.py`(자기 테스트) · `run_gate.sh`(자기 주석) · `SKILL.md:238`(지목 한 줄)뿐. **`.github/workflows/` 히트 0 · 에이전트 정의 히트 0** |
| **⑤** | X18-2 의 "same-module alias/getattr/dict/partial → generally zero-call failures" 분류가 실제 코드 경로와 맞는가 | **맞다** | `_call_sites` 는 `ast.Call` 중 `func` 가 `ast.Name` 이고 그 id 가 `names` 에 있는 것만 센다. 별칭 대입 후 호출은 다른 이름이므로 `callers` 가 비고 → 표 밖 helper 는 `assert callers` 빨강, 표 안 helper 는 `len(callers) == 1` 빨강. **Claude R18-5 「안전 방향」과 일치(합의)** |
| **⑥** | X18-1 이 지목한 소비처 `:720`·`:740` 이 `_MAINLINE_PHRASES` 의 **유일한** 소비처인가 | **참 — 저장소 전수 3건** | `grep -rn "_MAINLINE_PHRASES"` 저장소 전체 → 정의 `:624` · 소비 `:720` · 소비 `:740`. **다른 파일 히트 0.** codex 진술의 전제가 성립한다 |

**전제 오류로 무효화되는 codex 지적: 0건.** 6건 중 5건이 참, 1건(①)은 환경 제약이던 것을 이 회차가 보완했으며
그 보완 결과는 **codex 진술을 약화시키지 않고 오히려 하나(⑶)를 완결시켰다.**

### 6.1 게이트 17 ⑥·⑦ — **codex 무응답으로 단일 관점 판정임을 명시한다**

codex 는 `dump_python_snapshots.py` 와 `tests/test_python_snapshot_guard.py` 를 **읽은 명령이 stderr 에 있으나**
(`nl -ba .claude/skills/migration-safety-gate/scripts/dump_python_snapshots.py`) **최종 출력에 지적도
「지적 없음」 진술도 없다.** codex 레인이 이를 §5.3 마지막 항목에 **「답이 오지 않은 것」으로 자기 등재**했다.

| 게이트 17 항목 | Claude 판정 | codex | **관점 수** |
|---|---|---|---|
| **⑥** 실물 케이스 이름 중복 → 생성기·가드 양쪽 실패 | **해소** (§2.5 음성 대조 2종 각 1건 빨강 · 실물 `--check` exit 0 유지) | **무응답** | **단일 (Claude만)** |
| **⑦** 키 누락 / 빈 하한 메시지 갈래 분리 | **해소** (갈래 제거 변이 → 1 failed) | **무응답** | **단일 (Claude만)** |

> **「무응답」을 「지적 없음」으로 읽으면 게이트 17 ⑥·⑦ 이 교차 검증 없이 닫힌 것으로 오독된다**
> (codex §6 자기 경고). 이 표가 그 오독을 막는 기록이다. **단일 관점이라는 사실은 해소 판정을
> 뒤집지 않지만, 두 관점을 받은 것으로 세지도 않는다.**

---

## 7. 관점 수 갱신 (리더 규칙 5) — 게이트 17 cross §6.3 을 이어받는다

### 7.1 게이트 17 §6.3 의 이번 상태 — **이월 항목은 이번에도 focus 밖이었다**

| 게이트 17 §6.3 행 | 게이트 17 관점 수 | **게이트 18 상태** | 근거 |
|---|---|---|---|
| **ⓐ** Flyway 잠금 제거 → `FlywayBaselineGuardTest` 빨강 | 단일 (Claude만) · 두 회차 연속 | **여전히 단일 — 세 회차 연속.** 이번 focus 3축(음성 대조 재현 / 새 장치 자기 빈자리 / 표기 정직성)에 없고, 이 배치의 Kotlin diff 가 0줄이라 Claude 도 대상 아님으로 등재 | codex focus §질문 3축 · Claude §8 미실행표 |
| **ⓒ** 구판 생성기 복원 → 4 failed | 단일 (Claude만) · 두 회차 연속 | **여전히 단일 — 세 회차 연속.** 어느 쪽도 다시 돌리지 않았다 | 동일 |
| **⑤** 완전성 검사의 빈 표 통과(차단②) | 단일 (Claude만) | **✅ 두 관점 — 닫혔다.** 수정(`EXPECTED_MAINLINE_HELPERS` 정확 일치)이 양쪽에서 재현됐다(Claude ⑤a·⑤b / codex `{}`·`=0` 변이). **단, 그 수정물에서 새 빈자리(X18-1)가 나왔다** | Claude §2.3 · codex 축①⒜·축② |
| **⑩** `Connection.unwrap` | Claude 단독 · 실행 재현 0 | **무변경 — 이 배치의 대상 아님** | — |

### 7.2 게이트 18 에서 **새로 두 관점을 얻은 검증**

| 검증 | Claude | codex | **관점 수** |
|---|---|---|---|
| 옛 판(`d0a5255`) 러너 + 새 테스트 → **3 failed / 20 passed**, 실패 3건의 **이름 일치** | **재현** (`RUN_GATE_PATH` · 옛 판 sha256 `23d14464…`) | **재현** (`git show` + 프로세스 메모리 · 이름 3건 명시) | **✅ 둘 — 게이트 17 T-E 의 「이름 식별은 Claude 단독」이 여기서 닫혔다** |
| 옛 판에 세 입력 직접 → rc 0 | **재현** | **재현** | **✅ 둘** |
| 새 판 `23 passed` | **재현** | 미도달(샌드박스) → **이 회차가 보완** | **✅ 둘**(둘째는 이 cross 의 제3 근거) |
| 완전성 변이 `{}` → failed | **재현**(⑤a) | **재현**(축①⒜) | **✅ 둘** |
| 행 제거 + 상수 9 → failed | **재현**(③) | **재현**(축①⒝) | **✅ 둘** |
| `compare_parity.py:2072` 호출선 제거 → 대응 본류 테스트 빨강 | **재현**(§2.4 10/10 중 열째) | **재현**(축①⒞ 메모리 변이) | **✅ 둘 — 게이트 17 ⓑ 의 9자리가 10자리로 확대된 뒤에도 두 관점** |
| `_MAINLINE_ROOTS=()` → failed · `in_roots` 공허 | **재현**(실측 A) | **재현**(부기) | **✅ 둘 — 결론 일치** |
| 마커 행렬(주석 전용 rc 2 · 서브셸 rc 0 · 함수 정의 전용 rc 2 · 빈 `for` rc 2) | **재현** | **재현** | **✅ 둘 — 수치 전건 일치** |
| 이름 재사용 → 빨강(②) | **재현** | (축②-4 「유일성 단언 제거 → 중복 허용」으로 간접) | **부분 — Claude 직접 · codex 역방향** |
| 실물 `--check` exit 0(오탐 0) | **재현** | **무응답**(§6.1) | **단일** |

### 7.3 여전히 단일 관점인 것 (닫히지 않았다)

- **ⓐ Flyway 잠금 음성 대조** — **세 회차 연속 단일.**
- **ⓒ 구판 생성기 복원 4 failed** — **세 회차 연속 단일.**
- **게이트 17 ⑥·⑦ 해소 판정** — Claude 단독(codex 무응답, §6.1).
- **T18-1·T18-2 의 실측** — Claude 단독. 특히 T18-2 는 **「배선 0」이라는 범위 선언형 장치의 도달**을 재는
  자리이므로, 단일 관점이라는 사실을 심각도 완화 근거로 쓰지 않는다.
- **X18-2 의 외부 모듈 축 · X18-1 · X18-4** — codex 단독이나 **이 회차가 제3 근거로 실행 재현**했다.
  「단일 관점」은 아니되 **「Claude 1차가 반대 결론을 냈던 자리」**로 남는다.
- **`--check` 가 `app/**` 를 실제로 다시 읽는지** — 게이트 17 에 이어 **양쪽 미수행**.

---

## 8. 종합 기준 Phase 종료 조건 대비 현황

### 8.1 게이트 17 10항목 — 종합 판정

> Claude 1차 §5 의 판정에 codex 관점을 겹쳤다. **1차 판정을 고쳐 쓰지 않았고**, 갈리는 자리는 병기한다.

| 게이트 17 | 요지 | Claude 1차 | **codex 관점 반영 후** | 근거 |
|---|---|---|---|---|
| **①** 러너 zero-work → exit 0 | 확장 후 0건인데 통과 | **해소** | **부분 해소 — 잔여가 선언보다 넓다** | 세 입력은 닫혔다(양쪽 재현). 그러나 **X18-3 + R18-1 + R18-2** 가 같은 기제의 남은 9종을 지목 |
| **②** 테스트 이름 재사용 승인 | 유일성·결속 미검사 | **해소** | **부분 해소** — 유일성은 닫혔고 **결속은 문자열 grep(T18-1)** | Claude 실측 36 passed |
| **③** `nested` 면제 목록(은폐형) | 한 줄 추가로 회귀 면제 | **해소** | **해소 — 단, 탐지형의 도달에 외부 모듈 축이 빈다(X18-2)** | 면제 폐지 → AST 탐지형 전환은 양쪽 확인. 전환이 열째를 드러냈다 |
| **④** 표 판정 문구 미대조 | bogus 문구도 통과 | **부분 해소** | **부분 해소(유지)** — 양쪽이 같은 방향 | bogus·갈린 문구는 잡는다 / 주석으로 충족되는 자리는 남았다(T18-1) |
| **⑤** 빈 표에서 통과 (**차단②**) | 검사 대상 0건 통과 | **해소** | **부분 해소 — 같은 종류가 파생 상수에 남았다(X18-1)** | 표 크기는 정확 일치로 닫혔다(두 관점). `_MAINLINE_PHRASES` 는 빈 선언에서 통과 |
| **⑥** 실물 케이스 이름 중복 | 개수 위조 통과 | **해소** | **해소 — 단일 관점**(§6.1) | 생성기·가드 양쪽 1건씩 빨강 · 실물 `--check` exit 0 |
| **⑦** 키 누락/빈 튜플 동일 메시지 | 진단이 같아 엉뚱한 자리를 고침 | **해소** | **해소 — 단일 관점**(§6.1) | 갈래 제거 변이 → 1 failed |
| **⑨** SKILL.md 이중 기재 | 값 복사 | **해소** | **해소 — 양쪽 확인**. 부수 손실은 **충돌 S2** | codex 축③-4 "no issue found" · Claude `SKILL.md:238` 계약값 전문 0 |
| **⑬** LIMIT parametrize 셸 하나 | 원 사고 셸 zsh 미커버 | **부분 해소** | **부분 해소(유지)** | 러너를 부르는 셸은 여전히 bash 하나(T18-5) — codex 미검토 |
| **⑭** `RUN_GATE_PATH` 기본값 미단언 | 손잡이가 검사 대상을 바꿈 | **해소** | **충돌 — 리더 판정 필요** | Claude: `delenv` 실동작으로 해소 / codex: 그 테스트가 활성 override 를 가린다(X18-4). **제3 근거가 false-green 성립을 확인**(§4) |

**종합 집계: 해소 3(③·⑨ + ⑥⑦은 단일 관점 해소) · 부분 해소 5(①·②·④·⑤·⑬) · 충돌 1(⑭) · 미해소 0.**
(1차 단독 집계는 「해소 8 · 부분 2 · 미해소 0」이었다 — **codex 관점이 세 자리를 「해소」에서 「부분」으로 옮기고,
한 자리를 충돌로 되열었다.**)

### 8.2 조치 목록 — 심각도 순 · 마감 병기 · **착수 차단 여부는 별개 축**

> **판정하지 않는다.** 아래는 각 리뷰가 스스로 적은 마감을 옮긴 것이고, 「문면상 Phase 3 착수 이전인가」 열은
> 그 마감의 **문면**이 착수 이전을 가리키는지만 표시한다. **심각도를 "아직 안 쓰이는 게이트니까"로
> 스스로 낮추지 않았다.**

#### (가) 문면상 **Phase 3 착수 이전** — 8건

| 순위 | 항목 | 심각도 (C / X) | 마감 (각 리뷰 문면) | 근거 위치 |
|---|---|---|---|---|
| 1 | **X1** 빈 `_MAINLINE_PHRASES` → 완전성·대조군 공허 통과 | **충돌** — 지적 없음 / **high · no-ship** → **판정 필요** | codex: no-ship / **§6 X2 게이트 첫 실사용** | `test_parity_ci_gate.py:624-632` |
| 2 | **X2a** 외부 모듈·동적 임포트 helper 우회 | — / **high · no-ship** | codex: no-ship | `test_parity_ci_gate.py:979-1035` |
| 3 | **X4** 기본 대상 테스트가 활성 `RUN_GATE_PATH` 를 가린다 | **충돌** — (⑭ 해소 근거) / **high · no-ship** → **판정 필요** | codex: no-ship / 러너 실사용 전 | `test_run_gate.py:94-116` |
| 4 | **X3 + R18-1 + R18-2** 마커 기제의 선언이 도달보다 넓다(잔여 최소 9종) | **수정 필요 ×2** / **high · no-ship** | 러너를 게이트 근거로 처음 쓰기 전 | `run_gate.sh:27-34`·`:90-104` |
| 5 | **T1** ④ 문구 결속이 문자열 grep — 주석 한 줄로 충족 | **수정 필요** / — | 게이트 17 ④ 마감 승계(codex `no-ship` / Claude Phase 3 내) | `test_parity_ci_gate.py:1090`·`:1094` |
| 6 | **T2** CI 배선 탐지기가 `- run:`·블록 스칼라 미도달(스텝 33 중 21줄만, 그중 6줄은 헤더) | **수정 필요** / — | 러너를 게이트 근거로 처음 쓰기 전 | `test_run_gate.py:295` |
| 7 | **R3** 원장 `:548` 「9자리·9/9」 ↔ 코드 10 | **수정 필요** / — | **리더의 게이트 18 후속 원장 기재** | `00_progress.md:548` ↔ `test_parity_ci_gate.py:617` |
| 8 | **X5** `BASH_ENV` 마커 선점 — **이 회차가 우회 성립을 확인** | — / (codex 심각도 미부여) → **판정 필요** | 러너 실사용 전 | `run_gate.sh:97-98` |

#### (나) 문면상 **러너 실사용 전**(착수 이전이라 단정하지 않음) — 4건

| 순위 | 항목 | 심각도 | 마감 |
|---|---|---|---|
| 9 | **T3** 러너 계약 대조표 자신에게 강제자 0 | 권고 / — | 러너 실사용 전 |
| 10 | **T4** 대조표가 머리 주석 문장을 전부 덮지 않는다(Claude 3개 + codex 1개) | 권고 / (X18-3 포함) | 러너 실사용 전 |
| 11 | **T5** zsh 로 러너를 부르는 테스트 0(⑬ 잔여) | 권고 / — | 러너 실사용 전 |
| 12 | **R7** nounset 명시 도달 + 실측 파손 1종 — **충돌** | 권고 / 지적 없음 | 러너 실사용 전 |

#### (다) 리더 판단 · 이월 — 4건

| 순위 | 항목 | 심각도 | 마감 |
|---|---|---|---|
| 13 | **S2** SKILL.md 축약으로 사용 지점 경고 소실 — **충돌** | 권고 / 지적 없음 | **리더 판단(처방 선택)** |
| 14 | **T6** 커밋 메시지 「(테스트 고정)」 함수 경로 과대 | 권고 / — | 이월(등재만) |
| 15 | **X6** 마커 truncate → 거짓 rc 2(안전 방향) | — / (심각도 미부여) | 이월(등재만) |
| 16 | **R10** shellcheck 도달 0 (게이트 17 ⑮) · **R9** bash 5.x 미검토 | 사실 등재 / 미검토(사유) | 이월(등재만) |

**군집 사실 (판정 아님)** — (가) 8건 중 **6건이 「선언이 도달보다 넓다」 한 종류**이고(X1·X2a·X3·T1·T2 + R18-2),
**4건(X1·X2a·X3·X4)은 이 배치가 바로 이번에 세운 장치**다. Claude 1차 §7.2 가 같은 관찰을 「수정 필요 5건 중 4건」으로
적었고, codex 총평이 같은 것을 "four independent false-green/scope-integrity defects"로 적었다 —
**두 리뷰가 독립적으로 같은 군집을 지목했다.**

### 8.3 원장 `00_progress.md`(`e577838`) 「게이트 16 후속」 §6 착수 조건 표 — 이 게이트가 채우는 칸

> **읽기만 했다. 원장을 고치지 않는다.** 아래는 **사실 대조**이며 착수 판정이 아니다
> (§6 표 자신이 "여기 있는 「충족」 표기를 착수 판정으로 읽지 마라"고 적는다).

| 착수 조건 | 원장의 현재 기재 (`e577838`) | **이 게이트 결과가 채우는 칸 (사실)** |
|---|---|---|
| **⑤ 게이트 17 종결** (`17_gate16-fixes`, 대상 `1cb7bdf..48a791c`) | **「진행 중」** · 실행 경로 `안 돎` · "1단계 진행 중(`reviews/17_*` 미생성). 이번 배치 5 커밋은 아직 독립 리뷰를 받지 않았다" | **두 겹으로 사실이 갈렸다.** ⑴ 게이트 17 산출물 3종이 `36b5ed4` 로 실재한다("미생성"은 거짓). ⑵ **그 게이트의 지적 10항목을 닫은 배치(`107c8a5`·`318069b`)가 이번에 독립 리뷰를 받았다** — 종합 판정 **해소 3 · 부분 해소 5 · 충돌 1 · 미해소 0**(§8.1). **상태를 「종결」로 바꿀지는 리더 판정** — 부분 해소 5·충돌 1 이 남아 있다 |
| **③ Phase 3 착수 차단 6건** 중 **X3**(러너 신설) | 「수정 완료 2」의 하나 — "**규약 강제자 0 은 열린 채**" | **세 회차 연속 그 장치에 새 지적이 붙었다.** 게이트 16 A·B·C·D → 게이트 17 ① → **게이트 18 X3·X4·X5 + T2·T3·T4·T5 + R18-1·R18-2**. 「수정 완료」의 대상 장치가 매 회차 새 빈자리를 낸다는 **사실**만 적는다 |
| **③** 중 **X2**(본류 회귀 + CI 경로 명시) | 「수정 완료 2」의 다른 하나 · 심각도는 §3 ① | **표는 9→10자리로 넓어졌고 음성 대조가 두 겹(호출선 + 완전성)이 됐다**(양쪽 재현). 동시에 **그 완전성 장치 자신에 X1·X2a·T1 이 붙었고 원장 값이 낡았다(R3)** |
| **④ 게이트 16 종결** | 「부분」 — 남은 것: H·I·J·W · N · §8.3 이월 6건 | **무변경** — 이 배치의 대상이 아니다(Kotlin·계약 diff 0줄) |
| **⑥ Phase 2 잔여 L348** · **⑦ 게이트 14 미해소 6갈래** | 「아니오 1행」 / 「열림 — 마감 Phase 4」 | **무변경** — 이 배치가 건드리지 않았다. **어느 것도 Phase 3 대상 코드에 닿지 않는다**는 기존 판정도 그대로 |
| **① 게이트 15 종결** · **② 게이트 14 완전 재실행** | 충족 / 충족 | **무변경** |
| **(신설이 필요한 칸) ⑧ 게이트 18 종결** | (원장에 없음 — 이 게이트가 그 뒤에 생겼다) | **3단계 완주.** 1단계 `18_gate17-fixes_codex-reviewer.md`(36,266 B · `needs-attention` · high 4) · 2단계 `18_gate17-fixes_migration-reviewer.md`(35,063 B · 차단 0 · 수정 필요 5 · 권고 6) · 3단계 이 파일(**합의 4 · 충돌 4 · codex 단독 3 · Claude 단독 8 = 19행**) |
| **§3 사용자 판단 대기 6건** | ①~⑥ | **⑤(E·G 심각도)에 재료 추가** — 게이트 17 ⑥⑦(스냅샷 중복·메시지 갈래)이 **Claude 단독으로 해소** 판정됐다(codex 무응답, §6.1). 나머지는 무변경 |

---

## 9. 수렴 관찰 (리더 규칙 7) — **사실만. 추세 해석은 리더 몫이다**

| 게이트 | 대상 | Claude 차단 | codex high | cross 차단후보/high 행 | **이 회차가 새로 세운 장치에서 나온 빈자리** | **Phase 3 대상 코드(JDBC·인증·계약 테스트) 지적** |
|---|---|---|---|---|---|---|
| **15** `phase3-preflight` | `…614afed` | **2** | **8** (전부 high · "출하 차단") | 3 (X1·X2·X3) | **해당 없음** — 러너가 아직 없었다(X3 = 「러너 부재」 지적) | **다수** — X5(test-plan password) · X6(F3 강제자) · X7 · X13 · X8~X12(Flyway·JDBC) |
| **16** `gate15-fixes` | 게이트 15 후속 배치(6커밋) | **1** (차단② T-E) | **3** (K16-1~3) | 4 (A·B·C·D) | **4** — 게이트 15 X3 로 **신설된 러너**에서 A·B·C·D | **있음** — H·I·J·W(F3 DTO 강제자, 마감 Phase 3 **종료**) · M · N |
| **17** `gate16-fixes` | `1cb7bdf..48a791c` | **1** (차단② ⑤) | **3** | 5 (①②③⑤⑥) | **5** — 게이트 16 이 세운 **완전성 장치**에서 ②③④⑤ + 고쳐진 **러너**에서 ① | **있음** — ⑩(`Connection.unwrap`) · ⑪(전사 규약) · ⑫(preflight 상태 열) |
| **18** `gate17-fixes` | `36b5ed4..318069b` | **0** | **4** | 4 (X1·X2a·X3·X4) | **4 — 4/4 전부** 이 배치가 세운 것: 완전성 장치 신판(X1·X2a) · 러너 zero-work 기제와 그 테스트(X3·X4) | **0** — Kotlin·계약·프론트 **diff 0줄**(Claude K18-1·C18-1 · codex 대상 범위 밖) |

**표에서 곧바로 읽히는 사실 넷 (해석 없이)**

1. **Claude 차단 건수는 2 → 1 → 1 → 0 으로 내려왔다.** 게이트 18 이 **처음으로 차단 0** 이다.
2. **codex high 건수는 8 → 3 → 3 → 4 로 내려오다 이번에 다시 올랐다.** 단조 감소가 아니다.
3. **게이트 18 codex high 4건은 전부 "false-green/scope-integrity" 한 종류**이고(codex 총평 문면 그대로),
   **4/4 가 이 배치가 방금 세운 하네스 자기 검사 장치**다. 사용자 데이터 경로·제품 코드 지적은 **0건**이다.
4. **Phase 3 대상 코드(JDBC repository · 인증 API · 계약 테스트) 지적은 게이트 18 에서 0건**이다.
   게이트 15·16·17 에는 매번 있었다. 이 배치의 Kotlin·계약·프론트 diff 가 **0줄**이기 때문이다 —
   **「Phase 3 코드가 깨끗하다」가 아니라 「이번 회차가 Phase 3 코드를 보지 않았다」가 정확한 진술이다.**

**「새 장치 자기 빈자리」의 연속성 (사실)** — 게이트 **16·17·18 세 회차 연속**, 직전 배치가 세운 게이트 장치에서
다음 게이트가 새 빈자리를 찾았다. 건수는 4 → 5 → 4 다. codex focus 가 이 형태를 축② 로 명시 지정했고
("이 하네스에서 **세 회차 연속** 같은 일이 났다"), **이번 회차에도 4건이 나왔다.**

**단, 종류는 좁아졌다 (사실)** — 게이트 16 의 4건은 「빈 인자가 통과」·「인용 경계 소실」처럼 **기능 결함**을 포함했고,
게이트 18 의 4건은 전부 **「선언이 도달보다 넓다」 한 종류**다. Claude 1차 §7.2 와 codex 총평이 각각 독립적으로
같은 군집을 지목했다(§8.2 군집 사실).

---

## 10. 대조하지 못한 범위

| 범위 | 사유 |
|---|---|
| **codex 리뷰 부재·실패** | **해당 없음.** 1회 호출 exit 0 · verdict `needs-attention` · 출력 7,763 B, `result` 회수본(7,886 B)과 diff 는 session ID 꼬리 2줄뿐(codex §1.2). **재요청 불필요** |
| **codex 중간 진행 메시지 6건 · 실행 명령 46건의 전문** | 헬퍼가 약 90자에서 절단해 stderr 에 기록 — `result <job-id>` 는 최종 출력만 돌려주므로 회수 경로 없음(codex §5.2). **대조는 최종 출력으로만 했다.** 추측으로 이어붙이지 않았다 |
| **게이트 17 ⑥·⑦** (`dump_python_snapshots.py`·`test_python_snapshot_guard.py`) | **codex 무응답** — 읽은 명령은 stderr 에 있으나 최종 출력에 지적도 「지적 없음」도 없다. **단일 관점 판정으로 표기**(§6.1) |
| **ⓐ Flyway 잠금 · ⓒ 구판 생성기** 음성 대조 | 이번 focus 3축 밖 · 이 배치의 대상 아님 — **세 회차 연속 단일 관점**(§7.1) |
| **Kotlin 테스트** (`./gradlew :api:test` 등) | 양쪽 미실행 — Kotlin diff 0줄이라 대상 아님(양쪽 합의) |
| **골든셋** (`tests/golden`) | 프롬프트·스타일 규칙·LLM 설정 무변경 — 대상 아님(양쪽 합의) |
| **`--check` 가 `app/**` 를 실제로 다시 읽는지** | **양쪽 미수행** — 게이트 17 이월 그대로 |
| **shellcheck** | 저장소·CI 어디에도 없다(게이트 17 ⑮). `bash -n` 까지만 |
| **bash 5.x 에서의 DEBUG/functrace 거동** | Claude 미검토(사유 — 이 환경에 bash 3.2.57 하나뿐, CI 가 빨강으로 드러냄) · codex 는 샌드박스 bash 판을 적지 않았다. **양쪽 미확정** |
| **codex 축②-5 의 데코레이터·`functools.partial` 개별 실측** | codex 최종 출력이 X18-2 본문에 한 줄로 묶어 언급했을 뿐 개별 결과 없음(codex §5.3 자기 등재) |

### 10.1 「종합 중 발견 — 미교차」

> 두 산출물 어느 쪽에도 없는 관찰이다. **이 회차의 지적으로 세지 않는다** — 다음 리뷰 회차 범위로 제안한다.

| # | 관찰 | 근거 | 제안 |
|---|---|---|---|
| **Y-1** | **`BASH_ENV` 우회는 러너 밖에서도 한 번 발화한다** — 러너 자신을 실행하는 outer bash 도 BASH_ENV 를 source 하며, 그때는 `RUN_GATE_MARKER` 가 아직 없어 오류 한 줄이 stderr 에 찍힌다. 즉 **우회 흔적이 stderr 에 남는다**(탐지 실마리) | 이 회차 §1.1 ⑺ 실측 — BASH_ENV 파일이 두 번 source 됨 | X5 처방을 고를 때 「탐지형(흔적을 단언)」 선택지가 있음을 기록. **처방은 정하지 않는다** |
| **Y-2** | **codex 가 X18-1 을 high 로 올리면서 `_MAINLINE_PHRASES` 의 하드코딩 5개(`:628-632`)는 언급하지 않았다** — 그 5개가 표와 무관하게 존재한다는 사실은 Claude R18-4 에만 있다. 두 리뷰가 같은 상수의 **다른 절반**을 봤다 | codex §3.1 finding 1 ↔ Claude R18-4 3행 | 다음 회차 focus 에서 「파생 상수의 절반이 표 밖 하드코딩」을 명시 대상으로 |
| **Y-3** | **`test_default_target_is_the_tracked_repo_file` 의 sha256 대조는 손잡이를 지운 뒤 같은 경로를 두 번 읽는다** — `target` 과 `REPO / _RUNNER_REL` 이 동일 경로이므로 심볼릭 링크 외에는 무엇도 재지 못한다 | `test_run_gate.py:112-116` | X4 처방 설계 시의 재료. **이 회차는 지적으로 세지 않는다** |
| **Y-4** | **원장 §6 표에 「⑧ 게이트 18」 행이 없다** — ⑤ 행이 게이트 17 을 「진행 중」으로 적은 시점 이후 게이트가 하나 더 돌았다 | `00_progress.md`(`e577838`) §6 ↔ `reviews/18_*` 3종 | 원장 갱신 시 신설. **이 회차는 원장을 고치지 않는다** |

---

## 11. 리더에게 (2회차 종결)

1. **정본 경로** — `docs/migration/_workspace/reviews/18_gate17-fixes_cross.md`(이 파일).
   1단계·2단계 산출물은 **덮어쓰지 않았다.**
2. **집계 — 합의 4 · 충돌 4 · codex 단독 3 · Claude 단독 8 (총 19행).**
3. **충돌 4건 전부 양쪽 근거 전문 병기 · 어느 쪽도 삭제하지 않았다**(§4).
   그중 **X1·X4 는 Claude 1차의 통과 판정(R18-4 「도달 가능한 상태가 아니다」 · ⑭ 「해소」)을 codex 가 되연 자리**이고,
   이 회차가 **제3 근거로 codex 경로의 성립을 실행 확인**했다. **1차 판정은 고쳐 쓰지 않았다.**
4. **codex §4.5 전제 6건 전건 코드 대조 — 전제 오류로 무효화되는 codex 지적 0건.**
   그중 ①⑶(`BASH_ENV` 마커 선점)은 codex 가 샌드박스 제약으로 완주하지 못한 것을 **이 회차가 완주해 우회 성립을 확인**했다.
5. **게이트 17 ⑥·⑦ 은 codex 무응답이라 단일 관점 판정이다**(§6.1). 「지적 없음」으로 읽지 말 것.
6. **관점 수** — ⓐ Flyway 잠금 · ⓒ 구판 생성기는 **세 회차 연속 단일**(이월). 이번에 두 관점을 얻은 검증 8종은 §7.2.
7. **원장 §6 착수 조건 표에서 이 게이트가 채우는 칸은 §8.3 에 사실로 적었다** — ⑤ 행의 문면이 두 겹으로 갈렸고,
   ③ 의 두 자리(X3·X2)에 세 회차 연속 새 지적이 붙었으며, ⑧ 행은 원장에 아직 없다. **판정은 리더 몫이다.**
8. **수렴 표(§9)** — Claude 차단 **2→1→1→0**, codex high **8→3→3→4**(이번에 상승),
   새 장치 자기 빈자리 **4→5→4**(세 회차 연속), **게이트 18 의 Phase 3 대상 코드 지적 0건**
   (단, 이 배치의 Kotlin diff 가 0줄이라 **「보지 않았다」가 정확한 진술**이다).
9. **자기 승인 금지 준수** — Phase 3 착수 차단 여부를 **판정하지 않았다.** 심각도·마감과
   「문면상 착수 이전인가」만 표시했다(§8.2).
10. **저장소 무변조** — `HEAD` = `318069b` · 추적 파일 변경 0 · `cp` 복원 0 ·
    전건 `git checkout` + sha256 대조 · 일회용 worktree 제거 완료 · 원장은 읽기만 · 커밋 없음(§1.1).
