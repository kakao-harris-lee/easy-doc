# 게이트 19 — Claude 독립 리뷰 (1회차) · `19_gate18-fixes`

> **1회차다. codex 산출물은 이 회차에 존재하지 않는 것이 정상**이므로 재요청하지 않았고 실패로도
> 기록하지 않았다. 교차 대조표는 만들지 않는다 — 2차(교차 종합) 재호출의 몫이다.
> 이 파일만으로 Phase 종료 조건 충족을 보고하지 않는다.

---

## 1. 리뷰 범위 · 참조 · 절차

| 항목 | 값 |
|---|---|
| 어간 | `19_gate18-fixes` (리더 지정값 그대로) |
| 대상 | `bbe49a1..5d58832` — 커밋 3개(코드 2 + 원장 1) |
| 코드 커밋 | `e2282b3`(러너 표기 정직화 · 손잡이 제거 · CI 배선 탐지 YAML 교체 — X3·X4·T2) · `5d58832`(파생 상수 정확 일치 · 호출 측 완전성 · 결속 AST — X1·X2a·T1) |
| 원장 커밋 | `df365a4`(R18-1·R18-2·R3 반영 — **읽기만 했다**) |
| 변경 파일 | `run_gate.sh`(+39/−) · `tests/test_run_gate.py`(+229) · `tests/test_parity_ci_gate.py`(+185) · `00_progress.md`(+188) — **총 4파일** |
| 리더 제약 | **새 기제 금지 — 정확 일치 · 표기 정직화 · 기존 방법 교체만** |
| 리더 판정 | X4 → 손잡이 제거 |

**참조 정본** — `.claude/skills/kotlin-migration/SKILL.md` 「선언한 범위와 실제 도달을 대조한다」(규칙 1·3·4·5·6) ·
CLAUDE.md 「범위 대조」 절 · 계획 §5 Phase 7 · §6(X2 게이트) · `reviews/18_gate17-fixes_cross.md` ·
`docs/migration/_workspace/00_progress.md`(`df365a4` 판, **읽기만**).

### 1.1 절차 고지 (규칙 5)

- 변이는 전부 **일회용 `git worktree`**(`…/scratchpad/wt-g19`, detached `5d58832`)에서만 했다.
- **`cp` 를 쓰지 않았다.** 파일 변이는 `Path.write_bytes`, 사본 추출은 `git show`(스크래치패드 밖으로 나가지 않음),
  복원은 **전건 `git checkout -- <path>` + 매 회 sha256 대조**로 했다.
- 리뷰 종료 시 `git worktree remove --force` 로 제거했다(`git worktree list` 에 본 저장소만 남음).
- **본 저장소 최종 상태**: `HEAD` = `5d58832` · 추적 파일 변경 **0** · 커밋 **0** ·
  `run_gate.sh` `c8759c84…` · `test_run_gate.py` `5f542c96…` · `test_parity_ci_gate.py` `da1c2f7a…` ·
  `compare_parity.py` `984ed9e4…` · `ci.yml` `80206da3…` · `tests/conftest.py` `f8dd97d0…` —
  **착수 시 기록값과 전건 일치**. 종료 코드는 전부 파이프 없이 받았다.

### 1.2 기준선 실측 (변이 전)

| 대상 | 결과 |
|---|---|
| `pytest tests/test_parity_ci_gate.py` | **36 passed** |
| `pytest tests/test_run_gate.py` | **33 passed** (LIMIT 15건 포함) |
| `pytest tests/test_harness_scope_reach.py` | **37 passed** |
| `pytest`(전체) | **1242 passed · 68 skipped · 5 deselected · 5 xfailed** · exit 0 |
| `ruff check .` / `ruff format --check .` | 0 / 153 files already formatted |
| `mypy . .claude` | **Success — 137 source files** |
| `dump_python_snapshots.py --check` | **exit 0** (스냅샷 무영향) |
| CI 배선(YAML 파싱) | `ci:quality` 가 `test_run_gate.py`·`test_parity_ci_gate.py`·`test_harness_scope_reach.py` **셋 다 경로 명시**로 호출 |

### 1.3 인계 전 「도달 0」 상태의 해소 — **직접 확인함**

`5d58832` 커밋 메시지가 자기 등재한 상태(helper 3 + 상수 1이 만들어졌으나 어느 단언에도 배선되지 않음)를
AST/grep 으로 전수 확인했다. **전부 배선됐다.**

| 산출물 | 정의 | 호출·소비부 | 판정 |
|---|---|---|---|
| `EXPECTED_MAINLINE_PHRASES` | `:639` | `:1170` 단언 · `:1172` 메시지 | **배선됨** |
| `_root_helper_calls` | `:1017` | `:1184` (→ `:1185` `assert root_calls` · `:1189` `assert not dynamic_lookups` · `:1194` `assert not off_table`) | **배선됨** |
| `_asserted_output_phrases` | `:1061` | `:1249` 단언 | **배선됨** |
| `_string_constants` | `:1089` | `:1237`(비교기) · `:1243`(테스트) | **배선됨** |
| `_HELPER_SUFFIXES` | `:1008` | `:1056` · `:1161` | **배선됨** |
| `_DYNAMIC_LOOKUP_NAMES` | `:1012` | `:1048` | **배선됨** — 단 §2.1 R19-1 참조 |

---

## 2. 다섯 축별 지적

> 심각도 척도는 에이전트 규약 그대로다. **차단은 ① 사건 / ② 장치를 표기**하고, 마감은
> 「그 게이트가 처음 실제로 쓰이는 Phase」다. **착수 차단 여부는 판정하지 않는다**(리더 몫).

### 2.1 테스트 적정성 (핵심 축)

#### R19-1 — **차단(② 장치)** · `_DYNAMIC_LOOKUP_NAMES` 가 **빈 선언에서 통과**하고, 그 상태에서 X2a 가 그대로 재개방된다

- **근거 위치** — `tests/test_parity_ci_gate.py:1012-1014`(정의) · `:1048`(유일 소비) · `:1189`(`assert not dynamic_lookups`)
- **실측 (일회용 worktree · write_bytes + sha256 복원)**

| 변이 | 결과 |
|---|---|
| `_MAINLINE_ROOTS = ()` | **1 failed** (빨강 — 정상) |
| `_HELPER_SUFFIXES = ()` | **1 failed** (빨강 — 정상) |
| **`_DYNAMIC_LOOKUP_NAMES = frozenset()`** | **36 passed — 초록** |
| `_DYNAMIC_LOOKUP_NAMES = frozenset()` **+** `main` 에서 `getattr(_x19b, "external_problems")(None)` 으로 외부 모듈 helper 배선 | **36 passed — 초록** |

- **왜 차단인가.** 이 상수는 「이 AST 대조를 무력화하는 형태는 **없다**」를 단언하는 **범위 선언형**이다.
  SKILL.md 규칙 3·4가 명시로 금지하는 「빈 선언에서 통과」에 정확히 해당하고, 실측 넷째 행이 보이듯
  **비면 게이트 18 X2a(codex high · no-ship)의 사건 경로가 그대로 다시 열린다** — 검사 대상 0건인데
  스크립트가 성공으로 끝나는 형태다. 형제 상수 둘(`_MAINLINE_ROOTS`·`_HELPER_SUFFIXES`)은 빈 선언에서
  빨개지는데 **이것만 초록**이다.
- **제약과의 관계** — 처방은 리더가 허용한 **「정확 일치」** 그 자체다(`EXPECTED_MAINLINE_HELPERS`·
  **같은 커밋이 새로 만든 `EXPECTED_MAINLINE_PHRASES`** 와 동형). **새 기제가 필요 없다.**
  즉 제약 위반이라기보다 **제약이 허용한 처방을 새 상수에 적용하지 않은 누락**이다.
- **선례 대조** — 게이트 17 ⑤(완전성 검사의 빈 표 통과)가 **차단②**로, 게이트 18 X1(빈 파생 상수)이
  codex **high · no-ship** / cross **판정 필요**로 다뤄졌다. **같은 종류가 네 회차 연속**이다.
- **마감** — **Phase 3 착수 전** (§6 X2 게이트의 첫 실사용)
- **관련 종료 조건** — 계획 §6 X2 · SKILL.md 규칙 3(빈 선언 통과 금지)·규칙 4

#### R19-2 — **수정 필요** · X2a 가 **모듈 레벨 별칭 / `functools.partial`** 로 재개방된다

- **근거 위치** — `tests/test_parity_ci_gate.py:1017-1058`(`_root_helper_calls`) · 커밋 `5d58832` 본문
  「이 대조를 무력화하는 동적 조회 … **없음을 단언**한다 — 면제 목록이 아니라 **탐지형**이다」
- **실측** — 외부 모듈에 `external_problems` 를 두고 `main` 에 배선

| 배선 형태 | 결과 |
|---|---|
| 직접 호출 `external_problems(None)` | **1 failed** — `not ['external_problems']` (닫혔다 ✅) |
| **모듈 레벨 별칭** `_ext_alias = external_problems` → `_ext_alias(None)` | **36 passed — 초록** |
| **`functools.partial`** `_ext_partial = partial(external_problems)` → `_ext_partial(None)` | **36 passed — 초록** |

- **성질** — `_root_helper_calls` 는 `ast.Name`/`ast.Attribute` 의 **이름이 helper 접미사 규약에 맞을 때만**
  센다. 별칭·partial 은 이름이 규약 밖이라 helper 로 보이지 않고, `_DYNAMIC_LOOKUP_NAMES` 7종에도 없어
  「무력화 형태 없음」 단언도 통과한다. **「탐지형」 선언의 실제 도달이 닫힌 열거다.**
- **게이트 18 X2b 와의 관계 주의** — cross §3.3 은 「동일 모듈 별칭은 0-호출 실패(빨강, 안전 방향)」로
  합의했다. 그 논거는 **표에 있는 helper** 에만 성립한다. **새 외부 helper** 는 표에 행이 없어 실패시킬
  단언 자체가 없으므로, X2b 의 안전 방향 판정이 이 축을 덮지 않는다.
- **자기 등재 여부** — 커밋 메시지의 「남은 빈자리」 2건에 **이 형태는 없다.**
- **마감** — Phase 3 착수 전 · **관련 종료 조건** — §6 X2 · 규칙 1(범위는 근거를 넘지 않는다)

#### R19-3 — **수정 필요** · `_MAINLINE_ROOTS` 밖으로 본류가 늘면 X2a 축 전체가 다시 열린다 (레인 자기보고 ⑵ — 심각도 판정)

- **근거 위치** — `tests/test_parity_ci_gate.py:622`(`_MAINLINE_ROOTS = ("main", "compare_file")`) · `:1041`
- **실측** — `compare_parity.py` 에 `_mainline_stage_two()` 를 새로 만들고 거기서 외부 모듈 helper 를
  부른 뒤 `main` 이 그것을 호출 → **36 passed (초록)**
- **심각도 판정 근거** — 이 false-green 은 **적대적 편집이 아니라 평범한 리팩터링**(긴 `main` 을 단계
  함수로 쪼개기)에서 자연히 발생한다. `_MAINLINE_ROOTS` 가 실제 본류를 덮는지 재는 검사는 **없다**
  (`assert root_calls` 는 두 함수 중 **하나에라도** helper 호출이 남아 있으면 통과한다).
  레인은 이를 「보고에만」으로 두었으나, 발생 경로가 자연스럽다는 점에서 **수정 필요**로 올린다.
- **처방 비용 고지** — 「본류의 전이적 도달을 계산한다」는 **새 기제**에 해당할 수 있다.
  리더 제약을 유지한 채 닫으려면 상수 크기 정확 일치 + 「본류 밖 최상위 함수는 helper 규약 호출을 하지
  않는다」 정도가 후보다. **처방은 정하지 않는다.**
- **마감** — Phase 3 착수 전 · **관련 종료 조건** — §6 X2

#### R19-4 — **수정 필요** · `_MAINLINE_PHRASES` **내용 교체**로 대조군 2건이 X1 과 동일하게 공허해진다 (레인 자기보고 ⑴ — 심각도 판정)

- **근거 위치** — `tests/test_parity_ci_gate.py:625-633`(정의) · `:639`(`EXPECTED_MAINLINE_PHRASES`) ·
  소비 `:727-728`·`:747-748`
- **실측** — `_MAINLINE_PHRASES = tuple(f"ZZZ{i}" for i in range(15))` → **36 passed (초록)**
- **성질** — 개수 정확 일치는 **빈 형태만** 닫는다. 길이가 같은 다른 문자열이면 대조군 두 곳의
  `assert phrase not in output` 루프가 **언제나 참**이 되어, X1 이 지적한 「아무것도 부정하지 않는 대조군」
  상태가 그대로 재현된다. **결과 상태가 X1 과 같다** — 도달 경로만 다르다.
- **자연 발생 경로** — 비교기 문구가 바뀌어 하드코딩 5개 중 하나를 손으로 갱신할 때, 오타 하나로 그 항목이
  조용히 무의미해진다(개수는 그대로).
- **값싼 처방이 있다(참고, 처방 아님)** — 표 파생 절반은 `set(_MAINLINE_HELPERS.values())` 의 문구 집합이
  `_MAINLINE_PHRASES` 에 **포함되는지**를 단언하면 닫힌다. 새 기제가 아니라 기존 값의 대조다.
- **마감** — Phase 3 착수 전 · **관련 종료 조건** — §6 X2 · 규칙 3

#### R19-5 — **수정 필요** · LIMIT 잔여 목록 둘이 **빈 선언에서 통과**한다

- **근거 위치** — `tests/test_run_gate.py:158-168`(`_KIND_A_ENTERS_BUT_ZERO_WORK`) ·
  `:193-200`(`_KIND_B_CALLER_NEUTRALIZES`)
- **실측**

| 변이 | 결과 |
|---|---|
| `_KIND_A_ENTERS_BUT_ZERO_WORK = ()` | **24 passed · 1 skipped — exit 0** |
| `_KIND_B_CALLER_NEUTRALIZES = ()` | **31 passed · 1 skipped — exit 0** |

- **성질** — X3 를 닫은 증거(잔여 두 종류의 대표 입력 문서화)가 **한 줄 편집으로 전부 사라지는데 초록**이다.
  `_MAINLINE_HELPERS`/`_MAINLINE_PHRASES` 에는 정확 일치를 걸었으면서 **같은 배치가 새로 만든 이 두
  목록에는 걸지 않았다.** R19-1 과 같은 종류이나, 잃는 것이 「사건 탐지」가 아니라 「한계 문서화의 tripwire」
  라서 차단으로 올리지 않았다. **규칙 3의 문면은 범주적**이므로 리더가 R19-1 과 같은 층으로 올릴 수 있다.
- **마감** — 러너를 게이트 근거로 처음 쓰기 전 = Phase 3 착수 전 · **관련 종료 조건** — 규칙 3·규칙 6

#### R19-6 — **수정 필요** · X3 정정이 **반대 방향으로 과도**하다 — `:113` 오류 문구와 대조표가 실측보다 **좁다**

- **근거 위치** — `run_gate.sh:113`(「주석 전용·백슬래시-개행 — **이 러너가 아는 두 형태**」) ·
  `run_gate.sh:24-25` · 대조표 `tests/test_run_gate.py:21-22`(「잡는 두 형태」)
- **실측 (저장소 실물 러너, 변조 없음)**

| 입력 | rc |
|---|---|
| `# only a comment` | **2** |
| 백슬래시-개행만 | **2** |
| **`for x in ; do echo $x; done`** | **2** |
| **`f() { echo x; }`** | **2** |
| **`f() { echo x; }; g() { echo y; }`** | **2** |
| `case x in y) echo n;; esac` / `while false; do echo x; done` / `{ :; }` | 0 |

- **성질** — 게이트 18 X3 의 결함은 「선언이 도달보다 **넓다**」였고, 정정 결과는 「선언이 도달보다
  **좁다**」가 됐다. 둘 다 **닫힌 열거**라서 규칙 4가 요구하는 「종류로 댄다」를 (가)/(나) 잔여 쪽에만
  적용하고 **잡는 쪽에는 적용하지 않았다.** 실제 종류는 메시지 앞부분이 이미 정확히 적고 있다 —
  「어떤 명령도 실행 단계에 들어가지 않았다」. 괄호 안 열거가 그것을 두 형태로 좁힌다.
- **영향** — `f(){…}` 전용 입력으로 rc 2 를 받은 호출자가, 자기 입력에 해당하지 않는 두 형태만 안내받는다
  (오진단). 또 rc 2 를 내는 나머지 형태 셋에 **테스트가 없어** 조용히 바뀔 수 있다(T4 잔여와 같은 자리).
- **부수 표기 (같은 항목에 묶음)** — 머리 주석 `:32-35` 는 「**빈** 명령 치환 `$()`·백틱」이라 적는데
  LIMIT 대표 입력은 `` `true` ``(빈 치환이 아니다)다. 라벨은 「백틱 치환」이라 모순은 아니나 머리 주석의
  한정어와 어긋난다.
- **마감** — 러너 실사용 전 = Phase 3 착수 전 · **관련 종료 조건** — 규칙 4 · §6 규칙 6

#### R19-7 — **수정 필요** · X4 의 대체 선언(「환경변수로 대상을 바꾸는 손잡이는 없다」·「모든 테스트는 추적 실물을 겨눈다」)의 **강제자가 0**이고, 환경변수 경유 리다이렉트가 **여전히 실측된다**

- **근거 위치** — `tests/test_run_gate.py:49-56`(머리 주석) · `:77`(`_invoke` docstring) · `:72-92`
- **실측 A — 손잡이 제거는 확인됐다 (긍정 결과)**

| 조건 | 결과 |
|---|---|
| 추적 실물의 zero-work 판정을 `if false` 로 무력화, 손잡이 없음 | **3 failed / 30 passed** (`test_comment_only_arg_exits_2` · `test_backslash_newline_only_arg_exits_2` · `test_LIMIT_bash_env_preempts_marker_rc_is_0`) |
| 같은 파손 + `RUN_GATE_PATH` = 온전한 사본 | **3 failed** — 게이트 18 의 23 passed 경로가 **닫혔다** |
| 같은 파손 + `GATE_RUNNER`/`RUNNER`/`RUN_GATE` = 온전한 사본 | **3 failed** |

- **실측 B — 그러나 온전한 사본은 아직 끼워 넣을 수 있다**

| 경로 | 결과 |
|---|---|
| `tests/conftest.py` 에 autouse 픽스처를 넣어 모듈 전역 `_RUNNER` 를 사본으로 재바인딩 | **33 passed — 전건 초록** |
| **`PYTEST_PLUGINS` + `PYTHONPATH`**(저장소 편집 **0**) 로 플러그인을 붙여 `_RUNNER` 재바인딩 | **33 passed — 전건 초록** (대조군: 3 failed) |
| `_invoke(다른 경로, …)` 테스트를 새로 추가 | 34 passed — 아무 곳도 빨개지지 않음 |

- **성질** — 파일이 만든 손잡이는 제거됐다(A). 그러나 그 자리를 **산문 선언**이 대신했고 그 선언의
  도달은 0이다. `_invoke` 호출부 4곳(`:92`·`:186`·`:230`·`:237`)은 **오늘은 전부 `_RUNNER`** 지만
  그것을 재는 검사가 없다 — **같은 배치가 옆 파일에서는 정확히 이 형태를 AST 로 강제했다**
  (`_root_helper_calls`). 비대칭이 지적의 요지다.
- **범위 한정 (과대 지적 방지)** — `PYTEST_PLUGINS` 경로는 **이 파일 고유가 아니라 pytest 전반의 성질**이고
  저장소의 모든 테스트에 같은 정도로 성립한다. 따라서 **이 배치의 새 결함으로 세지 않는다.**
  지적 대상은 **`:51-52` 문면이 그 사실과 어긋난다는 것**이다(정직한 문면은 「이 파일은 대상 선택 손잡이를
  제공하지 않는다」). codex 가 X4 에 쓴 문장 — "a stale or injected CI/local environment can test a
  different script" — 은 그 경로에 한해 여전히 참이다.
- **마감** — 러너 실사용 전 = Phase 3 착수 전 · **관련 종료 조건** — §6 규칙 6(자기 검사)

#### R19-8 — **권고** · 대조표 `:23`·`:25` 가 **대리 지표**다 — 지목한 테스트가 그 주장을 재지 않는다

- **근거 위치** — `tests/test_run_gate.py:23-25`(「명령 치환은 functrace 와 무관하게 진입」 →
  `test_LIMIT_…[$(echo)]`) · `run_gate.sh:27-28`
- **실측 (functrace 제거 변이)**

| 입력 | functrace 있음 | functrace 없음 |
|---|---|---|
| `(echo sub)` | rc 0 | **rc 2** ← `test_subshell_command_is_work_not_zero_work` 가 빨개진다 (강제자 있음 ✅) |
| `$(echo)` | rc 0 | **rc 0** ← 주장은 **참**이나 이것을 재는 테스트는 없다 |
| `` `true` `` | rc 0 | rc 0 |

- **성질** — 「무관하다」는 **두 조건의 비교** 주장인데, 지목된 LIMIT 테스트는 functrace 가 켜진 한 조건만
  잰다. 주장 자체는 이번에 내가 실측으로 확인했으므로 **참**이다 — 결함은 **결속**이다.
- **마감** — 러너 실사용 전 · **관련 종료 조건** — §6 규칙 6 · 게이트 18 T4 계열

#### R19-9 — **권고** · `_DYNAMIC_LOOKUP_NAMES` 의 **범위가 근거보다 넓다**(리더 지적 3종 대비)

- **근거 위치** — `tests/test_parity_ci_gate.py:1012-1014` ↔ `:1026-1029`(docstring 이 지명하는 것:
  `getattr`·`__import__`·`importlib.import_module`·함수 안 `import`)
- **사실** — 실제 집합은 **7종**이다: `getattr` `__import__` `globals` `locals` `vars` `eval` `exec`.
  docstring·커밋 메시지가 든 근거는 4종이고, 나머지 3종(`globals`·`locals`·`vars`)에는 근거가 없다.
- **성질** — 방향은 **탐지형 확대**라 은폐형은 아니다(규칙 4 기준 안전 방향). 다만 규칙 1(범위는 근거를
  넘지 않는다)에 걸리고, **오탐이 났을 때의 처방이 「면제 한 줄 추가」로 흐를 위험**이 실질 리스크다 —
  그것이 게이트 17 ③이 지적한 은폐형으로의 회귀 경로다.
- **마감** — Phase 3 착수 전(R19-1 과 같은 상수를 손대는 김에) · **관련 종료 조건** — 규칙 1·규칙 4

#### R19-10 — **권고** · T3·T4·T5 는 **이 배치의 범위 밖이었고 미해소**이며, T4 는 **문장이 늘어 오히려 커졌다**

- **근거 위치** — 대조표 `tests/test_run_gate.py:12-42` ↔ `run_gate.sh` 머리 주석
- **사실**
  - **T3(대조표 자신의 강제자 0)** — 여전히 산문이다. 이름을 댄 테스트가 사라져도 빨개지지 않는다.
  - **T4(대조표가 머리 주석 문장을 다 덮지 않는다)** — 대조표는 LIMIT 두 종류 항목이 추가돼 넓어졌으나,
    머리 주석에 **새 문장**이 들어왔다: `run_gate.sh:39` 「러너는 협조하는 호출자를 위한 장치이지
    적대적 호출자를 막는 장치가 아니다」 — 대응 항목 없음. 게이트 18 이 센 미대응 문장 3개(자기 도달 ⑴ ·
    장치 분류 「면제 목록 없음」 · zsh 안전)도 그대로다.
  - **T5(zsh 로 러너를 부르는 테스트 0)** — `:372-392` 는 `bash`/`sh`/`zsh` 를 **바깥 셸**로만 쓰고
    안쪽 러너는 언제나 `bash {_RUNNER}` 다(`:385`). 미해소.
- **마감** — 러너 실사용 전 (게이트 18 마감 승계) · **관련 종료 조건** — §6 규칙 6

### 2.2 보안 불변식

#### R19-11 — **권고** · `:39` 의 면제 조항이 **열려 있고**, 「적대」가 아닌 **상속된 `BASH_ENV`** 도 같은 결과를 낸다

- **근거 위치** — `run_gate.sh:36-40`(잔여 (나) · 「적대적 호출자를 막는 장치가 아니다」) ·
  `tests/test_run_gate.py:217-232`
- **실측**

| 조건 | 결과 |
|---|---|
| 주변 환경에 `BASH_ENV`(마커 선점 스크립트)가 **이미 있는 상태**로 러너에 주석 전용 입력 | **rc 0** — zero-work 보장이 조용히 사라진다 |
| 같은 환경에서 `pytest tests/test_run_gate.py` | **3 failed** — 스위트는 그 상태를 **드러낸다**(탐지 있음 ✅) |

- **성질** — 문면은 원인을 「호출자의 능동적 무력화」로 못박는데, **상속된 환경변수는 호출자의 행위가
  아니다**(CI 베이스 이미지·셸 프로파일). 규칙 4가 「은폐형(면제 조항)은 넓히지 말고 탐지형으로 갈아탄다」
  고 정한 바로 그 형태이며, 여기서는 **범위가 근거(대표 3종)보다 넓다**.
- **완화 사실 (양쪽 다 적는다)** — LIMIT 테스트가 rc 0 을 **단언**해 문서화했고, 스위트가 같은 환경에서
  돌면 3건이 빨개져 드러난다. 즉 **탐지형 요소가 이미 붙어 있다** — 남은 것은 문면의 범위다.
- **보안 축 판정 우선권 고지** — 이 항목은 `privacy-gate` 의 감사 목록에 닿지 않는다(사용자 데이터·마스킹·
  암호·인증 경로 무관). 판정이 갈리면 `privacy-gate` 를 따른다.
- **마감** — 러너 실사용 전 · **관련 종료 조건** — 규칙 4

#### S19-1 — **검토함 — 지적 없음** · 이 배치는 개인정보·암호·인증 경로에 닿지 않는다

- `backend-kotlin/`·`contracts/`·`frontend/`·`app/`·`parity/` **diff 0파일**(전수 확인).
- 러너가 `[run_gate] cmd:` 로 명령 전문을 stdout 에 남기는 성질은 **무변경**이며 `:327-331` 테스트가 그대로
  고정한다. 새로 인자에 값을 넣는 코드는 없다. 마스킹·LLM 호출·AEAD·JWT·Argon2 경로 **무접촉**.
- 새로 만든 임시 파일은 `tmp_path` 안 `bash_env.sh` 하나이고 비밀값을 담지 않는다.
- **이월(이 배치가 만든 것이 아님)**: 게이트 18 S2(SKILL.md 축약으로 「인자에 비밀값을 넣지 마라」 경고가
  사용 지점에서 사라진 문제)는 **여전히 열려 있다** — 리더 판단 대기 항목.

### 2.3 계약 준수

**C19-1 — 검토함 — 지적 없음.** HTTP 계약에 닿는 변경이 **0줄**이다. `contracts/easy-doc-v1.yaml` 무변경,
`frontend/src/api/` 무변경. 상태 코드·`{"detail": …}`·snake_case·`no-store`·`Location`·RFC 5987·CORS 노출
헤더 어느 것도 이 배치의 대상이 아니다.

### 2.4 parity 위험

**P19-1 — 검토함 — 지적 없음(직접 영향).** `parity/fixtures/` 무변경, `compare_parity.py` 무변경,
`dump_python_snapshots.py --check` **exit 0**(스냅샷 무영향, 실행 확인).

**P19-2 — 사실 등재.** 이 배치가 손댄 `test_parity_ci_gate.py` 는 parity **게이트의 자기 검사**다.
R19-1·R19-2·R19-3·R19-4 는 전부 「비교기가 게이트를 우회했을 때 그것을 알아채는 능력」에 관한 것이므로,
**parity 판정의 신뢰도에 직접 걸린다.** 지적 자체는 테스트 적정성 축에 적었다.

### 2.5 Kotlin/Spring 관용성

**K19-1 — 검토함 — 지적 없음.** `backend-kotlin/` diff **0줄**. 모듈 경계·트랜잭션·`JdbcClient`·Flyway·
Testcontainers 어느 것도 대상이 아니다. `./gradlew` 계열은 **미실행**(§5).

---

## 3. 도달 범위 점검 — 다섯 축을 가로지르는 필수 구획

> 지적이 없으면 「검토함 — 지적 없음」, 보지 못했으면 「미검토(사유)」로 적는다. 비워 두지 않는다.

| # | 점검 항목 | 결과 |
|---|---|---|
| ⑴ | **「전역·모든·항상」 선언에 닿지 않는 경로가 있는가** | **지적 있음** — 이 배치가 새로 쓴 범위 선언 4개를 실행으로 대조했다. ⓐ 「모든 잡·모든 스텝의 `run` 값을 통째로 읽는다」(`test_run_gate.py:345-346`) → **참**(§3.1) ⓑ 「본류 함수 안의 **모든** `ast.Call`」(`:1022`) → **참** ⓒ 「정의 모듈과 **무관하게**」(`:1024`·`:1147`) → **직접 호출에 한해 참**, 별칭·partial 은 도달 밖(**R19-2**) ⓓ 「**모든** 테스트는 추적 실물을 겨눈다 / 환경변수 손잡이는 **없다**」(`:51-52`) → **오늘 참 · 강제자 0 · 환경 경유 리다이렉트 실측**(**R19-7**) |
| ⑵ | **그 게이트가 지금 어디서 도는가 — 도달 0 을 특히 의심** | **검토함 — 지적 없음.** `ci.yml` 을 YAML 파싱해 확인: `ci:quality` 가 `tests/test_run_gate.py`·`tests/test_parity_ci_gate.py`·`tests/test_harness_scope_reach.py` 를 **셋 다 경로 명시 스텝**으로 부른다(전체 수집 의존 아님). 러너 자신의 CI 배선은 **0**이고 그 사실을 `test_runner_itself_has_no_ci_wiring_but_this_file_does` 가 잰다. **인계 전 「도달 0」이던 helper 3 + 상수 1은 전부 배선됐다**(§1.3, AST 로 호출부 확인) |
| ⑶ | **측정이 대리 경로에서 이뤄지지 않았는가** | **지적 있음(2건).** ⓐ 대조표 `:23`·`:25` — 「명령 치환은 functrace 와 **무관**」이라는 **비교 주장**을 한 조건만 재는 테스트에 결속했다(**R19-8**). ⓑ 러너 음성 대조는 **저장소 실물**을 직접 파손해 측정했고 사본을 쓰지 않았다 — 이쪽은 **정상**(§2.1 R19-7 실측 A) |
| ⑷ | **검사의 기준이 검사 대상 자신에게서 나오지 않는가** | **검토함 — 지적 없음.** `EXPECTED_MAINLINE_PHRASES = EXPECTED_MAINLINE_HELPERS + 5`(`:639`)는 **손으로 고정한 리터럴 10**에서 유도되고, 재는 대상 `_MAINLINE_PHRASES` 자신에게서 나오지 않는다. 자기 참조가 아니다. `_asserted_output_phrases`·`_string_constants` 도 **테스트 소스와 비교기 소스라는 서로 다른 두 원본**을 대조한다 |
| ⑸ | **판정이 대리 지표로 이뤄지지 않는가** | **검토함 — 지적 없음.** 이 회차의 모든 해소 판정은 **변이 후 실패 건수와 실패 단언의 문면**으로 했고, 「종료 코드 0」이나 「지적 건수」를 근거로 쓰지 않았다. LIMIT 15건은 「rc 0 을 단언」이라는 그 주장 그대로 수집 목록으로 확인했다(§4 X3 행) |
| ⑹ | **규칙·패턴의 범위가 근거보다 넓지 않은가 (은폐형 확대 여부)** | **지적 있음(2건).** ⓐ `run_gate.sh:39` 「적대적 호출자를 막는 장치가 아니다」 — **열린 면제 조항**이고, 상속된 `BASH_ENV` 는 적대가 아닌데 같은 결과를 낸다(**R19-11**). ⓑ `_DYNAMIC_LOOKUP_NAMES` 7종 ↔ 근거 4종(**R19-9**, 방향은 탐지형이라 안전). **새 `.gitignore`·`noqa`·마커 제외·억제 패턴은 이 배치에 0건**(diff 전수 확인) |
| ⑺ | **음성 대조가 붙어 있는가 — 떼면 무엇이 깨지는가** | **지적 있음.** 붙어 있는 것: X1(`()` → 1 failed) · X2a 직접 호출(→ 1 failed) · T1(주석화 → 1 failed) · X4(파손 → 3 failed) · T2(5형식 전건 → 1 failed) — **전건 독립 재현했다**(§4). **떼어도 아무것도 깨지지 않는 것**: `_DYNAMIC_LOOKUP_NAMES`(**R19-1**) · `_KIND_A`/`_KIND_B`(**R19-5**) · 「모든 테스트는 추적 실물을 겨눈다」(**R19-7**) · 대조표 자신(T3, 이월) |
| ⑻ | **판정하는 코드가 자기 자신을 검사 대상에 넣었는가** | **검토함 — 지적 없음.** `mypy . .claude` **Success(137 파일)** · `ruff check .` 0 · `ruff format --check .` 0 — 세 게이트 스크립트/테스트가 전부 그 범위 안이다. `run_gate.sh` 는 `bash -n` 까지이고 **shellcheck 도달 0**은 게이트 17 ⑮ 그대로 이월(이 배치 무변경) |

---

## 4. 게이트 18 종결 판정표 (리더 요청)

> 판정 근거는 **전건 이 회차의 독립 실행**이다. 레인 보고를 인용하지 않고 다시 돌렸다.

| 항목 | 요지 | **판정** | 음성 대조 (이 회차 실측) | 잔여 |
|---|---|---|---|---|
| **X1** | 빈 `_MAINLINE_PHRASES` → 완전성·대조군 공허 통과 | **부분 해소** | `_MAINLINE_PHRASES = ()` → **1 failed**(`0 == 15`) ✅ | **내용 교체 형태는 열려 있다** — 길이 같은 15개로 치환 → **36 passed**(**R19-4**) |
| **X2a** | 외부 모듈 helper 가 완전성 게이트에 안 보인다 | **부분 해소** | 외부 모듈 helper 직접 배선 → **1 failed**(`not ['external_problems']`) ✅ | **별칭·partial → 36 passed**(**R19-2**) · **`_MAINLINE_ROOTS` 밖 본류 함수 → 36 passed**(**R19-3**) · **`_DYNAMIC_LOOKUP_NAMES` 빈 선언 + `getattr` → 36 passed**(**R19-1, 차단**) |
| **X3** | 마커 선언이 실제 탐지보다 넓다(잔여 최소 9종) | **해소** | LIMIT **15건 전건 수집·통과** = 종류별 대표 입력마다 rc 0 단언 ✅ · 머리 주석 (가)9 / (나)3 ↔ 테스트 9 / 3 **전건 대응** ✅ | **정정이 반대로 과도** — `:113`「아는 두 형태」인데 실측 rc 2 는 **≥4형태**(**R19-6**) · 두 목록이 **빈 선언에서 통과**(**R19-5**) |
| **X4** | 기본 대상 테스트가 활성 `RUN_GATE_PATH` 를 가린다 | **해소** | 실물 파손 → **3 failed** · 파손 + `RUN_GATE_PATH`=온전한 사본 → **여전히 3 failed** · 다른 후보 환경변수 3종도 **3 failed** ✅ (게이트 18 의 `23 passed` 경로가 닫혔다) | **대체 선언의 강제자 0** · conftest / `PYTEST_PLUGINS` 경유 재바인딩 → **33 passed**(**R19-7**) |
| **T1** | 문구 결속이 문자열 grep — 주석 한 줄로 충족 | **해소** | 단언 주석화+`pass` → **1 failed** ✅ · docstring 만 남은 helper 이름은 **세지 않음** 확인 ✅ · 문구 단언만 주석화(helper 결속 유지) → **1 failed** = `_asserted_output_phrases` **단독 도달 확인** ✅ | 없음 |
| **T2** | CI 배선 탐지기가 세 형식 중 하나만 본다 | **해소** | 심기 **5형식 전건 1 failed** — 블록 스칼라 `run: \|` · `- run:` 인라인 · `shell:`+`working-directory` 동반 · 폴드 스칼라 `run: >-` · matrix 표현식 포함 ✅ · 경로 명시 스텝 제거 대조군 → **1 failed** ✅ · `steps` 없는 `uses:` 잡 추가 → **크래시 없음** ✅ (읽는 run 스텝: 4잡 **31건**, 옛 `startswith` 방식은 21줄) | **`ci.yml` 한 파일만 읽는다** — 「CI 배선 0」 선언 대비. 현재 `.github/workflows/` 에 파일이 **그 하나뿐**이라 실해 0이고, 이 전제는 `test_harness_scope_reach.py:194` 도 공유한다(**하네스 전반 · 이 배치의 신설 아님**) |
| **R18-1** | 「서브셸·명령 치환 (실측)」 주장 절반 미재현 | **해소** | functrace 제거 변이 → `(echo sub)` **rc 2**(갈린다) · `$(echo)`·`` `true` `` **rc 0**(안 갈린다) — **정정 내용이 참임을 독립 확인** ✅ | 그 「무관」 주장에 강제자 없음(**R19-8**, 권고) |
| **R18-2** | 잔여가 닫힌 열거인데 실제로는 더 넓다 | **해소** | 잔여 선언이 1종 열거 → **두 종류(가)(나)** 로 재기술됐고, 「열거는 종류의 **대표**이지 잔여의 전부가 아니다」를 `:157` 이 명시 ✅ | (**R19-5** 의 빈 선언 통과가 이 문서화의 tripwire 를 지운다) |
| **R3** | 원장 `:548` 「9자리·9/9」 ↔ 코드 10 | **해소** | 원장 `00_progress.md:550` 이 「**[게이트 18 정정] 10자리 · 음성 대조 10/10**」으로 갱신됨(`df365a4`) · `:716` 이 R3 을 「해소 — 이 갱신」으로 등재 ✅ (**읽기만 했다**) | 상수↔원장을 잇는 **강제자는 여전히 0** — 다음에 값이 갈려도 아무 데서도 안 빨개진다(게이트 18 R3 의 원 지적 그대로 이월) |
| **(부속) 인계 전 도달 0** | helper 3 + 상수 1이 어느 단언에도 배선 안 됨 | **해소** | 6개 산출물 전건 호출부 확인(§1.3) ✅ | 없음 |
| **(참고) X5** | `BASH_ENV` 마커 선점 | **미해소(의도됨)** | 상속 `BASH_ENV` → 주석 전용 입력 **rc 0** 재현 · 스위트는 **3 failed** 로 드러냄 | 원장이 「열림」으로 등재. **R19-11** 은 그 처분이 아니라 **문면의 범위**에 대한 지적 |
| **(참고) Y-3** | `test_default_target…` 의 sha256 대조가 같은 경로를 두 번 읽음 | **해소(테스트 소멸)** | 그 테스트가 사라지고 추적 여부 단언이 `test_runner_exists_tracked_and_parses:95-109` 로 흡수됨(`is_file` + `git ls-files --error-unmatch` + `bash -n`) ✅ | 러너 **내용**에 대한 대조는 이제 없다(설계상 정당 — 작업본을 재는 것이 맞다) |

**집계 — 해소 6(X3·X4·T1·T2·R18-1·R18-2) + R3 해소 · 부분 해소 2(X1·X2a) · 미해소 0.**
충돌이던 **X1·X4 는 양 관점에서 각각 닫혔다** — X1 은 codex 가 든 직접 편집 경로가 실제로 빨개지고
(Claude 가 들었던 표-경유 경로는 원래 ⑤a/⑤b 가 막는다), X4 는 손잡이 자체가 사라져 두 해석이 함께
소멸했다. **다만 둘 다 같은 종류의 잔여를 남겼다**(R19-4 · R19-7).

### 4.1 리더 제약(새 기제 금지) 준수 판정

| 커밋 | 들어온 것 | **제약 판정** |
|---|---|---|
| `e2282b3` | 머리 주석 재기술(표기) · `:113` 문구 정정(표기) · `RUN_GATE_PATH` **제거**(손잡이 제거) · `_invoke` 명시 인자화(교체) · `startswith` → `yaml.safe_load`(**옆 파일의 기존 방법** `read_ci_job_names` 와 동일) · LIMIT parametrize 신설(테스트) | **준수.** 러너 본체의 **판정 기제는 1줄도 안 바뀌었다**(diff 는 주석·문자열·기제 밖). 마커·trap 을 더 쌓지 않았다 |
| `5d58832` | `EXPECTED_MAINLINE_PHRASES`(정확 일치 — 기존 `EXPECTED_MAINLINE_HELPERS` 전례) · `_root_helper_calls`(**같은 파일의 `_call_sites` AST 대조를 호출 측으로 뒤집은 것**) · `_asserted_output_phrases`·`_string_constants`(문자열 grep → **AST**, ③이 이미 쓰던 방법) | **대체로 준수 — 다만 `_DYNAMIC_LOOKUP_NAMES` 는 새 판정 축이다.** 「이 대조를 무력화하는 형태가 없다」는 이 파일에 전례가 없는 새 단언이다 |

**새 기제가 들어왔다면 그것의 자기 빈자리는 무엇인가** (리더 질문에 대한 답)

1. **`_DYNAMIC_LOOKUP_NAMES` (새 판정 축)** → 자기 빈자리 **둘**: **빈 선언에서 통과**(R19-1, 차단) ·
   **닫힌 열거라 별칭·partial 미도달**(R19-2). 형제 상수 둘은 빈 선언에서 빨개지는데 이것만 초록이다.
2. **`_root_helper_calls` (기존 대조의 방향 전환)** → 자기 빈자리 **하나**: 훑는 범위가
   `_MAINLINE_ROOTS` **닫힌 2종**이고 그 완전성을 재는 검사가 없다(R19-3).
3. **`_asserted_output_phrases`·`_string_constants` (grep → AST 교체)** → **자기 빈자리 발견 못 함.**
   docstring 제외·주석 제외·단독 도달을 각각 실측으로 확인했다(§4 T1 행).
4. **`yaml.safe_load` 교체** → 자기 빈자리 **하나**: 읽는 파일이 `ci.yml` 하나로 못박혀 있다
   (하네스 전반의 기존 전제와 같음 — 신설 아님).
5. **`_KIND_A`/`_KIND_B` LIMIT 목록 (새 문서화 장치)** → 자기 빈자리 **하나**: 빈 선언에서 통과(R19-5).

**군집 사실 (판정 아님)** — 이번 회차 지적 11건 중 **8건(R19-1·2·3·4·5·6·7·9)이 「선언이 도달과 어긋난다」
한 종류**이고, 그중 **6건이 이 배치가 방금 세운 장치**다. 게이트 16·17·18 에 이어 **네 회차 연속**
직전 배치의 게이트 장치에서 다음 게이트가 새 빈자리를 찾았다(건수 4 → 5 → 4 → 6).
**Phase 3 대상 제품 코드(JDBC·인증·계약) 지적은 0건**이며, 그 0 은 이 배치의 Kotlin·계약·프론트
diff 가 **0줄**이어서 생긴 것이다 — 「Phase 3 코드가 깨끗하다」가 아니라 **「이번 회차가 Phase 3 코드를
보지 않았다」가 정확한 진술**이다.

---

## 5. Phase 종료 조건 대비 현황 · 미해결 항목

> **자기 승인 금지 준수** — Phase 3 착수 차단 여부를 **판정하지 않았다.** 심각도·마감과
> 「문면상 착수 이전인가」만 표시한다. 이 1차 산출물만으로 종료 조건 충족을 보고하지 않는다.

### 5.1 심각도별 집계

| 심각도 | 건수 | 항목 |
|---|---|---|
| **차단(② 장치)** | **1** | R19-1 |
| **수정 필요** | **6** | R19-2 · R19-3 · R19-4 · R19-5 · R19-6 · R19-7 |
| **권고** | **4** | R19-8 · R19-9 · R19-10 · R19-11 |
| **판정 필요** | **0** | — |
| **검토함 — 지적 없음** | **9** | S19-1 · C19-1 · P19-1 · K19-1 · 도달 범위 ⑵⑷⑸⑻ · §1.3 배선 확인 |
| **미검토(사유)** | **4** | §5.3 |

### 5.2 마감별 정리 (착수 차단 여부는 리더 판정)

**문면상 Phase 3 착수 이전 — 6건**

| 순위 | 항목 | 심각도 | 마감 근거 |
|---|---|---|---|
| 1 | **R19-1** `_DYNAMIC_LOOKUP_NAMES` 빈 선언 통과 + X2a 재개방 | **차단(② 장치)** | §6 X2 게이트 첫 실사용 |
| 2 | **R19-2** 별칭·partial 로 X2a 재개방 | 수정 필요 | 동일 |
| 3 | **R19-3** `_MAINLINE_ROOTS` 밖 본류 함수 | 수정 필요 | 동일 |
| 4 | **R19-4** `_MAINLINE_PHRASES` 내용 교체 공허 | 수정 필요 | 동일 |
| 5 | **R19-5** LIMIT 두 목록 빈 선언 통과 | 수정 필요 | 러너 첫 실사용 |
| 6 | **R19-6** `:113` 문구가 실측보다 좁다 | 수정 필요 | 러너 첫 실사용 |

**러너 실사용 전 — 4건**: R19-7(수정 필요) · R19-8 · R19-10 · R19-11(권고)
**Phase 3 착수 전(함께 손대는 김에) — 1건**: R19-9(권고)

### 5.3 미실행 · 확인 불가

| 범위 | 사유 |
|---|---|
| **Kotlin 테스트**(`./gradlew :api:test` 등) | **미검토** — 이 배치의 `backend-kotlin/` diff 가 **0줄**이라 대상 아님 |
| **골든셋**(`tests/golden`) | **미검토** — 프롬프트·스타일 규칙·LLM 설정 무변경. `-m llm` 레인 미실행 |
| **`--check` 가 `app/**` 를 실제로 다시 읽는지** | **미수행** — 게이트 17·18 이월 그대로. exit 0 과 「키 20개/13개 일치」 출력만 확인했다 |
| **shellcheck** | **미실행** — 저장소·CI 어디에도 없다(게이트 17 ⑮). 러너는 `bash -n` 까지 |
| **bash 5.x 에서의 DEBUG/functrace 거동** | **미검토(사유)** — 이 환경에 bash 3.2.57 하나뿐. 이번 실측(functrace 유무 갈림·rc 2 형태 목록)은 **3.2 기준값**이며 CI(ubuntu-latest, bash 5.x)에서 달라질 수 있다. 게이트 18 이월 |
| **codex 관점** | **해당 없음** — 1회차는 codex 부재가 정상 상태다. 교차 대조는 2차에서 한다 |
| **`00_progress.md` 갱신** | **하지 않았다** — 읽기만 했다(`df365a4` 판) |
| **다른 워크플로 파일에서의 러너 배선** | **확인 불가 아님 — 대상 없음.** `.github/workflows/` 에 `ci.yml` **한 파일뿐**임을 확인했다. 파일이 늘면 T2 탐지기의 도달이 선언보다 좁아진다(§4 T2 잔여) |

### 5.4 게이트 18 이월 중 이 배치가 손대지 않은 것 (사실 등재)

T3(대조표 강제자 0) · T4(대조표 미대응 문장, **1문장 늘었다**) · T5(zsh 로 러너 호출 0) · R7(nounset 명시
도달, 충돌) · S2(SKILL.md 축약, 충돌) · T6 · X5(열림 — 원장 등재) · X6 · R9 · R10 — **전부 무변경**.
게이트 18 cross §4 의 **충돌 4건(X1·X4·R7·S2)** 중 X1·X4 는 수정으로 소멸했고(§4),
**R7·S2 는 리더 판단이 내려지지 않은 채 남아 있으므로 그대로 다시 올린다.**

---

## 6. 리더에게 (1회차 종결)

1. **산출물 경로** — `docs/migration/_workspace/reviews/19_gate18-fixes_migration-reviewer.md`(이 파일).
2. **집계 — 차단 1 · 수정 필요 6 · 권고 4 · 검토함(지적 없음) 9 · 미검토 4.**
3. **게이트 18 종결 — 해소 6 + R3 해소 · 부분 해소 2(X1·X2a) · 미해소 0.**
   충돌이던 **X1·X4 는 양 관점에서 각각 닫혔고**, 둘 다 같은 종류의 잔여를 남겼다.
4. **차단 1건의 요지** — `_DYNAMIC_LOOKUP_NAMES` 가 **빈 선언에서 통과**하고, 그 상태에서
   **X2a 의 사건 경로가 `getattr` 로 그대로 다시 열린다**(실측 36 passed). 처방은 리더가 이미 허용한
   「정확 일치」이며 **새 기제가 필요 없다.**
5. **제약 준수 판정** — 두 커밋 모두 대체로 준수. 새 판정 축은 `_DYNAMIC_LOOKUP_NAMES` 하나이고,
   **그것의 자기 빈자리가 이번 차단이다**(§4.1).
6. **교차 종합 재호출이 필요하다.** 이 회차는 codex 산출물 없이 수행했다.
   특히 **R19-1(차단)·R19-2·R19-6·R19-7** 은 단일 관점이므로 codex 축으로 되열릴 여지를 남긴다 —
   게이트 16·17·18 에서 **세 회차 연속 codex 가 Claude 의 통과 판정을 되열었다**는 사실을 감안하라.
7. **리더 판단이 미결인 채 이월된 것** — 게이트 18 충돌 **R7·S2**. 답이 오지 않았다는 이유로 닫지 않았다.
8. **저장소 무변조** — `HEAD` = `5d58832` · 추적 파일 변경 **0** · `cp` 복원 **0** ·
   전건 `write_bytes` + `git checkout --` + sha256 대조 · 일회용 worktree 제거 완료 ·
   원장은 **읽기만** · 커밋 **0**.
