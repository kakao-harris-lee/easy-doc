# 게이트 14 · 2단계 교차 종합 (정본) — `14_floor-hardening`

> # ⚠ codex 리뷰 **부분 수령** — 한도 소진(2026-08-20 복구), 하네스 규약(1회 재시도 후 누락 명시 진행) 적용
>
> codex 는 1차 시도에서 **명령 22회**를 실행하고 **어시스턴트 메시지 3건**을 남긴 뒤 `verifying` 단계에서 정지했고,
> 규약상 1회 재시도는 4초 만에 사용량 한도로 실패했다. **`findings` 배열은 세 번 모두 비어 있다** — codex 가
> 구조화된 지적을 **확정 제출한 것은 0건**이다.
>
> **확정 지적 0건은 「무지적」이 아니라 「미수령」이다.** 이 문서 어디에서도 codex 의 침묵을 합의·동의·통과로
> 읽지 않는다. codex 가 **답하지 않은 축**(특히 축 ④ 선언 범위 대 실제 도달 전부)에서 Claude 가 낸 지적은
> **교차 대조되지 않은 단독 지적**이며, 그 사실을 표의 「상태」 열에 매 행 적었다.
>
> **codex 언급 3건(C-1 후단 · C-2 · C-5)은 판정 없는 서술이므로 합의 가중치를 갖지 않는다.** `verdict` 는 세 번
> 모두 `needs-attention` 이나 이는 진행 중 기본값이고, 근거 파일·라인이 어느 항목에도 붙지 않았다.

> **회차**: 2차(교차 종합). **이 회차는 대조만 한다 — 두 산출물에 없던 새 지적을 만들지 않는다.**
> 종합 중 눈에 띈 것은 §6.2 「종합 중 발견 — 미교차」에 분리했고, 다음 회차 범위로 제안한다.
> **1차 산출물의 심각도를 이 회차에서 올리거나 내리지 않았다.**

---

## 1. 대조한 두 산출물

| 회차 | 경로 | 상태 |
|---|---|---|
| 1단계 · codex 독립 | `docs/migration/_workspace/reviews/14_floor-hardening_codex-reviewer.md` | **부분** — 확정 findings 0건, 언급 5항목(C-1~C-5), 축 ④ 전부 미수령 |
| 1단계 · Claude 독립 | `docs/migration/_workspace/reviews/14_floor-hardening_migration-reviewer.md` | **완전본** — 지적 F-1~F-12 + 이월 N-13·N-14·R-10, 최소 회차 조건 4개 판정 |
| 부속 | `docs/migration/_workspace/reviews/14_floor-hardening_codex-attempt1.log` | codex 1차 job 로그 보존본 |

**대상 범위**: `4dafde5..bbbdb6b`(13파일). codex 는 실행 시각 HEAD 진행으로 `4dafde5..f079492`(14파일)를 받았다 —
차집합 1건은 `docs/migration/_workspace/00_progress.md` 로 `codex-review` 스킬 §2.2 리뷰 면제 범주다.
**지정 13파일 누락 0** 이므로 대조 가능 범위에는 영향이 없다.

### 1.1 이 회차에 실행한 제3 근거 조회

> 충돌·전제 확인 목적의 **읽기 전용 실측**이다. `codex-reviewer` 산출물 §4.2 가 `U+0D3E` 전제 확인을
> 이 에이전트에 명시적으로 위임했고, C-5 는 Claude §4 와 정면으로 갈린다. 규약에 따라 **검증 결과를 제3의
> 근거로 추가할 뿐 어느 쪽 지적도 지우지 않는다.**

| # | 조회 | 결과 |
|---|---|---|
| W-1 | `visible_length` / `has_visible_reason` 직접 호출 (9탐침) | **C-2·C-3 전부 재현** — §3.2 |
| W-2 | `dump_parity_fixtures.py:2141-2151`, `:2183-2186` | **C-5 기제 확인** — 조건부 생략 + 대체 이름 보전 단언 — §3.3 |
| W-3 | `.github/workflows/ci.yml:380-470` | **C-1 후단 기제 확인** — 비감소 가드의 기준점이 저장소 내 파일 — §3.1 |

---

## 2. 교차 대조표

> 「상태」 열의 **codex 미수령**은 codex 가 그 축에 답하지 않았다는 뜻이며 **동의도 반대도 아니다.**
> 「심각도」는 **1차 산출물의 값을 그대로 옮긴 것**이다 — 이 회차에서 재판정하지 않았다.

| # | 항목 | 근거 위치 | 축 | Claude | codex | 상태 | 심각도 | 마감 | 관련 종료 조건 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 전체 게이트 하한이 **CI 에 실제 배선**돼 있다 | `ci.yml:433-452` · run 31854263996 | 도달 범위(parity) | 지적함(해소 판정 · 러너 실측 V-2·V-3) | 지적함(C-1 전단) | **합의** | 해소 | — | §5 Phase 2 · 게이트 13 X-1 |
| 2 | 선언 하한·정본(`BUILDERS`)·표시 파일을 **함께 축소하는 동기화 편집**은 자동 실패가 아니라 리뷰 신호에만 의존 | codex 근거 미제시 / `ci.yml:392-398` (W-3) | 도달 범위(parity) | **반대 견해** — §5.2 「실효 우회는 없다」(8시나리오 열거) | 지적함(C-1 후단, 판정 없는 서술) | **충돌** | **판정 필요** | Phase 2 종료 판정 전 | §6 게이트 · Phase 2 |
| 3 | 억제 사유 **문장부호 2자(`..`)** 가 `visible_length=2` 로 통과 | `scan_privacy_invariants.py:188-195` | 보안 불변식 | 지적함(**사실 인지** — 하한 2 근거 문단이 한계로 명기) | 지적함(C-2 전단, **반례**로 제시) | **합의(사실) · 판정 갈림** | Claude=권고 이하(규칙 6 준수) / codex=미판정 | — | §2.3 · I-항목 |
| 4 | 억제 사유 **결합 표 `Mc` 2자(`U+0D3E`)** 가 `visible_length=2` 로 통과 → 적중 1건 억제 + `marker_problems=[]` | `scan_privacy_invariants.py:159` (`Mc` 미제외) · W-1 실측 | 보안 불변식 | **안 함** (N-20 **해소** 판정) | 지적함(C-2 후단) | **codex 단독** | **판정 필요** (`privacy-gate` 우선권) | Phase 2 종료 판정 전 | §2.3 · 게이트 13 N-20 |
| 5 | RTL 제어·태그 문자·이형태 선택자·미할당·서로게이트는 `visible_length=0` 으로 **거절**된다 | `tests/test_privacy_scanner.py:1321-1383` · W-1 | 보안 불변식 | 지적함(음성 8케이스 상시) | 지적함(C-3) | **합의** | 지적 아님(음성 방향 확인) | — | §2.3 |
| 6 | 표지 union 상수 하나로 전 케이스를 통과시키는 구현은 **`absent` 에 걸린다** | `dump_parity_fixtures.py:2152-2165` | parity | 지적함(X-4 처방 준수 확인) | 지적함(C-4) | **합의** | 지적 아님 | — | 게이트 13 X-4 |
| 7 | 생성기 `title_markers` 가 **빈 목록으로 갈리는 방향**의 자기 신고 | `dump_parity_fixtures.py:2141-2151` vs `:2183-2186` (W-2) | 도달 범위(parity) | **반대 견해** — §4 「양방향 모두 성립」 | 지적함(C-5 — 「단언 자체가 사라져 자기 신고 못 함」) | **충돌** | **판정 필요** | Phase 2 종료 판정 전 | 게이트 13 X-3·X-4 |
| 8 | **F-1** `ci.yml` 강제 지점 기록이 거짓(`effective_scope` 부재 + 폐기 설계 기술) | `ci.yml:441` · V-5 | 도달 범위(parity) | 지적함 | **미수령**(축 ④) | **Claude 단독** | **수정 필요 · 차단② 승격 판정 필요** | **Phase 2 종료 판정 전** | §5 Phase 2 · §6 게이트 |
| 9 | **F-2** 단언 630 이 `parity-full-gate.txt`·원장 L352 에 미반영, 같은 문서 안 608↔630 자기모순 | V-8 · V-9 · `00_progress.md:352` vs `:384` | 도달 범위(테스트) | 지적함 | **미수령** | **Claude 단독** | **수정 필요** | **Phase 2 종료 판정 전** | §5 원장 · Phase 2 |
| 10 | **F-3** N-02 미집행 + 이탈 사유 미기록 (export 케이스 12→12) | V-6 · `dump_parity_fixtures.py:2017-2025` | parity | 지적함 | **미수령**(축 ② 부분) | **Claude 단독** | 수정 필요 | Phase 4 착수 전 | §5 Phase 2 |
| 11 | **F-4** N-03 도달 0 — C1·`*?<>` 가 `absent` 니들 9개 손열거에 없어 무단언 | `compare_parity.py:936-940` · `dump_parity_fixtures.py:2040-2050` vs `:2055-2059` | 도달 범위(parity) | 지적함 | **미수령**(축 ② 변형 시도 미답) | **Claude 단독** | 수정 필요 | **N-02 와 동시** | §5 Phase 2 · L348 |
| 12 | **F-5** B4-A·B4-B·N-03 음성 대조가 상시가 아님 (X-2 재발) | `tests/` grep · `bbbdb6b` 메시지 | 테스트 적정성 | 지적함 | **미수령**(축 ④ — 프롬프트가 정면으로 물었음) | **Claude 단독** | 수정 필요 | Phase 4 착수 전 | §6 게이트 · X-2 |
| 13 | **F-6** 조건 1 미충족 + R-9 재발 + 「판정 도구 변경」 표기 0회 | `git log` · V-12 | 테스트 적정성(절차) | 지적함 | **미수령** | **Claude 단독** | 수정 필요(절차) | 다음 커밋부터 | 게이트 13 §7.3 조건 1 |
| 14 | **F-6b** 「항목별 커밋」 ↔ 「경로 단위 커밋(`git commit -- <경로>`)」 규칙 충돌 | `142dac6` R-9절 · `bbbdb6b` 머리말 | 테스트 적정성(절차) | 지적함 | **미수령** | **Claude 단독** | **판정 필요** | Phase 2 종료 판정 전 | 게이트 13 §7.3 |
| 15 | **F-7 / F-7b / F-7c** 테스트가 저장소 실물 파일을 쓰는 구조 잔존 | `test_parity_ci_gate.py:328` · `test_privacy_scanner.py:361-373` · `test_golden_eval.py:529` | 테스트 적정성 | 지적함 | **미수령**(축 ④) | **Claude 단독** | 권고 | Phase 4 착수 전 | §6 게이트 |
| 16 | **F-8** 신규 음성 7건 중 1건이 완전 중복 → 「7종」이 대리 지표 | `test_parity_ci_gate.py:275-296` | 도달 범위(테스트) | 지적함 | **미수령** | **Claude 단독** | 권고 | 다음 커밋 | §6 게이트 |
| 17 | **F-9** 배타 단언의 접두 공유 시 거짓 실패 (집합 차 vs 부분문자열) | `dump_parity_fixtures.py:2062-2075` | parity | 지적함 | **미수령**(축 ② 경계 변형 미답) | **Claude 단독** | 권고 | Phase 4 착수 전 | §6 게이트 |
| 18 | **F-10** `allow_empty` 소재 설명이 기제보다 넓음 | `compare_parity.py:737-746` | 도달 범위(parity) | 지적함 | **미수령**(축 ④ — `allow_empty` 확산 경로 정면 질문) | **Claude 단독** | 권고 | — | §6 게이트 |
| 19 | **F-11** 「리뷰 게이트 이력」 표가 09 에서 끊기고 어떤 검사기도 보지 않음 | `00_progress.md:361-364` | 도달 범위(테스트) | 지적함 | **미수령** | **Claude 단독** | 권고 | — | §5 원장 |
| 20 | **F-12** `실행 경로` 열이 CI 도달을 과소 신고(`local:` 잔존) | `00_progress.md:342·346·347·350` · V-3 | 도달 범위 | 지적함 | **미수령** | **Claude 단독** | 수정 필요 | Phase 2 종료 판정 전 | §5 원장 · 규칙 3 |
| 21 | **N-13** 4조합 차분 측정 스크립트 미커밋(스크래치패드 상주) | §4-terdecies.2 자기 기술 | 보안 불변식 | 지적함(**미해소**) | **미수령** | **Claude 단독** | 수정 필요 | Phase 2 종료 판정 전 | 게이트 13 §8.3 #5 |
| 22 | **N-14** 실문서 코퍼스 사전확률 미착수 | `grep` 무소득 | 보안 불변식 | 지적함(**미착수**) | **미수령** | **Claude 단독** | 판정 필요 | Phase 2 종료 판정 전 | 게이트 13 §8.3 #7 |
| 23 | **R-10** 미해결 항목 정체성 키 장치 미집행 (`test_harness_scope_reach.py` 무변경) | `git diff --name-only` 0건 | 도달 범위(테스트) | 지적함(**미집행**) | **미수령** | **Claude 단독** | 수정 필요 | Phase 2 종료 판정 전 | 게이트 13 §8.3 #8 |
| 24 | 최소 회차 조건 4개 = 충족 2 · 부분 1 · **미충족 1**(조건 1) | 게이트 13 §7.3 | 전 축 | 지적함 | **미수령** | **Claude 단독** | 수정 필요(절차) | 다음 커밋부터 | 게이트 13 §7.3 |
| 25 | **X-1 ①②③ · B(미푸시) 해소** — 러너 실측 | run 31854263996 · V-1~V-4 | 도달 범위 | 지적함(해소) | 부분 동의(C-1 전단) | **합의(부분)** | 해소 | — | §8.3 #1·#2 |
| 26 | **N-06 해소** — 거부-겹침 케이스가 차단①을 잰다 | V-7 · 케이스 하한 · 원장 diverge | parity/보안 | 지적함(해소) | **미수령** | **Claude 단독** | 해소 | — | §8.3 #3 |
| 27 | 계약 준수 축 | — | 계약 준수 | **미검토(대상 코드 무변경)** | **미검토** | **양쪽 미검토** | — | Phase 4 | §2.2 |
| 28 | Kotlin/Spring 관용성 축 | Kotlin 변경 0줄 | 관용성 | **검토함 — 지적 없음** | **미수령** | **Claude 단독** | — | — | §3.2 |
| 29 | I-7·I-8·I-9 (AEAD·JWT·Argon2) | 대상 코드 Phase 3 이후 | 보안 불변식 | **미검토(사유)** | **미검토** | **양쪽 미검토** | — | Phase 4 | §2.3 |

**축별 합계** — 합의 4 · 충돌 2 · codex 단독 1 · Claude 단독 18 · 양쪽 미검토 2 · 판정 갈림 1.

> **이 분포 자체가 게이트 14 의 상태를 말한다.** 교차 대조의 핵심 산출은 「합의」가 아니라 「차이」인데,
> 이번에는 **차이의 대부분이 codex 미수령에서 나왔다** — 즉 이 게이트는 독립 관점의 **부하 검사를 통과한 것이
> 아니라 받지 못한 것**이다. Claude 단독 18건은 「codex 도 문제없다고 본 항목」이 **아니다.**

---

## 3. 충돌 항목 — 양쪽 근거 전문 병기 · **리더 판단 요청**

### 3.1 충돌 ① — 동기화 축소 편집이 자동 실패하는가 (표 #2)

**codex 근거 (원문, 메시지 2)**

> "초기 추적에서 전체 하한은 CI에 실제 배선돼 있지만 독립 기준은 아닙니다. 특히 선언 하한·정본(BUILDERS)·표시
> 파일을 함께 축소하는 **동기화 편집은 자동 실패가 아니라 리뷰 신호에만 의존**합니다."

파일·라인 미제시. `findings:[]` — 확정 지적 아님.

**Claude 근거 (§5.2 「새 우회를 찾아봤다 — 실효 우회는 없다」 8행 표에서 관련 3행)**

| 시나리오 | Claude 결론 | 막는 곳 |
|---|---|---|
| `BUILDERS` 에 도메인 **추가**(선언 미갱신) | `covers=False, marked=True` → "내려왔다" | 비교기 |
| `BUILDERS` **추가** + 선언 갱신, 표시 미갱신 | `domains(8) != canonical(9)` → "표시가 낡았다" | 비교기 |
| 선언·하한을 **함께 축소** | 셸 가드 통과 → 부분 게이트 진입 → `covers=False, marked=True` → "내려왔다" | 비교기 |

Claude 는 같은 절 말미에 **"강제가 두 파일에 갈려 있다는 점은 남는다 … X-1 이 난 곳이 바로 이 이음매"** 라고
스스로 적었다.

**제3 근거 (W-3 — 이 회차 실측, 어느 쪽도 지우지 않는다)**

- 비감소 가드의 기준점은 `$dfloor`(`.github/parity-declared-floor.txt`)이고 대조 상대는
  `$declaration`(`backend-kotlin/parity-domains.txt`)이다 — **둘 다 저장소 안의 파일**이다
  (`ci.yml:379-398`). 같은 커밋에서 양쪽을 줄이면 `comm -23` 이 비어 가드는 통과한다.
- **Claude 표의 3행은 모두 `BUILDERS`(정본)가 그대로 8 인 경우**다. codex 가 말한 것은 **`BUILDERS` 까지 함께
  줄이는 4중 동기화**이고, 그 경우 `declared_count == canonical_count` 가 다시 성립해 **전체 게이트 분기로
  들어가 종료 코드 0** 이 된다. 이 시나리오는 **Claude 의 8행 표에 열거돼 있지 않다.**
- 다만 `ci.yml:397` 의 오류 문구는 *"삭제가 의도라면 `$dfloor` 을 함께 고치고 **PR 에 근거를 적어라**"* 라고
  적는다 — **설계가 이 경우를 의도적으로 리뷰 신호에 위임**하고 있음을 코드가 스스로 밝힌다.

**→ 리더 판단 요청**: 두 진술은 **서로 다른 시나리오를 말하고 있어 어느 쪽도 틀리지 않았을 수 있다.**
판단이 필요한 것은 ⓐ 4중 동기화 축소를 **자동 실패로 만들 것인가**(정본 축소에 별도 근거 요구), 아니면
ⓑ `ci.yml:397` 이 이미 선언한 대로 **리뷰 신호 위임을 수용된 설계로 확정**할 것인가다.
ⓑ 를 택한다면 그 사실이 **문서에 명시**돼야 한다 — 지금은 §5.2 가 "실효 우회는 없다"로 읽혀 위임 사실이 가려진다.

### 3.2 충돌 ② — 억제 사유 하한의 실질 (표 #3·#4)

**codex 근거 (원문, 메시지 3)**

> "구체 반례가 확인됐습니다. `privacy-allow ... — ..` 와 Malayalam spacing mark 두 글자(`U+0D3E U+0D3E`)는 모두
> `visible_length=2` 로 승인되어 **실제 적중 1건을 억제하고 `marker_problems=[]`** 가 됩니다. 반대로 RTL 제어·
> 태그·variation selector·미할당·서로게이트는 0으로 거절됩니다."

**Claude 근거 (§3.3 N-20 행 · 하한 2 행)**

> N-20 **해소** — 판정을 `has_visible_reason` 한 곳으로 모으고 `visible_length`(유니코드 카테고리 기반)로 센다.
> 하한 2 — "관측 결함은 보이는 문자 **0개**이고 하한은 2 … **「문장부호만인 사유는 못 막는다」를 함께 적었다**.
> 규칙 6(범위가 근거를 넘지 않는가)을 지켰다"

**제3 근거 (W-1 — 직접 호출 실측)**

```
Po 2개 (마침표)         cat=['Po','Po']  visible=2  억제통과=True
Malayalam Mc 2자 U+0D3E cat=['Mc','Mc']  visible=2  억제통과=True
Devanagari Mc U+093E    cat=['Mc','Mc']  visible=2  억제통과=True
ZWSP 단독(관측 결함)     cat=['Cf']       visible=0  억제통과=False
RTL 제어 U+202E         cat=['Cf','Cf']  visible=0  억제통과=False
태그 문자 U+E0041       cat=['Cf','Cf']  visible=0  억제통과=False
variation selector      cat=['Mn','Mn']  visible=0  억제통과=False
미할당 U+0378           cat=['Cn','Cn']  visible=0  억제통과=False

_INVISIBLE_CATEGORIES = ['Cc','Cf','Cn','Co','Cs','Me','Mn','Zl','Zp','Zs']   # ← Mc 없음
```

**codex 의 C-2·C-3 은 전부 그대로 재현됐다.** 그리고 두 갈래는 성격이 **다르다**:

- **`Po` 갈래(표 #3)는 합의다.** 구현자가 `visible_length` docstring `:188-193` 에 *"문장부호만으로 된 사유는
  통과한다 … 관측된 결함이 아니고"* 라고 **스스로 적었고**, Claude 는 그것을 **규칙 6 준수의 근거**로 읽었다.
  codex 는 같은 사실을 **반례**로 읽었다. 사실은 같고 **판정이 갈린다.**
- **`Mc` 갈래(표 #4)는 codex 단독이고, 자기 신고 범위 밖이다.** docstring 이 한계로 선언한 것은 `Po` 뿐이고,
  `_INVISIBLE_CATEGORIES` 는 `Mn`·`Me` 만 제외한다 — **`Mc`(spacing combining mark)는 어디에도 언급이 없다.**
  결합 표만으로 이루어진 문자열은 화면에서 앞 기저 문자에 얹히므로 `Mn`·`Me` 를 뺀 것과 **같은 논거가 그대로
  적용되는데** 카테고리 목록에서 빠졌다. 즉 이것은 「선언한 한계」가 아니라 **선언과 도달의 어긋남**이다.
  `codex-reviewer` §4.2 가 확인을 요청한 전제(`U+0D3E` 가 `Mc` 인가)는 **참**이다.

**→ 리더 판단 요청**: ⑴ `Po` 갈래를 **선언된 한계로 수용**할 것인가, 아니면 codex 의 읽기대로 **막을 것인가**.
⑵ `Mc` 갈래는 Claude 의 N-20 **해소** 판정에 포함되지 않은 자리다 — **`privacy-gate` 로 보내 판정**을 받아야
한다(보안 축 판정이 갈리면 `privacy-gate` 가 우선한다). **이 회차에서 N-20 판정을 바꾸지 않았다.**

### 3.3 충돌 ③ — `title_markers` 빈 목록 방향의 자기 신고 (표 #7)

**codex 근거 (원문, 메시지 3)**

> "파일명 쪽은 현재 표지 집합에서는 단일 union 상수가 각 케이스의 `absent` 에 걸리지만, **생성기 쪽
> `title_markers` 가 빈 목록으로 갈리는 방향은 단언 자체가 사라져 비교기가 자기 신고하지 못합니다.**"

**Claude 근거 (§4 「검사 기준이 검사 대상 자신에게서 나오는가」 행)**

> "드리프트 자기 신고 주장은 **실제로 양방향 모두 성립한다** — 생성기 표지 비었는데 비교기 안 비면 **대체 이름
> 단언이 실패**하고, 반대면 **빈 요구로 실패**한다."

**제3 근거 (W-2 — 코드 실측)**

```python
# dump_parity_fixtures.py:2141-2151
*(
    [ _assert("contains_derived", rule="export_title_markers", path=f"{fmt.value}.filename") ]
    if markers_by_case[name]     # ← 비면 단언을 아예 붙이지 않는다
    else []
),
```

```python
# dump_parity_fixtures.py:2183-2186
+ [
    _assert("present", path=f"{fmt.value}.filename", needles=[_FALLBACK_NAME])
    for fmt in ExportFormat
    if not markers_by_case[name]   # ← 비면 대체 이름 단언이 대신 붙는다
]
```

**두 진술이 서로 다른 층을 말하고 있다.**

- **codex 가 옳은 부분**: `contains_derived` 는 **빈 요구가 되는 것이 아니라 생략된다**(`:2149`). 따라서
  **B4-A(`allow_empty=False` → 빈 요구 실패) 기제는 이 방향에서 발화하지 않는다.**
- **Claude 가 옳은 부분**: 생략과 **동시에** `present(_FALLBACK_NAME)` 단언이 붙으므로(`:2183-2186`), 실제 제목에서
  유도가 가능한 케이스인데 생성기만 빈 목록으로 갈리면 **Kotlin 이 내는 실제 파일명이 대체 이름을 담지 않아
  그 단언이 실패**한다. 자기 신고는 **다른 기제로** 성립한다.

**→ 리더 판단 요청**: 이 항목은 **코드 결함 여부가 아니라 기록의 정확성 문제로 귀결**될 수 있다 —
`dump_parity_fixtures.py:2051-2054` 주석이 자기 신고 경로를 *"`contains_derived` 가 **빈 요구로 떨어져 실패**한다"*
로 적는데, `:2149` 는 바로 그 빈 요구가 생기지 않도록 **의도적으로 막는다**. 두 주석이 서로를 부정한다.
**이 파생 관찰은 어느 리뷰에도 없으므로 §6.2 「종합 중 발견 — 미교차」로 분리했다.**

---

## 4. 조치 목록 — Claude 단독 완전본 기준 · 심각도 순

> 심각도·담당·마감은 **1차 산출물 §7 의 값을 그대로 옮긴 것**이다. 이 회차에서 재판정하지 않았다.
> 충돌 3건(§3)은 리더 판단 대기이므로 별도 구획에 둔다.

### 4.1 A — Phase 2 종료 판정을 막는 것

| 순 | 항목 | 심각도 | 담당 | 마감 |
|---|---|---|---|---|
| 1 | **F-1** — `ci.yml:441` 강제 지점 기록이 거짓(`effective_scope` 저장소 부재 + 폐기 설계를 현재 설계로 기술) | **수정 필요 · 차단② 승격 판정 필요** | `kotlin-implementer` | **Phase 2 종료 판정 전** |
| 2 | **F-2** — 단언 630 미반영 (`parity-full-gate.txt` = 152/608, 원장 `L352` = 152/608) + 문서 내 자기모순 | **수정 필요** | **리더**(원장 `L352` 608→630 집행) | **Phase 2 종료 판정 전** |
| 3 | **N-13** — 4조합 차분 측정 스크립트 미커밋(스크래치패드 상주 · 세션 종료 시 소실) | 수정 필요 | `kotlin-implementer` | **Phase 2 종료 판정 전** |
| 4 | **N-14** — 실문서 코퍼스 사전확률 미착수 | 판정 필요 | `privacy-gate` | Phase 2 종료 판정 전 |
| 5 | **R-10** — 미해결 항목 정체성 키 장치 미집행(`test_harness_scope_reach.py` 무변경) | 수정 필요 | `kotlin-implementer` + 리더 | Phase 2 종료 판정 전 |
| 6 | **F-12** — `실행 경로` 열이 CI 도달을 과소 신고 (`EXPECTED_REACH_TOKENS` 동반 갱신) | 수정 필요 | **리더** | Phase 2 종료 판정 전 |
| 7 | **F-6b** — 「항목별 커밋」 ↔ 「경로 단위 커밋」 규칙 충돌 | **판정 필요** | **리더** | Phase 2 종료 판정 전 |
| 8 | **L348 재판정** — F-3·F-4 를 알고도 `예` 로 올릴 것인가 | 판정 필요 | **리더** | Phase 2 종료 판정 전 |

### 4.2 B — 막지 않는 것

| 항목 | 심각도 | 담당 | 마감 |
|---|---|---|---|
| **F-3** — N-02 미집행·이탈 미기록 | 수정 필요 | `parity-verifier` | Phase 4 착수 전 |
| **F-4** — N-03 도달 0 (C1·`*?<>` 무단언) | 수정 필요 | `parity-verifier` | **N-02 와 동시** |
| **F-5** — B4-A·B4-B·N-03 음성 대조 비상시 (X-2 재발) | 수정 필요 | `kotlin-implementer` | Phase 4 착수 전 |
| **F-6** — 조건 1 미충족 + R-9 표기 누락 | 수정 필요(절차) | 전원 | 다음 커밋부터 |
| **F-7 / F-7b / F-7c** — 테스트의 실물 파일 쓰기 구조 (**사고 4건째 구조 잔존**) | 권고 | `kotlin-implementer` | Phase 4 착수 전 |
| **F-8** — 음성 테스트 1건 중복 | 권고 | `kotlin-implementer` | 다음 커밋 |
| **F-9** — 배타 단언의 접두 거짓 실패 | 권고 | `parity-verifier` | Phase 4 착수 전 |
| **F-10** — `allow_empty` 소재 설명 | 권고 | `parity-verifier` | — |
| **F-11** — 리뷰 게이트 이력 표가 09 에서 끊김 | 권고 | 리더 | — |
| 게이트 13 이월 — X-2 잔여·N-17·N-19·N-04·N-07·N-08·N-10·N-18 | 수정 필요/권고 | 각 담당 | Phase 4 착수 전 |

### 4.3 C — 리더 판단 대기 (충돌 · §3)

| 항목 | 상태 | 담당 |
|---|---|---|
| 충돌 ① 4중 동기화 축소 — 자동 실패로 만들 것인가 / 위임을 명시할 것인가 | **판정 필요** | 리더 (→ `parity-verifier`) |
| 충돌 ② `Mc` 결합 표 갈래 — N-20 해소 판정에 없던 자리 | **판정 필요** | **`privacy-gate`**(보안 축 우선권) |
| 충돌 ② `Po` 갈래 — 선언된 한계로 수용할 것인가 | **판정 필요** | `privacy-gate` |
| 충돌 ③ `title_markers` 빈 목록 방향 — 기제 확인됨, 기록 정정 필요 여부 | **판정 필요** | 리더 (→ `parity-verifier`) |

### 4.4 사고 4건째 — 구조 잔존의 처리 상태

Claude 1차 §5.3 판정을 그대로 옮긴다(재판정 없음). **직접 재발은 없고 구조는 남았다.**

- `test_parity_ci_gate.py:328` 이 모듈 전역 상수 `comparer.DECLARED_FLOOR_PATH` 를 그대로 쓴다. 현재 7함수가
  전부 `marker` fixture 를 받아 안전하나, **fixture 를 요청하지 않는 테스트가 한 건 추가되면 실물
  `.github/parity-declared-floor.txt` 를 덮어쓴다** — 사고의 정확한 형태다.
- 같은 저장소가 이미 구조적 해법을 갖고 있다: `tests/golden/test_baseline_gate.py:737-741` 이 같은 사고 뒤
  **`path` 인자 필수화**로 막았다. 격리를 fixture 에 결속하는 대신 이 형태로 옮기는 것이 처방이다.
- codex 는 이 축(④)에 **답하지 않았다** — 교차 대조되지 않은 단독 지적이다.

---

## 5. Phase 2 종료 판정 근거 — 최종 정리

### 5.1 이 회차에 새로 확정된 **긍정** 근거 (러너 실측)

| 근거 | 실측 | 닫힌 저지 항목 |
|---|---|---|
| run **31854263996** (`bbbdb6b`) | `kotlin` ✅ · `quality` ✅ · `frontend` ✅ (`llm-lane` 진행 중) | §8.3 **#1 (B — 미푸시·CI 첫 실행 없음)** |
| `kotlin` 잡 step 13 「parity 비교」 **success** | 로그가 `전체 게이트로 실행한다 (--only-domain 없이)` 분기를 탐 | §8.3 **#2 (X-1 — 전체 게이트 하한 CI 미발화)** |
| 전체 게이트 결과 | 도메인 **8/8** · 성질 판정 **153건**(단언 **630개**) · 판정 보류 0 · 미검증 0 · **불충족 0** | 「전 도메인이 도는가」·「그 게이트가 실제로 도는 곳에서 돌았는가」 |
| `quality` 잡 | `1229 passed, 5 deselected, 5 xfailed` · `test_parity_ci_gate.py` **14건** 수집·통과 | §8.3 **#2 음성 대조 상시화** |
| masking 케이스 84→85, 거부-겹침 회귀가 fixture 로 상시 | — | §8.3 **#3 (N-06 — 게이트가 차단①을 안 잼)** |

**이것이 이 회차의 가장 강한 근거다.** 게이트 13 이 「도달 0」으로 막았던 자리가 **러너에서 실제로 돌았고
초록**이라는 것이 실측으로 확인됐다. codex 도 이 부분만은 독립적으로 확인했다(C-1 전단 — 표 #1, **합의**).

### 5.2 남은 항목이 종료를 막는가 — 각각 판정

| 항목 | 성격 | **종료를 막는가** | 근거 |
|---|---|---|---|
| **F-1** (`ci.yml` 거짓 기록) | **장치의 기록** | **판정 필요 — 차단② 승격 여부** | 아래 §5.3 |
| **F-2** (608↔630 자기모순) | 원장 정확성 | **막는다** | 종료 조건 행 `L352` **자신**이 낡은 수치를 근거로 든다. 630 을 근거로 든 판정문과 608 을 든 종료 조건 행이 **같은 문서에서 서로를 부정**한다 — 이 상태로 낸 종료 판정은 자기 근거를 잃는다 |
| **F-12** (`실행 경로` 과소 신고) | 범위 선언형 장치 | **막는다** | 이 열은 *"이 게이트가 지금 어디서 도는가"* 에 답하는 자리다. CI 도달이 **생겼는데** `local:` 로 남아 있으면 표기가 거짓이 되고, 하필 §5.1 의 긍정 근거가 바로 그 CI 도달이다 |
| **N-13** (스크립트 미커밋) | 재현 가능성 | **막는다** | 게이트 13 §8.3 #5. 문서가 스스로 *"세션 종료와 함께 사라질 자리"* 라 적는다 — **시간이 지나면 영구 손실**이라 마감을 미룰 수 없다 |
| **N-14** (코퍼스 사전확률) | 보안 측정 | **판정 필요** | 게이트 13 §8.3 #7 이 Phase 2 저지 항목으로 세웠다. `privacy-gate` 판정 우선 |
| **R-10** (정체성 키 미집행) | 탐지형 장치 | **막는다** | *"항목이 조용히 사라진다"* 를 막으려던 장치가 **조용히 사라졌다** — 형태가 그 자체로 R-10 의 증거다. 이 장치 없이 낸 종료 판정은 이월 누락을 탐지할 수단이 없다 |
| **F-6b** (커밋 규칙 충돌) | 절차 규칙 | **판정 필요** | 두 규칙이 이 배치에서 처음 충돌했고 묵시적으로 ⓒ 를 택한 상태인데 **사유가 실제 원인을 안 적었다** |
| **F-3 · F-4** (N-02·N-03) | parity 범위 | **막지 않는다 — 단 L348 은 막는다** | 종료 조건 자체는 아니나, **L348(내보내기) 행을 `예` 로 올릴지**의 직접 근거다. 파일명 금지문자 요구의 일부(C1·`*?<>`)가 **어느 단언에도 안 걸린다** |
| **F-5 · F-7~F-11** | 테스트 적정성·기록 | **막지 않는다** | 마감 Phase 4 착수 전 |
| **충돌 ①②③** (§3) | 미판정 | **판정 필요** | 특히 **충돌 ②의 `Mc` 갈래는 N-20 「해소」 판정에 포함되지 않은 자리**다 — `privacy-gate` 판정 전에는 §8.3 #6 을 닫힌 것으로 볼 수 없다 |

### 5.3 F-1 의 차단② 승격 여부 — 판정에 필요한 두 읽기

> **1차 산출물이 「수정 필요 · 차단② 승격 판정 필요」로 올렸고, 이 회차에서 심각도를 바꾸지 않았다.**
> 리더 판정에 필요한 양쪽 논거만 정리한다.

**차단②로 올려야 한다는 읽기**
- 차단②의 정의는 *"사건을 탐지·차단하는 게이트가 무력화된 상태"* 이고, 여기에는 **위조 가능한 증거**가 포함된다.
  `ci.yml:441` 은 **강제 지점이 어디인지를 적은 유일한 기록**이며 그 내용이 거짓이다(`effective_scope` 는
  저장소 어디에도 없고 — V-5, 최종 설계는 그 개념을 **폐기**했다).
- 이 자리는 **X-1 이 실제로 난 이음매**다. 다음 사람이 이 주석을 근거로 되돌리면 **X-1 이 그대로 재발**한다.
  U-3 이 방금 고친 것과 **정확히 같은 형태**이고, U-3 의 논거(*"안 고치면 다음 사람이 이 근거로 되돌린다"*)가
  그대로 성립한다.
- **Kotlin 코드가 0줄이어도 장치 결함은 차단으로 올린다**는 규약이 적용되는 자리다.

**수정 필요에 머문다는 읽기**
- 코드의 강제는 **실제로 작동한다** — 러너 실측(§5.1)이 그것을 보였다. 틀린 것은 **주석 한 줄**이고
  탐지·차단 기능 자체는 무력화돼 있지 않다.
- 즉 「장치가 무력한 상태」가 아니라 「작동하는 장치를 잘못 설명한 상태」다.

**→ 갈림의 핵심**: **기록이 장치의 일부인가.** 이 저장소는 U-3 에서 *"결함/구식 설계를 의도처럼 적은 문장"* 을
고쳐야 할 것으로 이미 판정했고, 그 판정을 여기에 적용하면 차단② 쪽으로 기운다. **판정은 리더에게 넘긴다.**

### 5.4 codex 부분 수령이 종료 판정에 남겨야 할 단서

**Phase 2 를 종료하든 미루든, 아래 사실이 판정문에 함께 기록돼야 한다.**

1. **게이트 14 는 완전한 독립 관점을 받지 못했다.** codex 확정 findings **0건**, 축 ④(선언 범위 대 실제 도달)
   **전부 미수령**. 표 #8·#12·#15·#16·#18~#24 의 **Claude 단독 10여 건은 「codex 도 문제없다고 본 것」이
   아니라 「codex 가 보지 않은 것」**이다.
2. **하필 미수령 축이 이 전환의 반복 실패 축이다.** 축 ④ 는 계획 문서가 근거로 삼은 실패 7건의 공통 형태이고,
   프롬프트가 정면으로 물은 세 질문(테스트가 장치를 실제로 호출하는가 / `allow_empty` 확산 경로 /
   떼어도 아무 테스트가 안 깨지는 코드 지목)에 **답이 없다**. Claude 는 그 자리에서 **F-5·F-10·F-7 을 냈다** —
   즉 그 축에 지적이 실재했는데 독립 확인을 받지 못한 것이다.
3. **codex 가 부분 출력만으로도 Claude 가 못 본 것을 하나 냈다** — 충돌 ②의 `Mc` 갈래(표 #4). 22회 명령
   실행에서 나온 단 3건의 언급 중 1건이 Claude 의 **「해소」 판정 안쪽을 뚫었다.** 이것은 완전 수령 시
   추가 지적이 나올 개연성의 **직접 증거**이지 추정이 아니다.
4. **재실행은 2026-08-20 이전에는 크레딧 구매 없이 불가능하다.** 따라서 선택지는 셋뿐이다 —
   ⓐ 크레딧 구매 후 재실행해 게이트 14 를 닫는다 / ⓑ 부분 수령 상태로 종료하되 **위 1~3 을 종료 판정문에
   명시**하고 게이트 14 재실행을 **Phase 3 착수 조건**으로 이월한다 / ⓒ 2026-08-20 이후로 종료를 연기한다.
   **이 에이전트는 판정하지 않는다.**

### 5.5 §5 Phase 2 종료 조건 축별 — 종합 기준 현황

| 축 | 게이트 13 | **게이트 14 종합** | 미해결 |
|---|---|---|---|
| 외부 API·DB 없이 도는가 | 충족 | **충족** | — |
| 전 도메인이 도는가 | 충족 | **충족** (러너 8/8) | — |
| 값·성질 비교가 실제로 도는가 | 충족(범위 한정) | **충족 — 한정 폭 축소** (608→630) | F-3·F-4 (export 파일명 축) |
| 게이트가 재는 것을 실제로 재는가 | 미충족 | **충족(코드) · 기록 미충족** | **F-1** |
| 그 게이트가 실제로 도는 곳에서 돌았는가 | 미검증 | **충족** (V-2·V-3) | — |
| 원장이 상태를 정확히 적는가 | 미충족 | **미충족** | **F-2 · F-12** |
| 마스킹 정책 불변식이 회귀 없이 유지되는가 | 충족(코드)·게이트 미측정 | **충족 — 게이트가 잰다** | 충돌 ②(`Mc`) 판정 대기 |

**종합**: 7축 중 **5축 충족 · 2축 미충족**(기록 축). 게이트 13 대비 3축이 올라왔고, **남은 미충족은 전부
「원장·기록」 계열**이다 — 코드가 아니라 그 코드를 적은 문장이 막고 있다.

### 5.6 Phase 2 표 행별 최종 권고 (1차 §6.3 계승 · 재판정 없음)

| 행 | 권고 | 조건 |
|---|---|---|
| L342 마스킹 · L343 정규화 · L349 후처리 · L350 fixture · L351 선언 | **`예` 가능** | **F-2·F-12 선정정** — 원장 오류를 먼저 고친다 |
| L344 프롬프트 · L345 스타일 규칙 | `아니오` 유지 | X-9 미해소 |
| **L348 내보내기** | **⚠ 리더 재판정** | X-5 는 ⓐ 로 판정돼 열렸으나 **F-3·F-4 가 새로 섰다** — 파일명 금지문자 요구의 일부가 무단언 |
| **L352 종료 조건** | **`아니오` 유지** | 이 행 자신이 낡은 수치(152/608)를 근거로 든다(F-2). 그 밖 N-13·N-14·R-10·F-1 미해소 |

`EXPECTED_MET_YES_KEYS` 갱신 문자열은 **1차 산출물 §6.4 의 하네스 함수 실측값**을 그대로 쓴다(40자 클립 3행).
손으로 옮겨 적지 않는다.

---

## 6. 대조하지 못한 범위 · 종합 중 발견

### 6.1 codex 부재·실패로 대조하지 못한 범위

| 범위 | 상태 |
|---|---|
| **축 ④ 선언 범위 대 실제 도달 전부** | **미수령** — 테스트가 장치를 실제로 호출하는가 / `allow_empty` 확산 경로 / "떼어도 안 깨지는 코드" 지목. **Claude 단독 판정만 존재** |
| **축 ① 무력화 경로 4종의 개별 판정** | **미수령** — 프롬프트가 요구한 "CI 가 빨개지는가 / 조용히 통과하는가"가 어느 경로에도 없다. C-1 후단 한 문장이 전부 |
| **축 ② 변형 시도 개별 결과** | **부분** — 부분 union·순서 섞기·`marker_length(8)`·`marker_window(40)` 경계 변형 미답. C-4·C-5 두 방향만 |
| **파일·라인 근거** | **codex 출처로 표의 「근거 위치」를 채울 수 없다.** 표의 근거는 전부 Claude 또는 이 회차 W-1~W-3 실측 |
| "지적이 없는 축은 없다고 명시하라" | **미답** — codex 의 침묵을 무지적으로 읽을 근거가 없다 |
| 계약 준수 축 · I-7·I-8·I-9 | **양쪽 미검토** — 대상 코드 무변경 / Phase 3 이후 |
| 범용 품질 축(성능·유지보수성·일반 보안) | **범위 밖** — `multi-review` 별도 실행 권고(**7회차 연속**) |
| `llm-lane` 잡 결과 | 1차 시점 진행 중. F-7c 경로 확인 미완 |

### 6.2 종합 중 발견 — **미교차**

> **어느 리뷰에도 없던 관찰이다.** 교차 검증을 거치지 않았으므로 조치 목록(§4)에 넣지 않고, **다음 리뷰 회차의
> 범위로 제안**한다. 이 구획의 항목을 근거로 Phase 게이트를 판정하지 않는다.

| # | 관찰 | 발견 경위 | 제안 |
|---|---|---|---|
| **X-a** | `dump_parity_fixtures.py:2051-2054` 주석이 두 선언 드리프트의 자기 신고 경로를 *"`contains_derived` 가 **빈 요구로 떨어져 실패**한다"* 로 적는데, `:2149` 는 **바로 그 빈 요구가 생기지 않도록 조건부로 단언을 생략**하고 `:2168-2171` 주석은 그 생략 이유를 *"붙이면 빈 요구가 되어 B4-A 가 막는다"* 로 적는다. **같은 파일의 두 주석이 서로를 부정한다.** 실제 자기 신고는 `:2183-2186` 의 대체 이름 단언이 한다 | 충돌 ③ 판정을 위한 W-2 코드 실측 중 | **F-1 과 같은 형태**(장치의 강제 지점을 적은 기록이 코드와 다름)다. 다음 회차에서 F-1 과 **묶어서** 다룰 것을 제안한다 — 개별 오타가 아니라 **기록 계열의 반복**인지 판정이 필요하다 |
| **X-b** | `ci.yml:397` 오류 문구가 *"삭제가 의도라면 `$dfloor` 을 함께 고치고 **PR 에 근거를 적어라**"* 라고 적어, **동기화 축소의 리뷰 신호 위임을 설계가 스스로 선언**하고 있다. Claude §5.2 의 결론 *"실효 우회는 없다"* 는 이 선언과 나란히 읽히지 않는다 | 충돌 ① 판정을 위한 W-3 실측 중 | 충돌 ① 판정 시 **함께** 검토. 위임이 의도라면 §5.2 의 표현이 그 사실을 가리지 않도록 정정 필요 |
| **X-c** | `_INVISIBLE_CATEGORIES` 가 `Mn`·`Me` 를 제외한 논거(*"그것만으로 이루어진 문자열이 화면에 아무것도 남기지 않는다"*)는 `Mc` 에도 **같은 형태로 적용**되는데 `Mc` 는 목록에 없다. 이는 「선언한 한계」가 아니라 **선언과 도달의 어긋남**이다 | 충돌 ② 전제 확인(W-1) 중. **codex C-2 가 현상을 지목했고, 카테고리 논거의 비대칭은 이 회차 관찰** | 충돌 ② 를 `privacy-gate` 로 보낼 때 **함께** 전달. 다만 판정 권한은 `privacy-gate` 에 있다 |

---

## 7. 리더에게 — 요약

1. **codex 는 부분 수령이다.** 확정 지적 0건이며, **축 ④ 전부 미수령**이다. Claude 단독 18건을 「양쪽이 문제없다고
   본 것」으로 읽으면 안 된다. §5.4 의 네 단서를 종료 판정문에 그대로 실을 것을 권고한다.
2. **Phase 2 종료를 막는 것은 이제 코드가 아니라 기록이다.** 러너 실측으로 X-1·B·N-06 이 닫혔고 7축 중 5축이
   충족됐다. 남은 미충족 2축은 **F-1·F-2·F-12** — 전부 「원장·기록」 계열이다. 여기에 N-13·R-10 이 더해진다.
3. **판정이 필요한 것은 6건**: F-1 의 차단② 승격 여부 / F-6b 규칙 충돌 / L348 재판정 / N-14 /
   충돌 ①(동기화 축소) / 충돌 ②(`Mc` 갈래 — **`privacy-gate` 우선권**).
4. **충돌 3건 중 어느 쪽도 삭제하지 않았다.** 세 건 모두 **두 진술이 서로 다른 층·시나리오를 말하고 있어
   양립 가능**했고, 제3 근거(W-1~W-3)를 붙여 그 구조를 드러냈을 뿐 승자를 정하지 않았다.
5. **게이트 14 재실행**은 크레딧 구매 없이는 2026-08-20 이후에만 가능하다. Phase 2 를 지금 종료한다면
   **게이트 14 재실행을 Phase 3 착수 조건으로 이월**하는 것이 §5.4-3(부분 출력만으로도 Claude 의 「해소」
   판정을 하나 뚫었다)에 비춰 정합적이다 — 다만 **판정은 리더의 몫이다.**
