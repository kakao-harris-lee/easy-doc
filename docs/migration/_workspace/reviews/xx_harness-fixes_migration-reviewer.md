# `xx_harness-fixes` — migration-reviewer 1차 (Claude 단독 독립 리뷰)

**회차:** 1차 (독립). 교차 종합 아님.
**작성:** 2026-08-22 · **어간:** `xx_harness-fixes` (리더 지정)
**codex 산출물 미열람.** `reviews/xx_harness-fixes_codex-reviewer.md` 가 트리에 실재하지만(untracked) 1차 규약에 따라 읽지 않았다.
**읽은 것:** 1회차 산출물 3건 — `xx_harness_codex-reviewer.md` · `xx_harness_migration-reviewer.md` · `xx_harness_cross.md`. 이번 대상의 근거이므로 리더 지시대로 읽었다.

### 독립성 고지 (리더 요청, 2026-08-22 07:53)

**⑴ 리뷰 작성 중 codex 2회차 결론을 받은 경로는 없다.** 어떤 에이전트로부터도 메시지를 받지 않았고(`codex-r2b` 포함), `xx_harness-fixes_codex-reviewer.md` 를 열지 않았다. 그 파일명은 두 번 도구 출력에 나타났다 — 세션 시작 시 주어진 `git status` 스냅샷과, 내가 본 트리 미변조를 확인하려 돌린 `git status --porcelain -- docs/` 의 untracked 목록. **파일명만 보았고 내용은 읽지 않았다.** 정직하게 적을 것 하나: 그 파일의 mtime 은 **07:30:44** 이고 내 산출물은 **07:51:03** 이므로, 그 파일은 내 리뷰 **도중에 이미 디스크에 있었다.** 열지 않았다는 것의 근거는 내 도구 호출 이력뿐이다.

**⑵ 산출물 작성 후 리더가 codex 2회차 항목 하나를 알려 왔다.** 리더의 진행 상황 요청 메시지(07:53)에 *"그중 하나(`docs/` 통째 면제가 계획 문서 약화까지 면제시킨다)를 실행으로 확인했다"* 가 실려 있었다. 그것은 이 문서의 **F-10 / NC-C3 와 같은 자리**다.

**시간 순서상 이 문서의 F-10 은 그 고지의 영향을 받지 않았다** — 파일 mtime 07:51:03, 리더 메시지 07:53, 그리고 그 이전에 리더에게 보낸 보고에 이미 F-10 과 NC-C3 실측이 실려 있다. 다만 **2회차 교차 대조에서 F-10 을 「합의」로 적을 때는 이 고지를 함께 표시해야 한다** — 지금은 독립적으로 도달한 것이 맞지만, 이 사실이 기록에 없으면 다음 사람이 그것을 확인할 수 없다.

**⑶ 이후 다른 경로로 codex 결론이 들어오면 읽지 않고 리더에게 알린다.**

---

## 1. 리뷰 범위와 참조

**대상:** `cbf6e8d..26ee39a` 5커밋 (7파일 · +729/-35)

| 커밋 | 무엇을 고쳤나 | 원 지적 |
|---|---|---|
| `ed3df31` | 4축 사본 동기화 강제자 | 내 M-1·M-2 |
| `7efc7c4` | 아카이브 prefix 대조 · 포인터 절 단위 결속 · 클래스 정체성 스냅샷 | codex medium 3건 |
| `3ba7e04` | 리뷰 축 4 → 7, 계획 §5 즉시 중단 기준 매핑 | codex high #1 (= 내 C-5) |
| `e7faccc` | 리뷰 커버리지 강제자 | codex high #2 (= 내 C-3 잔여) |
| `26ee39a` | 부트스트랩 제약 기록 (원장 전용) | stop-time 게이트 |

**참조:** 계획 `docs/plans/2026-08-11-kotlin-react-migration.md` §5 Phase 7 즉시 중단 기준(454–461)·§6 · `CLAUDE.md` 「범위 대조」·「구현 전 리서치·계획」 · `kotlin-migration` SKILL.md 「선언한 범위와 실제 도달을 대조한다」 규칙 1~8 · `docs/migration/_workspace/00_progress.md`(1263–1330) · `.github/workflows/ci.yml`(19–231)

**심각도 척도:** 차단(① 사건 / ② 장치) · 수정 필요 · 권고 · 판정 필요. 차단·수정 필요에는 마감(그 게이트가 처음 실제로 쓰이는 시점)을 붙였다.

**핵심 결론 먼저.** 리더가 물은 "네 번째 걸음"은 **하나가 아니라 여섯**이다(F-1·F-2·F-3·F-6·F-8·F-13). 여섯 모두 같은 형태다 — 정정이 자기가 비판한 결함의 **한 칸 옆**을 열어 둔 채 「전부 막는다」로 적혔다. 그리고 그와 별개로 이 범위는 **CI quality 잡 전체를 어둡게 만들었다**(F-5) — 그 잡의 첫 관문인 `ruff check .` 가 이 범위에서 빨개졌고, 그래서 이번에 세운 강제자 셋을 포함해 그 잡의 **모든** mypy·pytest 스텝이 도달 0이다.

---

## 2. 축별 지적

### 2.1 계약 준수 — 검토함, 지적 없음

`contracts/**`·DTO·헤더·상태 코드·`frontend/src/api/` 를 건드리지 않는다. 축②(외부 HTTP 계약)는 필수 쪽에 그대로 남았다.

### 2.2 parity 위험

#### F-3 — 「즉시 중단 기준 전부 매핑」 강제자가 축 하나의 조용한 삭제를 통과시킨다 · **차단(② 장치)** · 마감: Phase 5 종료 게이트 전

`tests/test_harness_scope_reach.py:1368` `test_즉시_중단_기준이_전부_필수_축에_매핑된다` 는 내 C-5 의 강제자로 신설됐고, 판정 방식은 계획 문서를 읽어 **매핑 키가 부분문자열로 걸리지 않는 기준**을 찾는 것이다(`:1385-1391`).

문제는 계획 §5 의 불릿 수가 **7이 아니라 6**이라는 점이다(실측: 454–461, `- ` 불릿 6개). C-5 가 7로 센 것은 마지막 불릿을 의미 단위로 쪼갠 것이고, 그 불릿은 **두 위험을 한 줄에 담는다**:

```
- 골든 품질이 §4.6 합격선 미달(절대 팩트축 누락 발생 또는 기록된 하한선 대비 하락)이거나 최대 2회 호출 요구 위반
```

`_STOP_CRITERION_TO_AXIS`(`:1284`)는 이 한 줄에 키 **둘**(`"골든 품질이"` → 축⑦, `"최대 2회 호출 요구 위반"` → 축⑤)을 걸어 두는데, 판정은 `any(key in criterion ...)` 이라 **한 키만 살아 있어도 그 불릿은 매핑됨**이다. 따라서 한쪽 축을 지워도 검사는 조용하다.

**음성 대조 NC-A (격리 worktree).** 축⑦「골든 품질 합격선」을 사본 **넷 전부**에서 일관되게 제거했다 — `kotlin-migration/SKILL.md:158` · `codex-review/SKILL.md:56` · `CLAUDE.md:25` 의 이름 · `_AXIS_NAMES`(`:1267`) · 매핑 키.

| 변조 | 결과 | 판정 |
|---|---|---|
| **NC-A** 축⑦을 4개 사본 전부에서 제거 | **EXIT=0 · 42 passed** | **조용하다** |
| NC-C6 (대조군) 어느 키도 안 걸리는 새 기준 1줄 추가 | EXIT=1 · `매핑되지 않았다` | 잡는다 ✓ |
| NC-C4 (대조군) 계획에서 `중복 LLM 호출` 불릿 삭제 | EXIT=1 · `사라진 기준을 매핑이 아직 가리킨다` | 잡는다 ✓ |

지워지는 축이 **골든 품질 합격선**이라는 게 특히 나쁘다 — 계획 §4.6·`:361` 이 그것을 **Phase 5 종료 게이트의 두 차단축**이자 **Phase 7 진입 전 필수 통과**로 확정해 둔 항목이다. 대칭 확인: 축⑤·축⑥은 각각 불릿 4·5의 **유일한** 키를 갖고 있어 삭제가 잡힌다(NC-C4 계열). **조용히 지워지는 것은 축⑦ 하나**이고, 이유는 계획 불릿 6의 겹침이다.

#### F-9 — 매핑 키가 부분문자열이라 **새 기준을 흡수**한다 · **수정 필요** · 마감: 첫 묶음 회차

리더 질문 ②의 나머지 절반이다. `"골든 품질이"` 는 5글자짜리 일반 어구다.

**NC-C5.** 계획 §5 에 새 즉시 중단 기준 `- 골든 품질이 아니라 **응답 지연**이 SLA 미달이거나 큐 워커가 전면 정지` 를 넣었다 → **EXIT=0**. 성질이 전혀 다른 새 중단 사유가 「이미 축⑦에 매핑됨」으로 세어졌다. 대조군 NC-C6(같은 내용을 `"골든 품질이"` 없이 적음) → EXIT=1. 즉 **엉뚱한 기준에 붙는 경로가 실재**한다.

부수: `stale` 실패 메시지(`:1404-1407`)가 *"계획이 바뀌었으면 함께 지워라"* 로 **매핑 삭제를 지시**한다. 그 지시를 따르면 F-3 경로로 들어간다.

#### 그 밖 — 검토함, 지적 없음

`parity/fixtures/`·비교기·프롬프트·추출 코드는 이 범위에서 바뀌지 않았다.

### 2.3 보안 불변식

축①(마스킹 선행·저장 암호화·소유권 404·평문 로그/저장·인증)의 문면은 그대로이고 면제 목록도 늘지 않았다. **다만 F-10 이 축①·축④를 경로 규칙으로 우회시킨다.** 보안 축 판정이 갈리면 `privacy-gate` 판정을 따른다.

#### F-10 — `docs/` 면제가 축④(Phase 판정)와 축①(정책)을 정면으로 덮는다 · **수정 필요** · 마감: 첫 묶음 회차

리더 질문 ⑵의 답. `tests/test_review_coverage_reach.py:89` `_EXEMPT_PREFIX = "docs/"` · `:169-171` `_is_docs_only`.

**NC-C3.** `docs/plans/2026-08-11-kotlin-react-migration.md` **하나만** 고치는 커밋으로 §5 즉시 중단 기준 한 줄을 `- (완화) 골든 품질이 …` 로 개작했다 → 커버리지 강제자 **EXIT=0**, 매핑 강제자 **EXIT=0**. 그 파일은 ⑴ 축④(Phase 종료·착수 판정)의 정본이고 ⑵ F-3 매핑 강제자의 **유일한 입력**이다.

같은 면제에 드는 다른 파일들:

| 경로 | 무엇을 정하는가 | 닿는 축 |
|---|---|---|
| `docs/plans/2026-08-11-kotlin-react-migration.md` | Phase 종료 조건 · 즉시 중단 기준 | ④ |
| `docs/master-plan.md` | SSOT — 마스킹 범주 2종, 정책 결정 | ① |
| `docs/migration/_workspace/00_progress.md` | 커버리지 표·이연 장부 = 이 강제자의 입력 | ③·④ |
| `docs/migration/_workspace/03_rebuild-extraction-list.md` | Phase 8 폐기 게이트 P1·P2 | ④ |

기계가 「리뷰 필요 여부」를 말하는 자리는 지금 이 한 곳뿐이고(7축은 산문이라 강제자 0), 그 한 곳이 위 넷을 전부 면제한다. **선언한 범위(7축)보다 실제 도달이 좁은** 형태다.

정직하게 적을 것 — 원장 자신이 이 질문을 미해결로 표시해 뒀다(`00_progress.md:1316-1320`, "이 풀이는 아직 검증되지 않았다"). 내 답은 **아니다** — `docs/` 전부를 덮는 것은 옳지 않고, 최소한 `docs/plans/**`·`docs/master-plan.md`·`docs/migration/_workspace/03_rebuild-extraction-list.md` 는 면제에서 빼야 한다. 반면 `reviews/**`(심판문)와 원장의 **기록 갱신**은 면제가 맞다.

### 2.4 Kotlin/Spring 관용성 — 검토함, 지적 없음

Kotlin 소스 변경 없음. `tests/test_kotlin_class_snapshot_reach.py` 는 Kotlin 클래스 **이름**만 읽고 `backend-kotlin/` 을 건드리지 않는다. (작업 트리의 미커밋 Kotlin 변경은 C6 레인 소관 · 범위 밖 — 5절.)

### 2.5 테스트 적정성 / 게이트 적정성

#### F-5 — 이 범위가 CI quality 잡을 꺼 버렸다 (`ruff check .` 빨강) · **차단(② 장치)** · 마감: 즉시

깨끗한 트리에서 실측했다(격리 worktree, `git reset --hard`).

| 리비전 | `ruff check .` | 근거 |
|---|---|---|
| `cbf6e8d` (직전) | **EXIT=0 · All checks passed!** | — |
| `26ee39a` (HEAD) | **EXIT=1 · Found 2 errors** | E501 `tests/test_harness_scope_reach.py:1208`(101>100) · `:1429`(104>100) |

두 줄의 도입 커밋: `:1208` → `7efc7c4`, `:1429` → `ed3df31` (`git log -S` 실측).

`.github/workflows/ci.yml:72` 가 `- run: uv run ruff check .` 이고, 그 잡(19–231) 안에 `continue-on-error` 도 `if: always()` 도 **없다**(실측: 해당 범위 매칭 0건). 따라서 이 HEAD 에서 quality 잡은 5번째 스텝에서 죽고, **그 뒤 전부가 도달 0**이다 — `uv run mypy . .claude`, 경로 명시 pytest 8종(`:158`·`:163`·`:171`·`:180`·`:190`·`:198`·`:213`·`:226`), 그리고 blanket `- run: uv run pytest`(`:227`). blanket 스텝이 이번 신설 강제자 둘의 **유일한** 실행 경로이므로(F-4), 이 회차가 세운 장치 전부가 CI 에서 한 번도 돌지 않는다.

`CLAUDE.md` 「커밋 전 필수 통과: ruff → mypy → pytest」 위반이기도 하다.

**부수 사실 하나는 이 범위 밖이다.** `ruff format --check .` 는 `cbf6e8d` 에서 **이미 빨갰다**(EXIT=1, `tests/test_harness_scope_reach.py` 1파일 · 공백 2줄). 이 범위가 만든 것이 아니라 이전 회차(`0d632f9`)에서 들어왔고, **그 회차의 리뷰 3건이 그것을 못 봤다** — F-17 참조.

#### F-1 — 커버리지 표는 저자가 쓰고, 그 쓰기가 면제다 · **차단(② 장치)** · 마감: 첫 묶음 회차

리더 질문 ①⑶의 답. `tests/test_review_coverage_reach.py` 의 판정 입력은 원장의 두 표이고, 그 원장은 `docs/` 아래라 **자기 자신이 면제**다. 두 사실이 곱해지면 자기 승인 고리가 닫힌다.

**NC-C2 (격리 worktree, 커밋까지 진행).** 먼저 비면제 작업 커밋 하나(`tests/test_run_gate.py` 주석 1줄)를 만들었다 → **EXIT=1**(장치 작동, NC-C1). 이어서 **`docs/` 하나만** 바꾸는 커밋으로

- 커버리지 표에 `| \`전부\` | \`0d632f9..<HEAD>\` | 없음 |` 한 줄을 적고,
- 이연 장부의 `대기` 4줄을 전부 지웠다.

→ **EXIT=0 · 44 passed.** 리뷰 0건으로 전 구간이 「리뷰됨」이 되고, 축③에 닿는 미리뷰 빚 4건이 사라졌다. 산출물 칸은 문자 그대로 **`없음`** 인데 아무 검사도 반응하지 않는다 — 열 존재만 단언한다(`:214-221`).

파일 머리 주석이 *"범위가 실제로 리뷰됐는지는 재지 않는다"* 로 한계 하나를 정직하게 적었다. **적지 않은 것이 둘이다** — ⑴ 그 선언을 쓰는 커밋이 면제라 **두 번째 눈이 구조적으로 없다** ⑵ 장부 행 삭제에 append-only·단조성 가드가 **0**이다. ⑵ 는 같은 커밋 계열이 **아카이브에는 prefix 가드를 붙였다**는 점에서 특히 눈에 띈다 — 보호가 필요한 두 표 중 판정 입력인 쪽만 무보호로 남았다.

#### F-2 — 「장부에 적힘」이 표 행이 아니라 **절 전체 정규식**이다 · **차단(② 장치)** · 마감: 첫 묶음 회차

`tests/test_review_coverage_reach.py:87` `_SHA_IN_BACKTICKS` · `:145-147` `_recorded_shas` 는 이연 장부 **절 전체**에서 백틱 안 hex 토큰을 긁는다. 그 절은 산문이 표보다 길고(`00_progress.md:1283-1330`) 그 산문이 SHA 를 상시 인용한다.

| 변조 | 결과 | 판정 |
|---|---|---|
| **NC-H** `e7faccc` 의 **장부 행만** 삭제 (부트스트랩 산문의 `` `e7faccc` `` 는 남김) | **EXIT=0 · 2 passed** | **조용하다** |
| **NC-I** 새 비면제 커밋(`bd4def464`)을 표가 아니라 산문 한 줄 `(여담: \`bd4def464\` 는 급해서 그냥 넣었다.)` 로만 언급 | **EXIT=0 · 2 passed** | **조용하다** |
| NC-G (대조군) `대기` 4줄 삭제, 산문 언급 없는 3건 | EXIT=1 · `리뷰도 장부도 없는 커밋 3 건` | 잡는다 ✓ |

NC-G 가 **4건이 아니라 3건**을 신고한 것이 이 결함의 실물 증거다 — `e7faccc` 는 표 행이 아니라 `:1308` 의 산문 인용으로 초록이었다. **오늘 HEAD 에서 이미 그 경로로 통과하고 있다.**

결과: 형제 검사(`:229-235`)가 `커밋`·`상태`·`리뷰할 회차` 열의 존재를 강제하지만, **개별 커밋의 계상을 그 열이 채워진 행에 묶는 것이 없다.** 「적혔다」의 하한이 「어딘가에 SHA 가 언급됐다」로 내려가 있다.

#### F-4 — 신설 강제자 둘이 어디에도 배선되지 않았다 · **차단(② 장치)** · 마감: 즉시

이 저장소는 게이트 파일마다 **경로 명시 CI 스텝 + 자기 배선 단언**을 두는 규약을 갖고 있다. `ci.yml:140-158` 주석이 그 사유를 직접 적는다 — *"`- run: uv run pytest` 한 줄을 지워도 잡 이름 `quality` 가 남아…"*. 실제로 `test_run_gate.py:341-365` · `test_raw_control_chars.py:209-228` · `test_quality_gate_local.py:261` · `test_kotlin_gate_reach.py:2786` 이 각각 자기 스텝의 실재를 **장치 밖에서** 단언한다.

신설 두 파일에는 셋 다 없다(실측 — `grep -rn "review_coverage\|class_snapshot" .github/ pyproject.toml` = 0건):

| 항목 | `test_review_coverage_reach.py` | `test_kotlin_class_snapshot_reach.py` |
|---|---|---|
| `ci.yml` 경로 명시 스텝 | **없음** | **없음** |
| 자기 배선 단언(장치 밖) | **없음** | **없음** |
| 원장 `실행 경로` 등재 | **없음** | **없음** |
| 실제 실행 경로 | blanket `uv run pytest` 하나 | 같음 |

**NC-J2.** 두 파일을 삭제하고 배선 단언 계열 4파일을 돌렸다 → **EXIT=0 · 99 passed.** 삭제가 완전히 조용하다. 그리고 F-5 때문에 blanket 스텝 자체가 이 HEAD 에서 돌지 않으므로, 현재 두 강제자의 **CI 도달은 0**이다.

리더 질문 ⑥의 답: 자기참조는 **반만** 걸렸다. mypy 범위 안이고(`mypy` 두 파일 Success), `ruff check .` 범위 안이고, 축③ glob(`tests/test_*_reach.py`)에 걸린다. 그러나 **「이 게이트가 지금 어디서 도는가」를 장치 밖에서 단언하는 것이 0**이다 — 규칙 3이 가장 자주 지적한 형태 그대로다.

#### F-6 — 아카이브 prefix 대조가 **기준 이후 구간을 무보호**로 남긴다 · **수정 필요** · 마감: 다음 아카이브 append 전

`tests/test_harness_scope_reach.py:1195` `_ARCHIVE_BASELINE_REV = "0d632f9"` · `:1198` `test_아카이브가_append_only_다`. docstring 은 이렇게 적는다.

> prefix 대조가 막는 것: 앞부분 삭제 · 중간 삽입 · 기존 문면 개작 · 전량 교체 · 잘라내기.
> 통과시키는 것: **끝에 덧붙이기 하나뿐**이고 그것이 append-only 의 정의다.

**거짓이다.** 기준 리비전 이후 구간에 대해서는 개작·삭제도 전부 통과한다.

| 변조 | 결과 | 판정 |
|---|---|---|
| NC-B1 정당한 덧붙이기 (+96B) | EXIT=0 | 정상 ✓ |
| **NC-B2** 덧붙인 구간을 전량 개작(`## (개작) 위 기록은 무효다 — 근거 삭제`) | **EXIT=0 · 1 passed** | **조용하다** |
| **NC-B3** 기준 이후 구간 전량 삭제 | **EXIT=0 · 1 passed** | **조용하다** |
| NC-B4 (대조군) 기준 구간 안 1바이트 변조 | EXIT=1 | 잡는다 ✓ |

오늘 창은 **0바이트**다(기준 = 현재 = 248,319B, sha256 `ba23750a…`). 그러나 docstring `:1191-1193` 이 *"아카이브에 정당하게 덧붙인 뒤에는 그대로 둔다 … 이 상수를 올리는 것은 기존 내용을 고쳤을 때뿐"* 이라고 **갱신을 금지**하므로, 창은 append 마다 자라고 줄어드는 일이 없다. 이 하네스가 `MIN_TEST_CLASSES` 에서 겪은 라쳇 간격과 **같은 기제**다.

같은 자리의 세 번째 정정이다 — 계획 §3-b(라쳇이 함께 줄이기를 막는다: 오판) → C-4(존재 단언이 비움·전량교체를 통과) → 줄 수 하한(하한 위 훼손을 통과) → **prefix(기준 뒤 훼손을 통과)**. 매번 「전부 막는다」로 적혔다.

#### F-7 — 세 기준 리비전 핀에 방향 강제자가 0 · **수정 필요** · 마감: 첫 묶음 회차

이 회차는 수치 라쳇(`MIN_ARCHIVE_LINES`)을 **리비전 문자열 핀**으로 갈아탔다. 그 결과 세 핀이 생겼는데 어느 것도 방향이 강제되지 않는다.

| 핀 | 위치 | 방향 강제자 |
|---|---|---|
| `_ARCHIVE_BASELINE_REV` | `test_harness_scope_reach.py:1195` | **없음** |
| `_SNAPSHOT_REV` | `test_kotlin_class_snapshot_reach.py:66` | **없음** |
| `_BASELINE_REV` | `test_review_coverage_reach.py:81` | **없음** |

기존 분류 강제자(`test_kotlin_gate_reach.py:2192-2202`, `RATCHET_SCALAR_PINS` / `NON_RATCHET_PINS`)가 닿지 않는 이유는 둘이다 — ⑴ `THIS_TEST_PATH` **한 파일만** 본다 ⑵ 정규식이 `^([A-Z_]+)(?:: [^=]+)? = \d+$` 로 **수치만** 본다. 문자열 핀은 두 조건 모두 밖이다.

**NC-E3 / NC-E4.** `_SNAPSHOT_REV` 를 `ed3df31`(108개) → `90aff42`(98개)로 **내렸다** → EXIT=0 · 2 passed. 이어서 기준 밖 클래스 10개를 `TEST_CLASSES` 에서 지우고 `TEST_CLASS_COUNT` 를 98로 맞췄다 → **스냅샷 축 EXIT=0.**

**정직하게 적는다 — 전체 시스템은 막았다.** 같은 상태에서 `test_kotlin_gate_reach.py` 를 돌리면 EXIT=1 이고, 잡은 것은 스냅샷도 라쳇도 아니라 ⑴ `test_선언한_테스트_클래스와_트리에서_발견한_것이_정확히_일치한다`(`.kt` 를 지우지 않았으니 소스트리 대조) ⑵ `test_바닥_목록의_탐지기가_선언에_남아_있다`(피해자 중 `NamedReferenceGuardTest` 가 바닥 목록) 였다. 그러니 지적은 **「스냅샷 축이 자기 핀을 내리면 무력해진다」**이고 「시스템이 뚫린다」가 아니다.

#### F-11 — 비면제 작업 커밋마다 HEAD 가 구조적으로 빨갛다 · **수정 필요(설계 판정)** · 마감: 첫 묶음 회차

리더 질문 ①⑴의 답. 두 단계 풀이는 **성립한다** — NC-C1(작업 커밋만) EXIT=1 → NC-C2 계열(원장 기록 커밋 추가) EXIT=0. 원장 `:1306-1314` 의 기제 서술은 정확하다.

성립하는 대가가 적혀 있지 않다. **자기 SHA 를 자기 커밋에 못 적는다는 제약은 「HEAD 가 매 작업 커밋에서 빨갛다」와 같은 말이다.** 「커밋 전 통과」(`CLAUDE.md`)는 지켜지는데 「HEAD 에서 통과」는 매 작업 커밋에서 거짓이 된다. 영향을 받는 것들:

- 브랜치 push 의 tip 이 작업 커밋이면 CI 가 그 이유로 빨갛다
- `git bisect`·PR 커밋별 검사·리뷰어의 기준선 실행이 무관한 이유로 빨갛다
- stop-time 게이트가 세션 종료 직전 그 빨강을 본다
- `_SNAPSHOT_REV` 상향도 같은 2단 춤을 요구한다(삭제 커밋에서 빨강 → 상향 커밋에서 초록)

그 파일 자신이 *"늘 빨간 검사는 아무도 읽지 않아 탐지 능력이 0 이 된다"*(`:78-79`)고 적으면서 **간헐적으로 빨간 설계**를 택했다. 대안은 있다 — 판정 대상을 `HEAD` 가 아니라 `HEAD~1` 까지로 하거나, 원장 기록을 `--amend` 로 같은 커밋에 넣거나, 커밋 메시지 trailer 를 계상 경로로 인정하는 것.

#### F-12 — `대기` 에 시효·상한·`닫힘` 강제가 0 · **수정 필요** · 마감: 첫 묶음 회차

리더 질문 ⑤의 답. 원장 `:1291-1296` 에 축③에 닿는 커밋 4건이 `대기`(= 필수인데 미리뷰 = 빚)로 적혀 있다. 어휘 분리와 「거짓으로 비필수라고 적지 않는다」는 정직하고 옳다.

**충분하지 않다.** 「적으면 통과」는 「리뷰받은 것」과 **강제자 관점에서 같아진다**:

- `대기` 행의 수명에 상한이 없다. 4건이 무기한 초록으로 앉을 수 있다.
- `닫힘` 칸을 단언하는 검사가 0이다. 열 존재만 본다(`:229-235`).
- `상태` 칸의 **값**을 아무도 읽지 않는다 — `대기`/`이연` 구분은 사람 전용이라고 파일이 적었는데(`:57-60`), 그러면 그 칸은 강제력이 없다.
- F-2 때문에 행 자체가 필수도 아니다.

리더가 「빚」으로 명시한 것은 정직성의 상한이고, 장치의 하한은 그와 별개로 필요하다. 최소 조치: `대기` 행이 N 회차 이상 열려 있으면 실패, 또는 `대기` 행이 하나라도 있으면 다음 회차 개시를 차단.

#### F-8 — CLAUDE.md 「절 단위 결속」이 뒤따르는 평문 문단을 삼킨다 · **수정 필요** · 마감: 첫 묶음 회차

리더 질문 ④의 답. `:1321` `_claude_md_review_gate_section` 은 codex 지적으로 **파일 전체 → 절 단위**로 좁힌 것이고, 그 사유를 docstring 이 정확히 적는다. 절 경계 판정은 `:1341-1349` — `start+1` 부터 `startswith("**")` 또는 `startswith("#")` 인 첫 줄까지.

**앞당겨지는 쪽은 안전하다** — 잘리면 이름·포인터가 빠져 실패한다(포인터가 그 줄 맨 끝에 있으므로). 위험은 **반대 방향**이다: 뒤따르는 문단이 `**` 로 시작하지 않으면 절에 삼켜진다.

**NC-D1.** `CLAUDE.md:25` 를 `**리뷰 게이트:** codex 독립 리뷰는 **필수**다. 상세는 아래 문단.` 으로 줄이고, 7축 이름과 정본 포인터를 **바로 뒤 평문 문단**(`참고 — …`)으로 내렸다 → **EXIT=0 · 1 passed.** codex 가 파일 전체 범위에서 지적한 거짓 초록이 **한 문단 옆에서 그대로 재현**된다. 오늘 실제 경계는 타이트하지만(다음 줄이 `**Codex 실행 정책:**`), 그것은 현재 상태이고 검사가 허용하는 범위가 아니다.

조치는 두 줄이다 — 경계를 빈 줄로 끊거나, 이름·포인터를 `_REVIEW_GATE_HEADING` **자기 줄 안**에서 찾는다.

#### F-13 — 정체성 스냅샷의 **기준 이후 꼬리**가 라쳇 창과 같은 크기로 자란다 · **권고**

리더 질문 ③의 답. 두 부분으로 나뉜다.

**「규칙 8과 충돌하지 않는다」는 참이다.** 추가는 자유롭게 통과한다(containment 방향). Phase 안에서 이 파일을 고칠 일이 없다는 것도 참이다.

**「그 창을 실제로 닫는다」는 절반만 참이다.** `SKILL.md:301` 은 *"그 창을 실제로 닫는 것은 세 번째 축(정체성 containment)"* 이라 적고 `:299` 표의 「보장하지 않는 것」 칸에는 `@Test` 공백만 적는다. 실측:

| 시점 | 기준(`ed3df31`) | 현재 선언 | 라쳇 | 스냅샷이 못 지키는 이름 |
|---|---|---|---|---|
| HEAD `26ee39a` | 108 | 108 | 105 | **0** |
| 작업 트리(C6 레인 미커밋) | 108 | **111** | 105 | **3** |

기준 이후 추가된 클래스는 containment 밖이므로, 그 이름의 삭제는 스냅샷에 걸리지 않는다. 그리고 규칙 8이 라쳇을 Phase 경계에서만 올리므로 **라쳇 간격의 내용물이 정확히 「기준 이후 추가분」**이다. 즉 스냅샷은 창을 닫은 것이 아니라 **창의 대상을 바꿨다** — 「아무 3개」에서 「새로 추가된 3개」로. 그리고 새 클래스는 갓 쓴 코드의 테스트라 가장 지워지기 쉬운 쪽이다.

`:299` 의 「보장하지 않는 것」 칸에 두 줄이 더 필요하다 — ⑴ 기준 이후 추가분 ⑵ `_SNAPSHOT_REV` 하향(F-7).

#### F-14 — CLAUDE.md 같은 문단이 「일곱」과 「**넷**의 정본」을 함께 적는다 · **권고**

실측: `CLAUDE.md:25` 안에 `일곱 중 하나에 닿으면` 과 `**넷의 정본과 면제 목록은 …**` 이 **같은 문단에** 있다(`grep -c` = 1). 새 동기화 강제자는 이름 존재와 포인터 존재만 보므로(`:1451-1461`) 이 드리프트를 보지 못한다. M-2 가 지적한 「사본은 갈린다」가 **정정 커밋 안에서** 재발했다.

#### F-15 — 축 목록 자체에 하한·라쳇이 없다 · **권고**

F-3 의 NC-A 가 실증한 형태의 일반형이다. 축 개수를 줄이는 편집은 `_AXIS_NAMES` 를 포함한 4자리를 함께 고치면 통과한다. `_AXIS_NAMES`(`:1267`)는 **네 번째 사본**이기도 하다 — M-2 가 「셋이고 이미 갈렸다」고 지적한 목록에 강제자용 사본이 하나 더 늘었다. 사본으로서는 정당하다(강제자니까) 다만 그 사본에 방향이 없다.

#### F-16 — 커버리지 표는 현행 원장에서 영구히 자라야 하고, 그것은 분리 목표와 반대다 · **권고**

`_section`(`:150-166`)은 합본에서 **첫** 일치 절만 읽는다. 커버리지 절을 아카이브로 옮기면 이전 범위가 판정에서 사라져 그 구간 커밋이 전부 미계상이 된다(RED). 따라서 이 표는 회차마다 한 줄씩 **현행 파일에서** 자라야 한다. 계획 §4③ 목표는 `00_progress.md ≤ 700줄` 이고 현재 **1,283줄 이상**(m-1 당시 1,276)이다. 두 규약이 반대 방향을 가리킨다.

#### F-17 — 「트리 건강」 판정이 대리 지표였다 (내 이전 산출물) · **권고**

`xx_harness_cross.md` §5 가 **트리 건강 = 충족**을 적은 근거는 `mypy . .claude` Success + pytest 2파일이었다. `CLAUDE.md` 가 정한 순서의 **첫 관문(`ruff`)을 재지 않았다**. 실측하면 그 시점(`cbf6e8d`)에 `ruff format --check .` 는 **이미 EXIT=1** 이었다. 「종료 코드 0 을 검토했다로 바꿔 읽는」 형태의 반대편 — **재지 않은 것을 충족으로 적었다.** 이번 회차부터 트리 건강 판정에 `ruff check` · `ruff format --check` 를 포함한다.

#### F-18 — 비용 축소가 실제로 남았는지 재는 장치가 0 · **권고**

이 개조의 원래 목적은 게이트 39회차·리뷰 산출물 43,157줄의 축소였다. 이번 회차는 필수 축을 4 → 7로 늘리고, 「묶어도 되는 변경」 예시에서 **`테스트 추가`를 삭제**했다(양쪽 SKILL 동일). 남은 예시는 「모듈 내부 리팩터 · 성능 · 이름 바꾸기」 셋이다. 방향은 옳을 수 있으나(F-3 이 필요를 실증한다) **개조가 목표를 달성했는지 재는 지표가 어디에도 없다.** m-1(목표 미달 미기록)과 같은 계열이고, 다음 회차에 「회차 수·산출물 줄 수」를 커버리지 표 옆에 적는 것으로 닫을 수 있다.

---

## 3. 도달 범위 점검 (다섯 축을 가로지르는 필수 항목)

| 점검 항목 | 결과 |
|---|---|
| 「전역」·「모든」·「항상」 선언의 미도달 경로 | **지적 5건.** ⑴ F-6 「통과시키는 것은 끝에 덧붙이기 하나뿐」 → 기준 뒤 개작·삭제도 통과 ⑵ F-13 「그 창을 실제로 닫는다」 → 기준 이후 추가분은 무보호 ⑶ F-3 「즉시 중단 기준 전부 매핑」 → 축⑦ 조용히 삭제 가능 ⑷ F-1 「모든 변경은 정확히 한 번 리뷰」 → 저자의 한 줄로 만족 ⑸ F-10 7축 선언 vs `docs/` 전면 면제 |
| 그 게이트가 **지금 어디서 도는가** — 도달 0 의심 | **지적 2건, 둘 다 도달 0 실측.** ⑴ **F-5 — 이 HEAD 에서 CI quality 잡 전체가 도달 0**(`ruff` 5번째 스텝에서 죽고 `continue-on-error` 없음). 이번에 세운 강제자 셋 전부 포함 ⑵ F-4 — 신설 두 파일은 blanket pytest 하나에만 걸려 있고 삭제가 조용하다(NC-J2 · 99 passed) |
| 대리 경로에서 측정하지 않았는가 | **지적 2건.** ⑴ F-17 — 이전 회차가 트리 건강을 ruff 없이 판정, 그때 이미 빨갰다 ⑵ F-11 — 두 단계 풀이의 「검사가 지속적으로 초록을 유지한다」가 **HEAD 기준으로는 거짓**인데 커밋 전 실행으로 확인됐다 |
| 검사 기준이 검사 대상 자신에게서 나오지 않는가 | **지적 2건.** ⑴ **F-1 — 커버리지 판정의 기준(원장 표)을 판정 대상의 저자가 쓰고, 그 쓰기가 면제다.** 이 항목의 교과서적 형태다 ⑵ F-7 — 스냅샷의 기준이 `_SNAPSHOT_REV` 한 줄이고 그 줄의 방향이 강제되지 않는다. 반대로 잘된 것: 매핑 강제자가 **계획 문서를 읽는다**(자기 표가 아니라 외부 문서를 기준으로 삼았다 — 옳은 설계) |
| 대리 지표로 판정하지 않는가 | **지적 2건.** ⑴ F-2 — 「SHA 가 절 어딘가에 언급됐다」를 「장부에 적혔다」로 읽는다. HEAD 에서 이미 그 경로로 통과 중 ⑵ F-12 — 「행이 있다」를 「빚이 관리된다」로 읽는다(`상태`·`닫힘` 칸의 값은 아무도 읽지 않는다) |
| 규칙·패턴의 범위가 근거보다 넓지 않은가 (은폐형) | **지적 1건 + 확인 1건.** 지적: F-10 — `docs/` 전면 면제는 근거(원장 자기참조 하나)보다 훨씬 넓고 축④·축①을 덮는다. **은폐형이 맞다**(신고를 줄인다) → 규칙 4 ⑵ 에 따라 넓히지 말고 탐지형으로 갈아타야 한다. 확인: 면제 목록(§2.2 4행)은 이 범위에서 한 줄도 늘지 않았다 |
| 음성 대조가 붙어 있는가 | **부분.** 세 정정에 음성 대조가 붙어 있고 그 서술은 정확하다. 그런데 **셋 다 한 걸음 짧다** — F-6(prefix 뒤를 안 봤다) · F-8(절 뒤 평문 문단을 안 봤다) · F-3(축을 사본 전부에서 지우는 경우를 안 봤다). **커버리지 강제자(`e7faccc`)에는 음성 대조가 0건**이고, 그 파일이 이 회차에서 결함 3건(F-1·F-2·F-11)이 나온 자리다 |
| 판정하는 코드가 자기를 검사 대상에 넣었는가 | **반만.** 넣은 것: mypy 범위(2파일 Success) · `ruff check .` 범위 · 축③ glob. 안 넣은 것: **장치 밖 단언 0** — CI 경로 명시 스텝 0, 자기 배선 단언 0, 원장 `실행 경로` 등재 0(F-4). 이 저장소가 다른 게이트 4곳에 전부 붙여 둔 규약을 새 둘에는 붙이지 않았다 |

---

## 4. 이전 회차 지적의 상태 (근거 첨부)

| 원 지적 | 상태 | 근거 |
|---|---|---|
| **C-5** 4축이 즉시 중단 기준 4개를 안 담는다 | **해소** (조건부) | 축⑤⑥⑦ 신설 + 매핑 강제자. 다만 축⑦이 조용히 삭제 가능(F-3) |
| **C-3** 이연 장부 부재 | **부분 해소** | 장부·커버리지 표 신설 + 커밋 전수 대조. 계상 하한이 산문 언급(F-2), 표 쓰기가 면제(F-1), `대기` 무기한(F-12) |
| **C-4** 원장 비움·전량교체가 조용하다 | **해소 후 이동** | 비움·전량교체·개작·잘라내기 전부 잡힌다(NC-B4). 무보호 구간이 **기준 이후**로 옮겨졌다(F-6) |
| **M-1** 축③ 열거가 범주명보다 좁다 | **부분 해소** | `ci.yml`·`pyproject.toml` 추가됨. **탐지기 5종은 여전히 미포함** — `test_privacy_scanner.py`·`test_python_snapshot_guard.py`·`test_kotlin_comment_budget.py`·`test_raw_control_chars.py`·`test_llm_lane_scope.py` 는 `test_*_reach.py`·`test_*_gate*.py` 어느 glob 에도 안 걸린다(실측 재확인) |
| **M-2** 4축 사본 셋, 동기화 강제자 0 | **부분 해소** | 두 SKILL byte-identical 강제 + CLAUDE.md 절 단위 이름·포인터 검사. 절 경계가 뒤로 새고(F-8), 사본이 넷으로 늘고(F-15), 같은 문단에 「넷」이 남았다(F-14) |
| **M-3** 정확 일치는 diff 가시성이다 | **해소** | `SKILL.md:296-301` 표가 「보장하지 않는 것」을 명시하고 초판 문면을 거짓으로 표시했다. 창 크기를 재는 장치는 여전히 0(M-3 후반은 미해소) |
| **M-4** 4층 금지 범위 | **미해소** | 규칙 7 문면 불변. B-19 도달 0 |
| **C-1** stop-time 비활성화 | **해소** (사용자 결정) | `CLAUDE.md:27` 이 「모두 켜 둔다」로 되살렸고 세 레인 비대체 규정 신설 |
| **C-2** 묶음 리뷰의 `--base`/`--scope` 기본값 | **미해소** | 두 SKILL·스크립트 기본값 불변(실측). 커버리지 표가 범위를 적게 했으나 그것은 **기록**이고 호출 인자 강제가 아니다 |
| **M-5** 모델 강등 | **사용자 판단 대기** | `.claude/agents/codex-reviewer.md` 는 이 범위 밖(작업 트리에 미커밋 변경 존재) |
| **m-1** 분리 목표 미달 미기록 | **미해소** | 여전히 미기록. F-16 이 반대 압력을 추가 |
| **m-3** 병렬 레인 공유 트리 오염 | **미해소** | 규칙 5에 격리 조항 없음(실측). 이 리뷰는 전건 일회용 worktree 에서 돌렸다 |

---

## 5. Phase 종료 조건 대비 현황

| 항목 | 현황 |
|---|---|
| **트리 건강** | **미충족.** `ruff check .` **EXIT=1**(E501 2건, 이 범위 도입) · `ruff format --check .` **EXIT=1**(이 범위 전부터) · `mypy` 신설 2파일 Success · 대상 3파일 pytest 46 passed EXIT=0. **CLAUDE.md 필수 순서의 첫 관문이 빨갛다** |
| **§6 리뷰 게이트 운용** | **미충족.** 이 회차가 그 첫 단계다. 앞선 4커밋은 원장에 `대기`로 적힌 미리뷰 상태이고, 그 상태가 무기한 초록으로 유지된다(F-12) |
| **§5 즉시 중단 기준 탐지** | **부분 충족.** 7개 중 6이 필수 축에 실려 있고 강제자가 붙었다. 축⑦(골든 합격선)이 조용히 삭제 가능(F-3) |
| **§6 「보장이 어디로 갔는지」 추적** | **부분 충족.** 표 둘이 생겼고 커밋 전수 대조가 붙었다. 계상 하한이 산문 언급이라(F-2) 「적혔다」의 의미가 표 행보다 약하다 |
| **원장이 판정 근거로 신뢰 가능한가** | **부분.** 기준 리비전 이전 구간은 바이트 단위로 고정됐다(NC-B4). 기준 이후 구간은 무보호(F-6), 커버리지·장부 표는 무보호(F-1) |
| **게이트가 CI 에서 도는가** | **미충족.** 이 HEAD 에서 quality 잡이 5번째 스텝에서 죽어 그 뒤 전부 도달 0(F-5). 신설 두 강제자는 그 잡이 살아나도 blanket 스텝 하나에만 걸린다(F-4) |

**미해결 — 리더 판정 요청 (7건)**

1. **F-5** — 즉시 조치. E501 2줄을 접고 `ruff format` 을 적용할 것인가(별건 커밋인가 이 회차 안인가). 이것이 닫히기 전에는 「게이트를 세웠다」를 보고할 수 없다.
2. **F-1 · F-2** — 계상의 하한을 무엇으로 할 것인가. 최소안: `_recorded_shas` 를 **표 행 파싱**으로 바꾸고(`| \`sha\` |` 로 시작하는 줄만), 커버리지·장부 표에 prefix/단조성 가드를 붙인다.
3. **F-3 · F-9** — 매핑 판정을 「기준→축이 **정확히** 하나로 붙는다」로 바꿀 것인가. 계획 §5 마지막 불릿을 두 줄로 쪼개는 것이 가장 싼 조치다(계획 문서 편집이므로 F-10 과 함께 판단).
4. **F-10** — `docs/` 면제를 무엇으로 좁힐 것인가. 내 권고: `docs/plans/**` · `docs/master-plan.md` · `docs/migration/_workspace/03_rebuild-extraction-list.md` 를 면제에서 제외.
5. **F-4** — 신설 두 파일에 `ci.yml` 경로 명시 스텝 + 자기 배선 단언을 붙일 것인가(이 저장소 다른 게이트 4곳의 규약).
6. **F-11** — 두 단계 풀이를 유지할 것인가, 판정 대상을 `HEAD~1` 로 물릴 것인가.
7. **F-12** — `대기` 에 상한을 둘 것인가(내 권고: `대기` 행이 하나라도 있으면 다음 회차 개시 차단).

---

## 6. 미실행 · 확인 불가

- **codex 산출물 미열람** — 1차 규약. `xx_harness-fixes_codex-reviewer.md` 가 트리에 있으나 읽지 않았다. 교차 대조는 2차 `xx_harness-fixes_cross.md` 에서 한다. **이 문서만으로 Phase 종료 조건 충족을 보고하지 않는다.**
- **전체 pytest 스위트 미완주** — worktree 에서 2분 타임아웃. F-4 의 「삭제가 조용하다」는 배선 단언 계열 4파일(99 tests)로 판정했고, 그 4파일이 이 저장소에서 「게이트가 안 돌게 됐다」를 신고하는 유일한 자리다.
- **완전한 조용한 클래스 삭제 미실증** — F-7 의 NC-E4 는 `.kt` 파일을 지우지 않아 소스트리 대조가 잡았다. `.kt` 까지 지우는 5자리 편집은 Gradle 리포트 재빌드가 필요해 비용이 크다. 그래서 F-7 을 **차단이 아니라 수정 필요**로 적었고, 지적 범위를 「스냅샷 축 하나가 무력해진다」로 좁혔다.
- **`.claude/agents/codex-reviewer.md` 미커밋 변경 · `backend-kotlin/**` C6 레인 변경 · `tests/test_kotlin_gate_reach.py` 미커밋 변경** — 리더가 범위 밖으로 선언했다. 건드리지 않았다. 다만 **C6 레인 변경분은 축②(외부 HTTP 계약)에 정면으로 닿으므로 별도 회차 게이트가 필수**이고 이 문서로 대체되지 않는다. 또 그 미커밋 상태가 F-13 의 「무보호 3」을 이미 만들어 두었다(사실 기록이지 그 레인에 대한 지적이 아니다).
- **이 문서 자신이 F-10 의 예다** — `docs/` 아래에 놓이므로 커버리지 강제자에게 면제다. 심판문은 면제가 맞으나, 그 판정이 **역할이 아니라 경로**로 내려진다는 점이 F-10 의 요지다.

---

## 7. 실행 규약 준수 (규칙 5)

**음성 대조 전건 일회용 `git worktree` 에서 실행.** 공유 트리는 변조하지 않았다.

```
worktree: git worktree add --detach <scratchpad>/wt1 HEAD  → 26ee39a
정리:     git worktree remove --force <경로> && git worktree prune
          → git worktree list = 본 트리 하나 (확인)

복원 방식: worktree 내부 `git checkout -- .` / `git reset --hard <rev>` 만 사용. `cp` 미사용.
           worktree 커밋(NC-C1·NC-C2·NC-C3·NC-I)은 detached HEAD 라 어느 브랜치도 움직이지 않았다.

본 트리 sha256 (리뷰 종료 시점, 대상 파일 전건)
  00_progress.md                  ec736ef57fe91b9bcf5e708fa9d8af7fded7cb7717fe351897cbb6338656d198
  00_progress-archive.md          ba23750a6b47759e16f6056e47fce2be9723e4ab6a4db0f6fb5e357a3a5d60e1  ← 1회차 기준선과 동일
  CLAUDE.md                       147b28fd85f4eb0764d3e1210be4662908ab422fcd498a8699999fc5b13b314e
  kotlin-migration/SKILL.md       29e6d9c8a6e552234bfed9c65e721a707b65798ede1225253c44ad580d557f21
  codex-review/SKILL.md           e50bd75c04f06d46551f8a08869a8c1eec8b0914c4f94d24bce5ab21496d9096
  test_harness_scope_reach.py     73815921bf44051c2caf883c9b0c16fba7f91d8e57ba658b7c66e57855d7c697
  test_review_coverage_reach.py   7471737930472af01d99affecaf0190a99457b3da56feda9e60e9c337a723446
  test_kotlin_class_snapshot_reach.py 7324fb212a19bb4869f6f22cbe322c484ee98ec703e6694db7f21ba6be27252b
  2026-08-11-kotlin-react-migration.md f37b11043ee92756e63a524b5cfa7d9953a23d0f30b5f357604b8a1e365c5250

`git status --porcelain` 대상 파일 = 0건 (본 트리 미변조 확인).
아카이브 해시가 1회차 기준선(`ba23750a…`)과 **바이트 단위로 같다** — 이 리뷰가 공유 트리를 건드리지 않았다는 직접 증거다.

게이트 명령 전건 **파이프 없이** 실행하고 `$?` 를 직접 읽었다(zsh 에서 `${PIPESTATUS[0]}` 가 빈 값이 되어
파이프 사용 시 종료 코드가 실제로 사라진다 — 1회차에 실측한 사실).
```

**음성 대조 일람 (17건 + 파일 삭제 대조 1건)**

| # | 변조 | EXIT | 판정 |
|---|---|---|---|
| NC-A | 축⑦을 4개 사본 전부에서 제거 | **0** | 조용하다 → F-3 |
| NC-B1 | 아카이브 정당한 덧붙이기 | 0 | 정상 |
| NC-B2 | 기준 이후 구간 전량 개작 | **0** | 조용하다 → F-6 |
| NC-B3 | 기준 이후 구간 전량 삭제 | **0** | 조용하다 → F-6 |
| NC-B4 | 기준 구간 안 1바이트 변조 | 1 | 잡는다 ✓ |
| NC-C1 | 비면제 작업 커밋, 장부 기록 없음 | 1 | 잡는다 ✓ (F-11 의 근거) |
| NC-C2 | `docs/` 전용 커밋으로 전 구간 「리뷰됨」 선언 + `대기` 4줄 삭제 | **0** | 조용하다 → F-1 |
| NC-C3 | `docs/plans/**` 만 고쳐 즉시 중단 기준 완화 | **0** | 조용하다 → F-10 |
| NC-C4 | 계획에서 매핑된 불릿 삭제 | 1 | 잡는다 ✓ |
| NC-C5 | 새 기준이 기존 키를 문면에 포함 | **0** | 조용하다 → F-9 |
| NC-C6 | 새 기준, 어느 키도 안 걸림 | 1 | 잡는다 ✓ |
| NC-D1 | 축 이름·포인터를 뒤따르는 평문 문단으로 이동 | **0** | 조용하다 → F-8 |
| NC-E3 | `_SNAPSHOT_REV` 를 108개 → 98개 리비전으로 하향 | **0** | 조용하다 → F-7 |
| NC-E4 | 하향 후 기준 밖 10개 삭제 (스냅샷 축만) | **0** | 조용하다 → F-7 |
| NC-G | `대기` 4줄만 삭제 | 1 | **3건**만 신고 → F-2 의 실물 증거 |
| NC-H | `e7faccc` 장부 행만 삭제(산문 언급 잔존) | **0** | 조용하다 → F-2 |
| NC-I | 새 비면제 커밋을 산문 한 줄로만 언급 | **0** | 조용하다 → F-2 |

(NC-J2 는 파일 삭제 대조 — 신설 두 강제자 삭제 후 배선 단언 계열 99 passed EXIT=0 → F-4.)
