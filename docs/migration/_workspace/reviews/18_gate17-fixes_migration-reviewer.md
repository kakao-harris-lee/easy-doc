# 게이트 18 — Claude 독립 리뷰 (1회차) · `18_gate17-fixes`

> **1회차 = codex 산출물 없이 쓰는 독립 리뷰다.** codex 부재는 이 회차의 정상 상태이며 실패로
> 기록하지 않는다. 교차 대조표는 2회차(`18_gate17-fixes_cross.md`)에서만 만든다.
> 이 파일만으로 Phase 종료 조건 충족을 보고하지 않는다.

---

## 1. 리뷰 범위와 참조

| 항목 | 값 |
|---|---|
| 대상 | `36b5ed4..318069b` — 코드 커밋 2개 (`107c8a5` 러너 · `318069b` 완전성/중복/갈래) |
| 변경 규모 | 6파일 · +492 / −60. **새 파일 0개** |
| 닫으려는 것 | 게이트 17 교차 종합(`reviews/17_gate16-fixes_cross.md`) 표 **①~⑦ · ⑨ · ⑬ · ⑭** |
| 리더 판정 반영 | 충돌 ⑨ → **Claude R17-3 채택** (SKILL.md 에서 값 삭제·지목만) |
| 참조 정본 | 계획 §5 Phase 7 · §6 (X1·X2·X3) · CLAUDE.md 규칙 4·5 · `kotlin-migration` SKILL.md 「선언한 범위와 실제 도달을 대조한다」 |
| 원장 | `docs/migration/_workspace/00_progress.md` — **읽기만 했다.** 고치지 않았다 |

### 1.1 절차 고지 (규칙 5)

- 변이는 전부 **일회용 `git worktree`**(`…/scratchpad/wt-gate18`, detached `318069b`)에서 했다.
  본 저장소는 **추적 파일 변경 0** — 리뷰 전후 `git status --porcelain` 이 기존 untracked 3건뿐이고
  `HEAD` 는 `318069b` 그대로다.
- **`cp` 복원 없음.** 전부 `git checkout -- <path>` 로 되돌리고 **매 회 sha256 을 대조**했다.
  기준선: `compare_parity.py` `984ed9e4…` · `test_parity_ci_gate.py` `9f775c76…` ·
  `test_python_snapshot_guard.py` `822592ba…` · `dump_python_snapshots.py` `dfb28d31…` ·
  `run_gate.sh` `86df9e74…` · `ci.yml` `80206da3…` — **전건 복원 후 일치 확인**.
- 옛 판 러너·functrace 뺀 사본은 저장소가 아니라 **스크래치패드**에 뽑아 썼다
  (`git show d0a5255:…` → sha256 `23d14464…`).
- 종료 코드는 전부 파이프 없이 받았다.

### 1.2 기준선 (변이 전)

| 명령 | 결과 |
|---|---|
| `pytest tests/test_run_gate.py` | **23 passed** |
| `pytest tests/test_parity_ci_gate.py tests/test_python_snapshot_guard.py` | **48 passed** |
| `pytest tests/test_harness_scope_reach.py` | **37 passed** |
| `pytest`(전체) | **1232 passed · 68 skipped · 5 deselected · 5 xfailed** |
| `ruff check .` / `ruff format --check .` | 0 / 153 files already formatted |
| `mypy . .claude` | 0 (137 files) |
| `dump_python_snapshots.py --check` | **exit 0** (`[일치]` 2건 — 실물 diff 0 유지) |

---

## 2. 음성 대조 — 독립 재현 (레인 보고 18/18 대비 **18/18 + 자체 추가 12종**)

레인이 실측했다고 보고한 18종을 **전건 독립 재현**했다. 표본 추출은 하지 않았다 — 전건이 1~2초짜리
변이여서 표본을 뽑을 이유가 없었다. 아래는 **내가 직접 돌린 결과**이고 레인 보고와 별개 실행이다.

### 2.1 러너 (NC-1) — 옛 판 vs 새 판

| 대상 | 결과 |
|---|---|
| 옛 판 `d0a5255`(sha256 `23d14464…`) + **새** `test_run_gate.py` | **3 failed / 20 passed** — `test_comment_only_arg_exits_2` · `test_backslash_newline_only_arg_exits_2` · `test_unset_variable_reference_is_non_zero` |
| 새 판 | **23 passed** |

레인 보고(3 failed / 23 passed)와 **일치**. `RUN_GATE_PATH` 를 옛 판으로 돌려도
`test_default_target_is_the_tracked_repo_file` 이 통과한다 — ⑭ 의 `monkeypatch.delenv` 가
실제로 동작한다는 뜻이다.

### 2.2 러너 장치의 flag 별 음성 대조 (**레인 보고에 없던 자체 추가 5종**)

| 변이 | 결과 |
|---|---|
| `-o functrace` 제거 | **1 failed** — `test_subshell_command_is_work_not_zero_work` |
| `-o nounset` 제거 | **1 failed** — `test_unset_variable_reference_is_non_zero` |
| `-o pipefail` 제거 | **1 failed** — `test_pipe_failure_inside_runner_is_non_zero` |
| zero-work 판정 블록 무력화 (`if false`) | **2 failed** — comment_only · backslash_newline |
| DEBUG trap preamble 제거 | **6 failed** — 정상 명령이 전부 zero-work 오탐 |

**러너의 네 스위치 전부에 대응 음성 대조가 있다.** 떼면 밖에서 깨진다 — 규칙 5 충족.

### 2.3 완전성 장치 (⑤③②④) + 자체 추가 4종

| 변이 | 첫 실패 메시지 |
|---|---|
| **⑤a** 빈 표 `{}` · 상수 10 유지 | `본류 회귀 표가 0행인데 기대는 10행이다` |
| **⑤b** 빈 표 + **상수도 0 으로 동시 조작** (자체 추가) | `` `case_floor_additions` 은 본류 함수 ['main'] 에서 불리는데 본류 회귀 표에 없다`` |
| **③** `reference_problems` 행 제거 + 상수 9 | `` `reference_problems` 은 본류 함수 ['compare_file'] 에서 불리는데 …`` |
| **②** 테스트 이름 재사용 | `표의 테스트 이름이 겹친다 — 한 테스트가 두 helper 를 대표할 수 없다` |
| **④** bogus 문구 | `표의 문구 'bogus 문구' 를 `in output` 으로 단언하지 않는다` |
| **④'** 진짜지만 갈린 문구(`"결과 파일 없음"`) | 같은 단언에서 빨강 |
| **A** `_MAINLINE_ROOTS = ()` (자체 추가) | `` `case_floor_additions` 의 호출부 `main` 는 본류 함수도 표의 helper 도 아니다`` |
| **B** ROOTS 에서 `compare_file` 제거 (자체 추가) | `` `reference_problems` 의 호출부 `compare_file` 는 …`` |
| **C** ROOTS 에서 `main` 제거 (자체 추가) | `` `case_floor_additions` 의 호출부 `main` 는 …`` |

**⑤b 가 중요하다** — 표를 비우면서 상수까지 맞춰도 AST 경로가 잡는다. ⑤와 ③이 서로를 덮는다.

### 2.4 본류 호출선 10종 (`compare_parity.py`)

각 호출부를 `(lambda *a, **k: …)` 로 무력화하고 파일 단위로 돌렸다. **10/10 전부 대응 본류 테스트 빨강.**

| helper | 호출부 | 대응 본류 테스트 빨강 | 그 회차 총 실패 |
|---|---|---|---|
| `runtime_problem` | `compare_file:1984` | ✔ | 3 failed |
| **`reference_problems`** | **`compare_file:2072`** | ✔ `test_본류가_낡은_원장_기록을_막는다` | **2 failed** |
| `case_floor_problems` | `main:2269` | ✔ | 2 failed |
| `full_gate_floor_problems` | `main:2274` | ✔ | 3 failed |
| `structural_problems` | `main:2279` | ✔ | 3 failed |
| `provenance_problems` | `main:2282` | ✔ | 2 failed |
| `stale_ledger_problems` | `main:2329` | ✔ | 2 failed |
| `ledger_write_problems` | `main:2344` | ✔ | 2 failed |
| `case_floor_additions` | `main:2409` | ✔ | 2 failed |
| `spec_shape_problems` | `structural_problems:1207` | ✔ | 2 failed |

**부수 확인 (레인 보고와 같음)**: 열 회차 전부에서 완전성 테스트도 함께 빨개진다 —
호출부가 사라지면 `len(callers) == 1` 이 깨지기 때문이다. **호출선 제거가 이제 두 겹으로 잡힌다.**

### 2.5 중복 이름·메시지 갈래 (⑥⑦)

| 변이 | 결과 |
|---|---|
| 생성기(`dump_python_snapshots.py`) 중복 `raise` 제거 | **1 failed** — `test_실물_케이스_이름_중복을_check가_막는다` |
| 가드 테스트 중복 단언 제거 | **1 failed** — `test_실물_케이스_이름_중복을_가드가_막는다` |
| 하한 키 누락 갈래 제거(옛 `.get()` 판) | **1 failed** — `test_하한_키_누락과_빈_하한은_메시지가_다르다` |

**각각 정확히 1건만** 빨개진다 — 세 장치가 서로 독립이다. 실물 `--check` 는 **exit 0 유지**
(4개 섹션 `system_prompts`·`user_prompts`·`repair_prompts`·`postprocess` 전부 중복 0).

---

## 3. 다섯 축별 지적

> 심각도 척도: **차단(①사건 / ②장치)** · **수정 필요**(Phase 종료 조건 미충족) · **권고** · **판정 필요**.
> **차단 0건.** 아래 「수정 필요」는 전부 **이 배치가 세운 장치 자신의 도달 결함**이며, 오늘 게이트를
> 거짓 통과시키지는 않는다 — 그래서 차단②로 올리지 않았다.

### 3.1 테스트 적정성

#### **T18-1 [수정 필요] ④ 의 문구·helper 결속이 문자열 grep 이라 「주석 한 줄」로 충족된다 — 같은 커밋이 ③에서 버린 방법이다**

- **근거**: `tests/test_parity_ci_gate.py:1090` (`assert helper in test_source`) · `:1094`
  (`assert f'"{phrase}" in output' in test_source`). 둘 다 `inspect.getsource` 로 얻은
  **소스 텍스트**를 부분 문자열로 찾는다.
- **실측(worktree 변이)**: `test_본류가_빈_fixture를_막는다:765-768` 의
  `assert "빈 fixture" in output, (…)` 을
  `# assert "빈 fixture" in output — 주석으로만 남긴다` + `pass` 로 바꿨다 →
  **`36 passed`**(빨강 0). **그 본류 테스트는 문구를 더 이상 단언하지 않는데, 완전성 테스트는
  "표의 문구가 실제 단언에 있다"고 계속 판정한다.**
- **같은 함수 안의 반대 방향**: ③ 을 닫은 `_call_sites`(`:979-981`)의 docstring 은
  **「문자열 grep 이 아니다 — 주석·docstring 의 `reference_problems()` 같은 언급은 호출이 아니고,
  여기서는 세지 않는다」** 고 적는다. ④ 는 **그 문장이 금지한 방법**으로 구현됐다.
- **선언 대 도달**: 커밋 `318069b` 본문은 ④를 "표 문구가 테스트 소스의 `"<문구>" in output`
  **단언에 실제로 있고**"라고 적는다. 재는 것은 **단언**이 아니라 **텍스트**다.
- **게이트 17 ④ 판정**: **부분 해소.**
- **마감**: 게이트 17 ④의 원 마감 승계 — codex `no-ship` / Claude 「Phase 3 내」 → **Phase 3 착수 전**.
- **관련 종료 조건**: §6 X2.

#### **T18-2 [수정 필요] 새 CI 배선 탐지기가 `ci.yml` 의 세 스텝 형식 중 하나만 본다 — 「배선 0」 선언의 강제자가 CI 의 3분의 1을 못 본다**

- **근거**: `tests/test_run_gate.py:295`
  `run_lines = [line for line in ci.splitlines() if line.lstrip().startswith("run:")]`
- **실측(worktree 변이)**:

  | 사실 | 값 |
  |---|---|
  | `ci.yml` 의 스텝 총수 | **33** (들여쓴 `run:` 21 + `- run:` 12) |
  | 이 필터가 보는 줄 | **21줄** — 그중 6줄은 `run: \|` **헤더**라 본문은 못 본다 |
  | `- run:` 인라인 스텝 (보이지 않음) | **12줄** — `- run: uv run mypy . .claude`, `- run: uv run pytest`, `- run: npm ci` 등 |
  | `run: \|` 블록 스칼라 **본문** (보이지 않음) | **6블록 · 327줄** |
  | 블록 스칼라 **안**에 `run_gate.sh 'echo wired'` 삽입 | **1 passed** — 배선됐는데 초록 |
  | 들여쓴 `run:` 형태로 삽입 (대조군) | **1 failed** — 한 형식에서만 작동함을 확인 |

- **이 테스트가 강제하는 선언**: `run_gate.sh:56-58` 의 **「러너 자신을 부르는 CI 배선은 0」**.
  0 선언의 강제자가 CI 의 3분의 1을 구조적으로 못 본다 — CLAUDE.md 규칙 4 근거 5번
  (`mypy .` 가 점 디렉터리를 건너뜀)과 같은 형태다.
- **같은 저장소에 이미 옳은 방법이 있다**: `tests/test_harness_scope_reach.py:948-956`
  `read_ci_job_names()` 가 `yaml.safe_load` 를 쓰며 docstring 이
  **「문자열 grep 이 아니라 YAML 파싱이다 — … grep 은 없는 잡을 있다고 답하고, 그 순간 규칙 3이
  무의미해진다」** 고 적는다. 새 테스트는 옆 파일의 그 판단을 되돌렸다.
- **마감**: 러너를 게이트 근거로 처음 쓰기 전 = **Phase 3 착수 전**.
- **관련 종료 조건**: §6 규칙 6(자기 검사) · 규칙 4.

#### **T18-3 [권고] 러너 계약 대조표 자신에게 강제자가 없다 — 완전성 장치와 비대칭**

- **근거**: `tests/test_run_gate.py:12-39` 의 12줄 「계약 문장 ↔ 테스트 대응」표는 **산문**이다.
  이름을 댄 테스트가 지워지거나 개명돼도 어디서도 빨개지지 않는다.
- **확인**: `grep -rn "test_run_gate\|계약 문장" tests/test_harness_scope_reach.py` → **0건.**
- **비대칭**: 같은 배치가 `_MAINLINE_HELPERS` 에는 **실재(`callable`)·유일성·결속** 단언을 붙였다
  (`:1084-1101`). 러너 쪽 표에는 그중 아무것도 없다. 게이트 17 이 저쪽에서 「표가 문장이 됐다」를
  막으려고 붙인 장치가 이쪽에는 없는 채다.
- **마감**: 러너 실사용 전. **관련 종료 조건**: §6 규칙 6.

#### **T18-4 [권고] 머리 주석 문장 3개가 대조표에 없고 「테스트 없음(사유)」로도 적히지 않았다**

대조표는 `:12` 에서 **「머리 주석의 문장마다 검사가 있어야 한다」** 는 완전성을 선언한다. 빠진 문장:

| 머리 주석 문장 | 위치 | 상태 |
|---|---|---|
| 자기 도달 ⑴ 「러너를 경유하지 않은 명령 — 못 잡는다」 | `run_gate.sh:47-48` | **표에 없음.** 사유(원리상 관측 대상이 없다)는 타당하나 **적혀 있지 않다** |
| 장치 분류 「탐지형 — 면제 목록·무시 패턴 없음」 | `:43-44` | **표에 없음.** 「면제 목록 없음」은 게이트 17 ③이 다른 파일에서 실제로 깨진 종류의 선언이다 |
| 「zsh 에서 불러도 안전하다」 | `:10-12` | **표에 없음** → T18-5 |

리더가 지목한 「테스트 없는 문장 1개」의 사유는 **⑴에 대해서는 타당**하다. 다만 표가 그것을
**"미검토(사유)"로 구분해 적지 않고 그냥 빠뜨렸다** — 이 리뷰 역할 자신이 지키는
「검토함 — 지적 없음 / 미검토(사유)」 구분과 같은 형태의 빈칸이다. 그리고 빠진 문장은 **1개가 아니라 3개**다.

- **마감**: 러너 실사용 전. **관련 종료 조건**: §6 규칙 6.

#### **T18-5 [권고] 「zsh 에서 불러도 안전하다」의 도달 0 — 게이트 17 ⑬ 의 잔여**

- **근거**: 선언 `run_gate.sh:10-12` ↔ `tests/test_run_gate.py:75` `["bash", str(_runner()), *args]`.
  **22개 계약 테스트 전부 러너를 `bash` 로만 부른다.**
- ⑬ 이 parametrize 한 것(`:307` `["bash", "sh", "zsh"]`)은 LIMIT 테스트의 **바깥** 셸이지
  **러너를 실행하는 셸**이 아니다.
- **실측(수동)**: `zsh run_gate.sh` 6종 — `echo ok`→0 · 공백→2 · 주석전용→2 · `false | tail -1`→1 ·
  `exit 4`→4 · 빈문자열→2. `sh run_gate.sh` 4종 동일 거동. **선언은 참이나 재는 것이 없다.**
- **게이트 17 ⑬ 판정**: **부분 해소** — 지적된 그 자리는 닫혔고, 같은 선언의 다른 축이 남았다.
- **마감**: 러너 실사용 전.

#### **T18-6 [권고] 커밋 메시지의 「(테스트 고정)」이 함수 경로에 대해 과대하다**

- **근거**: `107c8a5` 본문 — "정상 명령·파이프·**함수**·`${X:-d}` 는 영향 없음(테스트 고정)".
  `tests/test_run_gate.py` 의 대조표·테스트에 **함수 항목이 없다**.
- **실측**: `f() { echo x; }; f` 는 functrace 유무와 무관하게 rc 0 (0 vs 0) — **무해함은 참**이다.
  틀린 것은 「테스트 고정」쪽이다.
- **부수 사실(지적 아님)**: **함수 정의만** 있는 입력(`f() { echo x; }`)은 **exit 2** 로 zero-work
  판정된다. 의도에 맞는 거동이며 문서화돼 있지는 않다.

### 3.2 parity 위험

#### **P18-1 [검토함 — 지적 없음] 중복 이름 실패가 실물에 오탐을 내지 않는다**

`dump_python_snapshots.py --check` → **exit 0**, `[일치] python-prompt-snapshot.json — 키 20개` /
`[일치] python-style-rules-snapshot.json — 키 13개`. 하한 4섹션 전부 중복 0.
§2.5 의 음성 대조 3/3 이 각각 **정확히 1건**만 빨갛게 만든다.

#### **P18-2 [검토함 — 지적 없음] 본류 호출선 10종 전부 재현 — 열째 포함**

§2.4 표. 비교기 sha256 `984ed9e4…` 변이 전후 동일. 게이트 17 cross §6.1 ⓑ 행이 기록한
「9자리」가 이 배치로 **10자리**가 됐고, 새 자리(`reference_problems` / `compare_file:2072`)도
**독립 재현됐다**.

#### **P18-3 [검토함 — 지적 없음] 열째의 판정 문구가 그 helper 에만 귀속된다**

`test_본류가_낡은_원장_기록을_막는다` 의 `"원장이 낡았다"` 는 `:2072` 를 무력화했을 때만 사라진다
(다른 9종 변이에서는 이 테스트가 초록). 두 대조군(`test_본류_대조군은_위조_문구_없이_실패한다`,
`test_본류_정상_산출물_대조군은_helper_문구를_내지_않는다`)이 `_MAINLINE_PHRASES` 전건 부정으로
귀속을 고정한다.

### 3.3 보안 불변식

#### **S18-1 [검토함 — 지적 없음] 러너 stdout 비밀값 전제가 유지된다**

- 전제 문장 `run_gate.sh:60-61` 유지. 근거 테스트 `test_command_text_and_exit_are_logged_to_stdout` 유지.
- **새로 생긴 표면 점검**: `RUN_GATE_MARKER` 환경변수와 마커 임시 파일에는 **명령 문자열이 들어가지
  않는다**(DEBUG trap 이 쓰는 것은 빈 줄 하나). `mktemp` 기본 권한. EXIT trap 정리 확인 —
  전 실행 후 `${TMPDIR}/run_gate.*` **잔존 0건**.
- 개인정보·평문 로그·마스킹·암호화·JWT 경로 **diff 0줄**.

#### **S18-2 [권고] SKILL.md 축약으로 사용 지점의 두 경고가 사라졌다 (계약 준수 축과 공유)**

리더 판정(R17-3 채택)에 따른 **의도된** 결과이므로 지적이 아니라 **손실 기록**이다. `SKILL.md:238`
에서 함께 사라진 것:

1. **「러너 호출 자체를 파이프에 태우면 무효다 — 파이프는 반드시 따옴표 안에 넣어라」**
   — 게이트 16 B 로 **실제 사고가 났던 그 함정**이다.
2. **「인자에 비밀값을 넣지 마라 — 전문이 stdout 에 기록된다」** — 보안 전제.

대체 강제자는 산문 한 문장(**「쓰기 전에 머리 주석을 읽어라」**)뿐이고 **도달 0**이다.
값을 옮기지 않으면서 함정만 남기는 길(형식 규칙만 남기고 값·한계는 지목)이 있는지는 **리더 판단**으로
넘긴다 — 이 회차가 처방을 정하지 않는다.

### 3.4 Kotlin/Spring 관용성

#### **K18-1 [검토함 — 지적 없음] Kotlin 무변경**

`git diff --stat 36b5ed4..318069b -- backend-kotlin/ frontend/ contracts/ app/ parity/` → **0줄**.
모듈 경계·트랜잭션·Flyway·LLM 어댑터 전부 이 배치의 사정권 밖이다.

### 3.5 계약 준수

#### **C18-1 [검토함 — 지적 없음] HTTP 계약 무변경**

`contracts/easy-doc-v1.yaml` · `frontend/src/api/` diff 0줄. 14개 엔드포인트 계약에 닿지 않는다.

#### **C18-2 [권고]** = S18-2 (같은 자리를 두 축에서 센다 — 건수는 **1건**으로 집계한다).

---

## 4. 도달 범위 점검 결과 (다섯 축을 가로지르는 필수 구획)

> 지적이 없으면 「검토함 — 지적 없음」, 보지 못했으면 「미검토(사유)」로 적는다. **비워 두지 않는다.**

### **R18-1 [수정 필요] 머리 주석의 「(실측)」 주장 절반이 재현되지 않는다 — 명령 치환**

- **근거**: `run_gate.sh:27-28` — 「functrace 가 없으면 서브셸 `(cmd)`·**명령 치환** 안의 명령이
  trap 에 안 잡혀 오탐이 난다(**실측**)」.
- **실측(functrace 뺀 사본을 스크래치패드에 만들어 같은 입력으로 대조)**:

  | 입력 | functrace 있음 | 없음 | 갈리는가 |
  |---|---|---|---|
  | `(echo sub)` | 0 | **2** | **갈린다 — 주장 참** |
  | `echo "$(echo hi)"` | 0 | 0 | **안 갈린다** |
  | `x=$(echo hi)` | 0 | 0 | **안 갈린다** |
  | `f() { echo x; }; f` | 0 | 0 | 안 갈린다 |
  | `echo a \| tail -1` · `true` · `# c` | 0/0/2 | 0/0/2 | 안 갈린다 |

  바깥 단순 명령이 **먼저** DEBUG 를 발화시키므로 명령 치환 여부가 마커를 좌우하지 못한다.
- **무게**: 이 문장이 실린 곳은 SKILL.md 가 이번 배치에서 **유일 정본으로 지목한 바로 그 머리 주석**이다.
  정본에 재현되지 않는 실측 주장이 남아 있으면, 다음 사람이 그것을 근거로 판단한다.
- **대조표도 절반만 적는다**: `tests/test_run_gate.py:22-23` 은 「functrace — **서브셸** 안 명령은
  zero-work 가 아니다」만 매긴다. 명령 치환 쪽은 테스트도 없다.
- **마감**: 러너를 게이트 근거로 처음 쓰기 전. **관련 종료 조건**: §6 규칙 6 · 규칙 4.

### **R18-2 [수정 필요] zero-work 「잔여」 선언이 닫힌 열거인데 실제 잔여가 더 넓다**

- **근거**: `run_gate.sh:29-34` 는 잔여를 **한 종으로 못박는다** —
  「**못 잡는 잔여(정직하게)**: 값이 빈 문자열로 **설정된** 변수만 있는 명령(`'$V'`, V="")」.
- **실측(전부 rc 0 통과 = 미탐지)**:

  | 입력 | rc | 선언에 있는가 |
  |---|---|---|
  | `$E` (E="") | 0 | **있음** |
  | `eval "$E"` (E="") | **0** | **없음** — 변수로 명령을 넘기는 **같은 통로**의 다른 형태 |
  | `bash -c ""` | **0** | **없음** — 러너가 잡으려는 바로 그 결함의 **한 겹 아래** |
  | `X=1` · `export FOO=1` | **0** | **없음** |
  | `:` · `true` | **0** | **없음** |

- **권장 처방도 좁다**: `${V:?}` 는 직접 확장형만 덮고 `eval "$V"` 형은 못 덮는다.
- **기제 대 문면**: 마커가 재는 것은 「**단순 명령 하나 이상이 실행 단계에 진입했다**」인데,
  오류 문구(`:104`)와 주석은 「**확장 후 실행된 명령이 0건**」이라 적는다. **선언이 도달보다 넓다.**
- **처방 방향(참고 — 판정은 리더)**: 탐지를 넓히거나, 잔여를 **닫힌 열거가 아니라 종류**로 다시 적는
  길이 있다(「첫 단순 명령이 실행 단계에 들어가면 마커가 찍히므로, 그 명령 자체가 아무 일도 하지
  않는 경우는 못 본다」).
- **마감**: 러너를 게이트 근거로 처음 쓰기 전. **관련 종료 조건**: 규칙 4(범위는 근거를 넘지 않는다).

### **R18-3 [수정 필요] 원장 `00_progress.md:548` 이 「9자리·음성 대조 9/9」인 채다 — 코드는 10**

- **원장 문면**: 「**본류 회귀는 8자리가 아니라 9자리다**」 · 「**음성 대조 9/9**」.
- **코드**: `tests/test_parity_ci_gate.py:617` `EXPECTED_MAINLINE_HELPERS = 10`, 실제 호출선 10.
- **강제자 0**: 상수와 원장을 잇는 검사가 없다. `test_harness_scope_reach.py`(37 passed)도 이 값을
  재지 않는다.
- **이 배치의 결함이 아니다** — 원장은 대상이 아니었다(읽기만). 그러나 방치하면 **다음 회차가 9를
  근거로 판정한다.** 저장소 전수 확인 결과 낡은 값이 남은 곳은 **이 한 줄뿐**이고,
  frozen review(`16_gate15-fixes_cross.md §6.1` 8자리 표)는 이력이므로 손대지 않는 것이 맞다.
  코드 주석 `:589`·`:595`·`:752` 는 8→9→10 전이를 **정확히 서술**한다.
- **마감**: 리더의 게이트 18 후속 원장 기재. **관련 종료 조건**: §6 X2.

### **R18-4 [검토함 — 지적 없음] `_MAINLINE_ROOTS`·빈 표에서 공허해지는 단언은 없다**

§2.3 의 9종 변이 전건 빨강. 특히 **레인 자기 감사가 「부분·잔여」로 남겼을 법한 자리** 셋을
직접 겨눴다:

| 물음 | 답 |
|---|---|
| `_MAINLINE_ROOTS` 가 **비면** 어떤 단언이 공허한가 | `in_roots` 단언(`:1054-1058`)은 공허해진다. 그러나 같은 루프의 `outside` 단언(`:1059-1063`)과 표 helper 루프의 `callers[0] in _MAINLINE_ROOTS or … in table`(`:1074`)이 **둘 다 빨개진다.** 실측 A |
| **한 항목이 빠지면** | `main` 제거·`compare_file` 제거 각각 빨강. 실측 B·C |
| 빈 표에서 **대조군 부정 단언**은 무엇을 부정하는가 | `_MAINLINE_PHRASES`(`:624-632`)는 표에서 유도한 10개 + **하드코딩 5개**로 이뤄져 있어, 표가 비어도 5개는 남는다 — **완전히 공허해지지는 않지만 절반 이하로 줄어든다.** 다만 그 상태 자체가 ⑤a/⑤b 로 먼저 빨개지므로 **도달 가능한 상태가 아니다** |

### **R18-5 [검토함 — 지적 없음] AST 대조의 별칭·`getattr`·`partial` 사각지대는 현재 실물이 없다**

`compare_parity.py` 전수 AST 조사 — helper 11종에 대해:
**비호출 `Name` 참조 0 · `Attribute` 호출 0 · `getattr`/`partial` 호출 0 · 모듈 레벨 호출 0 ·
클래스 메서드 안 호출 0.**

별칭이 새로 생기면 `callers` 가 비어 **빨강**(안전 방향)이다. 이론적 false-green 은
「직접 호출 1곳 **+** 별칭으로 본류에서 1곳」 혼합뿐이고, 현재 그런 코드가 없다.
`_call_sites` 가 `ast.AsyncFunctionDef` 를 다루지 않는 것도 같은 이유로 **빨강 방향**이다.

### **R18-6 [검토함 — 지적 없음] `ci.yml` 변경 불필요 판단은 맞다**

- 이 배치에 **새 테스트 파일 0개**(전부 기존 파일 안의 새 함수).
- 세 파일 모두 이미 경로 명시 스텝이 있다 — `:157` `test_parity_ci_gate.py` ·
  `:166` `test_python_snapshot_guard.py` · `:176` `test_run_gate.py`.
- **직접 확인**했고, 새 테스트가 그 안에 있음을 파일 단위 실행으로 검증했다.

### **R18-7 [권고] `-o nounset` 계약 변경의 명시가 머리 주석 한 곳뿐이다**

- 자식 셸 계약이 바뀌었는데(`$1`·미설정 변수 참조 → 비-0) 명시는 `run_gate.sh:35-38` ⒞ 한 곳이고,
  SKILL.md 는 값을 지우면서 산문 강제자만 남겼다(S18-2).
- **오탐 실측 — 현실 게이트 명령 11종 전부 무영향**: `pytest --version` · `pytest … \| tail -2`(0) ·
  실패하는 pytest(**4 전파**) · `git status --porcelain \| wc -l` · `${VAR:-d}` · `"$@"` · `"$*"` ·
  `$?` · `${PATH:0:4}` · 미사용 분기의 미설정 변수 · `(echo sub)`.
  **`uv run pytest …`·`./gradlew …` 형태에서 오탐 위험은 낮다.** 지적은 **문서 도달**이지 기능이 아니다.

### **R18-8 [검토함 — 지적 없음] preamble 이 낳는 부작용 3종**

| 우려 | 실측 |
|---|---|
| 자식 명령의 **줄 번호 어긋남** | 어긋나지 않는다. `bash -c $'echo a\\nnosuchcmd'` 는 원래 `line 1` 을 보고하고(bash 자체 오프셋), preamble 이 +1 해 **`line 2` = 사용자 명령의 실제 줄** 이 된다. 우연이지만 **회귀는 없다** |
| 마커 파일 **잔존** | 0건 (EXIT trap `rm -f`) |
| `functrace` 로 인한 **stderr 오염·성능** | 없음. DEBUG trap 이 첫 발화에서 `trap - DEBUG` 로 자기를 해제한다 |

### **R18-9 [미검토(사유)] bash 5.x 에서의 DEBUG/functrace 거동**

- 이 환경의 bash 는 **3.2.57(macOS) 하나뿐**이다(`which -a bash` → `/bin/bash`; homebrew bash 없음).
  zero-work 기제 전체가 **단일 bash 판에서만** 측정됐다.
- CI(`ubuntu-latest`)는 bash 5.x 다. **완화 사실**: `tests/test_run_gate.py` 가 `ci:quality` 경로
  명시로 돌므로, 거동이 갈리면 `test_comment_only_arg_exits_2`(또는 `test_subshell_…`)가
  **CI 에서 빨개진다** — 조용한 통과가 아니라 관측 가능한 실패다. 그래서 지적으로 올리지 않고
  **미검토로 남긴다.**
- 게이트 17 ⑬(셸 값 하나)과 **같은 형태**이므로 다음 회차 focus 후보로 기록한다.

### **R18-10 [검토함 — 지적 없음] 판정 코드가 자기 검사 범위 안에 있다**

`mypy . .claude` **0(137 files)** · `ruff check .` **0** · `ruff format --check` **0**.
새로 들어온 `_call_sites`·`_duplicate_case`·`test_runner_itself_has_no_ci_wiring_but_this_file_does`
전부 그 범위 안이다. 러너는 `bash -n`(`test_runner_exists_and_parses`)까지만 —
**shellcheck 도달 0** 은 게이트 17 ⑮ 그대로이며 이 배치가 건드리지 않았다(이월).

---

## 5. 게이트 17 항목별 해소 판정

> 게이트 17 교차 종합 표의 번호를 그대로 쓴다. 판정 근거는 §2 의 **독립 재현**이다.

| 게이트 17 | 요지 | **판정** | 근거 |
|---|---|---|---|
| **①** 러너 zero-work → exit 0 | 인자는 비지 않았는데 확장 후 0건 | **해소** | 세 입력 전부 exit 2(§2.1) + flag 음성 대조 5종(§2.2). **잔여 선언 폭 문제는 R18-2 로 신설** |
| **②** 테스트 이름 재사용 승인 | 유일성·결속 미검사 | **해소** | 이름 재사용 변이 → 빨강(§2.3). **결속의 grep 성질은 T18-1** |
| **③** `nested` 면제 목록(은폐형) | 한 줄 추가로 회귀 면제 | **해소** | 면제 폐지 → AST 탐지형. 전환이 **열째를 드러냈다**. 행 제거+상수 9 변이 → 빨강 |
| **④** 표 판정 문구 미대조 | bogus 문구도 통과 | **부분 해소** | bogus·갈린 문구는 잡는다. **주석으로 충족되는 자리가 남았다(T18-1, 실측 36 passed)** |
| **⑤** 빈 표에서 통과 (**차단②**) | 검사 대상 0건 통과 | **해소** | 정확 일치(`EXPECTED_MAINLINE_HELPERS = 10`) + AST 이중. **상수까지 맞춘 동시 조작(⑤b)도 잡힌다** |
| **⑥** 실물 케이스 이름 중복 | 집합으로 접혀 개수 위조 통과 | **해소** | 생성기·가드 **양쪽** 실패, 각 1건 빨강. 실물 `--check` exit 0 유지(오탐 0) |
| **⑦** 키 누락/빈 튜플 동일 메시지 | 진단이 같아 엉뚱한 자리를 고침 | **해소** | 갈래 분리 + 회귀 1건, 갈래 제거 변이 → 빨강 |
| **⑨** SKILL.md 이중 기재 (충돌 → R17-3 채택) | 값 복사 | **해소** | `SKILL.md:238` 에 계약값·한계 전문 **0**. 지목만 남았다. **부수 손실은 S18-2** |
| **⑬** LIMIT parametrize 값 하나 | 원 사고 셸 zsh 미커버 | **부분 해소** | LIMIT 테스트 바깥 셸 3종으로 확대(있으면 실행·없으면 skip). **러너를 부르는 셸은 여전히 bash 하나 — T18-5** |
| **⑭** `RUN_GATE_PATH` 기본값 미단언 | 손잡이가 검사 대상을 바꿈 | **해소** | `test_default_target_is_the_tracked_repo_file` — `git ls-files --error-unmatch` + sha256 이중 대조. `RUN_GATE_PATH` 를 켠 채 돌려도 통과(delenv 실동작 확인) |

**집계: 해소 8 · 부분 해소 2(④·⑬) · 미해소 0.**

---

## 6. Phase 종료 조건 대비 현황

> **판정하지 않는다.** 사실 대조만 하고 착수 차단 여부는 리더에게 넘긴다.

| 종료 조건 | 이 배치가 채우는 칸 | 남는 것 |
|---|---|---|
| **§6 X2** (본류 회귀 + CI 경로 명시) | 표 **9→10자리**, 면제형 → 탐지형, 빈 표 통과 차단, 이름 유일성·결속. **호출선 10/10 음성 대조 두 겹** | **T18-1**(④ grep 결속) · **R18-3**(원장 9자리) |
| **§6 X3** (러너 신설) | zero-work 3종 차단, 계약 문장별 테스트 23건, 자기 도달 테스트 신설 | **T18-2**(CI 탐지기 도달) · **T18-3**(대조표 강제자 0) · **T18-4/T18-5**(문장 3개 미대조) · **R18-1**(실측 주장) · **R18-2**(잔여 선언 폭) |
| **§6 X1** (스냅샷 하한) | 중복 이름 실패(생성기·가드), 메시지 갈래 분리 | 없음 — 이 배치 범위에서 닫혔다 |
| **§6 규칙 6**(자기 검사) | 러너 자신의 CI 배선 0 을 테스트로 잼(신설) | 그 테스트의 도달이 3분의 1(**T18-2**) · **shellcheck 도달 0**(게이트 17 ⑮ 이월) |
| **§5 Phase 7 즉시중단** | **해당 없음** — 이 배치는 하네스 전용이고 사용자 데이터 경로에 닿지 않는다 | — |

---

## 7. 심각도별 집계

| 심각도 | 건수 | 항목 |
|---|---|---|
| **차단(①사건)** | **0** | — |
| **차단(②장치)** | **0** | — |
| **수정 필요** | **5** | T18-1(④ grep 결속) · T18-2(CI 탐지기 도달) · R18-1(명령 치환 실측 주장) · R18-2(잔여 선언 폭) · R18-3(원장 9자리) |
| **권고** | **6** | T18-3 · T18-4 · T18-5 · T18-6 · S18-2(=C18-2, 1건으로 집계) · R18-7 |
| **판정 필요** | **0** | — |
| **검토함 — 지적 없음** | **10** | P18-1 · P18-2 · P18-3 · S18-1 · K18-1 · C18-1 · R18-4 · R18-5 · R18-6 · R18-8 · R18-10 |
| **미검토(사유)** | **1** | R18-9 (bash 5.x 거동 — 이 환경에 bash 3.2 하나뿐. CI 가 빨강으로 드러낸다) |

### 7.1 마감별 정렬

| 마감 | 항목 |
|---|---|
| **Phase 3 착수 전** | T18-1 · T18-2 · R18-1 · R18-2 |
| **러너 실사용 전** | T18-3 · T18-4 · T18-5 · R18-7 |
| **리더의 게이트 18 후속 원장 기재** | R18-3 |
| **리더 판단(처방 선택)** | S18-2 |
| **이월(등재만)** | T18-6 · R18-9 · shellcheck 도달 0(게이트 17 ⑮) |

### 7.2 군집 사실 (판정 아님)

「수정 필요」 5건 중 **4건이 같은 종류**다 — **장치의 선언이 그 장치의 도달보다 넓다.**
T18-1(단언이라 적고 텍스트를 잼) · T18-2(0 선언인데 CI 3분의 1만 봄) ·
R18-1(실측이라 적었으나 절반 미재현) · R18-2(잔여 1종이라 적었으나 4종 이상).
**같은 배치가 다른 파일에서는 정확히 그 결함을 닫았다**(③ 면제 → AST 탐지, ⑤ 하한 → 정확 일치).
장치를 세우는 손과 그 장치의 도달을 재는 손이 아직 같은 회차 안에서 맞물리지 않는다.

---

## 8. 미실행 · 확인 불가

| 범위 | 사유 |
|---|---|
| **bash 5.x 에서의 zero-work 기제** | 이 환경에 bash 3.2 하나뿐(R18-9). CI 가 빨강으로 드러내므로 조용한 통과는 아니다 |
| **Kotlin 테스트**(`./gradlew :api:test` 등) | 이 배치의 Kotlin diff 0줄 — 대상 아님 |
| **골든셋**(`tests/golden`) | 프롬프트·스타일 규칙·LLM 설정 무변경 — 대상 아님 |
| **`--check` 가 `app/**` 를 실제로 다시 읽는지** | 이번에도 미수행(게이트 17 이월 그대로) |
| **shellcheck** | 저장소·CI 어디에도 없다(게이트 17 ⑮). `bash -n` 까지만 |
| **Flyway 잠금 음성 대조 · 구판 생성기 복원** | 이 배치 대상 아님 — 게이트 16·17 에 이어 **세 회차 연속 미수행** |
| **codex 관점** | **1회차이므로 정상.** 2회차(`18_gate17-fixes_cross.md`)에서 대조한다 |

---

## 9. 리더에게 (1회차 종결)

1. **산출물 경로** — `docs/migration/_workspace/reviews/18_gate17-fixes_migration-reviewer.md`(이 파일).
2. **교차 종합 재호출이 필요하다.** codex 산출물(`18_gate17-fixes_codex-reviewer.md`)이 나오면
   같은 어간으로 2회차를 불러 `18_gate17-fixes_cross.md` 를 만든다. 이 파일만으로 Phase 종료를
   보고하지 않았다.
3. **집계 — 차단 0 · 수정 필요 5 · 권고 6 · 검토함(지적 없음) 10 · 미검토 1.**
4. **게이트 17 판정 — 해소 8 · 부분 해소 2(④ 문구 결속 · ⑬ 셸 축) · 미해소 0.**
5. **음성 대조는 전건 독립 재현했다**(레인 보고 18/18 → 18/18 일치) **+ 자체 추가 12종**
   (러너 flag 5 · ROOTS 3 · ⑤b 1 · functrace 대조 3). 표본 추출은 하지 않았다.
6. **저장소 무변조** — `cp` 복원 0, 전건 `git checkout` + sha256 대조, 일회용 worktree 는 리뷰 종료 시
   제거했다. 원장은 읽기만 했다.
7. **자기 승인 금지 준수** — Phase 종료·착수 차단 여부를 판정하지 않았다. 심각도와 마감만 붙였다.
