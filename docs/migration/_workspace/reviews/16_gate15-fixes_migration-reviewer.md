# 게이트 16 · 1단계 Claude 독립 리뷰 — `16_gate15-fixes`

작성 2026-08-18 · `migration-reviewer` **1회차(독립 리뷰 전용)** · 대상 `614afed..1cb7bdf` (6 커밋)

> **codex 산출물을 보지도 참조하지도 않고 작성했다.** 1회차 규약대로 교차 대조표를 만들지 않는다.
> codex 부재는 이 회차의 정상 상태이므로 실패로 기록하지 않는다. 교차 종합은 2차 재호출에서 한다.
> **이 산출물만으로 Phase 종료 조건 충족을 보고하지 않는다** — 정본은 `16_gate15-fixes_cross.md`다.

> **판정 기준.** 이 배치는 게이트 15 교차 종합(`reviews/15_phase3-preflight_cross.md`)의 Phase 3 차단
> 항목 X1·X2·X3·X5·X6·X11을 닫는 수정이다. 따라서 각 수정에 대해 **"그 결함을 닫았는가"**를 먼저
> 묻고, 그다음 **"닫으면서 새로 연 것이 있는가"**를 묻는다. 두 물음의 답이 갈리는 자리가 이 리뷰의
> 핵심 산출이다.

---

## 1. 리뷰 범위와 참조 절

### 1.1 대상 커밋

| 커밋 | 내용 | 닫으려는 게이트 15 항목 |
|---|---|---|
| `65a7eb6` | `scripts/run_gate.sh` 신설 + SKILL.md 규칙 5 규약 2줄 | **X3** (차단②) |
| `526bfeb` | test-plan X-F11~13 + preflight §5 전파 | **X5**, **X6**(전파 축) |
| `f9d78e0` | `MigrationStatementTracer` + TOCTOU 재현 + F3 반대 단언 정합화 + `MIGRATION_LOCK_KEY` internal | **X11**, **X6**(Kotlin 축) |
| `04ced00` | 본류 회귀 4건 + `PROMPT_CASE_FLOOR` + 파생 키 무조건 재생성 + 가드 테스트 + CI 스텝 2 | **X1**, **X2** |
| `4cba492` | CI 스텝 1 (스냅샷 가드 경로 명시) | X2 후속(리더 판정) |
| `1cb7bdf` | `build.gradle.kts` ktlint 6건 수동 해소 | — (곁다리) |

### 1.2 참조한 정본

- 계획 문서 §2.2(엔드포인트 계약)·§2.3(보안 불변식)·§3.1·§3.2(모듈 경계·기술 고정)·§4.2(Flyway 인수)·§4.5·§4.6(parity 위험)·§5 Phase 3·§6(게이트)
- `contracts/easy-doc-v1.yaml` **:325-431**(`x-request-field-constraints` 전문) · **:1500-1528**(`ValidationFailed`) · **:1660-1690**·**:1720-1732**·**:1820-1828**·**:1950-1962**(다섯 필드 스키마) — 요약본이 아니라 원문을 읽었다
- `reviews/15_phase3-preflight_cross.md` §2 전제 ③·④ · §3 표 전체 · §4 충돌 X2·X13 · §5.2
- `CLAUDE.md` 규칙 4(장치 분류 · 범위는 근거를 넘지 않는다) · `kotlin-migration` SKILL.md 규칙 5·6

### 1.3 범위 드리프트 고지

리뷰 중 저장소 HEAD가 `f73879b` → `c932b4f`로 이동했다(다른 레인). 실측으로 확인: **리뷰 범위 6커밋이
건드린 파일 전부가 `1cb7bdf..HEAD` 에서 무변경**(`git diff --stat` 0줄)이므로 이 문서의 인용 행 번호는
유효하다. `00_progress.md`는 리뷰 중에도 다른 레인이 편집 중(관찰 23:00 시점 ` M`)이라 **읽기만 했고
이 배치의 판정 근거로 쓰지 않았다.**

---

## 2. 다섯 축별 지적

심각도: **차단(①사건/②장치)** / **수정 필요** / **권고** / **판정 필요**.
근거 행 번호는 `1cb7bdf` 기준이며 **작성 후 전수 재대조했다**(§5.3).

### 축 1 — 계약 준수

#### C-A · X6(b) 해소 — 지적 없음

계약이 금지한 조합(`SignupRequest.email` + `@NotBlank`)을 정상이라 단언하던 자리가 닫혔다.
프로브 필드가 계약에 없는 `probe`로, 페이로드가 `ProbeValidationPayload`로 바뀌었다
(`ErrorProbeController.kt:151-159`·`:164-168`). 계약 `:386-411`(`fields` 다섯)·`:1508-1518`
(`@Size`/`@NotBlank` 구현이 계약 위반이라는 명문)과 대조해 **정합하다.**

`ProbeValidationPayload`의 kdoc이 이름 선택의 이유("이름이 계약 필드와 겹치면 그 필드가 스키마
층에서 검증돼도 된다는 뜻으로 읽힌다", `:164-167`)를 남긴 것도 다음 사람이 되돌리는 것을 막는다.

#### C-B · 계약 근거 없는 값 고정 — **권고**

`FrameworkErrorContractTest.kt:159`가 새로 `jsonPath("$.detail[0].type") { value("missing") }`를 건다.
이 값의 출처는 계약이 아니라 구현이다 — `GlobalExceptionHandler.kt:392`의
`"NotNull", "NotEmpty", "NotBlank", "Required" -> "missing"` 매핑이다. 계약
`ValidationFailed`(`:1519-1528`)는 `type` 어휘를 **예시 둘**(`missing`·`greater_than_equal`)로만
보이고 조항으로 고정하지 않는다.

문제의 방향은 F3 이후에 뚜렷해진다. 다섯 필드에 `@NotBlank`가 금지됐고 Bean Validation의 정당한
잔여 소비자는 `limit`·`offset`(정수 — `NotBlank`가 적용되지 않는다)뿐이다. 즉 이 배치는 **운영에서
도달할 소비자가 없는 매핑**을, 계약에 없는 합성 필드(`probe`)로, 계약 테스트에서 고정했다. 다음
사람은 이것을 계약 요구로 읽는다. `isArray()` 추가(`:154`)는 반대로 계약 `:1510-1511`에 직접
근거가 있어 정당하다.

#### C-C · 「근거 표기 규약」이 자기가 다스리는 세 행에 도달하지 않는다 — **수정 필요** (마감: Phase 3 첫 계약 테스트 작성 전)

`526bfeb`가 test-plan `:179-185`에 규약을 신설했다.

> "**계약 문구·상한 값·`detail` 전문은 여기에 옮겨 적지 않는다** — 게이트 15 별건 1이 실측한
> 그대로, 손으로 옮긴 계약 요약은 원본과 갈린다."

같은 커밋의 preflight `:143-145`도 같은 선언을 한다("상한이 얼마인지 … 이 문서에 전사하지 않는다").
그런데 **그 선언이 다스리는 바로 그 행들이 값을 전사한다.**

| 자리 | 전사된 것 | 계약 원본 |
|---|---|---|
| test-plan `:137` (X-F11) | "8자 미만", "정확히 8자" | `:394` `limit: 8` |
| test-plan `:139` (X-F13) | "정규화 후 255자 초과", "정확히 255자" | `:389` `limit: 255` |
| test-plan `:139` 「고정하는 계약 조항」 열 | `detail` 주석 **전문** — "길이 초과와 형식 오류가 같은 문구다(입력값 비반향)" | `:391` 의 주석 |
| test-plan `:82` (엔드포인트 표) | "이메일 255 초과", "비밀번호 8자 미만" | 같음 |
| preflight `:166`·`:168` | "8자 미만", "8자 정확히", "255 초과" | 같음 |

「고정하는 계약 조항」 열만 가리키는 좁은 독해를 택해도 X-F13의 그 열이 계약 주석을 전문 전사하므로
빠져나가지 못한다. **범위 선언형 장치가 자기 선언 범위에 도달하지 못한 상태**이고(CLAUDE.md 규칙 4),
하필 그 규약을 만든 계기가 「별건 1 — 손으로 옮긴 계약 요약이 원본과 갈렸다」다.

실질 위험은 지금 낮다(8·255는 안정적이고 키 경로·행 번호가 함께 있다). 심각도를 수정 필요로 올린
이유는 값이 틀려서가 아니라 **장치가 빈 채로 통과했기 때문**이다 — 이 하네스가 반복해 고쳐 온 형태다.

#### C-D · Phase 배치 표의 이중 등재 — **권고**

test-plan `:293`(Phase 3)이 X-F11·X-F12·X-F13을 명시하는데 `:294`(Phase 4)는 여전히 `X-F*`
와일드카드다. `:311-318` 단서는 **X-F9만** 해소하고, X-F10(기존)·X-F11·X-F12·X-F13이 두 Phase에
동시에 실리는 것은 다루지 않는다. X-F10은 이 배치 이전부터 있던 모호함이나 이 배치가 세 행을 더해
확대했다.

#### C-E · X13 종속 표기 — 검토함, 지적 없음

X-F12가 게이트 15 X13(리더 판정 대기)에 종속된다는 표기가 **세 자리**에 있다.

- test-plan `:138` — "⚠️ 이 행의 기대값은 **게이트 15 X13(리더 판정 대기)에 종속**된다 … **바뀌기
  전까지는 현행 조항이 기준이다**"
- test-plan `:367` — §6 통보표 리더 행
- preflight `:174-179` — 「미결 두 자리」 2번

그리고 **조항을 바꾸지 않고 검증만 채웠다**(계약 파일 무변경 — 이 배치의 diff에 `contracts/` 없음).
미결 판정을 검증 신설로 앞질러 확정하지 않았다. 이 축은 정확히 처리됐다.

#### C-F · 인용 행 번호 전수 대조 — 검토함, 지적 없음

`526bfeb`·preflight가 적은 계약 행 번호를 **전건 실측 대조**했다:
`:332-431`·`:354-375`·`:377-384`·`:386-411`·`:387-391`·`:392-396`·`:395`·`:1508-1514`·`:1510-1511`·
`:1516-1518`·`:1663-1670`·`:1665`·`:1674`·`:1726`·`:1823`·`:1956`. **전부 일치.** 키 경로를 함께 적어
파일이 움직여도 복구 가능하게 한 규약도 지켜졌다.

---

### 축 2 — parity 위험

#### P-A · X1 해소 확인 — 지적 없음 (실측 3건)

**⑴ 파생 키 무조건 재생성이 실물 스냅샷 diff 0을 유지한다.**

```
uv run python .claude/skills/migration-safety-gate/scripts/dump_python_snapshots.py --check
  → EXIT=0
  → [일치] python-prompt-snapshot.json — 키 20개
  → [일치] python-style-rules-snapshot.json — 키 13개
```

`if derived in previous:` 제거(`dump_python_snapshots.py` `build_style_snapshot`)가 실물에 부작용을
내지 않는다.

**⑵ 하한이 현재 완전하다.** 실물 스냅샷과 대조:

| 구획 | 스냅샷 | `PROMPT_CASE_FLOOR` | floor 밖 케이스 | floor에만 |
|---|---|---|---|---|
| `system_prompts` | 6 | 6 | 0 | 0 |
| `user_prompts` | 5 | 5 | 0 | 0 |
| `repair_prompts` | 5 | 5 | 0 | 0 |
| `postprocess` | 29 | 29 | 0 | 0 |
| **합계** | **45** | **45** | **0** | **0** |

P-A가 실측한 45케이스와 일치한다.

**⑶ "큐레이션 입력을 재계산할 원천이 없다"는 전제가 참이다.** 케이스 이름 6종
(`double_preamble_both_removed`·`fence_close_trailing_spaces`·`keep_signal_without_start`·
`compound_interior_only`·`gloss_sort_order`·`tag_injection_attempt`)을 `app/`·`tests/`에서 grep한
결과 **새 가드 테스트 1건 외 0건**이다. 따라서 **열거의 정본을 생성기 안 고정 상수로 둔 결정은
정당하다** — `STYLE_CONSTANTS`와 같은 층이라는 설명도 맞다. 재계산할 상류가 실제로 없는데 상류를
요구하는 것은 근거 없는 요구다.

#### P-B · 새 정본에 완전성 검사가 없다 — **수정 필요** (마감: **`app/**` 삭제 전**)

X1의 수정은 **"previous 스냅샷을 정본으로 믿음" → "`PROMPT_CASE_FLOOR`를 정본으로 믿음"** 이다.
새 정본은 옛 정본보다 낫지만 **자기 완전성을 검사받지 않는다.** 두 경로가 열려 있다.

**⑴ 빈 선언이 통과한다 (실측).** 한 구획의 튜플을 `()`로 비우면 그 구획의 축소가 그대로 통과한다.
tmp 사본만 쓰는 in-memory 실험(실물 무접촉):

```
postprocess 하한 = ()  ·  스냅샷 postprocess 29 → 1
  → [일치] python-prompt-snapshot.json — 키 20개
  → --check exit = 0        ← X1 이 되살아난다
```

`_floor_checked_cases`의 `missing`이 빈 리스트가 되어 그대로 통과한다. CLAUDE.md 규칙 4의
**"범위 선언형은 빈 선언에서 통과하면 안 된다"**에 정면으로 걸린다.

**⑵ 추가 미등재를 아무도 탐지하지 않는다.** 생성기 주석이 스스로 적었다 — "적는 것을 잊으면 그
케이스는 하한의 보호 밖이다(추가 자체는 막지 않는다)". 이쪽은 악의가 아니라 **망각**이 발동 조건이라
⑴보다 현실적이다. 등재되지 않은 케이스는 나중에 조용히 지워도 통과한다.

가드 테스트가 이 축을 **한 걸음 앞에서 멈췄다.** `tests/test_python_snapshot_guard.py:161-175`
(`test_하한_그룹이_스냅샷_그룹과_같다`)는 **그룹 키까지만** 대조한다. 같은 함수에서
`set(floor[sec]) == {c["name"] for c in doc[sec]}` 를 함께 보면 ⑴·⑵가 동시에 닫힌다 — 의도적 삭제는
두 곳을 같은 커밋에서 고치면 여전히 통과하므로 설계 의도를 해치지 않는다.

마감을 `app/**` 삭제 전으로 둔 이유: X1의 마감이 그것이고, 이 잔여가 여는 것이 **되돌릴 수 없는 창**
(큐레이션 45건의 영구 손실 — CLAUDE.md "재개발의 유일한 영구 손실 위험")이기 때문이다.

#### P-C · 스냅샷의 두 번째 소비자 — **권고**

`.claude/skills/python-kotlin-parity/scripts/dump_parity_fixtures.py:1423`이 프롬프트 스냅샷을
"프롬프트 전문의 정본"으로 참조한다. 스냅샷이 조용히 줄면 그 소비자의 판정 범위도 함께 준다 —
P-B를 닫을 근거를 하나 더한다.

**같은 종류의 두 번째 자리는 없다** — 실측 확인: `dump_parity_fixtures.py`는 스냅샷을 **케이스
원장으로 재사용하지 않는다.** X1과 같은 구조가 저장소에 하나 더 있지는 않다.

---

### 축 3 — 보안 불변식

#### S-A · 이 배치가 닿는 자리 — 검토함, 지적 없음

`scan_privacy_invariants.py` **exit 0**(BLOCK 후보 0건, 검사 190파일). 신규 코드 3종을 개별 점검:

| 신규 | 개인정보 노출 경로 | 판정 |
|---|---|---|
| `MigrationStatementTracer` | SQL 문면을 `ExecutedStatement.sql`에 보관. **테스트 소스셋 전용**이고, 실패 메시지를 만드는 `describeWindows()`(`:93-97`)는 SQL을 **아예 싣지 않는다**(스레드·나노초·문장 수만) | 지적 없음 |
| `tests/test_python_snapshot_guard.py` | tmp 사본만 쓰고 `--write`를 부르지 않는다. 파일 머리 `:12-19`가 절대 규칙으로 명문화 | 지적 없음 |
| `FlywayBaselineGuardTest` 신규 2건 | 합성 스키마·합성 컬럼명(`nickname`)만 쓴다 | 지적 없음 |

fixture·테스트에 주민등록번호·카드번호 패턴 없음. `codex-rerun.log` 보존본은 `c932b4f`라 지시대로
범위 밖.

#### S-B · 러너가 실행 명령 전문을 stdout에 찍는다 — **권고**

`run_gate.sh:36` — `echo "[run_gate] cmd: ${cmd}"`. 현재 용례에 비밀값은 없다. 다만 이 러너는 임의
셸 문자열을 받도록 설계됐고, "게이트 명령에 비밀값을 인라인하지 않는다"는 전제가 스크립트에도
SKILL.md에도 적혀 있지 않다. 한 줄 주석이면 닫힌다.

---

### 축 4 — Kotlin/Spring 관용성

#### K-A · 탐침이 실제로 가로챈다 — 지적 없음 (일회용 worktree 독립 재현)

본 저장소 무변조. `git worktree add … 1cb7bdf --detach` → 실험 → `worktree remove --force`.

| 회차 | 변조 | 결과 |
|---|---|---|
| A (기준선) | 없음 | **10 tests / 0 failures**, exit 0 |
| B (잠금 제거) | `withAdvisoryLock(connection) {` → `run {` | **10 / 2 failures**, exit 1 — **3회 연속 동일**(flaky 아님) |
| C (추적 사망) | `MigrationStatementTracer.record()` 본문 비움 | **10 / 1 failure**, exit 1 |

회차 B의 실패 2건은 정확히 이 배치가 신설·강화한 둘이다 — `동시 기동이 직렬화된다`
(DisplayName 「동시 기동의 임계 구간이 서로 겹치지 않는다」)와 `판정과 기록 사이에 스키마 변경이
끼어들지 못한다`(「판정 직전에 바뀐 스키마는 baseline 되지 않는다 — TOCTOU」). **다른 8건은 초록으로
남아 귀속이 분명하다.**

회차 C의 실패 메시지:

```
java.lang.AssertionError: [기동 3 개가 전부 스키마 질의를 남겨야 한다:]
Expected size: 3 but was: 0 in: []
  at ...FlywayBaselineGuardTest.동시 기동이 직렬화된다(FlywayBaselineGuardTest.kt:218)
```

→ **창 개수 선단언(`FlywayBaselineGuardTest.kt:216-218`)이 "추적이 죽었는데 겹침 0으로 조용히
통과"를 실제로 막는다.** 커밋의 주장이 검증됐다. `CyclicBarrier` 출발선(`:196`)도 실재한다.

이 항목이 이 배치에서 가장 잘된 자리다 — **대리 지표("예외가 안 났음")를 버리고 잠금이 지키기로
한 것 자체를 재는 쪽으로 옮겼고, 그 전환이 음성 대조로 증명된다.**

#### K-B · `MIGRATION_LOCK_KEY` 가시성 확대의 범위가 근거보다 넓다 — **권고**

`FlywayBaselineGuard.kt:191-196` kdoc이 못박는다 — "`private` 이 아니라 `internal` 인 **이유는
하나뿐이다** — 테스트가 같은 잠금을 잡아야 한다."

그런데 `internal companion object`(`:197`)는 동반 객체를 통째로 연다. 실제로 모듈 가시성이 된 것은
넷이다: `MIGRATION_LOCK_KEY`(`:204`) · `EXPECTED_ALEMBIC_HEAD`(`:207`) ·
`ALEMBIC_TABLE_EXISTS_SQL`(`:210-211`) · `ALEMBIC_HEAD_SQL`(`:214`).
**선언한 근거는 상수 하나, 실제 도달은 넷이다.** 파일 최상위 `internal const val` 하나로 좁힐 수 있다.

부작용 자체는 작다(같은 Gradle 모듈 안). 지적하는 것은 **kdoc이 참이 아니게 됐다**는 점이다 —
다음 사람은 "하나뿐"이라는 문장을 읽고 나머지 셋의 노출을 의도로 오인한다.

#### K-C · 탐침 선언이 실제 도달보다 넓다 — **권고**

`MigrationStatementTracer.kt:132-133`이 선언한다 — "가드가 직접 여는 커넥션과 Flyway 가 내부에서
여는 커넥션이 **모두** 이 자리를 지난다." 구멍이 둘 있다.

1. **`Statement.getConnection()`·`DatabaseMetaData.getConnection()`이 반환하는 커넥션은 원본이다.**
   `traceStatements`(`:162-178`)는 `DataSource.getConnection()` 경로만 프록시로 감싸므로, 그 원본에서
   만든 문장은 추적 밖이다.
2. **`prepareCall`이 반환하는 `CallableStatement`가 `is PreparedStatement` 갈래(`:174`)에 걸려
   `PreparedStatement` 계약으로만 프록시된다.** 호출부가 `CallableStatement`로 캐스팅하면
   `ClassCastException`이 난다.

현재 Flyway·가드 경로에서 둘 다 발생하지 않는다(회차 A가 초록). 지적은 **"모두"라는 낱말이 근거보다
넓다**는 것뿐이다 — 이 하네스가 추적하는 바로 그 형태다.

#### K-D · `MIN_CRITICAL_STATEMENTS = 6`은 전체 사망만 가른다 — **권고**

`FlywayBaselineGuardTest.kt:42`. 주석(`:35-41`)이 목적을 정직하게 적었다 — "목적은 '추적이 통째로
죽었는가'를 가르는 것뿐이다". 부분 사망(예: `createStatement` 경로만 빠지는 경우)은 6을 넘겨 통과할
수 있다. 실제 방어력은 `hasSize(CONCURRENT_STARTS)` 쪽에서 나오고 회차 C가 그것을 확인했다.
주석이 정직하므로 심각도는 낮다.

#### K-E · TOCTOU 음성 대조의 시간 의존 — **권고**

`INTRUDER_GRACE_MILLIS = 750L`(`:52`). 비대칭이 정확하다 — **양성 단언에는 영향이 없고**(잠금이
있으면 가드가 액터를 무조건 기다린다. `lockHeld.await()`(`:272`)가 순서까지 결정론적으로 만든다)
**음성 대조의 탐지력에만** 걸린다. 느린 러너에서 가드 왕복이 750ms를 넘기면 음성 대조가 조용히 힘을
잃는데, 그 사실이 드러날 자리가 없다. 주석(`:44-51`)이 이 비대칭을 이미 적었다는 점은 인정한다.

#### K-F · 손대지 않은 Flyway 지적 — 상태 갱신

| 항목 | 근거 | 이 배치 | 상태 |
|---|---|---|---|
| X8 빈 `alembic_version` 통과 | `FlywayBaselineGuard.kt:175` `actual.isEmpty()` | 무변경 | **미해소** |
| X9 풀 크기 1 자기 교착 | `:61-71` | 무변경 | **미해소** |
| X10 비협조 DDL TOCTOU | `:57-60` 주석의 범위 선언 | 재현 테스트 신설 | **부분 해소** — 같은 잠금을 존중하는 액터 축은 닫혔고, 테스트 kdoc `:251-253`이 "advisory lock 은 협조적이라 잠금을 무시하는 임의의 DDL 은 막지 못한다"고 한계를 명시했다. 비협조 DDL 축은 여전히 열림 |
| X12 대기 상한·진단 로그 없음 | `:93-109` | 무변경 | **미해소** |
| X16 `EXPECTED_ALEMBIC_HEAD` 지시 대상이 삭제 대상 안 | `:207` | 무변경 | **미해소** |
| X17 "셋이 동시에" 서술 vs worker 설정 | `:54-55` | 무변경 | **미해소** |

전부 cross §5.2 (나) "Phase 3 안에서 닫으면 되는 항목"이므로 **마감 위반은 아니다.** 상태만 갱신한다.

---

### 축 5 — 테스트 적정성

#### T-A · X11 음성 대조 독립 재현 — 지적 없음

K-A 참조. 커밋 메시지의 "4/4 빨강"은 **4회 반복 중 4회**를 뜻하고, 내 재현은 3회 반복 3회 동일
(테스트 2건 빨강). 주장과 일치한다.

#### T-B · X2 배선 축 음성 대조 독립 재현 — 지적 없음

일회용 worktree(`1cb7bdf --detach`), 되돌림은 전부 `git checkout --` + `git status --short` 확인.

| 변조 | 결과 |
|---|---|
| 없음 (대조군) | `tests/test_parity_ci_gate.py` **25 passed**, exit 0 |
| `compare_parity.py:1984` `bad_runtime = runtime_problem(…)` 무력화 | **2 failed** — `test_본류가_위조_runtime을_막는다` · `test_본류가_runtime_미선언을_막는다`. 기존 helper 테스트 **전건 초록** |
| `compare_parity.py:2282` `problems += provenance_problems(pair)` 무력화 | **1 failed** — `test_본류가_축소_fixture를_막는다`. 기존 helper 테스트 **전건 초록** |

커밋 주장이 그대로 재현됐다. **X2의 배선 축은 닫혔다** — codex C15-6이 지적한 "그 한 줄을 지우면
여섯 테스트가 전부 초록"이 이제 성립하지 않는다.

> **절차 부기 (다음에 이 음성 대조를 다시 돌릴 사람에게).** `:2282` 한 줄을 **주석 처리**하면
> `if pair.domain in BUILDERS:` 블록이 비어 `SyntaxError`가 나고 25건 중 15건이 수집 실패로
> 무너진다. 그것은 배선 증명이 아니라 임포트 붕괴다. `problems += []` 처럼 **문법을 유지한 채**
> 무력화해야 위 결과가 나온다.

#### T-C · X1 음성 대조 독립 재현 — 지적 없음

구판 생성기(`04ced00^`)로 되돌린 뒤 `uv run pytest tests/test_python_snapshot_guard.py`:
**4 failed / 1 passed**, exit 1.

- 실패: `test_케이스_축소_스냅샷은_check가_막는다` · `test_케이스_한_건_삭제도_막는다` ·
  `test_파생_키_단독_삭제를_check가_막는다`(탐지 3건) + `test_하한_그룹이_스냅샷_그룹과_같다`(1건)
- 통과: `test_손대지_않은_스냅샷은_check를_통과한다`(대조군만)

커밋의 "탐지 3건 + 하한 그룹 대조 1건 빨강, 대조군만 초록"과 **정확히 일치한다.**

#### T-D · 본류 회귀 4건의 설계 — **권고**

**설계 자체는 근거가 있다.** 합성 트리는 도메인 하나뿐이라 도메인 누락만으로도 exit 1이 나오므로
종료 코드로는 귀속이 안 된다. 그래서 문구로 단언하고, 대조군
(`test_본류_대조군은_위조_문구_없이_실패한다`)이 "이 문구가 조작 **때문에** 나왔다"를 고정한다.
T-B의 실측이 그 귀속을 확인했다. 약점은 둘이다.

**⑴ 문구가 두 벌이다.** 기대 문구가 비교기 소스와 테스트에 각각 있어, 문면을 고치면 빨개진다. 그때
사람이 "테스트를 느슨하게" 고치는 압력이 생긴다 — 이 저장소가 fixture↔비교기 드리프트에서 이미
겪은 모양이다. 다만 문구를 비교기에서 상수로 뽑아 공유하면 **검사 기준이 검사 대상 자신에게서
나오는** 반대 결함이 되므로, 지금 선택이 둘 중 나은 쪽이다. 기록만 남긴다.

**⑵ 같은 종류의 다른 자리 6개가 그대로다.** `_mainline_tree`가 케이스 하한을 합성 트리 자신에서 써
넣어(축 분리 목적 — 주석에 명시) 케이스 하한 축을 의도적으로 비껴간다. 그 결과 **본류 호출부가 단
한 곳뿐인 게이트 helper 중 이번에 회귀를 얻은 것은 둘뿐이다.**

| helper | 유일 본류 호출부 | 본류 회귀 |
|---|---|---|
| `runtime_problem` | `compare_parity.py:1984` | **있음(신설)** |
| `provenance_problems` | `:2282` | **있음(신설)** |
| `spec_shape_problems` | `:1207` | 없음 |
| `case_floor_problems` | `:2269` | 없음 |
| `full_gate_floor_problems` | `:2274` | 없음 |
| `stale_ledger_problems` | `:2329` | 없음 |
| `ledger_write_problems` | `:2344` | 없음 |
| `case_floor_additions` | `:2409` | 없음 |

**이것은 "다른 결함을 닫고 그 결함은 열어 둔" 형태가 아니다** — X2가 지목한 두 자리는 실측으로
닫혔다. 그러나 **결함의 구조는 여섯 자리에 그대로 있다.** cross §6.2가 이월한 "열거 유지 vs 구조
유도" 판정과 같은 자리이므로 리더에게 함께 올린다.

#### T-E · `run_gate.sh`의 "빈 호출은 실패다" 선언이 빈 문자열에 도달하지 않는다 — **차단②** (마감: **이 러너를 게이트 판정 근거로 쓰기 전 = 즉시**)

스크립트 머리 `:16`이 못박는다 — "빈 호출은 통과가 아니라 실패다(exit 2)."
실측(본 저장소, 파일 무변조 — 스크립트 호출만):

```
run_gate.sh ""       → [run_gate] cmd:      / [run_gate] exit: 0  / EXIT=0
run_gate.sh "   "    → [run_gate] cmd:      / [run_gate] exit: 0  / EXIT=0
run_gate.sh $UNSET   → [run_gate] 오류: 실행할 게이트 명령이 없다  / EXIT=2
run_gate.sh true     → EXIT=0   (대조군)
```

원인은 `:30`의 가드가 `[ "$#" -eq 0 ]`만 보고 `:35`의 `cmd="$*"`가 공백인지는 보지 않는 것이다.
따옴표 없는 확장은 `$#`이 0이 되어 잡히지만 **따옴표를 씌운 빈 변수·빈 문자열은 그대로 통과한다.**

즉 **아무 명령도 실행하지 않고 `[run_gate] exit: 0`을 찍는다.** 심각도 척도가 차단②의 예로 든
**"검사 대상 0건인데 성공으로 끝나는 스크립트"** 그 자체다. 그리고 이 장치의 존재 이유가 X3
—「대리 지표가 성공 코드가 되는 통로」— 를 닫는 것이었다.

**리더 계산에 남길 완화 요소**: 발동 조건이 좁다(호출자가 빈/공백 문자열을 넘겨야 한다). 다만
`run_gate.sh "$CMD"` 형태로 조립된 명령을 태우는 것이 이 러너의 상정 용법이므로 비현실적이지 않다.
**심각도를 스스로 낮추지 않는다** — 착수 차단 여부의 판정은 리더 몫이다.

#### T-F · 인자 재조립에서 인용 경계가 소실된다 — **수정 필요** (마감: T-E와 같음)

`:35-37`이 인자를 `cmd="$*"`로 합친 뒤 `bash -c`로 **재파싱**한다. 실측:

```
run_gate.sh uv run pytest -k 'a or b' --collect-only
  → [run_gate] cmd: uv run pytest -k a or b --collect-only      ← 따옴표 소실
  → ERROR: file or directory not found: or        EXIT=4
run_gate.sh echo 'a  b'   → 출력 `a b`             ← 공백 2 → 1, 한 인자가 둘로 갈림
```

스크립트 머리의 사용례(`:24-25`)는 이 제약을 적지 않는다. 오히려 **첫 예시가 인자를 그대로
나열하는 형태**(`run_gate.sh uv run pytest tests/x`)라 "argv를 그대로 넘겨도 된다"로 읽힌다.

관측된 사례에서는 오작동이 비-0으로 드러나 조용한 통과는 아니었다. 그래도 수정 필요로 두는 이유는
**게이트 명령이 의도와 다른 명령으로 바뀌는 것 자체가 판정 근거를 무효화하기 때문**이다 — 러너가
막으려던 것과 같은 종류의 손상이다. 구조적으로는 `$# -eq 1`이면 셸 문자열로, 아니면 argv를 직접
실행하는 형태가 T-E와 함께 좁혀진다.

#### T-G · 러너 자신이 어떤 검사 대상에도 없다 — **수정 필요** (마감: T-E와 같음)

- 저장소에 셸 린트가 없다. `ci.yml`에 `shellcheck` 실행 0건(`:521`의 `# shellcheck disable` 주석
  1건이 전부다).
- 러너의 계약(빈 호출 → 2 / 파이프 안 실패 → 비-0 / 종료 코드 전파)을 고정하는 테스트가 없다.
- `run_gate` 참조는 저장소 전체에서 SKILL.md `:238` 과 `00_progress.md`(다른 레인 미커밋) 둘뿐이다.

`kotlin-migration` SKILL.md 규칙 6("판정하는 코드일수록 자기 자신을 검사 대상에 넣는다")의 정확한
대상인데 **도달 0**이다. **T-E는 3줄짜리 테스트로 잡혔을 결함이다** — 이것이 규칙 6이 존재하는 이유다.

---

## 3. 도달 범위 점검 결과

> 다섯 축을 가로지르는 필수 구획이므로 비워 두지 않는다. **"검토함 — 지적 없음"과 "미검토"는 다른
> 정보다.**

### R-1 새 CI 스텝 4개의 실재·경로 정확성 — **검토함, 지적 없음**

`ci.yml`을 YAML 파서로 읽어 잡·스텝·순서를 실측했다.

| 스텝 idx | 잡 | 명령 | 참조 파일 실재 |
|---|---|---|---|
| 7 | `quality` | `uv run python .claude/skills/migration-safety-gate/scripts/dump_python_snapshots.py --check` | ✔ (신규) |
| 9 | `quality` | `uv run pytest tests/test_harness_scope_reach.py` | ✔ (기존) |
| 10 | `quality` | `uv run pytest tests/test_privacy_scanner.py` | ✔ (기존) |
| 11 | `quality` | `uv run pytest tests/test_parity_ci_gate.py` | ✔ (신규) |
| 12 | `quality` | `uv run pytest tests/test_python_snapshot_guard.py` | ✔ (신규) |
| 13 | `quality` | `uv run pytest` (전체 수집) | — |

- **경로 오타 0건.** 특히 생성기의 실물 경로가 `.claude/skills/**migration-safety-gate**/scripts/`
  (`python-kotlin-parity`가 아니다)인데 CI 명령이 정확히 그것을 가리킨다.
- 다섯 전부 전체 `uv run pytest`(idx 13)보다 **앞**이다 — 경로 명시가 전체 수집에 삼켜지지 않는다.
- `quality` 잡에 둔 판단이 맞다: 생성기가 `app.easyread`를 import 하므로 필요한 것은 JDK가 아니라
  `uv sync --locked` 환경이다.
- 주석이 각 스텝의 이유를 실측과 함께 적었고(`ci.yml:105-124`·`:150-166`), "이 스텝 자체의 삭제는
  이 스텝이 막지 못한다 — 그 diff 가 리뷰에 올라가는 것이 최종 방어선이다"라고 **자기 한계를 먼저
  적었다.**

### R-2 `run_gate.sh` 자기 도달 표기의 정직성 — **검토함, 지적 없음**

머리 주석 `:18-21`이 셋을 명시한다.

1. "자기 도달(정직하게): **local**"
2. "**CI 배선 0**"
3. "이 러너는 경유하지 않은 파이프 명령을 **탐지하지 못한다** — 즉 규약 자체의 강제자가 아니라,
   경유한 명령의 종료 코드를 정직하게 만드는 장치까지만이다"

실측 대조: `ci.yml`에 `run_gate` **0건** → 선언과 실제가 일치한다. SKILL.md 규칙 5도 "러너의 자기
도달은 스크립트 머리 주석이 **정본이다** — 여기 옮겨 적지 않는다"로 이중 기재를 피했다(레인 간 규약
드리프트를 만드는 그 구조를 안 만들었다). **이 배치에서 가장 정확하게 처리된 자리다.**

주의: 이 정직성은 **T-E·T-F·T-G를 상쇄하지 않는다.** 도달 범위를 정직하게 적은 것과 장치가 자기
계약을 지키는 것은 다른 축이다.

### R-3 이 배치가 새로 쓴 "전역"·"모든"·"항상" — **1건 발견**

| 표현 | 자리 | 판정 |
|---|---|---|
| "…커넥션이 **모두** 이 자리를 지난다" | `MigrationStatementTracer.kt:132-133` | **넓다** — K-C의 구멍 둘이 그 밖 |
| "파생 표는 **무조건** 다시 만든다" | `dump_python_snapshots.py` `build_style_snapshot` 주석 | **참** — 범위가 근거와 같다(파생 키 1개 한정). 실측으로 확인 |
| "면제 목록·무시 패턴 없음" | `run_gate.sh:15` | **참** — 코드에 없다 |
| "실행된 **모든** SQL 의 (스레드, 시작, 종료)을 남긴다" | `MigrationStatementTracer.kt:35` | 위 첫 행과 같은 자리 |

### R-4 은폐형 확대 — **검토함, 지적 없음**

이 배치에 신설된 무시 패턴·전역 예외·억제 조항·면제 목록 **0건**. `.gitignore`·`pyproject`의 억제
설정 무변경. `1cb7bdf`의 ktlint 해소도 `@Suppress` 부착이나 `ktlintFormat` 자동 적용이 아니라
**수동 수정**이고, 커밋 메시지가 자동 실행을 쓰지 않은 이유("`//:` 표기의 의도를 도구가 판단할 수
없다")를 적었다. CLAUDE.md 규칙 4의 "은폐형은 넓히지 않는다 — 탐지형으로 갈아탄다"를 지켰다.

### R-5 대리 지표 — **1건 해소, 1건 신규**

- **해소**: X11. "예외가 안 났음"(`failures.isEmpty()`)이라는 대리 지표를 버리고 **잠금이 지키기로
  한 것 자체**(임계 구간 겹침)를 재는 쪽으로 옮겼다. 음성 대조로 증명됨(K-A).
- **신규**: T-E. 대리 지표(종료 코드)를 정직하게 만들려던 장치가 **"0건 실행 → exit 0"** 이라는 같은
  종류의 구멍을 남겼다.

### R-6 자기 자신을 검사 대상에 넣었는가 — **1건 미달**

| 새 장치 | 타입 검사 | 린트 | 테스트 | 판정 |
|---|---|---|---|---|
| `dump_python_snapshots.py` 변경분 | `uv run mypy . .claude` 범위 안 | ruff | `tests/test_python_snapshot_guard.py`(신규) + CI 경로 명시 | ✔ |
| `tests/test_parity_ci_gate.py` 신규 4건 | 범위 안 | ruff | CI 경로 명시 | ✔ |
| Kotlin 신규 2파일 | 컴파일 | ktlint·detekt | `:infrastructure:test` | ✔ |
| **`run_gate.sh`** | — | **없음(셸 린트 0)** | **없음** | ✘ **T-G** |

### R-7 `1cb7bdf` 주석 내용 보존 — **검토함, 지적 없음 (실측)**

`build.gradle.kts` 전체에서 `//`·`//:` 기호를 걷어낸 **비어 있지 않은 주석 본문 71줄이 변경 전후
전건 동일**(python `difflib` 대조, 차이 0). 커밋 메시지의 "78줄"은 내 집계 기준(비어 있지 않은 줄만)과
다르나 **결론은 같다 — 내용 손실 0.**

`//:` 4건 제거는 "저장소 전체에서 이 네 줄에만 있다(grep 4건)"는 실측 근거와 함께 기록됐고, 표식이
하던 경계 구분 역할을 **빈 줄 하나로 옮겨** 기능도 보존했다. `:299`의 체인 개행은 순수 포매팅이다.

### R-8 검사 기준이 검사 대상 자신에게서 나오는가 — **1건 부분 해당, 지적 없음**

`_mainline_tree`가 케이스 하한을 합성 트리 자신에서 생성한다(T-D ⑵). 다만 **판정 근거 자체는 자기
참조가 아니다** — 축소를 잡는 것은 `provenance_problems`이고 그것은 `BUILDERS`에서 정본을
재생성해 대조한다. 축 분리 목적이 주석에 명시돼 있으므로 지적하지 않고 T-D에 사실만 남긴다.

`tests/test_python_snapshot_guard.py:37`의 `_SECTIONS`도 생성기 상수의 **복제**이지만, 그 복제가
바로 `test_하한_그룹이_스냅샷_그룹과_같다`의 독립 기준이 되므로 의도된 이중화다. 지적 없음.

### R-9 게이트가 지금 어디서 도는가 — **도달 0 = 1건**

| 새 장치 | 실행 경로 |
|---|---|
| `dump_python_snapshots.py --check` | `ci:quality` (idx 7) — **배선됨** |
| `tests/test_parity_ci_gate.py` 본류 회귀 4 | `ci:quality` (idx 11) — **배선됨** |
| `tests/test_python_snapshot_guard.py` | `ci:quality` (idx 12) — **배선됨** |
| Flyway 신규 2건 | `ci:kotlin` — 배선됨 |
| test-plan X-F11~13 | **`안 돎`** — Contract 게이트가 `안 돎`(`00_progress.md`). 계획이지 실행이 아니다 |
| `run_gate.sh` | **`local` · CI 0 · 실사용 0** — 이 배치의 다섯 커밋 중 러너를 실제로 경유한 검증이 **0건**이다(`526bfeb`는 "파이프 없이 종료 코드 직접 확인"으로 규약의 다른 갈래를 택했다) |

### R-10 preflight §5의 「강제자」 표기 — **권고**

preflight `:161`이 적는다 — "`00_contract-keeper_test-plan.md` §3 의 아래 항목이 이 금지의
**강제자**다." 이어지는 표(`:163-170`)의 여섯 항목(X-F11·X-F12·X-F13·X-F1·X-F2·X-F9·X-F10) **전부**가
현재 실행 경로 `안 돎`이다 — Contract 게이트 자체가 미실행이다.

즉 이 표는 **계획을 강제자로 소개한다.** 「2026-08-15 신설」 표기는 있으나 「아직 안 돎」은 없어,
preflight를 읽는 `kotlin-implementer`는 강제 장치가 이미 있다고 읽는다. 원장(`00_progress.md`)은
X5 행에 `안 돎`과 "고친 것은 계획이고 계약 테스트는 아직 없다"를 정직하게 적었으므로 **정보 자체는
저장소에 있다** — 다만 그것이 **구현자가 읽는 문서에는 없다.** 표에 상태 열 한 칸이면 닫힌다.

---

## 4. Phase 종료 조건 대비 현황

### 4.1 cross §5.2 (가) — Phase 3 착수 차단 후보 6건

| # | 이 배치의 처리 | 내 판정 | 남은 것 |
|---|---|---|---|
| **X3** | `65a7eb6` 러너 + SKILL.md 규약 | **부분 해소** | 규약 강제자 0(원장이 정직하게 병기) + 러너 자신에 **T-E(차단②)·T-F·T-G** 신규 3건 |
| **X2** | `04ced00` 본류 회귀 4 + CI 경로 명시 / `4cba492` 네 번째 대상 | **해소** (배선 축·CI 축 모두, 독립 재현) | 같은 구조의 다른 6자리(T-D ⑵) — 심각도 권고 |
| **X14** | 이 배치 밖 | **범위 밖** — 다른 레인이 미커밋 상태로 처리 중. 관찰 시점 기준 `:408` 모순은 해소된 것으로 보이나 **이 배치의 근거로 쓰지 않는다** | — |
| **X11** | `f9d78e0` | **해소** (독립 재현 3회) | K-C·K-D·K-E 권고 3건 |
| **X6** | `f9d78e0`(b축) + `526bfeb`(c축) | **부분 해소** | **(a) 강제자 0이 열려 있다** — 마감은 cross대로 Phase 3 해당 DTO 구현 커밋. R-10 참조 |
| **X5** | `526bfeb` | **해소**(계획 축) | C-C(전사 규약 자기 위반)·C-D(이중 등재) |

### 4.2 cross §5.2 (다) — X1

**해소.** 생성기 축(케이스 하한 + 파생 키 무조건 재생성)과 CI 배선 축(X-9 뒤 조각) 모두 닫혔고 음성
대조가 독립 재현됐다. 잔여는 **P-B(하한 완전성)** 이고 마감은 X1과 같은 **`app/**` 삭제 전**이다.

L344·L345 승격은 리더 판정(마감 축소가 아니라 CI 배선 완성)대로 처리됐고, 그 배선이 실재함을
R-1이 확인했다.

### 4.3 미해결 항목 요약

| 심각도 | 건 | 항목 |
|---|---|---|
| **차단②** | 1 | T-E (`run_gate.sh` 빈 문자열 → exit 0) — 마감 **즉시** |
| **수정 필요** | 4 | C-C(전사 규약 자기 위반, 마감 Phase 3 첫 계약 테스트 전) · P-B(하한 완전성, 마감 `app/**` 삭제 전) · T-F(인용 소실) · T-G(러너 자기 검사 0) |
| **권고** | 10 | C-B · C-D · P-C · S-B · K-B · K-C · K-D · K-E · T-D · R-10 |
| **판정 필요** | 0 | — |
| **이월(무변경 확인)** | 6 | K-F 표 — X8·X9·X10(부분 해소)·X12·X16·X17 |

**차단 항목의 착수 차단 여부 판정은 리더 몫이다.** 심각도와 착수 차단은 별개 축이므로 이 문서는
마감만 적고 판정을 내리지 않는다.

---

## 5. 미실행·확인 불가 항목

### 5.1 미실행

| 항목 | 사유 |
|---|---|
| codex 산출물 대조 | **1회차 규약** — 지시대로 보지도 참조하지도 않았다. 교차 종합은 2차 재호출 |
| Kotlin 전체 스위트(`./gradlew build`) | 돌리지 않았다. worktree에서 `FlywayBaselineGuardTest` 10건만 돌렸다. `1cb7bdf` 커밋 메시지의 "530 tests / 0 failures · 81 actionable tasks" 주장은 **미검증** |
| `ci.yml` 스텝의 실제 CI 실행 | 관측하지 않았다. YAML 파싱 · 잡 귀속 · 스텝 순서 · 참조 파일 실재까지만 확인 |
| I-1~I-12 보안 감사 전반 | `privacy-gate` 소관. 이 리뷰는 **이 배치가 닿는 자리**만 봤다(S-A) |
| `frontend/src/api/` | 이 배치가 건드리지 않았다 |
| 골든셋(`tests/golden`) | 프롬프트·스타일 규칙·LLM 설정 변경 없음 — 해당 없음 |

### 5.2 확인 불가 / 범위 밖

- **`00_progress.md`** — 리뷰 중에도 다른 레인이 편집 중이었다(관찰 23:00, ` M` 미커밋). 두 번 읽는
  사이에 내용이 바뀐 것을 실측했다(`run_gate` grep 결과가 0건 → 1건으로). 읽기만 했고 **이 배치의
  판정 근거로 쓰지 않았다.** X14의 해소 여부는 그 레인의 커밋으로 판정해야 한다.
- **X4·X4b**(모듈 경계 강제의 도달) — 이 배치가 `build.gradle.kts`를 건드렸으나 ktlint 포매팅뿐이고
  판정 본체(`:282-320`)의 로직은 무변경이다. 미해소이나 리뷰 범위 밖.
- **X7·X13·X15·X18~X22** — 이 배치가 손대지 않았다.
- **X13(F3 `measured_on: raw`)** — 리더 판정 대기 상태 그대로다. 이 배치가 **조항을 바꾸지 않고
  검증만 채운 것**이 옳은 처리다(C-E).

### 5.3 실험 절차 고지 (규칙 5 준수)

- 모든 변조 실험은 **일회용 `git worktree`**(`1cb7bdf --detach`) 안에서만 했다. 본 저장소 파일은
  읽기와 스크립트 호출 외에 건드리지 않았다.
- worktree 안의 되돌림은 전부 **`git checkout --`** 를 썼다. `cp` 복원은 하지 않았다.
- 두 worktree 모두 `git worktree remove --force` 로 폐기했고 `worktree list` 에서 사라진 것을
  확인했다.
- P-B ⑴의 실험은 파일을 쓰지 않는 **in-memory + tmp 사본** 방식이다(생성기를 모듈로 적재해 경로
  상수만 tmp로 돌렸다). 실물 스냅샷·하한 파일 무접촉.
- 종료 코드는 전부 **파이프 없이** 확인했다(SKILL.md 규칙 5). 출력이 길면 파일로 리다이렉트한 뒤
  grep 했다.
- 이 문서의 인용 행 번호는 작성 후 `1cb7bdf` 기준으로 **전수 재대조**했다.

---

## 6. 리더에게 (1회차 종결)

1. **산출물 경로** — `docs/migration/_workspace/reviews/16_gate15-fixes_migration-reviewer.md`
2. **교차 종합 재호출이 필요하다.** codex 산출물(`16_gate15-fixes_codex-reviewer.md`)이 준비되면 이
   문서와 함께 2차로 불러 주면 `16_gate15-fixes_cross.md`를 쓴다. **어간은 세 파일이 같아야 한다.**
3. **이 문서만으로 Phase 3 착수를 판정하지 말 것.** 특히 T-E(차단②)는 **단일 관점 판정**이라
   교차 검증이 없다 — 그 사실이 심각도를 낮추는 근거는 아니다(게이트 15 X3와 같은 상황).
4. **판정을 요청하는 자리 둘**
   - **T-D ⑵ / cross §6.2** — 본류 회귀를 "X2가 지목한 두 자리"로 둘지, "본류 호출부가 한 곳뿐인
     게이트 helper 8자리"라는 **종류**로 닫을지. CLAUDE.md 규칙 4의 "빈자리를 **종류로** 댈 수
     있으면 그 종류만큼 넓힌다"가 적용되는 자리이고, 실제로 종류로 댈 수 있다(위 표 8행).
   - **X6의 「해소」 판정** — 원장이 X6를 해소로 적었는데 그 항목이 이름에 든 첫 축("F3 금지에
     강제자 0")은 X5 행의 `안 돎`이 들고 있다. 두 행에 나눠 담는 것이 맞는지, X6의 마감(Phase 3
     DTO 커밋)이 그대로 살아 있는지 확인이 필요하다.
