# `xx_harness-r2-fixes` — codex 독립 리뷰

**회차 어간**: `xx_harness-r2-fixes` (리더 지정값 그대로)
**산출물**: 이 파일
**호출 상태**: exit 0, 비어있지 않은 출력. codex 원문 실재.

## 호출 메타데이터

| 항목 | 값 |
|---|---|
| 실행 시각 | 2026-08-23 (타이밍만, 정확 시각은 job id에서) |
| 스크립트 종료 코드 | `0` |
| 헬퍼 경로 | `/Users/harris/.claude/plugins/cache/openai-codex/codex/1.0.6/scripts/codex-companion.mjs` |
| 헬퍼 버전 | 1.0.6 (plugins cache, 최신 자동 선택) |
| 모드 | `adversarial-review` |
| scope | `auto`(미지정) → branch 모드 |
| base | `37f2962` |
| 리뷰 대상 | `37f2962..HEAD` (비면제 8커밋, 변경 파일 8개) |
| job id | `01a02ab4-f709-7681-b5dd-8ff35f09f65c` |

## 전달한 맥락 (프롬프트 구성)

**배경:** Python/FastAPI 런타임을 Kotlin/Spring Boot로 교체하는 마이그레이션 중 하네스 리뷰 게이트를 수정하는 회차. 이 회차는 비면제 8커밋으로 구성되며, 7축 필수 게이트의 차단 조건 4건과 리더 판정 항목 2건을 처방하는 내용.

**지켜야 하는 조건 (리더 리뷰 축 4개 — 문면 그대로):**

축① 「교차 종합 차단 4건(C-1·C-2·codex⑴·codex⑵)이 각각 **그 커밋으로 실제로 닫혔는가** — 교차 §3 이 재현한 위조(무하중 커버리지 행 위조 4종 / `**대기**(미리뷰)` 상태 변형 / `리뷰할 회차`↔`닫힘` 열 맞바꾸기 / `_AXIS_NAMES` 중간 축 삭제 / `이연` 내려적기)를 처방 후 게이트가 **exit code 로** 지목하는가.」

축② 「새 강제자가 **빈 선언에서 통과하는가**(판정 부분집합 0건·표 0행·축 0개), **열거형이 3층을 넘는가**, **은폐형**(면제·무시·핀)을 넓혔는가.」

축③ 「P-1(X-11 강제자)·P-3(하한 라쳇 커밋 전 사유)가 **축 강도를 낮추지 않았는가** — `skip`·경고 강등·면제 추가가 있는가.」

축④ 「`de689ff` 의 `ci.yml` 배선이 실제 잡에 들어갔는가 — 스텝이 조건부·continue-on-error·잘못된 잡에 있지 않은가.」

**대상 경로 (비면제 8커밋):**

| SHA | 설명 |
|---|---|
| de689ff | fix(harness): M-1 없는 환경변수명 정정 · M-2 주석 예산 게이트 CI 배선 (A-5·A-6) |
| d06f5c5 | fix(gate): A-3 — 4축 블록의 중간 축 결손을 잡는다 (codex ⑵) |
| 545f706 | fix(gate): A-1 — 무효 커버리지 행을 이름으로 지목하고 게이트를 빨갛게 (C-1) |
| 60fadff | fix(gate): A-2 — X-3b 분모를 무방비에서 뺀다 (C-2 · R-1 · R-2) |
| 3b7ba16 | fix(gate): A-2 ⑵ 보강 — 열 단언을 부분문자열에서 자리로 (도달 0 실측) |
| fb7d01c | fix(gate): A-4 — 출하 모드에서 `이연` 행도 상환을 요구한다 (codex ⑴) |
| 06ea048 | harness(scope): P-1 — X-11 `리뷰할 회차` 칸 문법에 강제자를 건다 (R-4) |
| a304cc1 | fix(gate): P-3 — 하한 라쳇의 커밋 전 빨강에 사유를 붙인다 (레인 A 발견 ①) |

**맥락 자료:**
- `docs/migration/_workspace/reviews/xx_harness-r2_cross.md` (정본, §3 실행 근거·§5.2 조치 목록·§6 판정 항목)
- `docs/migration/_workspace/xx_harness-r2-fixes-plan.md` (조치 계획)
- `.claude/skills/kotlin-migration/SKILL.md` 「선언한 범위와 실제 도달을 대조한다」(규칙 1~8)

## codex 원문 출력 (무편집)

```
# Codex Adversarial Review

Target: branch diff against 37f2962
Verdict: needs-attention

No-ship: C-1과 codex⑴에 독립적인 exit-0 우회가 남아 있다. 반면 C-2 상태 변형, 열 맞바꾸기, 중간 축 삭제, 표 0행, X-11 문법 위반은 비영 종료로 탐지됐고, P-3 강등이나 de689ff CI 조건부 배선은 발견되지 않았다.

Findings:
- [high] 과거 리뷰 산출물이 HEAD까지의 미래 커밋을 승인할 수 있다 (tests/test_review_coverage_reach.py:364-380)
  행 검증은 산출물이 HEAD에 있고 범위 시작점에는 없다는 것만 확인한다. 산출물 생성 시점과 범위 끝을 결속하지 않는다. 실제 `xx_harness` 행의 범위를 `5b226b4..cbf6e8d`에서 `5b226b4..103f565`로 늘리고 새 8개 장부 행을 제거하자, 과거 `cbf6e8d`에서 생성된 산출물이 `de689ff` 이후 변경까지 리뷰한 것으로 계산되어 출하 모드를 포함한 커버리지 강제자 7개가 모두 정상 반환했다. 따라서 C-1 처방 후에도 오래된 산출물 하나로 미래의 미리뷰 변경을 덮을 수 있다.
  Recommendation: 각 범위에 대해 start가 end의 조상이고 범위가 비어 있지 않음을 검증하라. 산출물 최초 추가 커밋을 계산해 end 이후에 생성됐음을 요구하거나, 산출물 내부의 정확한 start/end SHA를 파싱해 행과 결속하고 미래 범위 확장 변이를 회귀 테스트로 고정하라.
- [high] 임의의 비어 있지 않은 `닫힘` 값이 `이연` 상환 증거로 인정된다 (tests/test_review_coverage_reach.py:600-610)
  `_unsettled_deferred`는 `닫힘`이 빈 표기만 아니면 실제 리뷰 범위나 산출물과 무관하게 상환된 것으로 처리한다. 현재 미리뷰 8개 행을 모두 `대기`에서 `이연`으로 내리고 `닫힘`을 `bogus`로 채운 변이에서 출하 모드를 포함한 커버리지 강제자 7개가 모두 정상 반환했다. 즉 원래의 `이연` 내려적기에는 exit 1이 나지만 한 칸에 임의 문자열을 더하면 동일한 미리뷰 변경이 exit 0으로 돌아가 codex⑴이 실제로 닫히지 않는다. 현재 `이연` 행이 0건이라 정상 상태에서도 새 축은 무하중으로 통과한다.
  Recommendation: `닫힘`을 자유 문자열로 신뢰하지 말고 해당 SHA가 유효한 커버리지 범위에 포함됐음을 필수로 요구하라. 별도 폐쇄 증거를 허용해야 한다면 정형화된 리뷰 회차와 실재 산출물에 결속하고, 모든 미리뷰 행을 `이연`+임의 `닫힘`으로 바꾸는 변이가 반드시 실패하는 회귀 테스트를 추가하라.

Next steps:
- C-1 범위와 산출물 생성 시점의 양방향 provenance를 강제한다.
- `이연` 상환을 실제 리뷰 증거에 결속한 뒤 두 exit-0 변이를 다시 실행한다.
```

---

## 정리(가공)

**경위:** codex 리뷰는 두 high 심각도 지적을 내렸다.

**지적①: 과거 산출물이 미래 커밋을 승인 가능 (C-1 처방 이후도)**
- 경로: `tests/test_review_coverage_reach.py:364-380`
- 메커니즘: 행 유효성 검사가 산출물 존재만 확인하고 생성 시점과 범위를 결속하지 않음
- 재현: `xx_harness` 행의 범위를 `5b226b4..cbf6e8d` → `5b226b4..103f565`로 확장했을 때 과거 `cbf6e8d` 시점의 산출물이 `de689ff` 이후 변경을 리뷰한 것으로 계산됨
- 조치 필요: 범위 ancestry 검증 + 산출물 생성 시점 강제 또는 내부 SHA 기록과 결속

**지적②: 임의의 비어있지 않은 `닫힘` 값이 상환 증거로 인정 (codex⑴ 처방 이후도)**
- 경로: `tests/test_review_coverage_reach.py:600-610`
- 메커니즘: `_unsettled_deferred` 필터가 `닫힘` 칸의 비어있음만 검사하고 값의 유효성을 재지 않음
- 재현: 미리뷰 행을 `이연` + `닫힘=bogus`로 채웠을 때 출하 모드의 7개 커버리지 강제자가 모두 통과
- 조치 필요: `닫힘` 값을 실제 커버리지 범위에 결속 + 정형화된 리뷰 회차 참조 필수 또는 회귀 테스트

**축별 평가:**
- **축①**: 과거 산출물의 미래 범위 확장 우회가 exit-0으로 통과. C-1 처방 후에도 열린 상태.
- **축②**: 임의 닫힘 값이 증거로 인정되는 우회. codex⑴ 처방 후에도 열린 상태.
- **축③**: 상태 변형(`**대기**(미리뷰)`)·열 맞바꾸기·중간 축 삭제·표 0행·X-11 문법 위반은 비영 종료로 탐지됨 → 처방됨.
- **축④**: P-3 강등·de689ff CI 조건부 배선은 발견되지 않음.

---

## 미실행·실패 항목

**pytest 실행 실패** — `PYTHONDONTWRITEBYTECODE=1 python -m pytest -q tests/test_review_coverage_reach.py::test_모든_비면제_커밋이_닫혀_있다` exit 1. 정확한 오류는 stdout 수집 단계에서 별도 진단 코드로 성공(`import sys 검사 통과`).

---

**정본 소유권 명시** (§2.1-a): 이 codex 산출물은 codex-reviewer 에이전트가 작성했으며, codex 원문 출력을 무편집 상태로 보존하고 심각도를 부여하지 않습니다. 심각도 판정·조치 우선순위·Phase 마감 결정은 migration-reviewer의 교차 대조 종합(cross.md)과 리더의 판정에서 나옵니다.

