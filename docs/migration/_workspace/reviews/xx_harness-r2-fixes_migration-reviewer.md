# `xx_harness-r2-fixes` — Claude 단독 리뷰 (1차, 교차 대조 전)

**회차 어간**: `xx_harness-r2-fixes` (리더 지정값 그대로)
**범위**: `37f2962..103f565`. 비면제 8커밋 `de689ff` `d06f5c5` `545f706` `60fadff` `3b7ba16` `fb7d01c` `06ea048` `a304cc1`.
docs 전용 `b37e034` `d20447f` `ecfb2f7` `103f565` 는 맥락으로만 읽었다.
**이 파일은 codex 산출물을 읽지 않고 썼다.** 교차 대조는 2차 재호출의 `..._cross.md` 가 정본이다 — **이 파일만으로 Phase 종료를 보고하지 않는다.**

**참조**: 계획 §2.2·§2.3·§3.1·§3.2·§4.5·§4.6·§5·§6 / `.claude/skills/kotlin-migration/SKILL.md` 규칙 1~8·「선언한 범위와 실제 도달을 대조한다」 / 정본 심판문 `reviews/xx_harness-r2_cross.md` §3·§5.2·§6 / 조치 계획 `xx_harness-r2-fixes-plan.md`.

## 0. 측정 방법 — 읽기 판정을 쓰지 않았다

저장소 **밖** 일회용 worktree 셋에서 **게이트 exit code** 로 쟀다. `cp` 복원·`git stash` 미사용, 본 저장소 무변조(측정 종료 시 `git status --porcelain` 공란 확인).

| worktree | 트리 | 용도 |
|---|---|---|
| `wt-after` | `103f565` | 처방 후 |
| `wt-mix` | `103f565` 트리 + `tests/test_{review_coverage_reach,harness_scope_reach,kotlin_gate_reach}.py` 만 `37f2962` | **처방 전** — 데이터를 고정하고 게이트 코드만 되돌려 처방을 격리한다 |
| `wt-before` | `37f2962` | 기준선 확인용 |

`wt-mix` 를 만든 이유: 데이터(원장)와 게이트 코드가 함께 바뀐 범위라 `wt-before` 로 재면 「어느 쪽이 신호를 만들었는가」가 갈리지 않는다.

**평시 기준선** — `wt-mix` 4 passed exit 0 / `wt-after` 7 passed exit 0 (같은 원장). **출하 모드 기준선** — `wt-after` 는 미상환 `대기` 8건으로 정상 빨강(리더 실측과 일치). 출하 모드 변이는 이 교란을 피해 **대상 테스트의 개별 판정**을 함께 적었다.

---

## 1. 축① — 교차 §3 의 위조 5종이 실제로 닫혔는가

**결론: 5종 전부 닫혔다. 전건 처방 전 조용(또는 오지목) → 처방 후 빨강 + 위조 행 원문 지목.**

| ID | 변이 | 처방 전 | 처방 후 | 지목 |
|---|---|---|---|---|
| A1-a | 커버리지 행 산출물 칸에 **없는 파일** | **exit 0** (4 passed) | exit 1 | `test_커버리지_표에_무효_행이_없다` — `산출물이 HEAD 에 커밋돼 있지 않다: ['reviews/xx_harness_nonexistent.md']` + 행 원문 |
| A1-b | **다른 회차**의 실재 산출물로 채움 | **exit 0** | exit 1 | `산출물 칸이 규약을 어겼다 — 각 경로가 reviews/xx_harness_<리뷰어>.md 여야 한다` |
| A1-c | **codex 산출물 제거** | **exit 0** | exit 1 | `필수 역할이 선언되지 않았다: ['codex-reviewer']` |
| A1-d | **과거 산출물로 미래 범위 승인** (`01d3c48..103f565` 행 추가) | **exit 0** | exit 1 | `산출물이 시작 커밋 01d3c48 에 이미 있었다` + 3경로 열거 |
| A1-e | **리더 실사건 재현** — `HEAD` 에 없는 이번 회차 산출물로 8커밋 승인, 출하 모드 | exit 1 이지만 **오지목**(`리뷰도 장부도 없는 커밋 N건` 만) | exit 1 | 무효 행을 **이름으로** 지목 + 종전 두 실패도 유지 |
| A2-a | `**대기**` → `**대기**(미리뷰)` (리뷰된 3행) | **exit 0** | exit 1 | `test_장부_표의_모든_행이_판정에_든다` — `판정에 들지 못한 행 3 건 (표 데이터 행 32 · 판정 29)` |
| A2-c2 | **리뷰된 `대기` 전건**만 훼손(미리뷰 8행 보존) | **exit 0** | exit 1 | 두 축 동시 — `판정에 들지 못한 행 24 건 (32 · 8)` **와** `리뷰 범위에 든 대기 행이 0 건이다 — 이 검사가 아무것도 판정하지 않았다` |
| A2-b2 | 머리행 `닫힘` → `닫은 회차` **개명만** | **exit 0** | exit 1 | `머리행이 규약과 다르다` (자리별) |
| A2-b | `리뷰할 회차` ↔ `닫힘` **열 맞바꾸기** | exit 1 이지만 **오지목**(커밋 이름) | exit 1 | 머리행 자리 드리프트를 **먼저** 지목 |
| A3-a | 두 스킬에서 축 하나 **함께 삭제**, 번호 유지 | **exit 0** (45 passed) | exit 1 | `축 열거 블록에서 축 이름이 빠졌다: {kotlin-migration: ['문서 추출·내보내기 무결성'], codex-review: [...]}` |
| A4-a | 미리뷰 8커밋을 **`이연`·`닫힘` 공란**으로 내려적기, 출하 모드 | **exit 0** (4 passed) ← **출하 판정 통과** | exit 1 | `상환되지 않은 이연 행 8 건이 남은 채로 출하 판정을 시도했다` |
| P1-a | `리뷰할 회차` 칸을 산문으로 | **exit 0** | exit 1 | `문법을 어긴 행 1 건 (전체 32 행)` |
| P1-b | 백틱 없이 | **exit 0** | exit 1 | 같음 |
| P1-c | **문법 이전 핀 어간** `16_gate15-fixes` | **exit 0** | exit 1 | `'16_gate15-fixes' — 어간이 {phase}_{scope}[-{회차}] 문법을 어긴다` (핀이 이 자리로 새지 않음이 실측됨) |

**A4-a 가 이 회차의 가장 큰 소득이다.** 처방 전 `103f565` 트리에서 **한 낱말(`대기`→`이연`)이 exit 1 을 exit 0 으로 바꿨다** — 미리뷰 비면제 커밋 8건을 안은 채 출하 판정이 통과했다. 처방 후 그 통로가 닫힌다.

**A2-c(전건 훼손)는 처방 전에도 빨갰다** — 전건을 훼손하면 `_ledger_rows` 가 0행이 되어 옛 `assert rows` 가 진다. 즉 C-2 의 실제 구멍은 **부분 훼손**이고, 그것을 겨눈 A2-a·A2-c2 가 처방 전 exit 0 으로 구멍을 재현했다. 계획의 C-2 진단은 정확하다.

---

## 2. 축② — 새 강제자의 장치 분류 · 빈 선언 · 층수 · 은폐형

### 2.1 장치 분류표

| 항목 | 분류 | 층 구성 | 층수 | 빈 선언에서 통과하는가 |
|---|---|---|---|---|
| A-1 `_coverage_verdicts` + `test_커버리지_표에_무효_행이_없다` | **탐지형** | ⑴ 행 유효성 판정(기존 술어 재사용) ⑵ 무효 행 지목 | 2 | **아니다** (B1 실측) |
| A-2 ⑴ `_ledger_verdicts` + `test_장부_표의_모든_행이_판정에_든다` | **탐지형** | ⑴ 데이터 행 집계 ⑵ 판정 행 수와 대조 | 2 | **아니다** (B2) |
| A-2 ⑵ `_header_drift` | **탐지형** | ⑴ 첫 표 머리행 추출 ⑵ 자리별 대조 | 2 | **아니다** — 머리행 미발견 시 `(열 없음)` 으로 전건 드리프트 |
| A-2 ⑶ `assert judged` | **탐지형** | 기존 축 안의 단언 1개 | +0 | **아니다** (B1 에서 발화) |
| A-3 `_AXIS_NAMES` 전항목 포함 단언 | **탐지형** | 기존 테스트 안의 단언 1개 | +0 | **아니다** — `_AXIS_NAMES` 를 비우면 형제 축 둘이 잡는다 (B4 실측: 2 failed) |
| A-4 `_unsettled_deferred` + 출하 축 | **탐지형** | ⑴ 미상환 판정 ⑵ `assert rows` 빈 분모 방어 | 1(+방어) | **아니다** (B2) |
| P-1 `test_이연_장부의_리뷰할_회차_칸이…` | **탐지형**(강제·표현형 규약에 탐지자를 붙임) | ⑴ 칸 문법 판정 ⑵ 빈 분모 방어 | 2 | **아니다** (B2) |
| P-3 `uncommitted` 분기 | **강제·표현형** (판정 강도 불변, 사유만 갈라짐) | 기존 단언을 둘로 | +0 | 해당 없음 |
| A-6 `ci.yml` 경로 명시 스텝 | **배선**(파일 소실 탐지) | 형제 5개와 동형 | — | 해당 없음 |

**층수 판정 — 규칙 7 위반 없음.** 규칙 7 의 3층은 *열거형 탐지기 한 축*(`목록 → 라쳇·부분집합 → 빈 선언 방어`)에 대한 것이다. 이 회차의 신설물은 열거형 목록이 아니라 **구조 파서 + 대조 + 빈 분모 방어**이고, 최대 2층이다. 상위의 `_COVERAGE_AXIS_NAMES`(이름 소실 탐지)와 `ci.yml`(파일 소실 탐지)은 **저장소 전체가 공유하는 다른 종류의 장치**이지 이 축 위에 새로 쌓은 층이 아니다.

### 2.2 빈 선언 음성 대조 (규칙 4 ⑶)

| ID | 변이 | 결과 |
|---|---|---|
| B1 | 커버리지 표 **데이터 행 전건 삭제** | exit 1 · 4 failed — `「## 리뷰 커버리지」 절에 표 데이터 행이 0 건이다` 외 |
| B2 | 장부 표 **데이터 행 전건 삭제** | exit 1 · 6 failed — 신설 4축 **전부** 발화(`test_이연_장부의_리뷰할_회차_칸…` 포함) |
| B4 | `_AXIS_NAMES` 를 `()` 로 | exit 1 · 2 failed (`test_즉시_중단_기준이_전부_필수_축에_매핑된다` · `test_리뷰_4축_사본이_갈리지_않는다`) |

**신설 강제자 중 빈 선언에서 통과하는 것은 0건이다.**

### 2.3 은폐형 확장 여부

**확장 0.** `PRE_GRAMMAR_STEMS` 는 값이 한 줄도 바뀌지 않았고(diff 에 핀 항목 `+`/`-` 0건), P-1 은 그 목록을 **소비자로 삼지 않는다**(P1-c 실측으로 확인). 신설된 `pytest.skip`·`xfail`·`# noqa`·무시 패턴·면제 조항 **0건** — diff 의 `pytest.skip` 출현은 전부 산문/실패 메시지 안이고 실행 코드가 아니다.

---

## 3. 축③ — P-1·P-3 이 축 강도를 낮추지 않았는가

**낮추지 않았다.**

**P-3** — 미커밋 신규 하한 상수 `MIN_MRF_PROBE = 3` 을 핀 표에 넣고 실측:

| | 처방 전 (`wt-mix`) | 처방 후 (`wt-after`) |
|---|---|---|
| exit | **1** | **1** (강도 불변) |
| 메시지 | `라쳇 핀을 판정하지 못했다 — 판정 불가는 통과가 아니다` / `MIN_MRF_PROBE — 이력에서 그 상수를 한 번도 찾지 못했다` | `**미커밋 신규 상수 1 건** — 이력에 아직 없어 대조할 기준점이 없다` / `MIN_MRF_PROBE = 3` |

이름이 나오고 **여전히 빨갛다.** 사유 분기 대조 P3-b(기존 핀 `MIN_FLOOR_CLASSES` 를 이름 드리프트시킴) → exit 1, 메시지는 종전 버킷(`현재 파일에서 그 상수를 찾지 못했다`). **두 사유가 실제로 갈린다.**

**P-1** — 강도를 낮추는 성분이 없다. SKILL.md `:174` 의 정정은 「강제자는 없다」를 실제 강제자 경로로 **교체**하면서 남은 도달 0(그 회차가 실재하는지)을 장부에 **유지**했다(규칙 3 준수).

---

## 4. 축④ — `de689ff` `ci.yml` 배선

- 위치 `.github/workflows/ci.yml:241-242`, **`quality` 잡 안**(잡 범위 `19..247`). `if:` 조건 **없음**, 워크플로에 `paths:` 필터 **없음**, 해당 잡 `fetch-depth: 0` — 즉 **무조건 실행**이다.
- 형제 5개(`test_commit_gate_chain_reach` · `test_kotlin_class_snapshot_reach` · `test_review_coverage_reach` · `test_kotlin_gate_reach` · `test_quality_gate_local` · `test_raw_control_chars`)와 동형.

**음성 대조 A6-a** (`tests/test_kotlin_comment_budget.py` 삭제 후):

| 실행 | 결과 |
|---|---|
| 그 스텝 명령 (`pytest tests/test_kotlin_comment_budget.py`) | **exit 4** — `no tests ran` |
| blanket 수집(형제 게이트 2파일) | **350 passed** — 소실이 조용하다 |
| 파일 복원 후 같은 스텝 | exit 0 · 4 passed |

**M-2 는 닫혔다.** 경로 명시가 파일 소실을 잡고, blanket 수집은 못 잡는다는 것이 같은 트리에서 실측됐다.

**A-5** — 레인 A 계획의 `KOTLIN_GATE_REACH_REQUIRE_FRESH_REPORTS` → `KOTLIN_GATE_REACH_REQUIRE_REPORT` 정정. 실재 확인: `ci.yml:378` 이 그 이름으로 요구 모드를 켠다. **M-1 닫힘.**

---

## 5. 도달 범위 점검 — 이 8커밋이 세운 장치가 지금 어디서 도는가

| 장치 | 실행 경로 | 근거 |
|---|---|---|
| A-1 `test_커버리지_표에_무효_행이_없다` | `ci:quality` · `local:` | `ci.yml:237` 경로 명시 + blanket |
| A-2 ⑴⑵⑶ | `ci:quality` · `local:` | 같음 |
| A-3 축 결손 단언 | `ci:quality` · `local:` | `ci.yml:142,158` |
| **A-4 `이연` 상환 축** | **평시 판정 없음(warn 후 `return`)** · 출하 모드는 **`local:` 수동뿐** | 아래 R-b |
| P-1 `리뷰할 회차` 문법 | `ci:quality` · `local:` | `ci.yml:142,158` |
| P-3 사유 분기 | `ci:quality`(198) · `ci:kotlin` 요구 모드(324·350·368·379) · `local:` | |
| A-6 주석 예산 배선 | `ci:quality` | `ci.yml:242` |

### 지적 — 도달 범위

**R-a (권고 · 도달 범위(게이트 자신)) — 가짜 구분선으로 커버리지 데이터 행을 파서 시야 밖으로 뺄 수 있다.**
근거: `tests/test_review_coverage_reach.py:236-268` — `_table_data_lines` 는 「다음 줄이 구분선인 `|` 줄」을 머리행으로 보아 뺀다. 위조 행 **바로 뒤에** `|---|---|---|` 한 줄을 넣으면 그 행이 데이터 행 집합에서 사라진다.
실측 S1: `wt-after` 평시 **exit 0, 7 passed** — 사람이 마크다운에서는 보는 행을 게이트가 못 본다. A-2 ⑴ 이 선언한 「표처럼 보이는 행 수 = 판정된 행 수」가 이 자리에서 성립하지 않는다.
**우회는 아니다 — 깨지는 방향이 안전하다.** 음성 대조 S2(8행을 `이연`·공란으로 내려적고 그중 하나를 같은 방식으로 은닉, 출하 모드): **exit 1, 2 failed** — 숨긴 행은 「적힘」 자격도 함께 잃어 그 커밋이 미리뷰로 드러난다. 즉 커버리지를 **만들어 낼 수는 없고**, 사람에게만 보이는 과잉 주장을 만들 수 있다.
마감: 다음 회차(`xx_harness-r2-fixes` 후속). 이 회차 착수를 막지 않는다.

**R-b (판정 필요 · 도달 범위) — A-4 축의 CI 도달이 0이다.**
근거: `REVIEW_COVERAGE_REQUIRE_SETTLED` 를 켜는 자리가 저장소에 **0건**(`.github/**`·`.claude/**` 실측). 평시에는 `tests/test_review_coverage_reach.py:934-943` 이 `warnings.warn` 후 `return` 한다 — 도는 것은 빈 분모 방어(`assert rows`)뿐이다.
직전 회차가 **이 모드의 CI 배선 0을 「의도된 상태」로 판정**했고(`xx_harness-r2_migration-reviewer.md:187`) 이 레인은 그 판정을 물려받았다. 다만 **A-4 는 그 모드에서만 켜지는 첫 신설 축**이므로, 그 판정이 그대로 연장되는지는 리더 확인이 필요하다. 계획 §6-4 는 「하중 0 · 도달은 첫 `이연` 행」을 적었으나 **모드 자체의 배선 0**은 A-4 고유 한계로 적지 않았다.
마감: 첫 Phase 종료 판정.

**R-c (권고) — 계획 §6-8 의 「장부 표 두 조각」은 참이고 규모가 기재보다 크나, 소비자가 없다.**
실측(`103f565`): 장부 표에 빈 줄이 있어 `test_harness_scope_reach.py::parse_tables` 는 32행 중 **11행만** 본다(21행 미도달 — 계획 기재 「뒤 13행」보다 크다). 반면 커버리지 강제자 자신의 파서는 32/32 를 본다.
**그러나 무하중이다** — 장부 표는 `select_target_tables`(종료 조건 표·게이트 표)의 대상이 아니어서 절단된 뷰가 어떤 판정에도 들어가지 않는다(`EXPECTED_TARGET_TABLES = 6`, 장부 미포함). 계획이 그 무하중 사실을 적지 않아 위험이 실제보다 커 보인다.
리더가 **범위 밖 `384a53c`** 에서 빈 줄을 제거해 사실상 해소됐다(그 커밋은 이 회차 범위 밖이므로 이 리뷰가 판정하지 않는다).

**R-d (권고) — A-1 이 커버리지 표의 소급 기재를 막았다(오늘 하중 0).**
`_coverage_verdicts` 가 `_declared_artifacts` 로 `reviews/{stem}_<리뷰어>.md` 를 요구하고, 문법 이전 어간 핀은 이 자리에 적용되지 않는다(P-1 이 같은 이유로 배제). 종전에는 조용히 탈락하던 것이 이제 하드 실패다 — 과거 회차를 커버리지 표에 소급해 적을 일이 생기면 거짓 빨강이 된다. 계획 §6 미기재. 조치는 단언을 무르게 하는 것이 아니라 그때 사유를 리뷰에 올리는 것이다.

### 도달 범위 — 나머지 필수 항목

| 항목 | 결과 |
|---|---|
| 「전역」·「모든」·「항상」 선언에 미도달 경로 | **검토함 — R-a 외 지적 없음** |
| 대리 경로 측정 | **검토함 — 지적 없음.** 이 레인은 교차 §4 축② 지적을 정면으로 받아 `_coverage_ranges` 길이 같은 대리 측정을 쓰지 않고 exit code 로 쟀다. 이 리뷰도 같은 방식으로 재측정했다 |
| 검사 기준이 검사 대상 자신에게서 나오는가 | **검토함 — 지적 없음.** 원장(데이터)과 게이트(코드)가 다른 파일이고, `wt-mix` 로 데이터를 고정한 채 코드만 되돌려 자기참조를 끊었다 |
| 대리 지표 판정(지적 건수·종료 코드 0을 「검토했다」로) | **검토함 — 지적 없음** |
| 규칙·패턴의 범위가 근거보다 넓은가 | **검토함 — 지적 없음.** P-1 이 핀 19종을 넓히지 않았고(실측 P1-c), 계획 §5.5-5 가 그 기각을 기록했다 |
| 음성 대조가 붙어 있는가 | **검토함 — 지적 없음.** 신설 6항목 전부 「떼면 무엇이 깨지는가」가 이 리뷰에서 실측됐다 |
| 판정하는 코드가 자기 검사 범위 안인가 | **검토함 — 지적 없음.** 세 테스트 파일 전부 `ci.yml` 경로 명시 대상이고 `mypy . .claude` 범위 안이다 |

---

## 6. 축⑥ — 조치 레인이 심판문을 편집했는가

**편집 0건.** `git log --stat 37f2962..103f565 -- docs/migration/_workspace/reviews/` 출력 **공란**. 규약(심판 대상은 심판문을 고치지 않는다) 준수.

---

## 7. 다섯 축 중 이 회차에 대상이 없는 축

**추정으로 메우지 않는다.** 이 범위의 변경은 `tests/**`(3파일) · `.github/workflows/ci.yml` · `.claude/skills/kotlin-migration/SKILL.md` · `docs/**` 뿐이고 `backend-kotlin/**` · `contracts/**` · `parity/**` 무변경이다.

| 축 | 상태 |
|---|---|
| 계약 준수 | **범위 밖 — 대상 변경 0건** (미검토가 아니다) |
| parity 위험 | **범위 밖 — 대상 변경 0건** |
| 보안 불변식 | **범위 밖 — 대상 변경 0건.** 직전 회차도 양 심판 레인이 범위 밖으로 남겼다(교차 §7.1). 최종 판정 권한은 `privacy-gate` |
| Kotlin/Spring 관용성 | **범위 밖 — Kotlin 무변경** |
| 테스트 적정성 | **검토함** — §1·§2 가 그 축이다. 신설 6항목 전부 실패 경로 음성 대조가 붙었고 성공 경로만 있는 신설물은 0건 |

---

## 8. Phase 종료 조건 대비 현황

- **`xx_harness-r2` 교차 §5.2 차단 4건**: A-1(C-1) · A-2(C-2) · A-3(codex ⑵) · A-4(codex ⑴) — **실측으로 4건 전부 닫힘**(§1).
- **수정 필요 2건**: A-5(M-1) · A-6(M-2) — **닫힘**(§4).
- **리더 판정 2건**: P-1(R-4) · P-3(⑶) — **채택·구현·실측 확인**(§1·§3).
- **미해결(이월)**: R-a · R-b · R-c · R-d, 그리고 계획 §6-7 이 백로그로 넘긴 A-7(M-3) · P-2 · 교차 §7.2①.
- **직전 회차 충돌 항목**: 이 회차 산출물에서 새로 열린 충돌 없음. 종전 충돌의 리더 판단 여부는 `..._cross.md` 2차에서 확인한다.

**이 산출물만으로 Phase 종료 조건 충족을 보고하지 않는다.** 정본은 codex 원문과 대조한 `xx_harness-r2-fixes_cross.md` 다.

---

## 9. 미실행 · 확인 불가

1. **`codex-reviewer` 산출물 미참조 — 1차 규약대로다.** 실패로 기록하지 않는다.
2. **산출물 내용이 참인가는 여전히 재지 않았다** — 계획 §6-1 그대로. A-1 이 올린 것은 「무효 선언이 조용하지 않다」까지이고, `xx_harness` 반례(산출물 3건·codex 원문 0줄)는 이 처방으로도 통과한다.
3. **보안 불변식 축 판정 불가** — 대상 변경이 0건이라 감사 입력이 없다. `privacy-gate` 감사문 갱신분 없음.
4. **CI 실환경 미실행** — `ci.yml` 배선은 정적 구조(잡 소속·`if:` 부재·`paths` 부재·`fetch-depth: 0`)와 **해당 스텝 명령의 로컬 exit code** 로 판정했다. GitHub Actions 실행 로그로는 확인하지 않았다.
5. **`384a53c`(현재 저장소 HEAD)는 이 리뷰의 범위 밖**이다. 측정은 전부 `103f565` 에 고정된 worktree 에서 했다.
