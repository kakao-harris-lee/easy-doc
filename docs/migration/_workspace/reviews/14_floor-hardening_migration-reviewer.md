# 게이트 14 · 1단계 독립 리뷰 (Claude 단독) — `14_floor-hardening`

> **회차**: 1차(독립). codex 산출물은 이 회차의 입력이 아니다 — 부재는 정상이며 실패로 기록하지 않는다.
> **교차 종합**: `14_floor-hardening_cross.md`(2차 재호출)에서 수행한다. **이 문서만으로 Phase 2 종료 조건 충족을 보고하지 않는다.**

---

## 1. 리뷰 범위와 참조

| 항목 | 값 |
|---|---|
| 대상 커밋 | `4dafde5..bbbdb6b` (4커밋) — `3f86c59`(§4-terdecies) · `142dac6`(N-20·U-3) · `496319d`(B2·B3) · `bbbdb6b`(B1·B4·B5·U-2·U-3) |
| HEAD | `bbbdb6b` — `origin/feat/kotlin-migration-harness`와 동일(**푸시 완료**), 작업 트리 clean |
| 참조 계획 절 | §2.3(보안 불변식) · §3.2(모듈 경계) · §4.5·§4.6(parity 위험) · §5 Phase 2·Phase 7 · §6(게이트) |
| 참조 판정 기준 | `kotlin-migration` 스킬 "선언한 범위와 실제 도달을 대조한다" 전문 · `CLAUDE.md` 규칙 4(장치 분류) · `migration-safety-gate` I-항목 |
| 직전 정본 | `reviews/13_regression-and-pins_cross.md` |

### 1.1 이 회차에 실행한 조회 (읽기 전용 · 판정 근거)

| # | 조회 | 결과 |
|---|---|---|
| V-1 | `gh run list` / `gh api .../jobs` | **run 31854263996 (`bbbdb6b`)** — `kotlin` 성공 · `quality` 성공 · `frontend` 성공 · `llm-lane` 진행 중 |
| V-2 | 러너 `kotlin` 잡 step 목록 | step 13 **「parity 비교 (선언한 도메인 범위)」 success** |
| V-3 | 러너 `kotlin` 잡 로그 본문 | `전체 게이트로 실행한다 (--only-domain 없이)` → `요구 성질 충족: 도메인 8/8 / 성질 판정 153건(단언 630개) / 판정 보류 0건 / 참고 갈림 37건 / 미검증 0건 / **불충족 0건**` → `parity 전체 게이트 통과` |
| V-4 | 러너 `quality` 잡 로그 | `1229 passed, 5 deselected, 5 xfailed` · `tests/test_parity_ci_gate.py ..............` (**14건** = 기존 7 + 신규 7) |
| V-5 | `grep -rn effective_scope .claude .github tests` | **`.github/workflows/ci.yml:441` 한 줄뿐** — 코드에 그런 이름이 없다 |
| V-6 | `git show {4dafde5,bbbdb6b}:parity/fixtures/export/export.json` | 케이스 **12 → 12 (증가 0)** · 단언 141 → 159 |
| V-7 | 같은 방식 masking | 케이스 **84 → 85** · 단언 304 → 308 |
| V-8 | `.github/parity-full-gate.txt` 본문 | 도달 기록 = `성질 판정 152건 · 단언 608개` (**미갱신**) |
| V-9 | `00_progress.md` L352 | `성질 152·단언 608·갈림 36` (**미갱신**) / 같은 문서 384문단 = `성질 153·단언 630` |
| V-10 | 하네스 함수로 Phase 2 행 키 산출 | `parse_tables`·`identity_pair` 직접 호출 — §6.4에 실측 문자열 기재 |
| V-11 | 테스트 전수 조사(저장소 실물 파일 쓰기) | 위험 3건 — §5.3 |
| V-12 | `git log -1 --format=%B bbbdb6b \| grep -c "판정 도구 변경"` | **0** |

---

## 2. 리더 지정 — 게이트 13 「최소 회차 조건 4개」 각각 명시 판정

> **참조 위치 정정**: 리더는 `13_..._cross.md` **§10**을 지목했으나, §10은 판정 요청 6건 표다. 조건 4개는 **§7.3 말미 「최소 회차가 되기 위한 조건 (배치 제출 시 함께 낼 것)」**에 있다. 그 4개로 판정했다.

| # | 조건 | **판정** | 근거 |
|---|---|---|---|
| **1** | **B1~B5를 각각 별도 커밋으로** 낸다 | **미충족** | 배치는 4커밋이고 `bbbdb6b` 하나가 **B1·B4·B5·U-2·U-3**을 담는다. 게다가 그 커밋이 **판정 도구**(비교기 3곳)와 **판정 대상**(fixture)을 함께 바꿔 R-9가 지목한 형태 그대로다. B1 행이 요구한 *"제목에 「판정 도구 변경」 명시"*도 없다(V-12 = 0회). 이탈 사유는 적혀 있으나 성립하지 않는다 — §5.4 F-6 |
| **2** | **B2의 음성 대조를 커밋 메시지가 아니라 테스트로** | **충족** | `tests/test_parity_ci_gate.py:275-348` 7함수 상시. 러너에서 수집·통과 확인(V-4). 대조군(`test_정상_상태는_통과한다`)까지 포함 — N-16이 지적한 "1회성 음성 대조" 형태가 B2에서는 재발하지 않았다. **단 B4·B5로는 확장되지 않았다**(F-5) |
| **3** | **B5 이후 전체 게이트 재측정 + 그 수치로 원장·`parity-full-gate.txt`·커밋 메시지 세 곳 동시 기재** | **부분 충족** | 재측정은 됐고 **러너 실측**까지 있다(V-3: 153/630/불충족 0) — 요구보다 강하다. 그러나 **기재는 2곳뿐**: 커밋 메시지 + 원장 384문단. **`parity-full-gate.txt`는 608 그대로**(V-8), **원장 L352도 608 그대로**(V-9). 584↔608 갈림 재발 방지가 목적이었는데 **608↔630 갈림이 같은 문서 안에서 새로 났다** — F-2 |
| **4** | **배치 제출 시 X-5 판정 결과 명시** | **충족** | 원장 384문단 — *"X-5 충돌(#4 해소 기준)은 **목적 축 채택**(규칙 1 정합)"*. 배치 커밋 메시지에는 없으나 조건 문면은 "배치 제출 시 명시"이고 원장이 배치를 기술하는 자리다 |

**요약: 4개 중 충족 2 · 부분 충족 1 · 미충족 1.**

---

## 3. 다섯 축별 지적

### 3.1 계약 준수 — **미검토 (사유: 대상 코드 무변경)**

이 배치는 HTTP 계약 코드를 한 줄도 건드리지 않았다. export fixture의 `content_disposition` 단언(RFC 5987 `ext-value`·`ascii_only`)은 유지됨을 확인했으나(`dump_parity_fixtures.py:2124-2131`) 이번 변경 대상이 아니다. `{"detail":…}`·snake_case·no-store·소유권 404는 **양쪽 미검토**로 남는다 — 게이트 13과 같다.

### 3.2 parity 위험

| # | 지적 | 심각도 | 근거 | 마감 |
|---|---|---|---|---|
| **F-3** | **N-02가 집행되지 않았고 그 사실이 어디에도 없다.** 게이트 13 §7.3은 B5를 「N-06·N-02·N-03」을 닫는 커밋으로 지정했고 N-03은 *"N-02와 동시(선행 불가)"*였다. 실제로는 N-06·N-03만 닫혔다 — export 케이스 **12 → 12, 증가 0**(V-6). C1·보충평면·예약어 케이스는 여전히 **0건**이다. B3에는 이탈 사유 기재를 요구해 놓고 B5의 이탈은 커밋 메시지·원장 어디에도 없다 | **수정 필요** | `git show`(V-6) · `dump_parity_fixtures.py:2017-2025`(제목 7개 전부 C1·보충평면·예약어 없음) | Phase 4 착수 전 (N-02 원래 마감) |
| **F-4** | **N-03 수정의 도달이 0이다.** `FILENAME_FORBIDDEN`에 C1(U+0080–U+009F)을 넣었으나 ⑴ 어느 fixture 입력 제목도 C1을 담지 않고 ⑵ **`absent` 니들 목록 `dangerous`는 9개 손열거**(`/ \ .. " U+0000 U+001F U+007F : \|`)로 **C1이 없다**. 넓힌 집합이 쓰이는 곳은 **표지 분리뿐**이고, 그 방향으로 넓히면 C1에서 자르므로 요구 조각에 C1이 안 들어가 **C1을 남기는 구현도 통과한다**. 즉 N-03은 *거짓 실패*를 없앴을 뿐 **"C1이 파일명에서 제거된다"는 요구를 세우지 못했다.** `*`·`?`·`<`·`>`도 같다 — 선언 집합엔 있고 `absent` 니들엔 없으며 입력에도 없다 | **수정 필요** (도달 범위) | `compare_parity.py:936-940` · `dump_parity_fixtures.py:2038-2047`(`_FORBIDDEN_IN_FILENAME_SET`) vs `:2024-2034`(`dangerous`) | **N-02와 동시** — N-02가 닫히면 자동 해소되므로 F-3을 더 미루면 이 구멍이 함께 자란다 |
| **F-9** | **표지 배타 단언(B4-B)에 잠재 거짓 실패.** `exclusive_by_case`는 표지 **문자열 집합의 차집합**으로 계산하는데 단언은 **부분문자열**로 판정한다. 관계가 달라서, 새 제목이 기존 제목의 **접두를 공유**하면(예 `보고서요약본` ↔ `보고서요약본최종`) 짧은 쪽 표지가 긴 쪽 파일명에 실제로 들어 있어 **거짓 실패**한다. 현재 7제목엔 그런 쌍이 없어 초록이다. 게이트 13이 경계한 *"거짓 실패로 빨개지면 사람이 게이트를 약화시킨다"*의 씨앗 | 권고 | `dump_parity_fixtures.py:2062-2075` | Phase 4 착수 전 |
| **F-10** | **`allow_empty`의 소재 설명이 기제보다 넓다.** 주석은 *"그 선언을 케이스가 아니라 규칙 쓰는 쪽이 하게 해서"*라고 적었으나 실제로는 `call.arg("allow_empty", False)` — **fixture JSON의 단언별 데이터**다. 손으로 넣는 것을 막는 것은 그 문장이 아니라 **정본 대조(provenance)**다. 근거가 다른 장치에 있는데 이 문장이 안전을 보증하는 것처럼 읽힌다 | 권고 | `compare_parity.py:737-746` · `dump_parity_fixtures.py:1263-1275` | — |

**해소 확인 (parity 축)**

- **N-06 해소** — masking 84 → 85 케이스(V-7), `masking-card-rejected-overlaps-valid` 추가, `absent needles=["5054-9179-2961-1688"]`로 **거부-겹침 회귀를 직접 잰다**, `.github/parity-case-floor.txt`에 등재, `parity/reference-ledger/masking.json`에 `status: diverge` 기록. 차단①(게이트 12 #1)이 이제 게이트에 걸린다.
- **B4-A 해소** — `check_contains_derived`가 빈 요구를 기본 실패로 바꿨고, style 하한 둘만 `allow_empty=True`로 정당화됐다. export는 **케이스 이름이 아니라 유도 결과**(`markers_by_case[name]`)로 단언을 고르므로 X-3의 처방("이름으로 고르면 새 표본이 그 자리를 조용히 비운다")을 지켰다.

### 3.3 보안 불변식 — **판정 우선권은 `privacy-gate`**

| # | 항목 | 상태 | 근거 |
|---|---|---|---|
| N-20 | **해소** | 판정을 `has_visible_reason` 한 곳으로 모으고 `visible_length`(유니코드 카테고리 기반)로 센다. 억제(`_marker_touches:1163`)와 진단(`marker_problems:1200`)이 같은 함수를 쓴다 | `scan_privacy_invariants.py:154-206` |
| N-20 음성 | **상시** | `test_사유_유효성_3방향` 8케이스 파라미터화(ZWSP·WORD JOINER·ZWNJ·공백·NBSP·1글자 = 거절 / 정상·혼입정상 = 수락) + `test_보이는_문자_계수가_strip_과_다르다`(결함 기제 자체를 고정) + `test_사유_판정이_한_곳에서만_난다`(단일 판정점 강제) | `tests/test_privacy_scanner.py:1321-1383` |
| 하한 2 | 근거 안에 있다 | 관측 결함은 보이는 문자 **0개**이고 하한은 2 — 한 칸 여유가 공짜임을 실측(실제 표기 7건 최소 14자)으로 확인하고 **"문장부호만인 사유는 못 막는다"를 함께 적었다**. 규칙 6(범위가 근거를 넘지 않는가)을 지켰다 | `:158-170`, `visible_length` docstring |
| N-12 | **해소** | §4-terdecies.1 — 지표 4가 **항진명제**임을 인정하고 "5/5 충족"을 **실질 4/5**로 정정. 대조군(CARD 구판 0 → 신판 1)을 함께 둬 "차분 0이 무딘 측정 탓이 아님"을 보였다 |
| **N-13** | **미해소** | §4-terdecies.2가 규약을 정정했으나 **측정 스크립트는 여전히 미커밋**이고 *"스크래치패드에 실재한다(`Bundle.java`) — 세션 종료와 함께 사라질 자리"*라고 문서가 스스로 적는다. **재현 불가 상태가 지속되며, 시간이 지나면 영구화된다**(다시 써야 함) |
| **N-14** | **미착수** | §4-terdecies 어디에도 실문서 코퍼스 사전확률이 없다(`grep` 무소득). 게이트 13 §8.3 #7이 Phase 2 저지 항목으로 세운 것이다 |
| I-7·I-8·I-9 | **미검토(사유)** | 대상 코드가 Phase 3 이후. AEAD·JWT·Argon2 코드가 아직 없다 |
| 마스킹 선행 | **검토함 — 지적 없음** | 이 배치는 fixture·게이트만 건드렸고 마스킹 파이프라인 순서를 바꾸지 않았다 |

### 3.4 Kotlin/Spring 관용성 — **검토함 — 지적 없음**

이 배치의 Kotlin 코드 변경은 **0줄**이다(`git diff --stat` 대상 13파일 전부 Python 스크립트·fixture·문서·워크플로·테스트). 모듈 경계·트랜잭션·Flyway·LLM SDK 격리에 닿은 변경이 없다.

### 3.5 테스트 적정성

| # | 지적 | 심각도 | 근거 | 마감 |
|---|---|---|---|---|
| **F-5** | **B4·B5의 음성 대조가 상시가 아니다.** B4-B는 *"실증: union stem 주입 → exit 1 / 7건"*이 **커밋 메시지 1회성**이고, B4-A(빈 요구)와 N-03 정렬은 음성 대조가 **아예 없다**. `compare_parity`를 부르는 테스트는 `tests/test_parity_ci_gate.py` 하나뿐이고 그 안의 신규 7건은 **전부 full-gate 하한만** 본다(`grep -rn "allow_empty\|contains_derived\|FILENAME_FORBIDDEN\|title_markers" tests/` → 무관한 1건만). 게이트 13 **X-2**("새 게이트 장치 음성 테스트 0건")가 **새 장치 3종에서 그대로 재발**했다 | **수정 필요** | 위 grep · `bbbdb6b` 메시지 B4절 | Phase 4 착수 전 (X-2와 동일) |
| **F-7** | **사고 4건째의 형태가 구조로 남았다** — §5.3 참조 | 권고 | `tests/test_parity_ci_gate.py:328` | Phase 4 착수 전 |
| **F-8** | **신규 음성 7건 중 1건이 중복이다.** `test_선언_경로에서_표시가_없으면_막는다`(:290)는 `test_전체_경로에서도_하한이_돈다`(:275)와 **호출·상태가 완전히 같다** — 서명에서 `selected`가 사라지면서 두 "경로"가 하나가 됐는데 이름만 구분을 주장한다. "음성 5종/7종"으로 세면 **과대 계상**이다 | 권고 | `tests/test_parity_ci_gate.py:275-296` | 다음 커밋 |

**해소 확인 (테스트 축)**: B2의 7함수는 대조군을 포함하고, `_load_comparer`가 경로 부재 시 **skip이 아니라 실패**하도록 못박았다(`:222-225`) — "조용히 건너뛰는 테스트"를 만들지 않았다. `marker` fixture가 `FULL_GATE_PATH`와 `DECLARED_FLOOR_PATH`를 **함께** 격리한 것도 옳다(하나만 격리하면 다음 사람이 나머지를 그대로 쓴다).

---

## 4. 도달 범위 점검 (다섯 축을 가로지르는 필수 항목)

> 기준 전문은 `kotlin-migration` 스킬. 여기에는 결과만 적는다. **지적 없음과 미검토를 구분한다.**

| 점검 | 결과 |
|---|---|
| **"전역"·"모든"·"항상" 선언이 닿지 않는 경로** | **지적 있음 — F-4**(선언 집합은 C1까지 넓은데 실제로 재는 `absent` 니들은 9개 손열거) · **F-10**(`allow_empty` 소재 설명) |
| **그 게이트가 지금 어디서 도는가 — 도달 0을 특히 의심** | **해소 확인.** X-1의 도달 0이 닫혔다 — 러너 `kotlin` 잡 step 13이 실제로 돌았고(V-2) 로그가 전체 게이트 분기를 탄다(V-3). 신규 음성 7건도 러너 `quality` 잡에서 수집·통과(V-4). **그러나 F-4의 C1 정렬은 도달 0이고, N-13 측정 스크립트는 저장소 밖이라 도달 0이다** |
| **대리 경로에서 측정하지 않았는가** | **지적 없음.** B2가 CI 셸 대신 비교기 함수를 직접 부른 것은 대리가 아니라 **결함이 난 이음매를 정면으로 조준한 것**이고, 셸 쪽 가드는 기존 7함수가 그대로 본다. 파일 머리말이 남은 대리 경로 3종(추출본·러너 환경·휴리스틱 탐색)을 이미 명시한다 |
| **검사 기준이 검사 대상 자신에게서 나오는가** | **지적 없음.** `FILENAME_FORBIDDEN`을 `Export.kt`에서 읽지 않는 선택은 X-4형 순환 회피로 옳고, 그 대가(두 선언 드리프트)를 문서화했다. 드리프트 자기 신고 주장은 **실제로 양방향 모두 성립한다** — 생성기 표지 비었는데 비교기 안 비면 대체 이름 단언이 실패하고, 반대면 빈 요구로 실패한다. 다만 **비교기↔`Export.kt`** 방향은 자기 신고가 없다(F-4가 그 결과다) |
| **판정이 대리 지표로 이뤄지는가** | **지적 있음 — F-8**(중복 1건을 포함한 "7종"이 음성 커버리지의 대리 지표가 된다) · **§4-terdecies.1이 같은 결함을 스스로 잡아 정정했다**(지표 4 = 항진명제) |
| **규칙·패턴의 범위가 근거보다 넓은가 (은폐형은 넓히지 않는다)** | **지적 없음 — 오히려 모범.** §4-terdecies.2가 **은폐형 규약(위생 규약 4)을 자기가 넓혔음**을 인정하고 좁혔다. N-20의 하한 2도 근거(0개 관측)에 한 칸만 얹고 못 막는 것을 명시했다 |
| **음성 대조가 붙어 있는가 (떼면 무엇이 깨지는가)** | **지적 있음 — F-5.** B1·N-20은 상시 음성이 붙었고, **B4-A·B4-B·N-03은 붙지 않았다** |
| **판정하는 코드가 자기 자신을 검사 대상에 넣었는가** | **지적 없음.** `uv run mypy . .claude`·`ruff`가 `.claude` 루트를 덮고 러너 `quality` 잡이 통과했다(V-1). 신규 코드 전부 그 범위 안 |
| **범위 선언형 장치가 빈 선언에서 통과하는가** | **해소 확인.** `read_full_gate_mark`가 `exists()` → **내용 파싱**으로 바뀌어 `touch`한 빈 파일·주석 전용 파일이 막힌다. `domains`를 정본 개수와 대조한다 |

---

## 5. 리더 지정 특별 점검

### 5.1 게이트 13 차단·수정 전건의 해소 실측 (특별 점검 1)

| 전건 | 상태 | 음성 대조가 **상시**인가 | 근거 |
|---|---|---|---|
| **X-1 ① 유효 범위**(B1) | **해소** | **예** — `test_전체_경로에서도_하한이_돈다` | `compare_parity.py:1342-1400` · V-3 러너 도달 |
| **X-1 ② 내용 파싱**(B1) | **해소** | **예** — 빈 내용 / `domains` 불일치 2종 | `read_full_gate_mark:1291-1339` |
| **X-1 ③ 음성 테스트**(B2) | **해소** | **예** — 6종 + 대조군 (**명목 7종, 실질 6종** — F-8) | `tests/test_parity_ci_gate.py:275-348` |
| **#4 갈래 ⓐ 빈 요구**(B4-A) | 코드 해소 | **아니오** | `compare_parity.py:737-746` — 음성 테스트 0건 |
| **#4 갈래 ⓑ 입력↔출력 연관**(B4-B) | 코드 해소 | **아니오** — 커밋 메시지 1회성 | `dump_parity_fixtures.py:2062-2093` |
| **A 나머지 절반 — 거부-겹침**(B5) | **해소** | **예** — fixture 케이스가 곧 상시 회귀 | V-7 · 케이스 하한 등재 · 원장 diverge |
| **N-03**(B5 동반) | 코드 해소 · **도달 0** | **아니오** | F-4 |
| **N-20** | **해소** | **예** — 3방향 8케이스 + 기제 고정 + 단일 판정점 | `tests/test_privacy_scanner.py:1321-1383` |
| **U-2** | **해소** | 해당 없음(조사 항목) | 네 하한 파일 훑음 · `DECLARED_FLOOR_PATH`를 비교기도 읽게 됨 |
| **U-3** | **부분 해소** | 해당 없음 | 스캐너 2문장은 정정됐다(`Marker.reason` KDoc · `MARKER_RE` 주석). **비교기 docstring도 교체됐다.** 그러나 **같은 형태가 `ci.yml`에 새로 생겼다 — F-1** |

### 5.2 B1의 두 번 갈아탄 설계 — 진단이 옳은가 (특별 점검 2)

**진단은 옳다.** 커밋이 적은 공통 원인 — *"실행이 무엇을 골랐는가로 저장소의 상태를 판정하려 했다"* — 는 두 실패를 정확히 덮는다.

- 실패 ①(X-1): 기준이 `--only-domain` 목록이었고 CI는 8/8일 때 그 인자를 **주지 않으므로** 빈 목록 → `not selected` 즉시 반환.
- 실패 ②: "판정 범위"로 넓히자 `--only-domain masking` 같은 **정상적으로 좁힌 실행**이 전부 강등으로 걸렸다.
- 두 경우 모두 **입력이 실행의 선택**이었다. 선언 하한은 실행과 무관한 저장소 상태이므로 두 방향이 함께 닫힌다. **논리적으로 완결이다.**

**새 우회를 찾아봤다 — 실효 우회는 없다.**

| 시나리오 | 결과 | 막는 곳 |
|---|---|---|
| 표시 파일 삭제(8/8 경로) | `covers=True, marked=False` → **"하한을 고정하라"** | 비교기 (X-1 본체) |
| 표시 파일 `touch`(빈 파일) | `missing=[reached,domains]` → **"표시가 비어 있다"** | 비교기 ② |
| BUILDERS에 도메인 추가(선언 미갱신) | `covers=False, marked=True` → **"내려왔다"** | 비교기 |
| BUILDERS 추가 + 선언 갱신, 표시 미갱신 | `domains(8) != canonical(9)` → **"표시가 낡았다"** | 비교기 ③ |
| `parity-declared-floor.txt` 삭제 | `declared=∅` → `covers=False` → **"내려왔다"**. 셸에도 별도 가드(`test_선언_하한_파일이_없으면_실패한다`) | 비교기 + 셸 |
| `backend-kotlin/parity-domains.txt`만 축소 | 셸의 비감소 가드 → **"선언에서 사라졌다"** | **셸** |
| 선언·하한을 **함께** 축소 | 셸 가드 통과 → 부분 게이트 진입 → 비교기 `covers=False, marked=True` → **"내려왔다"** | **비교기** |
| `--only`로 케이스 골라 실행 | 의도적 면제(판단 근거 미비) | — |

**다만 강제가 두 파일에 갈려 있다는 점은 남는다.** 게이트의 **격(格) 결정**은 셸이 `backend-kotlin/parity-domains.txt`로 하고, **하한 판정**은 비교기가 `.github/parity-declared-floor.txt`로 한다. 위 표대로 지금은 두 가드가 서로의 구멍을 덮지만, **X-1이 난 곳이 바로 이 이음매**다. 그리고 그 이음매를 설명해야 할 `ci.yml` 주석이 지금 **거짓이다**(F-1). 구조적 위험이 남았다기보다 **그 구조를 적은 기록이 틀렸다**는 것이 이 회차의 지적이다.

| # | 지적 | 심각도 | 근거 | 마감 |
|---|---|---|---|---|
| **F-1** | **`ci.yml`의 강제 지점 기록이 거짓이다.** B3의 산출물 문장이 *"지금은 비교기가 판정 범위를 스스로 계산하므로(`effective_scope`)"*라고 적는데, ⑴ **`effective_scope`라는 이름은 저장소 어디에도 없다**(V-5 — 그 주석 한 줄이 유일한 출현), ⑵ 최종 설계는 "판정 범위"를 **명시적으로 폐기**하고 선언 하한으로 갈아탄 것이다(실패 ②). 즉 **버려진 설계를 현재 설계로 기술**한다. 이것은 U-3이 방금 고친 것과 **정확히 같은 형태**(결함/구식 설계를 의도처럼 적은 문장)이고, U-3의 논거 그대로 *"안 고치면 다음 사람이 이 근거로 되돌린다"*가 성립한다. 게다가 이 자리는 B3가 **일부러 기록을 남기려고 손댄 곳**이다 | **수정 필요** — 판정 필요(차단② 승격 여부) | `.github/workflows/ci.yml:441` · V-5 | **Phase 2 종료 판정 전** (§7.3 B3의 산출물이며, 강제 지점의 기록이 틀린 채로 종료 판정의 근거가 된다) |

### 5.3 사고 4건째 — 처리 적정성과 잔존 형태 전수 확인 (특별 점검 3)

**처리는 적정하다.** `marker` fixture가 표시 파일과 선언 하한을 **함께** `monkeypatch`로 tmp에 돌리고(`:253-268`), 사고 경위와 *"하나만 격리하면 다음 사람이 나머지를 그대로 쓴다"*는 이유까지 fixture docstring에 남겼다. 커밋 메시지에도 ⚠로 표시했다. 원장 384문단이 "4번째 오염, 새 형태 — 격리 수정"으로 기록한다.

**전수 확인 결과(V-11): 같은 형태의 직접 재발은 없다. 그러나 구조는 남았다.**

| # | 자리 | 상태 | 등급 |
|---|---|---|---|
| **F-7** | `tests/test_parity_ci_gate.py:328` — `comparer.DECLARED_FLOOR_PATH.write_text(...)`가 **모듈 전역 상수를 그대로 쓴다.** 지금은 `marker` fixture의 monkeypatch로 안전하지만, **그 fixture를 요청하지 않는 테스트가 하나 추가되면 같은 코드가 실물 `.github/parity-declared-floor.txt`를 덮어쓴다** — 사고의 정확한 형태다. 현재 7함수는 전부 `marker`를 받는다. 같은 저장소가 이미 구조적 해법을 갖고 있다: `tests/golden/test_baseline_gate.py:737-741`이 같은 사고를 겪고 **`path` 인자 필수화**로 막았다 | 격리는 fixture에 결속 — 다음 테스트가 상속하지 않는다 | **권고** (구조 개선) |
| F-7b | `tests/test_privacy_scanner.py:361-373` — 저장소 fixture 디렉터리에 `twin.*`를 만든다. `try/finally`라 assert 실패엔 견고하나 **SIGINT·크래시·병렬 실행엔 취약**하고, 남은 `twin.kt`가 다음 실행의 `iterdir()` 탐침 목록에 끼어든다. **같은 파일 887행은 이미 `tempfile`을 쓴다 — 두 자리가 갈려 있다** | 오염 파일 현재 없음 | 권고 |
| F-7c | `tests/golden/test_golden_eval.py:529` — `write_baseline(body)`가 `path` 없이 실물 `tests/golden/baseline.json`을 쓴다. 방어는 3중(`pytest.mark.llm` + `addopts="-m 'not llm'"` + `GOLDEN_RECORD_BASELINE` 옵트인)이라 확률은 낮으나 **셋 중 하나만 풀리면 제품 파일이 갱신된다** | 되돌림 장치 없음 | 권고 |
| — | `tests/test_harness_scope_reach.py` · `test_llm_lane_scope.py` · `tests/golden/test_baseline_gate.py` · `tests/scripts/**` · `tests/easyread/**` · `tests/api|workers|repositories|llm|services/**` | 쓰기 없음 또는 `tmp_path`/`tempfile` 격리 | **안전** |

**주의**: `llm-lane`이 아직 진행 중이므로 F-7c 경로는 이번 실행에서 아직 관측되지 않았다.

### 5.4 조건 1 미충족의 성격 — 규칙끼리 충돌한다 (특별 점검 1 부기)

| # | 지적 | 심각도 | 근거 |
|---|---|---|---|
| **F-6** | **조건 1 미충족 + R-9 재발 + 표기 누락.** `bbbdb6b`가 판정 도구(`full_gate_floor_problems`·`check_contains_derived`·`FILENAME_FORBIDDEN`)와 판정 대상(masking·style·export fixture)을 **한 커밋에** 담았다 — R-9가 지목한 형태다. B1 행이 요구한 *"제목에 「판정 도구 변경」 명시"*는 **0회**(V-12). 이탈 사유(*"두 스크립트가 모든 항목의 변경을 함께 지고 있어 **파일 단위로** 더 쪼갤 수 없었다"*)는 **요구가 파일 단위가 아니었으므로 성립하지 않는다** — `git add -p` 로 hunk 분할이 가능하다 | **수정 필요(절차)** | `git log`·`git show --stat` · V-12 |
| **F-6b** | **다만 같은 배치가 그 분할을 구조적으로 막는 규율을 새로 세웠다.** `142dac6`이 스테이징 경합 재발을 근거로 *"인덱스가 공유이므로 `git add` + `git commit`은 안전하지 않고… **`git commit -- <경로>` 한 단계만 쓴다**"*를 규율로 삼았다. 이는 **경로 단위 커밋**을 강제하므로 **hunk 분할과 양립하지 않는다.** 두 규칙(조건 1 = 항목별 커밋 / 새 규율 = 경로 단위 커밋)이 **이 배치에서 처음 충돌했고**, 커밋 메시지의 "파일 단위로 못 쪼갠다"는 진술은 사실 이 충돌의 결과다 — 사유로 적히지는 않았다 | **판정 필요** | `142dac6` 메시지 R-9절 · `bbbdb6b` 메시지 머리말 |

> **리더 판단 요청**: 두 규칙 중 무엇이 우선하는가. ⓐ 항목별 커밋 우선(병렬 레인을 직렬화하거나 워크트리 분리) / ⓑ 경로 단위 커밋 우선(조건 1을 "파일이 겹치지 않는 범위에서"로 완화하고 명시) / ⓒ 둘 다 유지하고 겹칠 때만 이탈을 사유와 함께 기록. **묵시적으로 ⓒ를 택한 상태인데 사유가 실제 원인을 안 적었다.**

### 5.5 원장·판정문·리뷰 산출물의 최종 자기 정합 (특별 점검 5)

| 항목 | 상태 | 근거 |
|---|---|---|
| **회차 기록 공백 03~06** | **해소** | `00_progress.md:361-364` — 「리뷰 게이트 이력」표에 4행 소급 기재, 표 머리말에 소급 사유 명시 |
| **회차 기록 공백 12** | **해소** | `:382` 산문 문단 (대상 `cd23aec..516c0e9`·차단 2건·정본 경로) |
| 회차 10 | **기록 있음** | `:376` 게이트 09 파동 문단 안에 인라인 |
| 회차 13·14 | 기록 있음 | `:384`·`:386` |
| **F-2 · 단언 수 갈림** | **미해소 — 새 갈림 발생** | 같은 문서 안에서 `:384`(153/630)와 **`:352`(152/608)**가 갈린다. `.github/parity-full-gate.txt` 도달 기록도 **152건/608개** 그대로(V-8). 게이트 13 §5가 잡은 584↔608이 **608↔630으로 재발**했고, 하필 **L352가 Phase 2 종료 조건 행 자신**이다 |
| **F-11 · 이력 표가 09에서 끊긴다** | 권고 | 「리뷰 게이트 이력」표는 03~09 7행뿐이고 10~14는 표 밖 산문이다. 표만 읽으면 12회차 중 **7회차만** 보인다. 이 저장소가 *"표에서 떨어진 행은 검사기 눈에서 사라진다"*를 근거 사고로 갖고 있는데(`test_harness_scope_reach.py` C절), **이 표는 `EXPECTED_TARGET_TABLES` 4개에 들지 않아 어떤 검사기도 보지 않는다** |
| **F-12 · 실행 경로 열이 낡았다** | **수정 필요** | L342(마스킹)·L350(fixture 생성)·L346·L347의 `실행 경로`가 여전히 `local:…--only-domain masking` 형태다. 그러나 러너가 **8도메인 전부를 `ci:kotlin`에서** 돌린다(V-3: `masking · masking.json — 성질 85건/단언 308개`). 이 열은 *"이 게이트가 지금 어디서 도는가"*에 답하는 자리인데 **CI 도달이 생긴 뒤로 갱신되지 않아 과소 신고 상태**다. `local:`은 도달 0을 드러내는 표기이므로, 도달이 생겼는데 그대로 두면 표기가 거짓이 된다 |

> **F-12 주의**: `실행 경로` 열의 표기를 고치면 `EXPECTED_REACH_TOKENS`(현재 52)가 함께 갱신돼야 한다. `충족` 열만 바꾸는 것과 **별개 작업**이다.

---

## 6. Phase 2 종료 조건 대비 현황 (특별 점검 4)

### 6.1 게이트 13 §8.3 「막는 것 10건」의 이 회차 상태

| # | 항목 | **상태** | 근거 |
|---|---|---|---|
| 1 | B — 8커밋 미푸시·CI 첫 실행 없음 | **해소** | HEAD == origin. run 31854263996에서 `kotlin`·`quality`·`frontend` 성공(V-1), parity step 실제 실행(V-2·V-3) |
| 2 | **X-1** — 전체 게이트 하한 CI 미발화 | **해소** | 설계 재설계 + 상시 음성 + **러너 도달 실측** |
| 3 | N-06 — 게이트가 차단①을 안 잼 | **해소** | 거부-겹침 케이스 + 하한 등재 + 원장 |
| 4 | N-12 — 움직일 수 없는 지표 | **해소** | §4-terdecies.1 |
| 5 | **N-13** — 4조합 차분 재현 불가 | **미해소** | 규약은 정정, **스크립트 미커밋**(스크래치패드 상주 — 사라질 자리) |
| 6 | N-20 | **해소** | 코드 + 상시 8케이스 3방향 |
| 7 | **N-14** — 코퍼스 사전확률 | **미착수** | §4-terdecies에 없음 |
| 8 | **R-10** — 이월이 사람 전사에만 의존 | **미집행** | `tests/test_harness_scope_reach.py` **무변경**(V: `git diff --name-only` 0건). §7.3의 P3(정체성 키 장치)가 문서에서 **P3 라벨을 N-20 판정에 재사용**하면서 밀려났다 — **"항목이 조용히 사라진다"를 막으려던 장치가 조용히 사라진 것**이라 형태가 그 자체로 R-10의 증거다 |
| 9 | §5 원장 | **부분 해소** | 회차 결손 해소(§5.5). **L352 수치 미정정 + 새 갈림(F-2) + 실행 경로 낡음(F-12)** |
| 10 | X-5 | **판정됨** | 목적 축 채택 |

**신규 저지 후보 (이 회차)**: F-1(ci.yml 거짓 강제 지점 — 마감 Phase 2 종료 판정 전) · F-2(원장·표시 파일 수치) · F-6b(규칙 충돌 판정).

### 6.2 §5 Phase 2 종료 조건 축별

| 축 | 게이트 13 | **이 회차** | 변동 근거 |
|---|---|---|---|
| 외부 API·DB 없이 도는가 | 충족 | **충족** | — |
| 전 도메인이 도는가 | 충족 | **충족** | 러너 8/8 |
| 값·성질 비교가 실제로 도는가 | 충족(범위 한정) | **충족 — 범위 한정 폭이 줄었다** | X-3·X-4 두 갈래가 코드로 닫혔다(단언 608→630). 남은 한정: **export 파일명 축의 C1·`*?<>`가 무단언**(F-4), N-02 미집행(F-3) |
| **게이트가 재는 것을 실제로 재는가** | 미충족 | **충족(코드) — 기록 미충족** | X-1 해소·러너 도달. **다만 그 강제 지점을 적은 문장이 거짓이다**(F-1) |
| 그 게이트가 실제로 도는 곳에서 돌았는가 | 미검증 | **충족** | V-2·V-3 러너 실측 |
| 원장이 상태를 정확히 적는가 | 미충족 | **미충족** | F-2·F-12 (회차 결손은 해소) |
| 마스킹 정책 불변식이 회귀 없이 유지되는가 | 충족(코드)·게이트 미측정 | **충족 — 게이트가 잰다** | N-06 해소로 차단①이 fixture에 걸렸다 |

### 6.3 Phase 2 표 최종 권고 — 예로 올릴 행 / 유지할 행

| 행 | 게이트 13 권고 | **이 회차 최종 권고** | 근거 |
|---|---|---|---|
| L342 마스킹 포팅 | N-06 닫은 뒤 `예` | **`예` 가능** | N-06 해소. **단 실행 경로 열을 함께 고칠 것**(F-12) |
| L343 텍스트 정규화 | `예` 가능 | **`예` 가능** | — |
| L344 프롬프트 | `아니오` 유지 | **`아니오` 유지** | X-9 미해소 |
| L345 스타일 규칙 | `아니오` 유지 | **`아니오` 유지** | X-9 + 게이트 11 C-3·C-4 |
| **L348 내보내기** | X-5 판정 대기 | **⚠ 판정 필요 — 나는 `아니오` 유지를 권고한다** | X-5가 ⓐ로 판정돼 게이트 13 기준으로는 `예`가 열렸다. **그러나 이 회차에 F-3(N-02 미집행·미기록)과 F-4(C1 요구 무단언)가 새로 섰다.** 이 행이 주장하는 것이 "파일명 생성 포팅"인데 파일명 금지문자 요구의 일부가 **어느 단언에도 안 걸린다.** 게이트 13이 몰랐던 사실이므로 리더 재판정 대상 |
| L349 LLM 응답 후처리 | `예` 가능 | **`예` 가능** | — |
| L350 fixture 생성 | `예` 가능 | **`예` 가능 (수치·실행 경로 선정정)** | 8/8 생성. masking 85케이스/308단언로 현행화 + F-12 |
| L351 선언 | `예` 가능 | **`예` 가능 (미해결 셀 선정정)** | — |
| **L352 종료 조건** | `아니오` 유지 | **`아니오` 유지** | **이 행 자신이 낡은 수치(152/608)를 근거로 든다**(F-2). 그 밖에 N-13·N-14·R-10·F-1 미해소. B와 X-1은 해소됐으므로 근거는 게이트 13보다 좁아졌다 |

> **공통 전제(게이트 13에서 계승)**: 어느 행을 올리든 **원장 오류를 먼저 고친다.** 이번엔 F-2가 추가됐다 — L352를 안 올리더라도 그 셀의 수치는 고쳐야 한다. 고치지 않으면 「단언 630」을 근거로 든 판정문과 「608」을 든 종료 조건 행이 같은 문서에서 서로를 부정한다.

### 6.4 `EXPECTED_MET_YES_KEYS` 갱신 목록 — **하네스 함수 실측 문자열**

> `tests/test_harness_scope_reach.py`를 모듈로 적재해 `parse_tables` → `identity_pair(table.caption, row[0])`를 **직접 호출**해 얻은 값이다. **손으로 적지 말고 이 문자열을 그대로 쓴다** — 40자 클립이 걸린 행이 3개 있다.

**현재 Phase 2 등재분 2개** (변경 없음)

```python
("Phase 2 — 순수 도메인 로직 포팅", "placeholder 보존 검사 포팅"),
("Phase 2 — 순수 도메인 로직 포팅", "보정 채택 판정 포팅"),
```

**§6.3 권고대로 올릴 경우 추가할 5개**

```python
("Phase 2 — 순수 도메인 로직 포팅", "개인정보 마스킹 포팅 (`app/privacy/masking.py`)"),
("Phase 2 — 순수 도메인 로직 포팅", "텍스트 정규화·제어문자 제거 포팅"),
("Phase 2 — 순수 도메인 로직 포팅", "LLM 응답 후처리 포팅"),
("Phase 2 — 순수 도메인 로직 포팅", "Python/Kotlin 공용 JSON fixture 생성 (`parit"),   # ← 40자 클립
("Phase 2 — 순수 도메인 로직 포팅", "도메인마다 `backend-kotlin/parity-domains.txt"),  # ← 40자 클립
```

**보류·유지 행의 키 (올릴 때만 추가 — 지금은 넣지 않는다)**

```python
# L348 — 리더 재판정 대상(F-3·F-4)
("Phase 2 — 순수 도메인 로직 포팅", "내보내기 파일명·`Content-Disposition` 생성 포팅"),
# L344 · L345 — 아니오 유지
("Phase 2 — 순수 도메인 로직 포팅", "프롬프트 렌더링과 동적 어려운 말 목록 포팅"),
("Phase 2 — 순수 도메인 로직 포팅", "스타일 규칙 포팅 (`app/easyread/style_rules.py"),  # ← 40자 클립
# L352 — 아니오 유지
("Phase 2 — 순수 도메인 로직 포팅", "종료 조건: 외부 API·DB 없이 도는 parity suite 가 양쪽"),  # ← 40자 클립
```

**함께 확인할 것**

1. `충족` 셀의 **본문**을 바꿔야 `met_verdict`가 `True`가 된다 — 현재 L350·L351은 `아니오 — 8/8 …` 형태다.
2. `EXPECTED_ROWS`(45)는 행 수라 변동 없다. **`EXPECTED_REACH_TOKENS`(52)는 F-12를 집행하면 바뀐다** — 실행 경로 표기를 고치는 커밋에서 함께 갱신한다.
3. 이 상수를 고치는 diff 자체가 *"판정 범위를 건드렸다"*는 신호다(상수 docstring). 리뷰에서 근거를 요구할 것.

### 6.5 CI 상태 — 리더 기술 대비 실측 정정

| 리더 기술 | **실측** |
|---|---|
| 1차 kotlin·frontend 통과 + quality 재실행 통과 | run 31838885595(`cb26a77`) = **failure**(quality의 `ruff format --check`만 실패, kotlin·frontend 성공). run 31853472768(`4dafde5`) = **quality·kotlin·frontend 전부 success**, llm-lane만 cancelled → 리더 기술 맞음 |
| 2차 llm-lane 진행 중 | run 31853472768의 llm-lane은 **cancelled**(진행 중 아님) |
| 3차 배치 실행 발화 | **발화를 넘어 3잡이 이미 통과했다** — run 31854263996(`bbbdb6b`): kotlin ✅ · quality ✅ · frontend ✅ · llm-lane **진행 중**. **이 회차 판정의 가장 강한 근거이므로 정정해 올린다** |

---

## 7. 조치 목록 — 심각도 · 담당 · 마감

**A. Phase 2 종료 판정을 막는 것**

| 순 | 항목 | 심각도 | 담당 | 마감 |
|---|---|---|---|---|
| 1 | **F-1** — `ci.yml` 강제 지점 기록이 거짓(`effective_scope` 부재 + 폐기 설계 기술) | **수정 필요 · 차단② 승격 판정 필요** | `kotlin-implementer` | **Phase 2 종료 판정 전** |
| 2 | **F-2** — 단언 630이 `parity-full-gate.txt`·원장 L352에 미반영, 문서 내 자기모순 | **수정 필요** | **리더** | **Phase 2 종료 판정 전** |
| 3 | **N-13** — 측정 스크립트 미커밋(스크래치패드 상주, 소실 임박) | 수정 필요 | `kotlin-implementer` (§4-terdecies.2 보존 지침대로) | **Phase 2 종료 판정 전** |
| 4 | **N-14** — 실문서 코퍼스 사전확률 | 판정 필요 | `privacy-gate` | Phase 2 종료 판정 전 |
| 5 | **R-10** — 미해결 항목 정체성 키 장치 미집행 | 수정 필요 | `kotlin-implementer` + 리더 | Phase 2 종료 판정 전 |
| 6 | **F-12** — 실행 경로 열이 CI 도달을 과소 신고 | 수정 필요 | **리더** | Phase 2 종료 판정 전 (표를 고치는 그 커밋에서) |
| 7 | **F-6b** — 「항목별 커밋」 vs 「경로 단위 커밋」 규칙 충돌 | **판정 필요** | **리더** | Phase 2 종료 판정 전 |
| 8 | **L348 재판정** — F-3·F-4를 알고도 `예`로 올릴 것인가 | 판정 필요 | **리더** | Phase 2 종료 판정 전 |

**B. 막지 않는 것**

| 항목 | 심각도 | 담당 | 마감 |
|---|---|---|---|
| **F-3** — N-02 미집행·이탈 미기록 | 수정 필요 | `parity-verifier` | Phase 4 착수 전 |
| **F-4** — N-03 도달 0 (C1·`*?<>` 무단언) | 수정 필요 | `parity-verifier` | **N-02와 동시** |
| **F-5** — B4-A·B4-B·N-03 음성 대조 비상시 (X-2 재발) | 수정 필요 | `kotlin-implementer` | Phase 4 착수 전 |
| **F-6** — 조건 1 미충족 + R-9 표기 누락 | 수정 필요(절차) | 전원 | 다음 커밋부터 |
| **F-7 / F-7b / F-7c** — 테스트의 실물 파일 쓰기 구조 | 권고 | `kotlin-implementer` | Phase 4 착수 전 |
| **F-8** — 음성 테스트 1건 중복 | 권고 | `kotlin-implementer` | 다음 커밋 |
| **F-9** — 배타 단언의 접두 거짓 실패 | 권고 | `parity-verifier` | Phase 4 착수 전 |
| **F-10** — `allow_empty` 소재 설명 | 권고 | `parity-verifier` | — |
| **F-11** — 리뷰 게이트 이력 표가 09에서 끊김 | 권고 | 리더 | — |
| 게이트 13 이월 — X-2 잔여·N-17·N-19·N-04·N-07·N-08·N-10·N-18 | 수정 필요/권고 | 각 담당 | Phase 4 착수 전 |

---

## 8. 미실행 · 확인 불가

| 범위 | 상태 |
|---|---|
| **codex 교차 대조** | **미수행 — 1차이므로 정상.** 2차 재호출에서 `..._cross.md`로 수행한다 |
| `llm-lane` 잡 결과 | **관측 중** — run 31854263996에서 진행 중. 결과가 나오면 F-7c 경로와 함께 확인 필요 |
| 계약 축(RFC 5987·`{"detail":…}`·snake_case·no-store·소유권 404) | **미검토** — 이 배치가 대상 코드를 건드리지 않았다 |
| I-7·I-8·I-9 (AEAD·JWT·Argon2) | **미검토** — 대상 코드가 Phase 3 이후 |
| `tests/golden` 골든셋 | **미실행** — 이 배치는 프롬프트·스타일 규칙·LLM 설정을 바꾸지 않았으므로 `CLAUDE.md` 실행 요건에 걸리지 않는다. 다만 masking 케이스 1건 추가가 LLM에 가는 본문을 바꾸지는 않음을 확인했다(fixture 전용) |
| 범용 품질 축(성능·유지보수성·일반 보안) | **범위 밖** — `multi-review` 별도 실행을 리더에게 권고한다(**6회차 연속 권고**) |
| 게이트 08·09 집계 불일치 원인 추적 | 게이트 13에서 미검토로 남았고 이 회차에서도 하지 않았다 |
| `parity-full-gate.txt` 외 하한 파일의 값 소비(U-2 잔여) | 커밋이 4파일을 훑었다고 적었고 코드로 확인했으나, **`parity-case-floor.txt`의 케이스 id가 실제로 전건 대조되는지는 이 회차에서 재실행하지 않았다** |

---

## 9. 다음 단계

1. 리더는 **§7 A의 8건**을 처분한다 — 특히 **F-1·F-2는 Phase 2 종료 판정 전**이다.
2. `codex-reviewer`를 같은 어간(`14_floor-hardening`)으로 실행하고, 그 원본과 이 문서를 입력으로 **2차 교차 종합**을 재호출한다.
3. **이 문서 단독으로 Phase 2 종료 조건 충족을 보고하지 않는다.** 정본은 `14_floor-hardening_cross.md`다.
