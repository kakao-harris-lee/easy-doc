# 07_core-rebuild 회부 5건 — `privacy-gate` 판정

> **작성**: 2026-08-13 · **어간**: `07_core-rebuild` · **회부처**: `reviews/07_core-rebuild_cross.md` §4(충돌 2건) · §6(조치 순위 3·10)
> **대상 커밋 범위**: `c11a404..f73879b` (5커밋)
> **역할 경계**: 이 문서는 **판정만 한다.** 코드를 고치지 않았고, 저장소의 어떤 제품 파일도 수정하지 않았다(§6 위생 참고).

---

## 0. 판정 요약

| # | 항목 | 판정 | 마감 | 수신자 |
|---|---|---|---|---|
| 1 | X-2 전각 주민등록번호 미검출 | **수정** (불변식 1 커버리지 **위반** / 순서 **준수**) | KDoc **즉시** · 구현 **Phase 2 종료 전** | `kotlin-implementer` → `parity-verifier` |
| 2 | X-5 provenance 래퍼 공개 생성자 | **수용(조건부)** — 타입 봉쇄 기각, **사용 규약 명문화 + 탐지기**를 조건으로 | 규약 **즉시** · 탐지기 **application 조각과 동시** | `kotlin-implementer` |
| 3 | X-8 `scan_privacy_invariants.py` CI 미배선 | **수정 — 마감을 Phase 3 → 지금으로 당긴다** | **이번 커밋 범위와 함께** | 리더(승인) → `kotlin-implementer`(배선) |
| 4 | X-11 `baseUrl` 노출 · 응답 크기 상한 부재 | **수정 — Phase 5 유지, 단 근거 정정 + 해제 조건 신설** | **Phase 5** (당기지 않는다) | `kotlin-implementer` |
| 5 | `ModelDraft.toString()` 본문 노출 (추가 회부 · §4-bis) | **수정** (불변식 4 **잠정 위반**) — 회부된 1종이 아니라 **value class 3종 + 스캐너 목록** | **즉시** | `kotlin-implementer` |

**§5 Phase 7 즉시 중단 기준 해당: 0건.** 다섯 중 어느 것도 "마스킹 전 본문이 LLM·로그로 전송됨"에 해당하지 않는다 — 판정 1~4는 현재 저장소에 **provider를 부르는 프로덕션 경로가 없어서**(회부 시점 실측), 판정 5는 **세 타입을 로거 인자로 넘기는 줄이 실재하지 않아서**다. 따라서 **차단 사유서(`07_privacy-gate_blocking.md`)를 만들지 않는다.**

> **회차 주기**: §1~§4는 2026-08-13 회부(`c11a404..f73879b`) 판정. **§4-bis는 2026-08-14 추가 회부**(`f73879b..6d8e88c`, application 조각) 판정이다. 그 사이 `kotlin-implementer`가 §1~§4의 조치를 대부분 닫았으므로, 각 절의 상태 주석을 함께 읽을 것.

---

## 1. 판정 1 — X-2 전각 주민등록번호 (충돌 2)

### 1.1 실측이 두 리뷰보다 넓다 — 먼저 이것부터

두 리뷰와 fixture는 이 결함을 **"성별코드 `[1-8]`이 ASCII라 전각 RRN이 통과한다"** 하나로 서술했다. JDK 21 단일 파일 실행으로 두 패턴을 전수 투입한 결과, **결함은 하나가 아니라 두 종류이고 카드번호에도 있다.**

실행: `java Probe2.java` (스크래치패드, 저장소 미수정). 패턴은 `Masking.kt:155-156`을 그대로 옮기고 `UNICODE_CHARACTER_CLASS`를 켠 것.

**주민등록번호 (`(?<!\d)\d{6}[ \t]*-?[ \t]*[1-8]\d{6}(?!\d)`)**

| 입력 (전부 합성값 — fixture 자신의 표기를 따랐다) | 결과 |
|---|---|
| `900101-1234567` (ASCII 하이픈) | MATCH |
| `900101 1234567` (일반 공백 U+0020) | MATCH |
| `９００１０１-１２３４５６７` (전각 숫자) | **NO MATCH** |
| `900101-１234567` (성별코드만 전각) | **NO MATCH** |
| `９００１０１-1234567` (앞 6자리만 전각) | MATCH |
| `900101－1234567` (전각 하이픈 U+FF0D) | **NO MATCH** |
| `900101−1234567` (마이너스 U+2212) | **NO MATCH** |
| `900101–1234567` (엔 대시 U+2013) | **NO MATCH** |
| `900101<NBSP>1234567` (U+00A0) | **NO MATCH** |
| `900101<IDSP>1234567` (전각 공백 U+3000) | **NO MATCH** |
| 아라비아-인도 숫자 표기 | **NO MATCH** |

**카드번호 (`(?<!\d)\d{4}[- ]?\d{4}[- ]?\d{4}[- ]?\d{4}(?!\d)`)**

| 입력 | 결과 |
|---|---|
| `1234-5678-9012-3456` | MATCH |
| `１２３４-５６７８-９０１２-３４５６` (전각 숫자 + ASCII 하이픈) | **MATCH** |
| 전각 숫자 16자리 무구분 | **MATCH** |
| 아라비아-인도 숫자 16자리 무구분 | **MATCH** |
| `1234－5678－9012－3456` (전각 하이픈) | **NO MATCH** |
| `1234<NBSP>5678<NBSP>9012<NBSP>3456` | **NO MATCH** |
| `1234<IDSP>5678<IDSP>9012<IDSP>3456` | **NO MATCH** |

### 1.2 빈자리를 **종류로** 댄다 (CLAUDE.md 규칙 4 ⑴)

결함의 구조는 하나다 — **유니코드 인식 패턴 안에 남은 ASCII 전용 리터럴.** 두 갈래로 갈린다.

- **종류 A · ASCII 전용 숫자 클래스** — RRN 성별코드 `[1-8]`. `\d`는 `UNICODE_CHARACTER_CLASS`로 전각·아라비아-인도를 잡는데 이 한 자리만 ASCII라 **매치가 끊긴다.** 그래서 "앞 6자리만 전각"은 잡히고(끊기는 자리를 안 지나감) "성별코드만 전각"은 안 잡힌다. 카드번호에는 숫자 클래스가 전부 `\d`뿐이라 **종류 A가 없다.**
- **종류 B · ASCII 전용 구분자 리터럴** — RRN의 `[ \t]`·`-`, 카드의 `[- ]`. **두 패턴 모두에 있다.** 전각 하이픈·마이너스·엔 대시·NBSP·전각 공백이 전부 뚫는다.

`searchView`(`Masking.kt:282`)는 이 둘을 못 막는다. `INVISIBLE_RANGES`(`:249-260`)는 **보이지 않는** 문자만 지우는데, 전각 숫자·전각 하이픈·NBSP·전각 공백은 **보이는** 문자다. 선행 판정 `02_privacy-gate_control-char-verdict.md` §5.3이 *"`\d` 문제와 **독립**이다 … 두 수정이 모두 필요하다"*고 이미 예고한 자리다.

### 1.3 ① 전각 표기가 범주 "주민등록번호"에 포함되는가 — **포함된다**

**근거.**

1. master-plan 3.2(2026-08-12)가 좁힌 것은 **범주**다 — 5종에서 2종으로. `CLAUDE.md`가 금지한 것도 범주 재확대다: *"뺀 3종을 다시 잡도록 패턴을 넓히면 그것은 개선이 아니라 정책 위반이다."* 뺀 3종은 **전화번호·이메일·계좌번호**이고, 전각 주민등록번호는 그중 어느 것도 아니다. **표기 체계를 넓히는 것은 범주를 넓히는 것이 아니다.** Claude P-2의 정리가 이 점에서 옳다.
2. 범주는 데이터가 **무엇인가**로 정의된다. `MaskCategory.RRN`의 KDoc(`Masking.kt:15-18`)과 계약(`MaskedItemResponse`)이 못박은 것은 라벨 `"주민등록번호"`이지 "ASCII로 적힌 주민등록번호"가 아니다. 전각으로 적힌 고유식별정보는 여전히 고유식별정보다.
3. **저장소가 이미 이 해석을 선언해 두었다.** `UnicodeRegex.kt:17-20`이 *"주민등록번호가 전각 숫자로 적혀 있으면 ASCII `\d`는 통째로 놓친다. 놓친 개인정보는 그대로 외부 모델로 나간다"*고 적고, 그것을 막으려고 `UNICODE_CHARACTER_CLASS`를 켰다. 요구사항 해석은 이미 내려져 있었고 구현이 거기 못 미친 것이다.
4. fixture가 든 제외 사유 — *"Kotlin에 Python보다 넓은 구현을 요구하게 되므로"* — 는 **2026-08-12 재개발 전환으로 실효했다.** `CLAUDE.md`가 *"현재 Python 구현의 출력을 정답으로 삼아 Kotlin 결과를 거기에 맞추기"*를 금지 목록에 올렸다. 이 점에서 codex #1의 정리가 옳다. **두 리뷰는 서로 다른 조항을 짚었고 둘 다 참이다.**

**소관 정리.** fixture가 적은 *"`privacy-gate` 판정 §5.4의 범위 밖"*은 `02_privacy-gate_control-char-verdict.md` §5.4 마지막 줄(*"`masking-known-gap-rrn-fullwidth`(전각)는 이 판정의 범위 밖이다. 별개 사안이며 여기서 판정하지 않는다"*)을 가리킨다. 그것은 **"privacy-gate 소관이 아니다"가 아니라 "그 판정 문서에서 다루지 않는다"**는 뜻이다. 별개 사안으로 미뤄 둔 것을 지금 이 문서가 받는다. **소관 확정: `privacy-gate`.** fixture와 Claude 라우팅은 충돌하지 않았고, fixture 문장이 오해를 부른 것이다(§1.6에서 수정 대상으로 올린다).

### 1.4 ② 구현 방향 — **문자 클래스 확장. 마스킹 전 정규화는 기각한다**

**정규화를 기각하는 근거는 선행 판정에 이미 있다.** `02_privacy-gate_control-char-verdict.md` §5.1: *"**입력 문자열을 정규화해서 마스킹에 넘기면 안 된다.** 그렇게 하면 `restores_input`이 깨지고, 복원이 깨지면 내보내기가 잘못된 원문을 꽂는다."* 이 제약은 표기 정규화에도 그대로 걸린다 — 전각을 ASCII로 접어 마스킹하면 `MaskedItem.original`이 접힌 값이 되고, `restoreForExport`가 사용자 본문을 **다른 글자로** 되돌린다.

"탐색용 뷰에서 접기"(`searchView`처럼 좌표 대응을 유지하는 방식)는 §5.1이 허용한 형태이지만, 이 건에는 **채택하지 않는다.** 이유 셋:

1. **카드번호를 못 고친다.** 실측대로 카드번호의 전각 숫자는 **이미 잡힌다.** 카드 쪽 결함은 전부 종류 B(구분자)인데, 숫자 접기 뷰는 구분자를 건드리지 않는다. 뷰 방식을 택하면 구분자 접기까지 뷰에 넣어야 하고, 그 순간 뷰가 "무엇을 접는지" 목록이 패턴과 이중으로 관리된다.
2. **종류 A는 뷰 없이 더 완전하게 닫힌다.** 성별코드를 `\d`로 바꾸고 매치 후 **값이 1~8인지** `Character.digit(c, 10)`으로 판정하면 전각뿐 아니라 **모든 유니코드 십진 숫자 체계**가 한 번에 덮인다. 전각만 열거하는 방식보다 넓고, 열거 누락이 재발하지 않는다.
3. **뷰 경로는 이미 있고 목적이 다르다.** `searchView`는 "보이지 않는 문자"를 지우는 장치다. 여기에 표기 접기를 얹으면 한 장치가 두 불변식을 지게 되어, 나중에 한쪽을 고칠 때 다른 쪽이 조용히 깨진다.

**구현 지침 (`kotlin-implementer`)**

- **종류 A.** `Masking.kt:155`의 `[1-8]`을 `\d`로 바꾸고, `candidateSpans`가 낸 스팬을 `spans`에 넣기 전에 **성별코드 위치 문자의 십진값이 1..8인지** 판정한다. 판정에 실패한 스팬은 **버리되 구간을 점유하지 않는다** — 점유하면 뒤이은 CARD 패턴이 같은 자리를 못 잡는다(현재 `:350`의 겹침 검사가 `spans`만 보므로, 추가하지 않으면 자동으로 안전하다).
  - **과잉 마스킹 회귀 가드 필수.** 성별코드 `9`·`0` 거부는 이미 단언돼 있다(`MaskingTest.kt:68-86`). 이 단언이 **전각 `９`·`０`에 대해서도** 성립해야 한다 — 값 판정으로 바꾸면 자동으로 성립하지만, 단언 없이 두면 다음 회차에 되돌아간다.
- **종류 B.** RRN의 `[ \t]`·`-`와 CARD의 `[- ]`를 **명시적으로 열거한 문자 집합**으로 넓힌다. 최소 집합:
  - 하이픈류: `-`(U+002D), `－`(U+FF0D), `−`(U+2212), `–`(U+2013), `—`(U+2014), `‐`(U+2010)
  - 공백류: `U+0020`, `\t`, `U+00A0`(NBSP), `U+3000`(전각 공백), `U+2007`, `U+202F`
  - **`\s`를 쓰지 마라.** 개행이 들어가면 서로 다른 줄의 숫자열이 붙어 **진짜 과잉 마스킹**이 된다 — `02_privacy-gate_control-char-verdict.md` §5.2가 같은 이유로 탭·개행을 뺐다. 열거 집합에 개행·캐리지리턴을 넣지 않는다.
  - 이 집합은 `Masking.kt` 안에 **상수 하나**로 두고 RRN·CARD가 함께 참조한다. 두 벌로 적으면 다음 확장에서 한쪽만 늘어난다(`CATEGORY_ALTERNATION`이 같은 이유로 이미 그렇게 돼 있다 — `:199-200`).
- **오프셋·복원 영향: 없다.** 두 수정 모두 **패턴만** 바꾸고 파이프라인 단계를 추가하지 않는다. `escapeLookalikes` → `searchView` → `candidateSpans` → 원문 좌표 절단 순서가 그대로이고, `MaskedItem.original`은 계속 **원문에서** 잘린다(`:370`). 따라서 `restores_input`은 유지되고 마스킹 선행 불변식도 건드리지 않는다.
  - **단 하나 확인할 것**: `escapeLookalikes`의 KDoc(`:216-221`)이 *"`!`는 `[[`와 한글 라벨 사이에만 들어가는데, 주민등록번호·카드번호 패턴은 **숫자와 `-`·공백만** 잇기 때문에 어떤 매치도 그 자리를 가로지르지 않는다"*고 근거를 적고 있다. 구분자 집합을 넓혀도 이 문장은 성립하지만(`!`·`[`·`]`·한글을 넣지 않으므로), **넓힌 집합에 `!`·`[`·`]`가 들어가지 않는지** 구현 시 확인하고 KDoc의 "`-`·공백" 표현을 새 집합에 맞춰 갱신한다.

### 1.5 ③ 카드번호 쪽 표기 공백 — **있다. 단 종류가 다르다**

§1.1·§1.2가 실측으로 답한다. **카드번호에는 종류 A가 없고(전각 숫자 이미 검출) 종류 B만 있다.** 두 리뷰 어디도 카드번호를 보지 않았으므로 이것은 이 판정의 신규 지적이고, 교차 검증을 받지 않았다 — `parity-verifier`가 fixture로 재현해 확정하는 것을 해제 조건에 넣는다(§1.6).

### 1.6 ④ `UnicodeRegex.kt:17-20` KDoc — **즉시 수정 대상 확인. 판정 중 이미 수정됨(부분)**

**확인한다.** 회부 시점 문장은 제공하지 않는 보호를 약속하고 있었다:

> *"**마스킹** — 주민등록번호가 전각 숫자로 적혀 있으면 ASCII `\d`는 통째로 놓친다. 놓친 개인정보는 그대로 외부 모델로 나간다."*

이 문장은 `UNICODE_CHARACTER_CLASS`를 켠 **이유**로 적혀 있어, 읽는 사람은 "그래서 지금은 잡힌다"로 읽는다. 실제로는 성별코드 `[1-8]` 때문에 **여전히 놓친다.**

> **판정 작성 중 상태 변경 (2026-08-13).** 이 판정을 쓰는 동안 `kotlin-implementer`가 병행 작업으로 해당 KDoc을 이미 고쳤다(작업 트리 미커밋). 새 문장은 *"이 플래그가 주민등록번호 전각 표기를 잡아 주지는 않는다"*를 절 제목으로 달고 `[1-8]`이 ASCII 리터럴이라 플래그의 영향을 받지 않음을 정확히 적었으며, 처분을 `privacy-gate` 판정 대기로 넘기고 **로직을 바꾸지 않았다**고 밝혔다. **적절한 처리이고 ④는 이것으로 충족된다.** 함께 `PlaceholderRestoration.toString`(X-6)도 재정의됐고, 카드 전각 숫자 fixture `masking-card-unicode-digit-fullwidth`가 추가됐다 — 후자는 §1.1의 카드 실측과 일치한다.
>
> **다만 새 KDoc도 종류 B를 모른다.** 새 문장은 결함을 `[1-8]` 하나로 서술하고 *"카드번호처럼 패턴이 `\d`로만 이뤄진 것은 … 전각·아랍인도 숫자 표기도 잡힌다"*고 적는데, 이는 **숫자 자리에 대해서만 참**이다. 카드번호도 전각 하이픈·NBSP·전각 공백에서는 뚫린다(§1.1). 종류 B를 반영해 한 번 더 갱신해야 한다.
>
> **패턴 자체는 그대로다** — `Masking.kt:155-156`의 두 정규식이 회부 시점과 동일함을 재확인했다. 종류 A·B 판정은 현재 코드에 그대로 유효하다.

**함께 갱신할 것 (`parity-verifier`)**

- fixture `masking-known-gap-rrn-fullwidth`(`parity/fixtures/masking/masking.json:1420`)의 제외 사유 문장 *"Kotlin에 Python보다 넓은 구현을 요구하게 되므로"* — **실효했으므로 삭제**한다. 문장 *"이 건은 `privacy-gate` 판정 §5.4의 범위 밖이다"* 도 **이 문서가 판정했으므로 갱신**한다.
- `known_gap`을 해제하고 `absent` 단언으로 전환한다. `reference.masked_text`가 현재 **미마스킹 기대값**을 담고 있어(`"번호 ９００１０１-１２３４５６７ 확인."`, `items: []`) 그대로 두면 개선이 회귀로 잡힌다. 전환 시 `reference_divergence`가 뜨는 것이 정상이며 원장에 사유와 함께 기록한다(§5.4 선례와 같은 절차).
- **추가 케이스** — `parity/fixtures/masking/*.json`을 전수 훑어 **구분자 변형(전각 하이픈·마이너스·엔 대시·NBSP·전각 공백)을 담은 케이스가 0건**임을 실측했다. 종류 B는 fixture에 전혀 없다:
  `masking-rrn-fullwidth-hyphen`(U+FF0D) · `masking-rrn-nbsp`(U+00A0) · `masking-rrn-ideographic-space`(U+3000) · `masking-card-fullwidth-hyphen` · `masking-card-nbsp` · 성별코드 전각 `９`·`０` **거부** 가드 · 개행으로 갈린 두 숫자열이 **붙지 않는지** 보는 과잉 마스킹 가드.

### 1.7 판정

**불변식 1(원문은 마스킹한 뒤에만 LLM으로 간다) — 순서 준수 / 커버리지 위반.** 선행 판정 §6과 같은 틀이다: 호출 **순서**를 우회하는 경로는 없고(타입 강제가 실재한다), 마스킹의 **완전성**이 실측으로 반증됐다.

**처분: 수정.** 차단하지 않는 근거 둘 — ⑴ 이 결함은 Python에서 상속된 것이지 Kotlin이 만든 회귀가 아니므로 §5 Phase 7 즉시 중단 기준("마스킹 전 본문이 LLM이나 로그로 전송됨")이 겨냥하는 신규 노출이 아니다, ⑵ **현재 provider를 부르는 프로덕션 경로가 없어** 실제 전송이 일어나지 않는다. 다만 **INV-02(마스킹 범주 2종)는 Phase 2 게이트 항목**이므로 이것이 닫히기 전에는 Phase 2가 닫히지 않는다.

**해제 조건** — 넷 다 충족해야 한다.
1. 종류 A·B 수정이 `Masking.kt`에 반영되고, §1.4의 과잉 마스킹 가드가 단언으로 붙어 있을 것.
2. §1.6의 fixture 전환·추가가 끝나고 masking 도메인 parity가 **실제로 돌아** 판정 건수가 0이 아닐 것(X-1과 함께 닫힌다).
3. `UnicodeRegex.kt` KDoc이 사실과 일치할 것.
4. **재확인은 회신을 신뢰하지 않고** §1.1과 같은 방식으로 표를 다시 돌린다. 추가로 `docs/golden/` 실문서에서 마스킹 건수가 **늘지 않는지**(과잉 마스킹 회귀 가드) 확인한다 — 선행 판정 §5.5와 같은 절차.

---

## 2. 판정 2 — X-5 provenance 래퍼 공개 생성자 (충돌 1)

### 2.1 사실 확인

codex #2의 사실 주장은 정확하다. `Masking.kt:454-455`·`:463-464`가 `@JvmInline value class ModelDraft(val value: String)`·`ReviewedBody(val value: String)`이고 **둘 다 public 생성자·public 프로퍼티**다. `LlmPrompt.forRepair`(`LlmPrompt.kt:85`)가 `ModelDraft`만 요구하므로 `ModelDraft(원문)`이 컴파일된다.

Claude 측 주장도 정확하다. 기본·사고 경로는 닫혔다 — 인자 순서를 뒤집으면 타입이 어긋나고, `edited_text ?? easy_text` 무조건 복원은 `restoreForExport`가 `reviewed == null`에서 복원하지 않게 바꿔 막았다(`:527-535`).

### 2.2 타입 봉쇄는 **실행 가능하지 않다** — 새 근거

두 리뷰 어디도 모듈 경계를 보지 않았다. 실측한다:

- `application/build.gradle.kts:10` — `api(project(":core"))`
- `api/build.gradle.kts:13` · `worker/build.gradle.kts:13` — `implementation(project(":application"))`

Kotlin `internal`은 **Gradle 모듈(컴파일 단위) 경계**다. `ReviewedBody`의 생성자를 `core` 내부로 좁히면 **`application`이 그것을 만들 수 없다.** 그런데 `ReviewedBody`를 만들어야 하는 계층이 정확히 거기다 — HTTP 요청에서 사람이 제출한 `edited_text`를 받는 자리. **요구되는 흐름 자체가 타입 봉쇄와 양립하지 않는다.**

남는 선택지는 "생성자를 `private`으로 하고 이름 있는 팩터리를 연다"(`ReviewedBody.fromHumanSubmission(...)`)인데, 이것은 방어가 아니다. 정본 하네스가 같은 형태를 이미 판정했다 — `00_progress.md:311`:

> *"무엇을 정본으로 삼든 그 정본도 저장소 안에 있고 같은 커밋에서 함께 바뀔 수 있다 … **한 칸 더 옮기는 것은 방어가 아니라 이동이다.**"*

`ReviewedBody(모델응답)`을 손으로 쓸 수 있는 사람은 `ReviewedBody.fromHumanSubmission(모델응답)`도 손으로 쓸 수 있다. 호출 한 칸이 늘 뿐 고의·명시 경로는 그대로 열려 있다. 그때의 결론도 코드 강화가 아니라 **탐지 + 리뷰 게이트**였다.

### 2.3 판정 — **수용(조건부)**

문서화된 한계를 수용한다. 단 **무조건 수용이 아니다.** `00_progress.md:313`이 세운 기준을 그대로 적용한다 — *"이 검사는 축소를 불가능하게 만들지 않고 **조용할 수 없게** 만든다."* 지금 이 자리는 조용하다. 새 `ReviewedBody(...)` 생성 지점이 생겨도 아무것도 빨개지지 않는다. **조용함을 없애는 것이 수용의 조건이다.**

**조건 1 — 사용 규약 명문화 (마감: 즉시, 이번 조각에)**

`Masking.kt`의 해당 KDoc과 `docs/migration/_workspace/00_requirements-inventory.md`에 **application 계층이 지켜야 할 규약**으로 아래를 적는다. 지금 KDoc은 "무너진다"는 **경고**만 있고 "누가 만들어도 되는가"라는 **규칙**이 없다.

- **`ReviewedBody`를 만들 수 있는 곳은 한 곳뿐이다** — HTTP 요청 경계에서 사람이 제출한 `edited_text` 필드를 읽는 어댑터. 그 필드가 요청에 **실제로 실려 왔을 때만** 만든다.
- **`ReviewedBody`를 만들면 안 되는 것들** — `easy_text`(초안), LLM 응답, 후처리 산출물, 워커 재처리 경로, 내보내기 경로에서 "본문을 고르는" 코드. `core`·`infrastructure`에서는 만들지 않는다.
- **`ModelDraft`를 만들 수 있는 값** — `LlmCompletion.text`와 그 후처리 결과, 또는 저장된 `easy_text` 컬럼. **사용자 업로드 원문으로 만들지 않는다** — 그것이 `LlmPrompt.forRepair`를 통해 마스킹 없이 provider로 나가는 경로다(codex #2가 지목한 절반).
- **`edited_text`는 사람이 실제로 제출하기 전까지 `null`이어야 한다.** 검수 화면을 열 때 초안을 자동 저장하는 구현은 이 통제를 통째로 무너뜨린다. 이미 `Masking.kt:434-436`이 "Phase 4 문서 API가 지켜야 할 요구사항"으로 적었으나 **요구사항 대장에 없다** — 대장으로 올려 Phase 4 게이트에서 세어지게 한다.

**조건 2 — 생성 지점 탐지기 (마감: application 조각과 동시)**

`ReviewedBody(`·`ModelDraft(` 생성 지점을 **허용목록으로 열거해 단언하는 테스트**를 둔다. 새 생성 지점이 생기면 실패하고, 실패를 풀려면 허용목록에 줄을 추가해야 하므로 **그 diff가 리뷰에 올라간다.** 형태는 `LlmPromptTest.kt:24-39`를 그대로 본뜨면 된다 — X-7(순위 2)이 `MaskedText`에 대해 요구하는 것과 **같은 장치이므로 함께 만든다.**

> 마감이 "application 조각과 동시"인 이유: 지금은 생성 지점이 core·테스트뿐이라 허용목록이 비어 있는 것과 같다. 위험이 실제로 무는 시점은 교차 종합 §6 순위 4가 적은 대로 **application이 이 타입들을 만들기 시작할 때**다. 다만 조건 1(규약)은 그 코드를 쓰기 **전에** 있어야 뜻이 있으므로 즉시다.

**해제 조건**: 조건 1의 규약 문장이 KDoc과 요구사항 대장 양쪽에 있을 것, 조건 2의 탐지기가 실재하고 **음성 대조**(허용목록 밖에 생성 지점을 하나 심으면 실패하는지)가 확인될 것. 재확인은 회신을 신뢰하지 않고 직접 돌린다.

---

## 3. 판정 3 — X-8 `scan_privacy_invariants.py` CI 미배선

### 3.1 배선을 권고하기 전에 실측했다 (규칙 2: 실제로 도는 경로에서 측정)

**측정 1 — 지금 무엇을 잡는가.** 전수 실행:

```
uv run python .claude/skills/migration-safety-gate/scripts/scan_privacy_invariants.py
→ 검사 파일 171개 · BLOCK 후보 0건 · 종료 코드 0
```

WARN만 뜬다: `EXC-BODY` 1건(`app/easyread/collection.py:691`, 길이만 보간 — 오탐) · `ZIP-NO-BUDGET` 5건(전부 `app/`) · `CACHE-HEADER` 32건(분포 확인용, 정상) · `RETENTION-PURGE` 14건(위치 확인용, 전부 `app/`). **BLOCK 규칙 6종(LOG-BODY·LLM-VENDOR-SDK·LLM-RAW-INPUT·OWNERSHIP-403·PLAINTEXT-PERSIST·SECRET-LITERAL·XML-DTD)이 전건 무적중이다.**

→ **지금 배선하면 즉시 초록이다.** 배선이 진행 중인 작업을 막지 않는다.

**측정 2 — 도달 범위.** `iter_files(False)`를 읽기 전용으로 열거:

```
총 171 — app 53 · backend-kotlin 73 · frontend 40 · scripts 5   (.kt 66개)
```

`backend-kotlin`이 **가장 큰 몫**이다. 선언이 허구가 아니다.

**측정 3 — 음성 대조(스크래치패드 합성 저장소).** 제품 파일은 건드리지 않고, 스크래치패드에 `backend-kotlin/.../Bad.kt`를 만들어 `REPO_ROOT`만 그쪽으로 돌려 스캔:

```
DETECTED LOG-BODY      Bad.kt:4  logger.info("masking text=" + text)
DETECTED EXC-BODY      Bad.kt:5  throw IllegalStateException("failed on body " + body)
DETECTED OWNERSHIP-403 Bad.kt:7  ResponseEntity.status(403)
DETECTED SECRET-LITERAL Bad.kt:8 val apiKey = "sk-ant-…"(난수꼴)
DETECTED XML-DTD       Bad.kt:9  DocumentBuilderFactory.newInstance()
BLOCK 후보: 4
```

**스캐너는 Kotlin에서 실제로 작동한다.** "선언은 있는데 도는지 모른다"가 아니라 **"돌면 잡는다는 것이 확인됐고 도는 자리만 없다."**

> 이 음성 대조는 처음에 제품 파일 `Masking.kt`에 위반을 주입하는 방식으로 시도했다가 **거부됐다.** 거부 사유가 정확했다 — 사용자 지시("코드는 수정하지 마라")와, `CLAUDE.md` 2026-08-13 이력이 기록한 *"`cp` 복원이 프롬프트에서 멈춰 **변조 파일이 디스크에 남는다**"* 는 실패가 그것이다. 합성 저장소로 옮겨 같은 증거를 얻었고, `git status backend-kotlin/`로 제품 파일 무변경을 확인했다.

### 3.2 판정 — **수정. 마감을 Phase 3 → 지금으로 당긴다**

**당기는 근거 넷.**

1. **비용이 0이다.** 측정 1대로 오늘 초록이다. 마감을 미룰 때 흔히 드는 사유("배선하면 기존 위반이 쏟아져 작업이 멈춘다")가 **성립하지 않는다.**
2. **이번 범위가 처음으로 물리게 만들었다.** 사용자 문서 본문·개인정보를 실제로 다루는 파일이 들어왔고, `Secret.reveal()`(`core/security/Secret.kt:25`)이 **평문을 꺼내는 공개 통로**다. `MaskedItem.original.reveal()`을 로거 인자로 넘기는 코드는 컴파일되고, 그것을 잡기로 한 유일한 장치가 아무 데서도 돌지 않는다. `LOG-BODY`의 `BODY_NAMES`에 `original`이 들어 있으므로 **배선만 되면 잡힌다.**
3. **`CLAUDE.md` 규칙 4가 이 방향을 지정한다.** 이 장치는 **탐지형**이다(은폐형·범위 선언형이 아니다). 규칙은 은폐형만 넓히기를 금지하고 *"탐지형으로 갈아탄다"*고 적었다. 탐지형의 도달을 넓히는 것은 규칙이 권장하는 방향이다.
4. **Phase 3까지 미루면 그 사이 유입이 무탐지 구간에 들어간다.** Phase 3은 repository·인증 API — `PLAINTEXT-PERSIST`·`OWNERSHIP-403`·`SECRET-LITERAL`이 처음 물리는 국면이다. 그때 배선하면 **Phase 2에 들어온 코드는 한 번도 스캔받지 않은 채** 지나간다. mypy 도달 사고(2026-08-13 이력)와 정확히 같은 형태다.

### 3.3 배선 지침 (실행은 리더 승인 후 `kotlin-implementer`)

**어느 잡 — `quality`.** `kotlin` 잡이 아니다. 근거: `SCAN_ROOTS`가 `app`·`frontend/src`·`scripts`를 함께 덮으므로(측정 2에서 98개 = 57%가 backend-kotlin 밖) Kotlin 잡에 넣으면 **선언한 범위보다 좁은 자리에 붙는다** — 규칙 3이 금지하는 형태다. `quality` 잡은 이미 `uv sync --locked`를 마쳐 이 스크립트에 필요한 것이 전부 갖춰져 있고(DB·JDK 불필요), 별도 설치 스텝이 없다.

**어느 위치 — `- run: uv run mypy . .claude`(ci.yml:82) **바로 다음**, `- run: uv run alembic upgrade head`(:83) 앞.** 근거: ⑴ 스캐너 자신이 mypy 대상이므로(2026-08-13 이력) 타입 검사를 통과한 직후에 실행하는 것이 순서상 맞다, ⑵ DB·마이그레이션·pytest 같은 느린 단계 앞이라 실패가 빠르다, ⑶ `alembic`·`pytest`는 `app/` 런타임 의존이고 이 스캔은 순수 정적 검사라 서로 독립이다.

**어떤 인자 — 전수. `--changed`를 쓰지 마라.**
- `--changed`의 기본 base가 `main`인데(`DEFAULT_BASE_REF`), 이 브랜치처럼 오래 사는 브랜치에서는 범위가 계속 넓어지거나 fork point 이동으로 흔들린다.
- 전수가 171파일로 **충분히 싸다**(로컬 1초 미만).
- 종료 코드 3(빈 범위) 처리를 CI에서 따로 다뤄야 하는 복잡도가 생긴다.
- **`--no-fail`을 붙이지 마라.** 붙이면 BLOCK 후보가 있어도 초록이 되어 게이트가 장식이 된다.

권고 형태:

```yaml
      # 데이터 보호 불변식 기계 스캔. SCAN_ROOTS 가 app·backend-kotlin·scripts·frontend/src
      # 를 함께 덮으므로 kotlin 잡이 아니라 여기에 둔다 — 좁은 잡에 붙이면 선언한 범위보다
      # 좁은 자리에서 돌게 된다.
      # 배선 시점 실측(2026-08-13): 검사 171파일(backend-kotlin 73) · BLOCK 후보 0 · exit 0.
      # 음성 대조: 합성 .kt 에 로거 본문 보간·403·난수 키·DTD 팩터리를 심으면 BLOCK 4건 검출.
      # --no-fail 을 붙이지 않는다 — 붙이면 BLOCK 이 있어도 초록이라 게이트가 장식이 된다.
      - name: 데이터 보호 불변식 스캔 (BLOCK 후보 0건 유지)
        run: uv run python .claude/skills/migration-safety-gate/scripts/scan_privacy_invariants.py
```

**막지 못하는 것을 함께 적는다.** 이 스텝 자체의 삭제는 이 스텝이 막지 못한다 — `ci.yml:99-100`이 같은 한계를 이미 정직하게 선언했고, 최종 방어선은 그 diff가 리뷰에 올라가는 것이다. 여기서 **한 칸 더 옮기지 않는다**(§2.2와 같은 판단).

**해제 조건**: 위 스텝이 `ci.yml`의 `quality` 잡에 실재할 것, 첫 실행에서 종료 코드 0일 것, 그리고 `--no-fail`이 붙어 있지 않을 것. 재확인은 `ci.yml`을 직접 읽어서 한다.

---

## 4. 판정 4 — X-11 `baseUrl` 노출 · 응답 크기 상한 부재

### 4.1 근거 정정 — "설정으로 열려 있다"는 **현재 사실이 아니다**

Claude S-4는 *"`AnthropicSettings.baseUrl`이 **설정으로 열려 있고** … 문서 본문이 나가는 대상이 **설정 한 줄로** 바뀐다"*고 적었다. 실측하면 현재 커밋에서 성립하지 않는다.

- `EasyDocProperties.LlmProperties`(`api/config/EasyDocProperties.kt:57-65`)의 필드는 `provider`·`model`·`effort`·`anthropicApiKey`·`openaiApiKey` **다섯뿐이다. `baseUrl`이 없다.**
- `application.yml`(api·worker) 어디에도 base url 키가 없다.
- **`AnthropicSettings(`를 생성하는 비테스트 코드가 0건이다.** 유일한 생성 지점이 `infrastructure/src/test/.../AnthropicTestSupport.kt:33`이다.
- `AnthropicProvider`를 빈으로 등록하는 `@Bean`·`@Component`가 없다(`infrastructure` 전체에서 `SecretConverter`·`FlywayBaselineGuard` 둘뿐).

즉 `baseUrl`은 **생성자 기본 인자**로 열려 있을 뿐이고, 그 기본값은 컴파일 상수 `ANTHROPIC_BASE_URL = "https://api.anthropic.com"`이다. 외부 설정·환경변수로 이 값을 바꿀 경로가 **지금은 존재하지 않는다.** 바꾸려면 코드를 고쳐야 하고, 그 diff는 리뷰에 올라간다.

**S-4가 지적한 위험 자체는 유효하다** — 다만 그것은 **현재 노출**이 아니라 **Phase 5 배선이 만들 수 있는** 노출이다. Claude가 마감을 Phase 5로 잡고 *"Phase 5 배선 때 운영 설정이 이 값을 노출하는지 privacy-gate가 판정해야 한다"*고 적은 것은 **정확한 처리**였다.

### 4.2 응답 크기 상한 — 결함은 실재하되 현재 노출 아님

`post()`(`:206-207`)가 `.body(ByteArray::class.java)`로 응답을 **통째로 힙에 읽는다.** 상한이 없다. 악의적·오동작 엔드포인트가 거대한 본문을 돌려주면 OOM이다. 다만 ⑴ 대상이 `api.anthropic.com`으로 고정돼 있고, ⑵ TLS가 중간자를 막고, ⑶ **호출하는 프로덕션 경로가 없어** 현재 도달 가능한 사건 경로가 아니다.

### 4.3 판정 — **수정. Phase 5 유지. 당기지 않는다**

당기지 않는 근거:
- **현재 노출이 0이다.** 두 항목 모두 사건 경로에 프로덕션 호출자가 없다.
- **당길 자리가 없다.** 이 결함이 실제로 무는 시점이 정확히 Phase 5(LLM provider 배선 + 워커 큐)다. Phase 2·3에 고쳐 두면 배선하면서 다시 손대게 된다.
- **당기면 손해가 난다.** 지금 당장 열려 있는 것은 판정 1(INV-02 게이트 항목)과 판정 3(탐지 도달 0)이다. 노출 0인 항목으로 구현자 시간을 옮기면 그 둘이 늦어진다.

**단 Phase 5 판정을 지금 미리 못박는다 — 그때 다시 논의하지 않기 위해서다.**

**해제 조건 (Phase 5에서 이 넷을 확인한다)**

1. **`baseUrl`을 `EasyDocProperties`·`application.yml`·환경변수에 바인딩하지 않는다.** 프로덕션 배선은 `AnthropicSettings`의 기본값(컴파일 상수)을 그대로 쓰고, 생성자 파라미터는 **테스트 전용**으로 남긴다. 설정으로 열어야 할 사유가 생기면 그것은 **`privacy-gate` 재판정 사안**이지 구현 재량이 아니다 — 문서 본문이 나가는 **대상**을 바꾸는 값이기 때문이다.
2. 1을 어기고 열어야 한다면, **스킴은 `https`로 고정하고 호스트는 허용목록**으로 제한한다. 평문 `http`·임의 호스트가 통과하면 문서 본문이 TLS 없이, 또는 제3자에게 나간다.
3. **응답 본문 크기 상한을 건다.** `max_tokens` 기본이 16,000이므로 정상 응답은 1MB를 크게 밑돈다. 넉넉히 잡아도 상한이 실효하도록 두고, **선언된 `Content-Length`를 믿지 말고 실제로 읽은 바이트로 판정한다** — `app/ingest/extractors.py`의 zip 예산 주석이 세운 것과 같은 원칙(*"선언 크기는 위조 가능하다 — 실제로 읽은 바이트만 믿을 수 있다"*). 초과 시 던지는 예외 메시지에 **본문 조각을 싣지 않는다**(현재 `failure()`의 규약 유지).
4. 1~3에 대한 회귀 단언이 있을 것. `StubAnthropicServer`가 이미 있으므로 거대 본문 응답 케이스를 심으면 3은 바로 검증된다.

**해제 조건 확인 방법**: Phase 5 종료 전 `EasyDocProperties`·`application.yml`을 직접 읽어 `baseUrl` 바인딩 부재를 확인하고, 크기 상한 회귀 테스트를 실행 결과로 받는다.

---

## 4-bis. 판정 5 — `ModelDraft.toString()` 본문 노출 (추가 회부 · 2026-08-14)

> **회부**: 리더 · `kotlin-implementer`가 application 조각(`f73879b..6d8e88c`)을 끝내며 올린 신규 관측.
> 근거 위치: `02_kotlin-implementer_conversion-usecase.md` §5.3 · §8.1 ③.
> 구현자는 **자기 결과 타입(`ConversionResult.Converted`)은 가렸고 `ModelDraft` 타입 선언은 판정 대상이라 손대지 않았다** — 옳은 경계 판단이다.

### 4-bis.1 실측 — 결함은 `ModelDraft` 하나가 아니다

`kotlinc-jvm 2.2.20`으로 저장소 타입 3종의 선언 형태를 그대로 옮겨 합성 재현했다(제품 파일 미수정). 본문에는 **마스킹 범주 밖이라 정책상 무마스킹 통과하는 값**(전화번호·이메일)을 일부러 섞은 합성 문자열을 썼다.

| 경로 | `ModelDraft` 출력 |
|---|---|
| 문자열 보간 `"$draft"` | `ModelDraft(value=…본문 전체…)` |
| 명시 `toString()` | `ModelDraft(value=…본문 전체…)` |
| **`Any` 인자 (로거 형태)** | `ModelDraft(value=…본문 전체…)` |
| 컬렉션 안 | `[ModelDraft(value=…본문 전체…)]` |

**네 경로 전부 본문을 통째로 찍는다.** 구현자 관측이 정확하다.

**그리고 같은 결함이 두 개 더 있다** — 구현자는 자기 코드가 닿은 `ModelDraft`만 보고했다:

| 타입 | 선언 위치 | `toString` | 담는 것 |
|---|---|---|---|
| `MaskedText` | `Masking.kt:53-54` | **없음 — 본문 노출** | 마스킹된 본문(전화·이메일·계좌 무마스킹 포함) |
| `ModelDraft` | `Masking.kt:625-626` | **없음 — 본문 노출** | LLM 1차 변환문 |
| `ReviewedBody` | `Masking.kt:639-640` | **없음 — 본문 노출** | 사람이 제출한 검수본 |
| `PlaceholderRestoration` | `Masking.kt:139` | 있음(X-6에서 수정) | 복원된 최종 본문 |
| `LlmPrompt` / `LlmCompletion` / `Secret` / `ConversionResult.Converted` | 각 파일 | 있음 | — |

`grep "override fun toString" backend-kotlin/*/src/main`이 6건을 내는데 `Masking.kt`에서는 **139행 하나뿐**이다. **저장소의 `@JvmInline value class`는 정확히 이 셋이고, 셋 다 빠져 있다.**

### 4-bis.2 ① 본문 자체가 로그 금지 대상인가 — **그렇다. 독립된 근거 둘 중 하나만으로도 충족된다**

**근거 A — "문서 본문"은 개인정보와 **별개로** 금지된다.** `CLAUDE.md` 보안·데이터 규칙 첫 줄: *"로그에 **문서 본문**·개인정보를 절대 남기지 않는다. **로깅은 문서 ID·길이·처리 상태까지만.**"* 금지 대상이 `·`로 이어진 **둘**이고, 뒷문장은 **허용목록**이다. `ModelDraft`가 담는 것은 사용자 문서를 다시 쓴 글이므로 첫 번째 항목에 그대로 해당한다. **개인정보가 한 글자도 없어도 금지다.** 계획 §4.4가 로그에 남길 것을 *"conversion id, 상태, 시도 횟수, failure code만"*으로 열거한 것도 같다 — 본문은 그 목록에 없다.

**근거 B — 실제로 개인정보가 들어 있다.** "마스킹된 입력에서 파생됐으니 안전하다"는 추론은 **성립하지 않는다.** 마스킹 범주는 주민등록번호·카드번호 **2종뿐**이고, `Masking.kt:127-142`가 명시적으로 적었다 — *"**이 셋(전화번호·이메일·계좌번호)은 문서에 섞여 들어오면 마스킹 없이 그대로 LLM(국외 포함)으로 전송된다.** … 명시적으로 감수하기로 한 대가다."* 그 셋은 전송을 감수한 것이지 **로그 적재를 감수한 것이 아니다.** 같은 절이 *"로그·응답 노출 규칙은 축소와 무관하게 유지"*(`CLAUDE.md` 아키텍처 규칙 2)라고 못박았다. 마스킹 범주 축소는 **LLM 전송 경계**의 결정이고 **로그 경계**를 건드리지 않았다. §4-bis.1 실측에서 전화번호·이메일이 그대로 찍힌 것이 이것이다.

**근거 C — §2 판정과 맞물려 위험이 증폭된다.** `ModelDraft`의 생성자는 공개이고, §2에서 내가 수용한 것은 **"사용 규약 + 탐지기를 조건으로 한" 잔여 위험**이었다. 규약은 관례이지 타입 보장이 아니다. 규약이 깨져 `ModelDraft(원문)`이 만들어지면 그 안에는 **마스킹 전 원문(주민등록번호·카드번호 포함)**이 들어 있고, 무방비 `toString()`은 그것을 **로그로 내보내는 증폭기**가 된다. 즉 §2의 수용은 "새 생성 지점이 조용할 수 없게" 만드는 것을 전제로 했는데, 이 결함은 그 전제의 반대편에서 조용한 유출 경로를 하나 더 연다.

### 4-bis.3 ② 형제 타입과의 비대칭 — **해소해야 한다. 그리고 회부된 것보다 넓게**

비대칭은 실재하고(§4-bis.1 표), **결함의 구조가 하나**다 — `CLAUDE.md` 규칙 4 ⑴이 요구하는 대로 빈자리를 **종류로** 댄다:

> **종류 · 문서 텍스트를 감싸는 `@JvmInline value class`.** 일반 class·data class에 적용한 `toString` 규율이 value class에는 한 번도 적용되지 않았다.

이 종류는 **구조적으로 재발한다** — 다음에 본문을 감싸는 래퍼를 만드는 사람도 같은 자리를 빠뜨린다. `LlmPrompt`가 *"`data class`가 아닌 이유"*를 KDoc 한 절로 설명해 둔 것(`LlmPrompt.kt:39-45`)이 value class 셋에는 **닿지 않았다.** 따라서 판정은 **`ModelDraft` 한 건이 아니라 종류 전체**에 내린다.

**은폐형이 아닌가 — 아니다(규칙 4의 분류).** `toString` 재정의는 탐지기를 억누르는 조치가 아니라 **위험 자체를 없애는 조치**다(본문이 애초에 문자열에 들어가지 않는다). 이 저장소가 스스로 택한 방향과 같다 — *"검사를 얹는 것보다 만들 수 없게 하는 쪽"*(`Masking.kt:168`). 다만 **그것만으로 끝내지 않는다**: 아래 조치 2가 탐지형 절반이다.

### 4-bis.4 실측 — 처방이 실제로 듣는가, 그리고 무엇을 못 막는가

`@JvmInline value class`에 `toString()`을 재정의해 같은 네 경로를 다시 돌렸다:

```
1) 보간        -> ModelDraft(48자)
2) toString()  -> ModelDraft(48자)
3) Any (로거)  -> ModelDraft(48자)     ← 박싱되는 로거 경로에서도 듣는다
4) 컬렉션      -> [ModelDraft(48자)]
5) .value 직접 -> 문의 02-1234-5678 …  ← 재정의로 막지 못하는 유일한 경로
```

**재정의는 네 경로 모두에서 듣는다.** 못 막는 것은 `.value`를 직접 꺼내 넘기는 줄 하나이며, 이는 `MaskedText`가 이미 선언해 둔 한계와 같은 성질이다(공개 프로퍼티).

**그 남은 한 줄을 탐지기가 잡는가 — 지금은 못 잡는다.** `scan_privacy_invariants.py`의 `BODY_NAMES`에 이 저장소가 쓰는 식별자가 빠져 있다. `LOG-BODY` 규칙에 직접 투입한 결과:

```
  MISSED  logger.info("변환 완료 {}", draft)
  MISSED  logger.info("변환 완료 {}", modelDraft)
  MISSED  logger.info("변환 완료 {}", reviewed)
  MISSED  logger.info("변환 완료 {}", result)
  CAUGHT  logger.info("변환 완료 {}", easyText)
  CAUGHT  logger.info("변환 완료 {}", body)
  CAUGHT  logger.info("변환 완료 {}", maskedText)
```

`draft`·`modelDraft`·`reviewed`가 전부 빠져 있다(`reviewed`는 `BODY_NAMES`의 `review`가 `\b` 경계라 걸리지 않는다). **타입도 안 가리고 탐지기도 안 보는 상태**였다. 판정 3으로 스캐너를 CI에 붙였으므로 지금 이 목록을 고치면 즉시 도달한다.

### 4-bis.5 판정 — **수정**

**불변식 4(로그에 본문·개인정보 없음) — 잠정 위반.** "잠정"인 이유: 현재 이 세 타입을 로거 인자로 넘기는 줄이 저장소에 **실재하지 않는다**(구현자가 자기 결과 타입을 가려 두었다). 사건이 아니라 **장치의 빈자리**다. §5 Phase 7 즉시 중단 기준 **해당 없음** — 실제 전송이 일어나지 않았다.

**마감: 즉시.** 미루지 않는 근거 셋 — ⑴ 타입당 한 줄이고 위험이 0이다, ⑵ 이번 조각이 **첫 프로덕션 `ModelDraft` 생성 지점**(`ConvertDocumentUseCase`)을 들여와 실제 값이 존재하기 시작했다, ⑶ 형제 6종 중 3종만 고쳐 둔 비대칭이 곧 X-6을 낳은 형태 그 자체다 — 남겨 두면 같은 지적이 다음 회차에 다시 올라온다.

**조치 (수신자: `kotlin-implementer`)**

1. **`MaskedText`·`ModelDraft`·`ReviewedBody` 셋 모두** `toString()`을 재정의해 **길이만** 남긴다. `ModelDraft` 하나만 고치면 비대칭이 남는다. 형태는 형제 타입과 같게 한다(`PlaceholderRestoration`·`LlmPrompt`가 선례).
2. **`scan_privacy_invariants.py`의 `BODY_NAMES`에 `draft`·`modelDraft`·`reviewed`·`reviewedBody`·`easyText`계열 누락분을 더한다.** ⑴이 위험을 없애는 절반이면 이것이 **탐지하는 절반**이다 — 규칙 4가 "은폐형 대신 탐지형으로 갈아탄다"고 한 것을 둘 다 두는 것으로 만족시킨다. **이름을 빼지 말고 더하기만 한다**(그 목록에서 이름을 빼는 것은 탐지를 줄이는 것이다 — `SECRET-LITERAL` 주석이 같은 원칙을 이미 적었다).
3. **회귀 단언.** 세 타입 각각에 대해 `toString()`이 본문을 담지 않음을 실행으로 단언한다. **길이 표기만 확인하지 말고 "본문 문자열이 결과에 포함되지 않는다"를 단언**한다 — 전자는 형식이 바뀌면 조용히 통과한다. `Secret`·`LlmPrompt`의 기존 단언과 같은 자리에 둔다.
4. KDoc에 **왜** 재정의했는지 한 줄을 남긴다. `LlmPrompt.kt:39-45`의 "`data class`가 아닌 이유" 절이 value class 셋에 닿지 않은 것이 이 결함의 원인이므로, 다음 사람이 새 래퍼를 만들 때 읽을 자리에 근거를 둔다.

**해제 조건**: 세 타입 모두 재정의가 실재할 것 · `BODY_NAMES` 확장 후 §4-bis.4의 7개 탐침에서 MISSED가 0일 것 · 조치 3의 단언이 **본문 미포함**을 보는 형태일 것. 재확인은 회신을 신뢰하지 않고 §4-bis.1·§4-bis.4와 같은 방식으로 직접 돌린다.

---

## 5. 불변식 판정표 (이번 회부 범위에 걸리는 것만)

| # | 불변식 | 판정 | 근거 |
|---|---|---|---|
| 1 | 원문은 마스킹한 뒤에만 LLM으로 간다 | **순서 준수 / 커버리지 위반** | 순서: `MaskedText` 생성 통로 단일(`Masking.kt:53-68`), `LlmPrompt` 생성자 private(`LlmPrompt.kt:46`). 커버리지: §1.1 실측 — RRN 종류 A·B, CARD 종류 B |
| 2 | LLM 호출 최대 2회 | **확인 불가 (대상 코드 없음)** | 호출 상한을 강제할 application 계층이 아직 없다. X-3 소관이며 이 회부 범위 밖 |
| 4 | 로그에 본문·개인정보 없음 | **잠정 위반** (2026-08-14 재판정 — 이전 판정 "준수(단 탐지 도달 0)"에서 변경) | **변경 사유**: `@JvmInline value class` 3종(`MaskedText`·`ModelDraft`·`ReviewedBody`)의 기본 `toString()`이 본문을 통째로 찍음을 `kotlinc` 실측으로 확인 — §4-bis.1. 이전 판정은 `LlmPrompt`·`LlmCompletion`·`Secret`만 보고 value class를 보지 않았다. 사건은 없고(로거 인자로 넘기는 줄 0건) **장치의 빈자리**라 "잠정". 탐지 도달은 판정 3으로 닫혔으나 `BODY_NAMES`가 `draft`·`modelDraft`·`reviewed`를 몰라 이 셋에는 **도달하지 않는다**(§4-bis.4) |
| 6 | private 응답 헤더 유지 | **준수 (이번 범위 무변경)** | `PrivateResponseHeadersConfig.kt` 및 회귀 4종 실재. 이번 5커밋이 건드리지 않았다 |
| 13 | LLM 계약 no-training 전제 명시 | **준수** | `AnthropicProvider.kt:28-32`에 명시 주석 실재 |

**확인 불가로 남긴 것과 이유**

- 불변식 3·5·7·8·9·9b·10·11·12 — **대상 코드가 이번 범위에 없다.** 저장·소유권·AEAD·Argon2·JWT·파서·보존 파기·삭제 경로는 Phase 3 이후 조각이다. "확인하지 않음"이지 "위반 없음"이 아니다.
- §1.5의 카드번호 종류 B 지적은 **이 판정의 신규 발견**이라 교차 검증을 받지 않았다. `parity-verifier`의 fixture 재현으로 확정한다.

---

## 6. 수신자 · 마감

| 수신자 | 요청 | 마감 |
|---|---|---|
| `kotlin-implementer` | §1.6 `UnicodeRegex.kt` KDoc 사실 정정 — **1차 완료(병행 작업)**. 종류 B(구분자)를 반영해 한 번 더 갱신 | **즉시** |
| `kotlin-implementer` | §2.3 조건 1 — provenance 사용 규약을 KDoc + 요구사항 대장에 명문화 | **즉시** |
| `kotlin-implementer` | **§4-bis.5 조치 1** — `MaskedText`·`ModelDraft`·`ReviewedBody` **3종 모두** `toString` 재정의(길이만) | **즉시** |
| `kotlin-implementer` | **§4-bis.5 조치 2** — `scan_privacy_invariants.py` `BODY_NAMES`에 `draft`·`modelDraft`·`reviewed`·`reviewedBody` 추가(빼지 말고 더하기만) | **즉시** |
| `kotlin-implementer` | **§4-bis.5 조치 3·4** — "본문 미포함"을 보는 회귀 단언 3건 + KDoc 근거 한 줄 | **즉시** |
| 리더 | §3.3 CI 배선 승인 | **즉시** |
| `kotlin-implementer` | §3.3 `quality` 잡 배선(승인 후) | 승인 직후 |
| `kotlin-implementer` | §1.4 종류 A·B 수정 + 과잉 마스킹 가드 | **Phase 2 종료 전** |
| `parity-verifier` | §1.6 fixture 사유 갱신 · `known_gap` → `absent` 전환 · 신규 케이스 7종 | **Phase 2 종료 전** |
| `kotlin-implementer` | §2.3 조건 2 — 생성 지점 탐지기(X-7과 함께) | application 조각과 동시 |
| `kotlin-implementer` | §4.3 해제 조건 1~4 | **Phase 5** |
| `migration-reviewer` | 보안 축 종합 리뷰에 이 판정 포함 | 다음 회차 |
| 리더 | **Phase 2 종료 판정 시**: 판정 1이 INV-02 게이트를 열어 두고 있음. 판정 3은 §6 Security 게이트의 탐지 도달 항목 | Phase 2 종료 전 |

**차단 사유서 미작성.** §0대로 §5 Phase 7 즉시 중단 기준 해당 0건이고 프로덕션 노출 경로가 없다. 다섯 중 어느 것도 다른 레인의 작업을 멈추게 하지 않는다 — 다만 판정 1은 **Phase 2 종료를 막는다**(INV-02).

**추적.** 위 요청들은 해제될 때까지 상태를 추적한다. 수정 완료 회신은 **그대로 신뢰하지 않고** 각 절의 "해제 조건 확인 방법"으로 다시 확인한 뒤에만 해제한다.

---

## 7. 부기 — 산출물 위생

- 이 문서에 **실제 문서 본문·개인정보·암호문·키를 옮겨 적지 않았다.** §1.1의 표에 쓴 번호는 전부 합성값이며, fixture(`masking-known-gap-rrn-fullwidth`)가 이미 쓰고 있는 표기를 그대로 따랐다. `sk-ant-…`로 표기한 것은 음성 대조용으로 **스크래치패드에서 생성한 난수 문자열**이고 실제 키가 아니다.
- **§4-bis의 `toString` 실측에 쓴 본문도 합성값이다.** 전화번호·이메일을 일부러 섞은 것은 "마스킹 범주 밖이라 통과한다"는 정책 결과를 눈에 보이게 하기 위해서이고, `02-1234-5678`·`hong@example.go.kr`은 **예약된 예시 도메인(RFC 2606 `example.`)과 임의 숫자**다. 실측 출력을 이 문서에 옮길 때도 본문을 `…본문 전체…`로 줄여 실제 문자열이 산출물에 남지 않게 했다.
- **제품 파일을 하나도 수정하지 않았다.** 음성 대조는 스크래치패드 합성 저장소에서만 수행했고, `git status backend-kotlin/`·`git diff --stat backend-kotlin/`이 비어 있음을 실행으로 확인했다(§3.1 부기).
- 프로토타입·스캔 리포트·프로브 소스는 스크래치패드에만 두었고 저장소에 커밋하지 않는다.
