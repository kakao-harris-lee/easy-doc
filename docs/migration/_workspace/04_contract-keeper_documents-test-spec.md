# documents·conversions 계약 테스트 명세 — `#4`~`#9`

**작성:** contract-keeper / **일자:** 2026-08-19 / **Phase:** 4 (첫 작업 단위 `documents`)
**정본 계약:** `contracts/easy-doc-v1.yaml` — 이 문서는 **계약이 아니라 계약을 고정하는 테스트의 정의**다.
**잇는 문서:** `00_contract-keeper_test-plan.md` §2 #4~#9 · §3 횡단 · §3-1 미확정 · §5 Phase 4 배치 /
`03_contract-keeper_auth-test-spec.md` · `03_contract-keeper_workspaces-test-spec.md`(같은 형식 · 이월 항목의 출처) /
`03_kotlin-implementer_phase4-preconditions.md` §2·§5 / `00_contract-keeper_changelog.md`(2026-08-19 · C-4)

---

## 0. 이 문서의 규약 — 값을 전사하지 않는다

**상한의 숫자, `detail` 문구, 헤더 값 문자열, enum 원소, 미디어 타입 문자열, 파일명 상수,
금지 문자 집합을 이 문서에 적지 않는다.** 경계는 「상한」·「하한」·「기본값」이라는 **역할
이름**으로 부르고 집합은 「지원 확장자 집합」·「형식 3종」처럼 **가리키는 말**로 부른다.
실제 값은 **계약 파일에서 읽는다**(§4 파서 요건). 근거는 auth 명세 §0·workspaces 명세 §0과
같다 — 손으로 옮긴 계약 요약이 원본과 갈린 실측(게이트 15 별건 1)과, 그 규약이 자기 세 행에서
빈 채로 통과한 것(게이트 16 N).

**이 규약의 도달 범위 — 이 문서 전체다.** 표·산문·각주를 가리지 않는다.

```bash
grep -nE '"[^"]*(비어 있|이하 문서|이하여야|지원 형식|찾을 수 없|끝난 뒤|유실된|너무 큽|지원하지 않)[^"]*"|no-store|nosniff|attachment;|easy-read|UTF-8'"''"'|\b(4000|10485760|500000|52428800|10485|255|80|64)\b|\b(docx|pdf|hwpx|txt)\b|\b(pending|processing|done|failed)\b|주민등록번호|카드번호' \
  docs/migration/_workspace/04_contract-keeper_documents-test-spec.md
# 기대: 이 코드 블록 자신의 패턴 문자열 외 0건
```

**적는 것과 적지 않는 것의 경계** — auth 명세 §0의 표와 같다. 키 경로·역할 이름·`detail`의
**타입**·헤더와 컴포넌트와 필드와 파라미터의 **이름**은 적고, 그 자리에 들어가는 **값**은 적지
않는다. **상태 코드만은 숫자로 적는다**(케이스의 이름이 곧 상태 코드다). 기대값의 근거는 이
문서가 아니라 계약 파일이며, 이 문서의 숫자가 계약과 갈리면 **틀린 것은 이 문서다.**

**이 단위에서 새로 생긴 경계 하나 — 문자 블록의 이름.** §2-6이 다루는 파일명 금지 집합은
제어문자 블록(C0·DEL·C1)을 포함한다. **블록 이름은 적고 코드포인트는 적지 않는다** —
`C1`은 그 자리를 부르는 식별자이고 코드포인트는 계약 `x-filename-charset.forbidden`에 사는
값이다. 테스트는 집합을 그 노드에서 읽는다(P-23).

**행 번호는 2026-08-19 실측이며 키 경로와 함께 적는다.** 파일이 움직이면 행은 틀리고 키는 남는다.
**같은 날 이 배치가 `Content-Disposition` 헤더에 조항 하나를 더하면서 `:1230` 이후가 41줄
밀렸다** — 아래 행 번호는 **그 변경 이후** 값이다. **갈리면 키 경로가 이긴다.**

---

## 1. 범위

| 안 | 밖 |
|---|---|
| 계약 #4·#5·#6·#7·#8·#9의 요청/응답 계약 (`:905-990` · `:996-1039` · `:1046-1079` · `:1092-1122` · `:1123-1177` · `:1184-1306`) | #1~#3·#10~#14 — 앞 단위에서 마감 |
| **X-D1 하한선의 남은 4곳** — 이 배치가 10곳을 전부 닫는다 | 하한선 목록을 늘리거나 줄이는 일 — 불변식이다 |
| X-F9의 `edited_text` 케이스 (F3 다섯 필드 중 **마지막** 마감) | F3 `password`의 측정 축 — 게이트 15 X13 리더 판정 대기 |
| **지침 3**(`limit`/`offset` Bean Validation)과 **지침 8**(`handleHandlerMethodValidationException`)의 **첫 실행 경로** | 그 예외를 어느 어드바이스에서 어떻게 잡는가 — 구현 판단 |
| **L348 F-3·F-4** — 내보내기 파일명 금지 집합의 **경계 단언**(§2-6) | 같은 F-3·F-4의 **parity fixture 커버리지 몫** — 그 레인 소관(§7-3 O-19) |
| 인라인 헤더 2건의 **처분 판정**과 형식 검증 항목 (§7-1) | 셋째 인라인 헤더를 허용할지 — 대상이 생기는 커밋의 문제 |
| `x-open-asymmetry`의 **판정 근거와 선택지** (§7-2) | 그 **확정** — 리더·사용자 |
| 파서 추가 노드 P-23~P-37 · 음성 대조 N-20~N-30 | 파서·계층의 **구현** 선택 — `kotlin-implementer` 판단 |
| 단언해야 할 **나간 바이트** | 그것을 어떻게 내는가 |

**조항이 없어 이 배치가 쓸 수 없는 것 둘** — test-plan §3-1의 **X-J1**(내보내기에서
`withheld`·`ambiguous`가 비어 있지 않을 때의 처분, OQ-2)과 **X-J3**(마스킹 본문 채널, OQ-4)은
**확정 Phase가 4**로 등록돼 있는데 조항이 아직 없다. §7-3 O-17이 그 자리이며, 조항 없이
케이스를 쓰면 테스트가 계약이 된다.

---

## 2. 케이스 표

「계약 지목」은 **테스트가 기대값을 읽어야 하는 자리**다. 「계층」은 §5가 정의한다.

### 2-1. `POST /documents` (`paths./documents.post` `:905-990`)

| ID | 요청 | 단언 | 계약 지목 | 계층 | 출처 |
|---|---|---|---|---|---|
| **DC-1** | 유효한 토큰 + JSON 본문 | 성공 상태 = `responses`의 유일한 2xx 키 · 본문 최상위 키 집합이 **정확히** `DocumentCreatedResponse.required` · 사적 헤더 2종이 **각각 하나씩**(개수까지) | `:946-960` · `DocumentCreatedResponse.required` `:1882` · `x-global-response-headers` `:547-569` | C-M | §2 #4 ① · **X-D2** · X-D2b · X-E1/E2 |
| **DC-2** | 〃 | `Location` 헤더가 있고, 그 값이 **`paths`의 `/conversions/{conversion_id}` 템플릿에 본문 `conversion_id`를 끼운 것과 같다**. 두 값을 각각 계약에서 읽어 조립한다 — 형식 문자열을 코드에 적지 않는다 | `:951-957`(인라인 선언) · `paths` 키 집합(P-15) · `DocumentCreatedResponse.properties.conversion_id.description` `:1889` | C-M | **X-D4** · §7-1 ⑵ |
| **DC-3** | 〃 | 성공 상태의 `headers` 키 집합이 계약의 그것과 **같다** — `Location`이 선언에서 빠지면 DC-2가 아니라 이 단언이 먼저 깨진다 | `:950-957` | C-M | P-2 |
| **DC-4** | multipart 본문 (파일 파트) | 같은 성공 상태 · 같은 키 집합. **JSON 경로와 기대값이 하나여야 한다** | `requestBody.content` 키 집합 `:933-944` · `DocumentFileRequest` `:1855-1879` | **C-R** | §2 #4 ① · **X-G1** |
| **DC-5** | multipart이되 `Content-Type`의 대소문자를 뒤섞어 보냄 | **성공한다** — 비교가 대소문자를 가리지 않는다(RFC 9110). 가리면 이 요청이 JSON 경로로 새어 422가 된다 | `:911-913` description | **C-R** | **X-G1** |
| **DC-6** | multipart에서 파일 파트 이름을 틀리게 / 파일이 아닌 값으로 | 422 · `detail` **문자열** | `DocumentFileRequest.required` `:1857` | **C-R** | **X-G2** |
| **DC-7** | multipart의 작업 공간 파트가 **빈 문자열** | **미지정과 같게** 다뤄 성공한다. 형식 오류면 422이고 **제출한 값이 `detail`에 담기지 않는다** | `DocumentFileRequest` `:1855-1879` · `x-error-body-universality` 입력값 비반향 | **C-R** | **X-G3** |
| **DC-8** | 본문이 공백뿐 | 422 · `detail` **문자열** · 값이 422 예시 중 **빈 본문 갈래**와 같다 | `:971-987` examples · `DocumentTextRequest.properties.text` `:1835-1845` | C-M | §2 #4 ④ |
| **DC-9** | 본문 길이가 **상한 초과**(원시) | 422 · `detail` **문자열**(배열이 **아님**을 명시 단언) · 값이 422 예시 중 **길이 초과 갈래**와 같다 | `fields[?text]` `:415-419` · `:971-987` | C-M | **X-F1** |
| **DC-10** | 본문 길이가 **정확히 상한**(원시) | **통과** — 경계 한쪽만 걸면 off-by-one이 남는다 | `fields[?text].limit` `:417` | C-M | **X-F1** |
| **DC-11** | **원시 길이는 상한 초과, 제어문자를 걷어내면 상한 이하**인 본문 | **422다**(통과가 아니다). `edited_text`의 같은 모양(CU-6)은 **통과**여야 하므로 **두 케이스가 서로의 대비**다 | `fields[?text].measured_on` `:418` · `DocumentTextRequest...x-service-constraint` `:1837` | C-M | **비대칭 대비** — §7-2 |
| **DC-12** | 업로드 바이트가 **상한 초과**인 파일 | **413**(422가 아니다) · `detail` **문자열** · 사적 헤더 2종 있음 | `x-input-limits.max_upload_bytes` `:328` · `PayloadTooLarge` `:1594-1607` | **C-R** | **X-F3** · X-D2 |
| **DC-13** | 업로드 바이트가 **정확히 상한**인 파일 | **통과**(413이 아니다) | 같음 | **C-R** | **X-F3** |
| **DC-14** | 지원 확장자 집합의 **각 원소**, 대문자·소문자 섞어서 | **전부 통과** — 집합을 계약에서 읽어 케이스를 유도한다 | `x-input-limits.supported_upload_formats` `:331` | **C-R** | **X-F4** |
| **DC-15** | 집합 밖 확장자 / 구버전 워드 컨테이너 / 텍스트 레이어 없는 문서·암호 걸린 문서 / 압축 해제량이 예산 초과 / XML 외부 엔터티 선언 | 전부 422 · `detail` **문자열**. 구버전 컨테이너는 **전용 안내 문구**이며 그 값도 계약에서 읽는다 | `.legacy_doc_policy` `:332-334` · `.rejected_pdf` `:335` · `.zip_uncompressed_budget_bytes` `:330` · `.max_extracted_chars` `:329` | **C-R** | **X-F5·F6·F7·F8** |
| **DC-16** | 남의 작업 공간 식별자 | **404**(403이 **아님**을 명시 단언) · `detail` **문자열** · 값이 404 예시와 같다 | `:962-969` · `info.description` 소유권 절 `:83-86` | C-I | **X-B1** |
| **DC-17** | ⑴ 없는 작업 공간 식별자 ⑵ 남의 작업 공간 식별자 | 두 응답의 **상태·본문 바이트·헤더 집합이 완전히 동일**하다 | 같음 | C-I | **X-B2** |
| **DC-18** | 저장은 됐으나 큐 등록에 실패한 상황 | **502** · `detail` **문자열** · 후속 조회에서 그 변환의 `failure_code`가 계약이 지정한 값이다 | `BadGateway` `:1658-1672` · `:930-931` description · `ConversionResponse.properties.failure_code` `:1993-1998` | C-I | **X-C6** |
| **DC-19** | 큐 의존성이 배선되지 않은 구성 | **503**(502가 **아님**을 명시 단언) · `detail` **문자열** | `ServiceUnavailable` `:1673-1694` | C-P | **X-C6** |
| **DC-20** | `Authorization` 없음 | 401 · `WWW-Authenticate` 값 = 헤더 컴포넌트의 `const` · 본문 최상위 키 집합 **정확히** `ErrorResponse.required` | `:961` · `Unauthorized` `:1553-1585` | **C-R** | X-A1 · **X-C8 / E-2** |
| **DC-21** | **위조 토큰 + 빈 본문** | **401**(422가 아니다) — 인증이 입력 검증보다 먼저다 | `info.description` 인증 우선순위 절 `:88-96` | **C-R** | **X-A3** |
| **DC-22** | 본문에서 필수 필드를 뺀 요청 | 422 · `detail` **배열** · 항목 최상위 키 집합이 **정확히** `ValidationErrorItem.required` | `DocumentTextRequest.required` `:1829` · `ValidationErrorItem.required` `:1728-1729` | C-M | **X-C2** |
| **DC-23** | DC-8·DC-22 | **같은 422에서 문자열 모양과 배열 모양이 모두 나온다** | `ErrorResponse.properties.detail.oneOf` `:1719-1724` | C-M | **X-C3** |

> **DC-11이 이 배치에서 가장 조용히 깨지는 자리다.** 계약이 두 필드에 **다른 측정 축**을
> 지정했는데(§7-2), 구현이 정규화를 한 곳에 몰아 두면 DC-11만 초록에서 빨강으로 뒤집히고
> 나머지는 전부 통과한다. 이 케이스가 없으면 비대칭이 **조용히 해소된 것처럼** 지나가고,
> 그러면 §7-2의 판정 대상 자체가 사라진다.

### 2-2. `GET /documents` (`paths./documents.get` `:996-1039`)

| ID | 요청 | 단언 | 계약 지목 | 계층 | 출처 |
|---|---|---|---|---|---|
| **DL-1** | 유효한 토큰 | 200 · 사적 헤더 2종(값·**개수**) · 본문 키 집합 **정확히** `DocumentListResponse.required` | `:1022-1029` · `DocumentListResponse` `:2043-2074` | C-M | §2 #5 ① · **X-D1**(하한선) |
| **DL-2** | 문서 2건 이상 | `items[]` **각 항목**의 키 집합이 **정확히** `DocumentListItem.required`. 완료 전 항목에서도 **키가 하나도 생략되지 않는다** | `DocumentListItem.required` `:2002-2004` | C-I | **X-E2** |
| **DL-3** | 〃 | `source_format` 값이 그 필드의 enum 집합 안이다. 집합을 계약에서 읽어 대조한다 | `DocumentListItem.properties.source_format.enum` `:2011` | C-I | **X-E4** 인접 |
| **DL-4** | 타인 문서가 함께 존재 | 응답에 **타인 소유 항목이 0건**이다 | `:1001-1003` description | C-I | 소유자 범위 한정 |
| **DL-5** | 페이지 파라미터 하한 미만 / 상한 초과 / 음수 시작점 | 422 · `detail`이 **배열**(문자열이 **아님**을 명시 단언) · 항목 키 집합 정확히 3 | `x-input-limits.list_limit` `:336` · `.list_offset` `:337` · `ValidationFailed` `:1608-1640` | C-M | **지침 3·8 첫 대상**(§3-1) · X-C2 |
| **DL-6** | 파라미터가 **정확히 하한** / **정확히 상한** / 시작점이 **정확히 하한** | **통과** — 경계 양쪽 | 같음 | C-M | §2 #5 ④ |
| **DL-7** | 파라미터를 아예 주지 않음 | 통과하고, 응답의 페이지 필드가 계약의 **기본값**과 같다 | `.list_limit.default` · `.list_offset.default` `:336-337` | C-M | §2 #5 ① |
| **DL-8** | 다음 쪽이 있는 상태 / 없는 상태 | 다음 쪽 유무 필드가 각각 참·거짓이다. **총 개수 필드는 없다** — DL-1의 「정확히」가 그 추가를 겸해 잡는다 | `DocumentListResponse` `:2043-2074` | C-I | §2 #5 ① |
| **DL-9** | 남의 작업 공간 식별자로 걸러 조회 | **404**(빈 목록이 **아님**을 명시 단언) | `:1031-1035` · `:1013-1020` | C-I | **X-B1** |
| **DL-10** | `Authorization` 없음 | 401 · `WWW-Authenticate` | `:1030` | **C-R** | X-A1 |
| **DL-11** | **위조 토큰 + 범위 밖 페이지 파라미터** | **401**(422가 아니다) | `info.description` `:88-96` | **C-R** | **X-A3** — 계약 산문이 지목한 실측 케이스 그대로 |

### 2-3. `DELETE /documents/{document_id}` (`paths./documents/{document_id}.delete` `:1046-1079`)

| ID | 요청 | 단언 | 계약 지목 | 계층 | 출처 |
|---|---|---|---|---|---|
| **DD-1** | 내 문서 | 204 · **본문 길이 0** · 사적 헤더 2종 **있음**(전역 부착으로 부호 반전) | `:1066-1067` · `x-global-response-headers.applies_to` `:553-558` | C-I | §2 #6 ① · **X-D2** |
| **DD-2** | **타인 소유** 문서 | 404 · **403이 아님을 명시 단언** · `detail`이 404 예시와 같다 | `:1069-1075` · `:83-86` | C-I | **X-B1** |
| **DD-3** | ⑴ 없는 식별자 ⑵ 타인 소유 식별자 | 상태·본문 바이트·헤더 집합이 **완전히 동일** | 같음 | C-I | **X-B2** |
| **DD-4** | 삭제 성공 직후 같은 식별자로 재요청 | 404(204 아님) — 멱등 구현이 이 자리에서 갈린다 | `:1066-1075` | C-I | workspaces WD-8과 같은 근거 |
| **DD-5** | 삭제 후 그 문서의 변환 조회 | 404 — **변환 결과도 함께 파기된다**. 계약이 summary에서 그것을 선언한다 | `:1049` summary · `:1051-1059` description | C-I | §2 #6 ① |
| **DD-6** | UUID가 아닌 경로 변수 | 422 · `detail` **배열** · 항목 키 정확히 3 | `:1076-1077` · `ValidationFailed` | C-M | §2 #6 ④ |
| **DD-7** | `Authorization` 없음 | 401 · `WWW-Authenticate` | `:1068` | **C-R** | X-A1 |

### 2-4. `GET /conversions/{conversion_id}` (`paths./conversions/{conversion_id}.get` `:1092-1122`)

| ID | 요청 | 단언 | 계약 지목 | 계층 | 출처 |
|---|---|---|---|---|---|
| **CR-1** | 내 변환 (완료 상태) | 200 · 사적 헤더 2종(값·**개수**) · 본문 키 집합 **정확히** `ConversionResponse.required` | `:1103-1110` · `ConversionResponse.required` `:1945-1948` | C-M | §2 #7 ① · **X-D1**(하한선) |
| **CR-2** | 상태 enum의 **각 값**에 해당하는 변환 | 상태 값이 그 집합 안이고, **완료 전 상태에서도 키가 하나도 생략되지 않는다**(값이 null일 뿐) | `ConversionStatus.enum` `:1749` · `ConversionResponse.description` `:1949-1956` | C-I | **X-E2 · X-E4** |
| **CR-3** | 완료 전 상태 | 배열 필드 둘이 `null`이 아니라 **빈 배열**이다 | `:1973-1988` | C-I | **X-E3** |
| **CR-4** | 실패 상태 | 실패 코드 필드가 비어 있지 않고 **문서 본문·모델 응답이 담기지 않는다**(길이 상한도 계약에서 읽어 대조) | `:1993-1998` | C-I | §2 #7 추가 · privacy 인접 |
| **CR-5** | 마스킹 항목이 있는 완료 변환 | 각 항목의 키 집합 **정확히** `MaskedItemResponse.required` · 범주 값이 **2종 집합 안**이고 그 밖의 값(영문 코드·사라진 범주)이 **0건** · 자리표시자가 계약 `pattern`에 맞는다 | `MaskedItemResponse` `:1895-1927` · `.properties.category.enum` · `.properties.placeholder.pattern` | C-I | §2 #7 ① · 마스킹 2종 |
| **CR-6** | 〃 | 유실 자리표시자 필드의 각 원소가 계약 `items.pattern`에 맞는다 | `ConversionResponse.properties.missing_placeholders` `:1980-1988` | C-I | X-E3 인접 |
| **CR-7** | **타인 소유** 변환 | 404 · **403이 아님을 명시 단언** | `:1112-1118` · `:83-86` | C-I | **X-B1** |
| **CR-8** | ⑴ 없는 식별자 ⑵ 타인 소유 식별자 | 상태·본문·헤더 **완전히 동일** | 같음 | C-I | **X-B2** |
| **CR-9** | UUID가 아닌 경로 변수 | 422 · `detail` **배열** | `:1119-1120` | C-M | §2 #7 ④ |
| **CR-10** | `Authorization` 없음 | 401 · `WWW-Authenticate` · 본문 키 집합 정확히 `ErrorResponse.required` | `:1111` | **C-R** | X-A1 · X-C8 |

> **CR-1이 X-D1 하한선의 셋째 자리다.** 이 응답에는 가려졌던 실제 개인정보가 실린다
> (`MaskedItemResponse.properties.original`). 전역 필터가 있어도 **개별 단언을 지우지 않는
> 이유**가 여기서 가장 뚜렷하다 — 필터가 빠지면 이 응답이 프록시에 캐시된다.

### 2-5. `PUT /conversions/{conversion_id}` (`paths./conversions/{conversion_id}.put` `:1123-1177`)

| ID | 요청 | 단언 | 계약 지목 | 계층 | 출처 |
|---|---|---|---|---|---|
| **CU-1** | 완료된 내 변환 + 유효한 수정본 | 200 · 사적 헤더 2종(값·**개수**) · 응답 키 집합이 **`ConversionResponse.required`와 같다**(조회와 같은 스키마) | `:1147-1154` · `:1139-1140` description | C-I | §2 #8 ① · **X-D1**(하한선 · §2.7 해결 1) |
| **CU-2** | 〃 | **AI 초안 필드가 보존된다** — 저장 전 값과 같고, 수정본 필드만 바뀐다. 검수 시각 필드가 채워진다 | `:1128-1130` description · `ConversionResponse.properties.edited_text` `:1964-1969` | C-I | §2 #8 ① |
| **CU-3** | 아직 완료되지 않은 변환 | **409**(422도 404도 아님) · `detail`이 409 예시와 같다 | `:1161-1167` | C-I | §2 #8 추가 |
| **CU-4** | 수정본이 **제어문자만**으로 이뤄짐 | 422 · `detail` **문자열** · 값이 422 예시 중 **빈 값 갈래**와 같다 — 「제어문자만 담긴 수정본」이 빈 값과 같은 취급을 받는다 | `:1135-1137` description · `:1168-1175` examples · `ConversionReviewRequest.properties.edited_text` `:1932-1942` | C-I | §2 #8 ④ |
| **CU-5** | 정규화 후 길이가 **상한 초과** / **정확히 상한** | 각각 422(문자열 `detail`, 배열이 **아님**) / 통과 | `fields[?edited_text]` `:420-424` | C-I | **X-F2** |
| **CU-6** | **원시 길이는 상한 초과, 제어문자를 걷어내면 상한 이하** | **통과** — DC-11의 대비 쌍이다. 스키마 층 `maxLength` 구현이면 여기서 깨진다 | `fields[?edited_text].measured_on` `:423` · `x-service-constraint.measured_on` `:1934` | C-I | **X-F9**(`edited_text` — F3 다섯 필드의 **마지막** 마감) |
| **CU-7** | 통과한 수정본을 저장한 뒤 다시 조회 | 저장된 값이 **정규화된 값**이다(원시가 아니다) — 정규화가 **저장까지** 갔음을 보인다. CU-6만으로는 판정에만 쓰고 버리는 구현이 통과한다 | `:1135-1137` | C-I | X-F9 축(WC-8과 같은 형태) |
| **CU-8** | **타인 소유** 변환 | 404 · **403이 아님** | `:1156-1160` | C-I | **X-B1** |
| **CU-9** | 필수 필드를 뺀 본문 | 422 · `detail` **배열** · 항목 키 정확히 3 | `ConversionReviewRequest.required` `:1930` | C-M | X-C2 |
| **CU-10** | 같은 자원에 **PATCH** | **PATCH는 이 경로의 계약에 없다.** 서비스되는 (경로, 메서드) 집합이 계약 `paths`와 같다는 단언이 이것을 겸한다 — 개별 케이스를 두지 않는다 | `paths` 전체(P-15) | C-M | §7-3 **O-15** |
| **CU-11** | `Authorization` 없음 | 401 · `WWW-Authenticate` | `:1155` | **C-R** | X-A1 |

### 2-6. `GET /conversions/{conversion_id}/export` (`paths./conversions/{conversion_id}/export.get` `:1184-1306`)

| ID | 요청 | 단언 | 계약 지목 | 계층 | 출처 |
|---|---|---|---|---|---|
| **CE-1** | 완료·검수된 내 변환 + 형식 파라미터의 **각 값** | 200 · 각 값에 대응하는 **미디어 타입**이 계약의 200 `content` 키와 같다. **형식↔미디어 타입 대응을 코드에 두지 않고** 두 노드를 계약에서 읽어 짝짓는다 | `ExportFormat.enum` `:1762` · `responses.200.content` 키 집합 `:1278-1284` | **C-R** | §2 #9 ① · **X-D6**(문자셋 표기 포함) |
| **CE-2** | 〃 | 사적 헤더 2종(값·**개수**) | `:1276-1277` · `x-private-response-headers.applies_to` | **C-R** | **X-D1**(하한선 — **10곳의 마지막**) |
| **CE-3** | 〃 (한글이 든 제목) | `Content-Disposition`에 **ASCII 대체 파라미터**와 **RFC 5987 확장 파라미터**가 **둘 다** 있다. 구조는 계약 인라인 헤더의 `schema.examples` 첫 원소를 **파싱해** 얻는다 — 형식 문자열을 코드에 적지 않는다 | `:1221-1275`(인라인 선언) · `.schema.examples` `:1274-1275` | **C-R** | **X-D5** · §7-1 ⑵ |
| **CE-4** | 〃 | 확장 파라미터를 퍼센트 디코딩한 값이 **제목에서 유도된 이름**이고, ASCII 대체 파라미터의 확장자와 요청한 형식이 대응한다 | 같음 · `ExportFormat.description` `:1763-1765` | **C-R** | X-D5 |
| **CE-5** | 제목에 **금지 집합의 문자**가 든 문서 — 제어문자 세 블록(C0·DEL·C1)과 경로 구분자·따옴표·예약 문자를 **각각** | 확장 파라미터를 퍼센트 디코딩한 값에 **금지 집합의 문자가 하나도 없다**. 집합은 계약에서 읽고 케이스를 그 집합에서 **유도한다** — 손으로 열거하지 않는다 | **`x-filename-charset.forbidden`** `:1241`(2026-08-19 신설) · `.measured_on` `:1240` · `.check` `:1242-1249` | **C-R** | **L348 F-3·F-4** · §7-1 ⑵ |
| **CE-6** | 제목이 금지 문자만으로 이뤄져 **통째로 지워지는** 문서 | 그래도 이름이 비어 있지 않다(대체 이름이 선다) · 헤더가 여전히 계약 구조를 만족한다 | `:1222-1237` description · `.check` | **C-R** | L348 인접 |
| **CE-7** | 형식 파라미터 **누락** | 422 · `detail`. 파라미터가 `required: true`임을 계약에서 읽어 케이스를 세운다 | `parameters[?format]` `:1208-1213` | C-M | §2 #9 ④ |
| **CE-8** | 형식 enum **밖의 값**(과거 후보 형식 포함) | 422 | `ExportFormat.enum` `:1762` · `:1300-1304` | C-M | §2 #9 ④ |
| **CE-9** | 아직 완료되지 않은 변환 | 409 · `detail`이 409 예시 중 **미완료 갈래**와 같다 | `:1291-1299` | C-I | §2 #9 추가 |
| **CE-10** | 검수본이 없고 유실 자리표시자가 **비어 있지 않은** 변환 | 409 · `detail`이 409 예시 중 **유실 갈래**와 같고 **미완료 갈래와 다름**을 함께 단언 | `:1291-1299` · `:1197-1202` description | C-I | §2 #9 추가 |
| **CE-11** | 같은 상태이되 **검수본이 있는** 변환 | **막지 않는다**(200) — 최종 판단은 담당자 몫이다. CE-10과 이 케이스가 한 쌍이어야 조항이 고정된다 | `:1200-1202` | C-I | §2 #9 추가 |
| **CE-12** | 검수본이 있는 변환 / 없는 변환 | 산출물의 본문이 각각 검수본 / AI 초안에서 온다 | `:1194-1195` description | C-I | §2 #9 ① |
| **CE-13** | **타인 소유** 변환 | 404 · **403이 아님** | `:1286-1290` | C-I | **X-B1** |
| **CE-14** | **위조 토큰 + enum 밖 형식 값** | **401**(422가 아니다) | `info.description` `:88-96` | **C-R** | **X-A3** — 계약 산문이 지목한 실측 케이스 그대로 |
| **CE-15** | `Authorization` 없음 | 401 · `WWW-Authenticate` | `:1285` | **C-R** | X-A1 |

> **CE-5는 이 배치가 L348에 대해 새로 여는 자리다.** 종전에는 금지 집합이 구현
> (`backend-kotlin/core/.../easyread/Export.kt`)과 게이트 12 판정에만 있었고 **어느 단언에도
> 걸리지 않았다**(F-4). 2026-08-19에 계약이 그 집합을 오라클로 받았으므로(§7-1 ⑵) 이제
> 경계에서 잴 수 있다. **다만 이것으로 L348을 「예」로 올리지 않는다** — 그 판정은 리더의
> 것이고, F-3·F-4의 parity fixture 몫은 그대로 남는다(§7-3 O-19).

---

## 3. 횡단 항목 — 이 단위에 걸리는 것과 걸리지 않는 것

| 항목 | documents 배치 처리 | 근거 |
|---|---|---|
| **X-A1** 401 전체에 `WWW-Authenticate` | DC-20·DL-10·DD-7·CR-10·CU-11·CE-15 | `WWWAuthenticateBearer` |
| **X-A2** 실패 균일성 | **auth에서 마감.** 이 단위는 401 갈래를 새로 만들지 않는다(같은 인증 필터). **정정된 범위**(자격증명이 제시된 갈래)는 test-plan §2 X-A2 행에 2026-08-19 반영됨 | `x-auth.failure_uniformity` |
| **X-A3** 인증이 입력 검증보다 먼저 | **DC-21·DL-11·CE-14 — 계약 산문이 실측으로 지목한 세 케이스가 전부 이 배치에 있다**(`:93-95`). workspaces에서 본문·경로 변수 두 축을 열었고, 여기서 **쿼리 파라미터** 축이 는다 | `:88-96` |
| **X-A4·X-I1·X-I2** | **auth에서 마감.** 신규 없음 | auth 명세 §3 |
| **X-A5** 짧은 서명 키 → 503 | **신규 없음** — 같은 구성에서 같은 경로를 지난다. WL-7이 대표 1건 | workspaces 명세 §3 |
| **X-B1** 소유권 은닉 404(403 **아님**) | DC-16·DL-9·DD-2·CR-7·CU-8·CE-13 — **여섯 오퍼레이션 전부**. 이 배치가 소유권 축의 대상이 가장 많은 단위다 | `:83-86` |
| **X-B2** 없는 자원 ≡ 타인 자원 | DC-17·DD-3·CR-8 (문서·변환·작업 공간 참조 세 종류의 식별자 각각) | 같음 |
| **X-C1** 비밀번호 미반향 | **이 단위에 대상이 없다** — 요청에 자격증명 필드가 없다. 다만 **입력값 비반향의 다른 얼굴**이 DC-7에 있다(제출한 식별자가 `detail`에 담기지 않는다) | — |
| **X-C2** 422 항목 키 3개 | DC-22·DD-6·CR-9·CU-9·**DL-5** | `ValidationErrorItem` |
| **X-C3** 문자열/배열이 둘 다 | DC-23 | `ErrorResponse.detail.oneOf` |
| **X-C4 / E-3** 오류 `Content-Type` | DC-8·DC-12·CE-9의 응답에서 확인 | `x-error-body-universality` E-3 |
| **X-C5** 500 두 문구 | **신규 없음** — 기존 `ErrorContractTest`가 프로브로 덮는다 | — |
| **X-C6** 502 ≠ 503 | **DC-18·DC-19 — 이 배치가 여는 첫 실행 경로다.** 계약이 두 코드를 가른 이유(재시도 가치)가 실물로 서는 자리 | `BadGateway` · `ServiceUnavailable` · `:930-931` |
| **X-C7 / E-1** `sendError` → `/error` 본문 | **기존 `ContractErrorBodyReachTest`가 덮는다.** 신규 없음 | `x-error-body-universality` E-1 |
| **X-C8 / E-2** 401 본문이 구현 수단을 가리지 않음 | DC-20·CR-10의 **본문 키 집합** 단언. 이 배치가 더하는 것은 **새 경로에서도 서는가**뿐 | E-2 |
| **X-C9 / E-4** 컨테이너 생성 응답의 본문 5종 | **이 단위 밖이다 — 열린 채로 이월한다.** 대상이 늘지 않았다 | `x-error-body-universality.x-unmeasured` |
| **X-D1** 하한선 10곳 | **DL-1·CR-1·CU-1·CE-2 — 남은 4곳이 전부 여기 있다. 이 배치로 10곳이 닫힌다** | `x-private-response-headers.applies_to` |
| **X-D2** 오류·204 응답에도 헤더 있음 | DC-12·DD-1(**204**)·DC-8·CE-9 | `x-global-response-headers` `:553-558` |
| **X-D2b** 중복 부착 부재 | DL-1·CR-1·CU-1·CE-2에서 **개수까지** 단언. **CE-2가 특히 중요하다** — 파일 응답은 헤더 조립 경로가 JSON 응답과 다르다 | `enforcement` |
| **X-D2c~e** 컨테이너 도달·밸브 음성 대조·결합 | **신규 없음.** 대상이 늘지 않았다 | `x-phase3-measurement` |
| **X-D4** `Location` = 본문 식별자 | **DC-2 — 첫 실행 경로** | `:950-957` |
| **X-D5** `Content-Disposition` 양쪽 표기 | **CE-3·CE-4 — 첫 실행 경로** | `:1221-1275` |
| **X-D6** 문자 인코딩 표기 | **CE-1**(형식별 미디어 타입을 계약 키에서 읽으므로 자동으로 포함) | `responses.200.content` |
| **X-E1** snake_case | DC-1·DL-1·DL-2·CR-1의 키 집합 대조가 겸한다. **이 배치가 그 축의 시험대다** — 여러 단어 필드가 가장 많다 | `info.description` 필드 이름 절 |
| **X-E2** 키 생략 금지 | CR-2·DL-2 — 「완료 전에도 전부 존재」가 이 축의 본론이다 | `ConversionResponse.description` |
| **X-E3** 빈 배열 대 null | CR-3 | 같음 |
| **X-E4** 상태 4종 밖 없음 | CR-2 (+ DL-3의 원본 형식 enum) | `ConversionStatus` |
| **X-F1·X-F2** 본문·수정본 상한 | DC-9·DC-10 / CU-5 — **`detail` 타입 단언 포함** | `fields[?text]` · `fields[?edited_text]` |
| **X-F9** 정규화 후 경계 | **CU-6·CU-7 — F3 다섯 필드의 마지막(`edited_text`) 마감** | `fields[?edited_text].measured_on` |
| **X-F3~X-F8** 업로드 상한·형식·거절 | DC-12~DC-15 | `x-input-limits` |
| **X-G1~X-G3** multipart | DC-4·DC-5·DC-6·DC-7 | `requestBody.content` · `DocumentFileRequest` |
| **X-H1~X-H4** CORS | **이 단위에 신규 대상 둘** — 노출 헤더 목록의 **두 원소가 이 배치에서 처음 실물로 나간다**(접수 주소·다운로드 파일명). 서버 쪽 노출 선언은 여기서 확인하고 **브라우저 확인은 X-H5로 이월**한다 | `x-cors.expose_headers` `:196-` |
| **X-H5·X-H6** 브라우저 | **이월** — Phase 6. C 계층으로는 증명되지 않는다 | 같음 |
| **X-J1·X-J3** | **조항 없음 — 쓸 수 없다**(§7-3 O-17) | test-plan §3-1 |
| **X-J2** 계약 직접 파싱 | §4 전체 | test-plan §3-1 |

### 3-1. 지침 3·8 — **이 단위가 첫 대상이다**

auth 명세 §3과 workspaces 명세 §3-1이 두 지침의 마감을 「`limit`/`offset`이 생기는 단위」로
이월했다. **그 단위가 여기다.**

| 무엇 | 사실 |
|---|---|
| 이 단위가 **새로 여는 것** | 쿼리 파라미터가 이 저장소에 처음 생긴다(`paths./documents.get.parameters` `:1004-1020`). 계약이 그 자리에 **하한·상한·기본값을 스키마 제약으로** 선언한다 |
| **지침 3** — Bean Validation으로 강제 | 계약이 이 두 파라미터를 **스키마 층**으로 못박았다(`x-input-limits`의 인라인 주석 · `ValidationFailed`의 경계 서술). 따라서 위반의 `detail`은 **배열**이다 — F3 다섯 필드와 **정반대**이며, 그 대비가 F3 판정의 근거였다(`x-contrast-case`) |
| **지침 8** — `handleHandlerMethodValidationException` | 제약 애너테이션이 붙은 파라미터의 위반은 이 예외로 나온다. 명시 처리하지 않으면 검증 실패가 `{"detail": …}` 밖으로 샌다. **DL-5가 그 실행 경로다** |
| 함께 들어오는 것 | `spring-boot-starter-validation`이 클래스패스에 처음 들어온다. **그 순간 F3 음성 대조의 1차 방벽(의존성 부재)이 사라진다** — 그때부터 `RequestFieldConstraintLayerTest`가 유일한 강제자다(`03_contract-keeper_auth-verification.md` §7 · workspaces 명세 §3-1이 예고한 전환) |

**DL-5의 `detail` **타입** 단언이 이 지침들의 마감 판정이다.** 상태 코드만 보면 문자열
`detail`을 내는 구현도 통과하고, 그러면 지침 3이 요구한 「스키마 층 강제」가 실제로 섰는지
알 수 없다. **역방향도 마찬가지다** — F3 다섯 필드에 `@Size`가 들어오면 DC-9·CU-5의 타입
단언이 빨강이 된다. 두 축이 서로의 대비 사례이므로 **같은 커밋에 함께** 들어가야 한다(§6).

**「DL-5가 초록이면 지침 8이 덮였다」로 읽지 않는다.** DL-5가 재는 것은 **나간 바이트**이고,
그것을 어느 핸들러가 만들었는지는 계약의 관심 밖이다. 지침 8이 요구하는 것은 그 갈래가
**계약 밖으로 새지 않는 것**이며, DL-5는 정확히 그것만 판정한다.

---

## 4. OQ-3 파서 요건 — 추가로 읽어야 하는 노드

**조항은 2026-08-15에 닫혔다: 직접 파싱이다.** 아래는 auth의 P-1~P-15, workspaces의
P-16~P-21, **및 P-22**에 **더해지는** 것이다. 이미 등재된 노드는 다시 적지 않는다 — 경로에
매개되지 않는 일반 접근자(P-1~P-4·P-8~P-10·P-13~P-15·P-17)는 이 배치의 여섯 오퍼레이션이
자동으로 그 범위에 든다.

> **P-22 사후 등재 (2026-08-20 · 게이트 26 K5).** `P-22`는 구현 레인이 D-2 판정 커밋에서
> `backend-kotlin/api/src/test/kotlin/kr/easydoc/api/support/ContractSpec.kt:409`에
> `deletionRefusalPrecedenceExample()`(= `paths./workspaces/{workspace_id}.delete.description`의
> 삭제 거절 **우선순위 산문**)으로 신설했고, **어느 명세에도 등재되지 않았다.** 이 문서의 초판이
> 그 사실을 모른 채 같은 번호를 인라인 헤더 `schema.examples[0]`에 다시 매겨 **한 ID가 두 노드**가
> 됐다. 여기서 **`ContractSpec.kt`의 것을 P-22로 확정**하고(선점 + `03_kotlin-implementer_workspaces-fixes.md:217`
> 등 기존 인용 보존), 이 문서가 쓰던 노드를 **P-37**로 옮긴다. **workspaces 명세는 사후 편집하지
> 않는다** — 그 문서 §7 O-12가 세운 선례(「auth 명세를 사후 편집하지 않고 여기서 정정한다」)를 따라
> 등재를 **현재 문서**에서 한다.
>
> 번호만 바꾸면 같은 사고가 다음 배치에서 다시 난다. 원인은 우연한 충돌이 아니라
> **`P-*`에 단일 소유 문서가 없다**는 구조다(`X-*`는 `00_contract-keeper_test-plan.md` §3 한 곳에서만
> 정의되고 배치 명세는 참조만 한다). 탐지 장치는 계획 §3-3의 `ParserNodeRegistryTest`이며
> **이 문서가 그 강제자의 입력**이다 — ⑴ 한 ID를 둘 이상의 명세가 정의하면 실패 ⑵ `ContractSpec.kt`
> KDoc 라벨 중 어느 명세에도 등재되지 않은 것이 있으면 실패 ⑶ 정의 ID 합집합이 1..max 연속
> ⑷ 총수 정확 일치. **이 편집 직후 실측값**: 세 명세의 정의 행 **36**(auth 15 · workspaces 6 ·
> documents 15) + `ContractSpec.kt` 전용 등재 **1**(P-22) = **합집합 37, P-1~P-37 연속**.
> 두 숫자를 각각 고정한다 — 합집합만 재면 정의가 명세에서 코드로 새어도 초록이다.
> 구조 이전(`P-*`를 test-plan §3으로 단일화)은 계획 §10 L-4로 이월했다.

### 4-1. 새로 읽어야 하는 노드

| ID | 노드 (키 경로) | 쓰는 케이스 | 하드코딩하면 무엇이 새는가 |
|---|---|---|---|
| **P-23** | **`x-filename-charset`의 `forbidden`·`measured_on`** — 같은 헤더 노드의 형제 `:1238-1271` | **CE-5·CE-6** | 금지 집합이 테스트 코드에 복제된다 — 그러면 계약과 구현이 갈려도 테스트가 **자기 사본**과 대조해 초록이다. F-4가 지적한 「요구가 어느 단언에도 안 걸린다」를 「요구가 자기 사본에만 걸린다」로 바꿔 놓는 셈 |
| **P-24** | `x-input-limits.max_upload_bytes` `:328` | DC-12·DC-13(경계 양쪽) | 상한이 바뀌어도 옛 경계를 잰다 |
| **P-25** | `x-input-limits.list_limit`·`list_offset`의 `min`·`max`·`default` `:336-337` | DL-5·DL-6·DL-7 | 같음. **기본값까지 읽어야** DL-7이 산다 |
| **P-26** | `x-input-limits.supported_upload_formats` `:331` | **DC-14의 케이스 유도** | 지원 형식이 늘어도 검사 범위가 늘지 않는다 — 새 형식이 **검사 자체를 받지 않는다**(P-15가 경로에서 막는 것과 같은 형태) |
| **P-27** | `x-input-limits`의 `legacy_doc_policy`·`rejected_pdf`·`zip_uncompressed_budget_bytes`·`max_extracted_chars` `:329-335` | DC-15 | 전용 안내 문구와 예산 값이 코드에 복제된다 |
| **P-28** | `components/schemas/ExportFormat.enum` `:1762` | **CE-1·CE-8의 케이스 유도** | 형식이 늘거나 줄어도 케이스가 그대로다 |
| **P-29** | `paths..../export.responses.200.content`의 **키 집합**(미디어 타입) `:1278-1284` | **CE-1** | 형식↔미디어 타입 대응표가 테스트에 손으로 복제된다. 문자셋 표기 하나가 빠져도 알 수 없다 |
| **P-30** | `paths..../export.parameters[?name=="format"]`의 `required`·`in`·`schema.$ref` `:1208-1213` | CE-7·CE-8 | 파라미터 이름·필수 여부가 코드에서 온다. 계약이 선택으로 바꿔도 테스트가 필수를 요구한다 |
| **P-31** | `components/schemas/ConversionStatus.enum` `:1749` | CR-2 | 상태 집합이 코드에 복제된다 — DB CHECK와 같은 집합이라 갈리면 저장 실패로 나타난다 |
| **P-32** | `components/schemas/MaskedItemResponse`의 `required` · `properties.category.enum` · `properties.placeholder.pattern` `:1895-1927` | **CR-5** | 마스킹 범주 2종이 코드에 복제된다. 이 값은 **화면 문구 자체**라(React가 그대로 렌더링) 복제본이 갈리면 화면이 갈린다 |
| **P-33** | 스키마 `required` 넷 — `DocumentCreatedResponse` `:1882` · `DocumentListResponse` `:2045-2047` · `DocumentListItem` `:2002-2004` · `ConversionResponse` `:1945-1948` | DC-1·DL-1·DL-2·CR-1·CU-1 | 키 집합이 코드에 복제된다. **Jackson 네이밍 사고가 이 대조에서만 잡힌다** |
| **P-34** | `x-request-field-constraints.fields[]`에서 `DocumentTextRequest.text` `:415-419` · `ConversionReviewRequest.edited_text` `:420-424`의 `limit`·`measured_on`·`detail` | DC-9~DC-11 · CU-4~CU-7 | F3 강제자 축이 두 필드에서 빈다 |
| **P-35** | **P-34와 3자 대조**(§4-3) — `x-input-limits.max_convertible_chars` `:326` · `.max_review_chars` `:327` · 두 스키마의 `x-service-constraint` `:1837`·`:1934` | 같음 | 같은 상한이 계약 안에 **세 벌**이다. 한쪽만 읽으면 다른 쪽이 갈려도 아무 데서도 안 걸린다 |
| **P-36** | `paths./documents.post.requestBody.content`의 **키 집합** `:933-944` + `DocumentTextRequest`·`DocumentFileRequest`의 `required`·속성 이름 | DC-4·DC-6·DC-7·DC-22 | multipart 파트 이름이 코드에 복제된다. **자동 생성물에 없는 유일한 요청 본문**이라 계약 파일이 유일한 기록이다 |
| **P-37** | 응답 **인라인 헤더**의 `schema.examples` 첫 원소 — `paths.<path>.<method>.responses.<status>.headers.<name>.schema.examples[0]` | DC-2(참고) · **CE-3·CE-4** | 인라인 갈래는 `const`가 없어 P-3(컴포넌트 `const`)으로 못 읽는다. 구조 오라클이 없으면 헤더 형식 문자열이 코드에 복제되고, 계약이 형식을 바꿔도 테스트가 옛 형식을 요구한다. **P-17이 경로 인라인 `examples`를 오라클로 쓴 것과 같은 갈래다** |

### 4-2. 파서가 지켜야 하는 성질

**auth 명세 §4-2의 다섯 성질과 workspaces 명세의 여섯째(합성 스키마)가 그대로 적용된다.**
여기 다시 적지 않는다.

**이 배치가 더하는 성질 하나.**

7. **정규 표현 조각을 읽었으면 그것으로 케이스를 유도한다.** P-23·P-32는 문자 집합·패턴을
   돌려주는데, 이것을 「존재하는지」만 확인하고 케이스는 손으로 만드는 배선이 가장 흔한 실패
   형태다. 그러면 계약을 읽었다는 **외양**만 남고 실제 대상은 코드에서 온다. 집합이 늘었을 때
   케이스도 함께 늘어야 하며, 그 결속은 §4-4의 음성 대조가 잰다.

### 4-3. P-35의 자리 — 두 필드 모두 **삼중 선언**이다

workspaces 명세 §4-3이 `name` 한 필드에서 세 벌을 확인했다. **이 배치의 두 필드도 같은 형태다**
(2026-08-19 실측).

| 축 | `text` | `edited_text` |
|---|---|---|
| `x-input-limits` | `:326` | `:327` |
| `x-request-field-constraints.fields[].limit` | `:417` | `:422` |
| 스키마의 `x-service-constraint.max_length` | `:1837` | `:1934` |

**`measured_on`의 어휘가 둘인 것도 같다** — `fields[].measured_on`은 한국어 산문이고
`x-service-constraint.measured_on`은 토큰이다. 대조는 **매핑을 거쳐** 하고, 매핑에 없는 값이
나오면 **실패한다**(workspaces §4-3의 규약 그대로). **이 배치에서는 그 매핑이 특히 중요하다** —
두 필드의 토큰이 서로 **다르며**, 그 다름이 곧 §7-2의 판정 대상이다. 매핑이 느슨해 둘을 같게
읽으면 비대칭이 **파서 층에서** 사라진다.

**계약은 고치지 않는다.** 세 벌을 합치는 것은 구조 변경인데 근거가 없다 — 세 값이 실제로 같고
절이 나뉜 데는 이유가 있다(값 / 적용 방식 / 스키마 표식). 탐지로 충분한 자리를 구조 변경으로
풀지 않는다.

### 4-4. 음성 대조 — 계약 값을 바꾸면 테스트가 깨지는가

**일회용 worktree에서만 한다. 규칙 5를 따른다 — `cp`로 복원하지 않는다.**

| # | worktree에서 바꿀 노드 | **깨져야 하는 케이스** | 안 깨지면 무엇이 틀린 것인가 |
|---|---|---|---|
| N-20 | **`x-filename-charset.forbidden`에서 C1 구간만 제거** | **CE-5의 C1 케이스**(다른 블록 케이스는 **깨지지 않아야** 한다) | **P-23이 배선되지 않았다** — 금지 집합이 코드에서 온다. F-4가 지적한 자리가 그대로 남아 있다는 뜻 |
| N-21 | `ExportFormat.enum`에서 원소 하나 제거 | CE-1·CE-8 | P-28이 케이스를 유도하지 않는다 |
| N-22 | export 200 `content`의 키 하나에서 문자셋 표기 제거 | **CE-1**(X-D6) | P-29가 배선되지 않았다 — 미디어 타입이 코드에서 온다 |
| N-23 | `x-input-limits.max_upload_bytes` 값 변경 | DC-12·DC-13(경계 양쪽) | 상한이 코드에 복제돼 있다 |
| N-24 | `x-input-limits.list_limit.max`만 변경 | DL-5·DL-6 + **P-35 대조 단언** | 이중·삼중 선언 대조가 배선되지 않았다 |
| N-25 | **`fields[?text].measured_on`을 `edited_text`와 같은 축으로 변경** | **DC-11**(CU-6은 **깨지지 않아야** 한다) | **두 필드의 측정 축이 한 값으로 뭉개져 있다** — §7-2의 판정 대상이 파서에서 사라진 상태 |
| N-26 | `MaskedItemResponse.properties.category.enum`에서 원소 하나 제거 | CR-5 | P-32가 배선되지 않았다 — 화면 문구가 테스트 코드에서 온다 |
| N-27 | `ConversionResponse.required`에서 키 하나 제거 | CR-1·CU-1 | P-33이 배선되지 않았다 |
| N-28 | `paths`의 `/conversions/{conversion_id}` 경로 템플릿 변경 | **DC-2** + 그 경로를 쓰는 CR-*·CU-*·CE-*의 URL 조립 전건 | `Location` 형식이 코드에서 온다(P-37·P-15 결속) |
| N-29 | `paths..../export.parameters[?format].required`를 거짓으로 | CE-7 | P-30이 배선되지 않았다 |
| N-30 | 인라인 헤더 `schema.examples[0]`에서 파라미터 하나 제거 | **CE-3** | P-37이 배선되지 않았다 — 헤더 구조가 코드에서 온다 |

**과잉 결합도 함께 본다.** 각 변경에서 깨지는 것이 **위 열의 케이스뿐**인지 확인한다.
무관한 테스트가 함께 깨지면 파서가 값을 엉뚱한 곳으로 흘리고 있는 것이다. **N-20·N-25는
「깨지지 않아야 하는 짝」을 함께 적었다** — 그 짝이 함께 깨지면 두 축이 한 값으로 묶여 있다는
뜻이고, 그것은 하드코딩과 같은 등급의 결함이다.

**복원 절차(규칙 5).**

```bash
git -C <worktree> diff --stat          # 변경이 계약 파일 1개뿐인지 확인
git -C <worktree> checkout -- contracts/easy-doc-v1.yaml
shasum -a 256 <worktree>/contracts/easy-doc-v1.yaml contracts/easy-doc-v1.yaml   # 두 해시 일치 확인
# 대조가 끝나면 worktree 자체를 제거한다
```

게이트 실행·음성 대조처럼 **종료 코드가 판정 근거인 명령은 파이프 없이 직접** 돌린다.

**여전히 열린 채로 이월하는 것 — N-10**(같은 헤더 이름이 서로 다른 컴포넌트를 가리키게 만들기)이
auth에서 등재되고 workspaces에서도 밟히지 않았다. **이 배치에서 성격이 조금 달라졌다** —
`44eec3f`의 fail-closed 4자리 중 하나가 「컴포넌트 ↔ 인라인 혼재」를 끊으므로 그 갈래는 이미
강제자가 있다. 남은 것은 **컴포넌트끼리 갈리는** 경우이고, 대상이 늘지 않았으므로 여기서 새로
요구하지 않는다.

---

## 5. 계층 지목

test-plan 0-1이 요구하는 것은 **HTTP 경계**다. 이 배치는 앞 두 단위와 달리 **바이트가 오가는
케이스**가 있어 축의 무게가 다르다.

| 표기 | 도구 | 이 배치에서 쓰는 자리 |
|---|---|---|
| **C-M** | `@WebMvcTest` + MockMvc | 디스패처를 **통과해** 만들어지는 JSON 응답 — 성공 응답 모양, 어드바이스가 만든 422, 헤더 |
| **C-R** | `@SpringBootTest(RANDOM_PORT)` + 실제 소켓 | **업로드·다운로드 전부**와 필터가 만드는 401 |
| **C-I** | 위 둘 중 하나 **+ Testcontainers PostgreSQL 픽스처** | 소유권 404·409 충돌·상태별 응답 모양·검수 저장의 보존 |
| **C-P** | 구성이 다른 컨텍스트(속성 주입) | DC-19(큐 미배선 503) |

### 5-1. 업로드·다운로드를 C-R로 두는 것은 양보할 수 없다

**앞 두 단위에서 C-R은 「필터가 만드는 응답」 하나였다. 여기서 이유가 하나 더 붙는다.**

- **업로드 상한(DC-12·DC-13)은 서블릿 컨테이너가 먼저 거절할 수 있다.** multipart 크기 제한은
  핸들러에 닿기 전 컨테이너 층에서 걸리는 구성이 흔하고, 그때 나가는 응답은 §5.2가 실측한
  「필터에 닿는가」 문제와 **같은 계열**이다. MockMvc는 컨테이너를 띄우지 않아 이 경로를
  재현하지 못하면서 **통과한다** — 게이트 21에서 겪은 「측정한 것처럼 보이는 통과」와 같은
  형태다.
- **multipart 파싱 자체(DC-4~DC-7)가 컨테이너 기능이다.** MockMvc의 multipart 요청은 손으로
  조립한 파트 목록이라 **경계 문자열·`Content-Type` 대소문자 같은 실제 실패 원인이 재현되지
  않는다.** DC-5가 재려는 것이 바로 그 대소문자다.
- **`Content-Disposition`의 헤더 값 인코딩(CE-3~CE-6)은 컨테이너가 쓴다.** 헤더 값은 latin-1만
  담을 수 있고, 그 제약을 어기는 값은 **컨테이너가 쓰는 시점에** 드러난다. CE-5가 겨냥하는
  보이지 않는 제어문자는 특히 그렇다 — 목 응답 객체는 무엇이든 담아 준다.

**반대로 소유권 404(DC-16·DD-2·CR-7·CU-8·CE-13)는 C-M으로 충분하다.** 그 응답은 컨트롤러 또는
어드바이스가 만들고 디스패처를 통과한다. 전건을 RANDOM_PORT로 올리면 실행 시간만 늘고 잡는
것은 같다 — workspaces §5의 판정 그대로다.

---

## 6. 같은 변경 단위 요건 (G-1 형태)

**나중에 넣으면 그사이 회귀가 지나간다.**

| 단언 | 들어가야 하는 커밋 | 근거 |
|---|---|---|
| DL-1·CR-1·CU-1·CE-2의 사적 헤더 **개별** 단언(X-D1 하한선 **남은 4곳**) | 해당 엔드포인트를 **구현하는 그 커밋** | 리더 판정 부수 결정 1. **이 4곳으로 하한선 10곳이 닫힌다** — 하나라도 미루면 목록이 다시 「일부만 고정된 상태」로 돌아간다 |
| DC-9~DC-11 · CU-4~CU-7 (X-F1·X-F2·X-F9 + 비대칭 대비) | **두 DTO·서비스를 구현하는 그 커밋** | F3 강제자 축에서 `text`·`edited_text`가 마감되는 커밋이다. 나중에 넣으면 그사이 배열 `detail`이 지나간다 |
| **DL-5의 `detail` 타입 단언** | **`spring-boot-starter-validation`이 들어오는 그 커밋** | 그 의존성이 F3 음성 대조의 1차 방벽을 없앤다(§3-1). 방벽이 사라지는 커밋과 대체 강제자가 서는 커밋이 갈리면 그 사이가 무방비다 |
| DC-16·DC-17·DD-2·DD-3·CR-7·CR-8·CU-8·CE-13 (X-B1·X-B2) | **소유권 조회를 구현하는 그 커밋** | 403이 새는 것은 스타일이 아니라 보안 계약 위반이다(계획 §5 Phase 7 즉시 중단 기준) |
| DC-21·DL-11·CE-14 (X-A3) | **컨트롤러가 인증 필터 뒤에 처음 붙는 커밋** | 순서가 뒤집힌 상태로 한 커밋이라도 지나가면 그동안 API 표면이 토큰 없이 탐색된다 |
| **CE-5·CE-6 (L348 F-3·F-4)** | **내보내기 엔드포인트를 구현하는 그 커밋** | 계약 조항이 2026-08-19에 생겼고(§7-1 ⑵) 마감이 이 Phase다. 조항만 있고 단언이 없는 상태가 이어지면 F-4가 「집합이 계약에 있는데도 무단언」으로 모양만 바꿔 남는다 |
| DC-2 (X-D4) · CE-3·CE-4 (X-D5) | 각 엔드포인트를 **구현하는 그 커밋** | 두 헤더는 CORS 노출 목록의 두 원소이고, 빠져도 **상태 코드는 정상**이라 다른 단언에 걸리지 않는다 |
| §4의 파서 P-23~P-37 + N-20~N-30 | **이 단위의 첫 계약 테스트 커밋** | 성질 5 — 넓힌 만큼 음성 대조도 함께 |

---

## 7. 판정과 미결 — 조항을 넘지 않는다

**테스트가 계약보다 넓어지면 테스트가 계약이 된다.**

### 7-1. 인라인 헤더 2건의 처분 (리더 지시 1)

**전제 정정을 받아들인다.** `03_kotlin-implementer_phase4-preconditions.md` §2.1이 실측으로
「오늘 계약의 인라인 헤더는 0건이 아니라 2건」임을 확인했고, 그 사실이 이 판정의 출발점이다.

#### ⑴ 컴포넌트화할 것인가 — **인라인을 유지한다** (변경 없음)

**근거를 G1~G4에서 지목할 수 없다.** 그것이 이 판정의 전부다.

| 근거 후보 | 성립하는가 |
|---|---|
| **G1** 계약이 사실과 다르다 | 아니다 — 두 헤더의 선언은 실제로 나가는 것을 정확히 적고 있다 |
| **G2** 요구사항이 요구한다 | 아니다 — `$ref`를 요구하는 요구사항이 없다. OpenAPI도 인라인 헤더를 정상 문법으로 허용한다 |
| **G3** 정책 결정이 바뀌었다 | 아니다 |
| **G4** 지킬 수 없거나 지켜지지 않는 형태다 | **아니다 — 그리고 이것이 가장 그럴듯해 보였던 후보다.** X24-5가 드러낸 것은 「인라인 갈래를 파서가 조용히 무시한다」였고 그것은 **조항의 결함이 아니라 강제 수단의 도달 0**이었다. `44eec3f`가 파서를 fail-closed로 만들어 그 자리를 닫았다 — **탐지를 넓혀 해결된 자리를 구조 변경으로 되돌리지 않는다** |

**바꾸면 잃는 것이 있다.** 오늘 `components/headers`의 세 항목은 **전부 `schema.const`를 갖고,
`ContractHeaderDeclarationTest`가 「컴포넌트 갈래는 전부 `const`」를 단언한다.** 두 헤더의 값은
문서마다 계산되므로 `const`를 가질 수 없고, 컴포넌트로 옮기려면 **그 단언을 약화시켜야 한다.**
탐지형 장치를 느슨하게 만드는 방향이므로 채택하지 않는다.

**얻는 것도 없다.** 컴포넌트는 재사용을 위한 자리인데(캐시 금지 헤더 둘은 각각 10곳에서
`$ref`된다) 이 둘은 **각각 한 응답에서만** 쓰인다. 줄일 중복이 없다.

**대신 마감의 강제자는 그대로 둔다.** `inlineHeaderNames()`를 두 이름으로 고정한 테스트가
셋째 인라인 헤더가 들어오는 커밋을 실패시킨다. **그때 정할 것이 무엇인지도 이미 적혀 있다** —
값이 고정이면 컴포넌트로 옮겨 `const`를 주고, 계산되면 형식을 재는 단언을 함께 넣는다.
이 판정은 그 규칙의 **첫 적용 사례**이며, 두 헤더는 둘째 갈래에 속한다.

> **바꾸지 않기로 하는 것도 판정이다** — 기록하지 않으면 같은 제안이 다시 올라온다.
> 계약 파일 `:1231-1236`에 이 판정을 한 문단으로 남겼다.

#### ⑵ 형식 검증 조항 — 하나는 **조항 없이** 서고, 하나는 **조항을 더했다**

두 헤더의 사정이 다르다. 같은 처방을 두 자리에 바르지 않는다.

| 헤더 | 형식을 계약에서 읽을 수 있는가 | 처분 |
|---|---|---|
| `Location` | **있다 — 새 조항이 필요 없다.** 값은 `paths`의 경로 템플릿(P-15가 이미 읽는다)에 응답 본문의 식별자를 끼운 것이고, 그 대응은 `DocumentCreatedResponse.properties.conversion_id.description`이 이미 선언한다 | **변경 없음.** DC-2가 두 노드를 읽어 기대값을 **조립한다** — 형식 문자열이 계약에도 코드에도 두 벌로 생기지 않는다 |
| `Content-Disposition` (구조) | **있다 — 새 조항이 필요 없다.** `schema.examples` 첫 원소가 두 파라미터를 모두 담은 완전한 값이다 | **변경 없음.** CE-3이 예시를 **파싱해** 구조를 얻는다. `examples`를 오라클로 쓰는 것은 P-17(경로 인라인 `detail` 예시)이 이미 선 갈래다 |
| `Content-Disposition` (파일명 문자 집합) | **없었다 — 조항을 더했다** | **`x-filename-charset` 신설**(`:1238-1271`, 근거 **G2**) |

**셋째 행이 이 배치의 유일한 계약 변경이다.** 근거·비파괴 판정·통보는 changelog에 있고,
여기에는 **왜 이 자리가 계약인지**만 적는다.

- **요구는 실재하고 지목할 수 있다** — 계획 §5 Phase 2의 종료 조건 「내보내기 파일명·
  `Content-Disposition` 생성 포팅」(원장 L348)이다. 그 요구의 일부가 **어느 단언에도 걸리지
  않는다**는 것이 게이트 14 F-3·F-4와 게이트 16 후속 R-1이었고, **마감이 「Phase 4 착수 전」**
  이었다. 지금이 그 시점이다.
- **없던 것은 성질이 아니라 오라클이다.** 구현은 이미 그 성질을 만족한다. 계약에 기계가독
  집합이 없으면 경계 테스트가 집합을 **코드에 복제**하고, 그러면 계약과 구현이 갈려도 테스트는
  자기 사본과 대조해 초록이다 — X-J2가 금지하는 형태이며 F-4를 **모양만 바꿔** 남긴다.
- **계약이 정하는 것은 나간 바이트다.** 파일명이 헤더 값으로 나가고, 그 값을 디코딩한 이름이
  사용자 파일 시스템에 닿는다. 어느 층에서 걷어내는지는 구현 판단이다.
- **넓히지 않았다.** 조항은 이미 실재하는 구현의 성질을 그대로 적었을 뿐이고, 형식·상태 코드·
  본문은 하나도 건드리지 않았다. React 런타임 영향 0(그쪽은 확장 파라미터를 **읽기만** 한다).
- **이것으로 L348을 닫지 않는다.** 그 판정은 리더의 것이고, F-3·F-4의 **parity fixture 몫**은
  그대로 남는다(O-19). 이 배치가 한 것은 **경계에서 잴 수 있게 만든 것**까지다.

### 7-2. `x-open-asymmetry` — 판정 근거와 선택지 (리더 지시 4)

**계약 위치:** `x-request-field-constraints.x-open-asymmetry` `:431-442` ·
관련 필드 `:415-419`(`text`) · `:420-424`(`edited_text`) · 스키마 표식 `:1837` · `:1934`.
**확정 Phase는 4다. 이 절은 판정을 내리지 않는다 — 근거와 선택지를 갖춘다.**

#### 무엇이 비대칭인가

두 필드가 같은 상한을 쓰는데 **재는 대상이 다르다.** 하나는 원시 값을, 다른 하나는 제어문자를
걷어낸 값을 잰다. 계약은 이 사실을 있는 그대로 적고 미결로 표시했다.

#### 이 배치가 새로 확인한 사실 — **계약이 든 근거의 실체가 달라졌다**

계약이 정규화를 먼저 하는 **이유**로 적은 것은 「워드 컨테이너(XML)가 담지 못하는 문자를
저장해 두면 내보내기 시점에 터져 사용자가 원인을 알 수 없는 500을 받는다」였다.

**Kotlin 재구축에서는 그 귀결이 재현되지 않는다**(2026-08-19 실측) —
`backend-kotlin/core/.../easyread/Export.kt`의 내보내기 경로가 제목과 본문 **양쪽에서**
제어문자를 걷어낸 뒤 산출물을 만든다. 즉 내보내기는 어떤 입력에도 그 이유로는 터지지 않는다.

**이것은 「비대칭을 유지해도 된다」는 뜻이 아니다.** 근거가 옮겨간다는 뜻이다 —
남는 위험은 **500**이 아니라 ⑴ 저장·응답에 보이지 않는 제어문자가 남는 것,
⑵ 사용자가 센 글자 수와 서버가 센 글자 수가 갈리는 것이다. 판정은 이 새 근거 위에서 해야 한다.

#### 선택지

| 안 | 내용 | 바뀌는 와이어 동작 | React 런타임 영향 | 걸리는 검증 |
|---|---|---|---|---|
| **(가)** | **현행 유지** — 두 축을 그대로 둔다 | 없음 | 없음 | DC-11·CU-6이 서로의 대비로 그대로 선다 |
| **(나)** | `text`도 정규화 후로 통일 | **있다** — 원시로는 상한을 넘고 정규화 후에는 넘지 않는 붙여넣기가 422에서 202로 뒤집힌다(사용자 가시) | 없음(그쪽은 본문을 그대로 보낸다) | **DC-11의 기대값이 뒤집힌다.** 계약 `:418`·`:1837`·이 명세를 같은 단위로 고쳐야 한다 |
| **(다)** | `edited_text`를 원시로 통일 | 있다 | 없음 | **사실상 배제** — 「제어문자만 담긴 수정본을 빈 값과 같게 다룬다」는 별도 조항(`:1135-1137`)과 정면으로 충돌한다. 그 조항까지 함께 뒤집는 변경이 된다 |

#### 계약 소유자의 권고와 그 한계

**권고는 (나)다.** 계약이 한 파이프라인의 두 입구에 다른 자를 대고 있고, 그 다름을 정당화하던
근거가 위에서 옮겨갔기 때문이다. 사용자에게 설명할 수 없는 차이이기도 하다 — 같은 글을
붙여넣기로 넣을 때와 검수 화면에서 고칠 때 상한이 다르게 동작한다.

**그러나 계약 소유자가 단독으로 정하지 않는다.** 이유 둘이다.

1. **G 근거를 단독으로 세울 수 없다.** (나)를 G2로 세우려면 「저장되는 본문에 제어문자를
   남기지 않는다」는 요구를 지목해야 하는데, 오늘 요구사항 문서에 그 문장이 없다. G1도
   아니다 — 계약은 현재 동작을 **정확히** 적고 있다. 남는 것은 **G3(정책 결정)**이고,
   **계약 소유자는 정책을 만들지 않는다.**
2. **이미 리더에게 올라가 있는 같은 갈래의 질문이 있다.** 게이트 15 **X13**(`password`의 측정
   축)이 판정 대기 중이고, 그것도 「어느 자를 대는가」의 질문이다. 두 자리를 따로 정하면
   측정 축 규칙이 필드마다 갈린다. **함께 판정하기를 요청한다.**

**확정 전까지는 현행 조항이 기준이다.** 이 명세는 (가)를 전제로 DC-11·CU-6을 세웠고,
그것이 **판정을 앞질러 확정하지 않는 유일한 방법**이다 — 검증을 (나)로 미리 써 두면 그
테스트가 계약이 된다. 판정이 (나)로 나면 계약 `:418`·`:1837`, DC-11, N-25, test-plan §6을
**같은 변경 단위로** 고친다.

### 7-3. 관찰

| ID | 관찰 | 판정 |
|---|---|---|
| **O-14** | **`/health`의 `description`이 전역 부착 조항과 모순된다** — 「캐시 금지 헤더도 붙지 않는다(실측 확인 2026-08-12)」(`:1491`)가 남아 있는데, 같은 날 리더 판정으로 전역 부착이 요구가 됐고 `x-global-response-headers.x-superseded`가 그 제외 목록을 **명시적으로 무효화**했다. 구현·테스트는 이미 붙이는 쪽이다(`HealthContractTest`) | **이 배치에서 고치지 않고 리더에게 올린다.** G1이 명백하고 한 문장이지만 **#14는 이 단위 밖**이며, 무관한 엔드포인트를 Phase 4 계약 개정에 끼워 넣으면 그 개정의 blast radius를 읽을 수 없게 된다. 오독 위험은 낮다(정본 절이 이미 무효를 선언했고 테스트가 반대를 강제한다). **다음 문서 커밋 또는 리더가 지정하는 단위에서 한 줄로 처리할 것을 제안한다** |
| **O-15** | 검수 저장이 **PUT**인데 작업 공간 이름 변경은 **PATCH**다. 「부분 갱신이니 PATCH가 맞지 않나」가 자연스럽게 떠오르는 자리 | **바꾸지 않는다.** ⑴ G 근거가 없다 — 계약은 사실을 정확히 적고 있고 요구사항도 정책도 이것을 말하지 않는다. ⑵ **React 런타임이 이 메서드에 의존한다**(`frontend/src/api/client.ts`의 검수 저장 호출) — `x-change-policy.escalate_to_leader` ②에 해당해 소유자 단독 범위 밖이다. ⑶ 두 메서드가 다른 데는 이유가 있다: 검수 저장은 자원 전체를 제출하는 형태다. **CU-10이 이 판정을 강제한다**(계약 밖 메서드를 서비스하지 않는다) |
| **O-16** | **목록 응답에 총 개수 필드가 없다**(#5) — 화면이 다음 쪽 유무만 쓴다 | **바꾸지 않는다.** 2026-08-12 재검토에서 이미 판정된 항목이다(요구하는 화면이 없는데 비용만 확정된다). DL-1의 「정확히」 단언이 **추가되는 것도** 잡는다 — 없는 것이 맞다는 것을 지키는 장치다 |
| **O-17** | **X-J1(OQ-2)·X-J3(OQ-4)의 확정 Phase가 4인데 조항이 없다.** X-J1은 내보내기에서 `withheld`·`ambiguous`가 비어 있지 않을 때의 처분이고(오늘 계약의 409는 유실 자리표시자 한 갈래뿐이다), X-J3은 마스킹 본문 채널의 계약화 여부다 | **조항 없이 케이스를 쓰지 않는다 — 리더에게 올린다.** 둘은 **함께 판정해야 한다**(test-plan §3-1의 판정 그대로: 탈출 표기를 계약 어휘로 올리면 X-J1이 막으려는 유입 경로 하나가 준다). **X-J1은 사용자에게 보이는 동작 변경**(내보내기가 막힌다)이라 소유자 단독 범위 밖이다. **판정이 늦어지면 이 Phase의 게이트를 닫을 때 「조항이 확정됐는가」를 먼저 물어야 한다** — 미확정인 채로 닫으면 그 조항은 영영 검증되지 않는다 |
| **O-18** | **X-C9 / E-4의 남은 5종**(컨테이너 생성 응답의 본문 모양)이 여전히 미측정 | **이 단위 밖 — 열린 채로 이월한다.** 대상이 늘지 않았다. 「충족」으로도 「위반」으로도 적지 않는다 |
| **O-19** | **F-3·F-4의 parity fixture 몫**은 이 명세가 닫지 않는다 — 금지 파일명 집합이 parity 산출물 검사에 도달하지 않는 문제(게이트 16 후속 R-1)는 손열거가 그 레인 스크립트 안에 있다 | **사실을 등재하고 그 레인으로 넘긴다.** CE-5가 여는 것은 **HTTP 경계 축**이다. 「경계에서 잰다」와 「fixture가 그 니들을 담는다」는 다른 도달이며, 하나로 다른 하나를 닫았다고 읽으면 안 된다 |
| **O-20** | **CORS 노출 헤더 두 원소가 이 배치에서 처음 실물로 나간다.** 서버가 노출을 선언하는 것과 브라우저가 읽는 것은 다른 문제이고, 후자는 C 계층으로 증명되지 않는다 | **서버 축은 §3에서 확인하고 브라우저 축(X-H5)은 Phase 6으로 이월한다.** 이 배치에서 「노출 헤더가 검증됐다」로 읽히면 안 된다 — 두 헤더가 실재하게 된 지금이 그 오독이 가장 쉬운 시점이다 |

---

## 8. 통보

| 대상 | 내용 |
|---|---|
| `kotlin-implementer` | §2 케이스 표·§4 파서 추가 노드(P-23~P-37)·§5 계층·§6 커밋 단위가 이 배치에서 계약이 요구하는 검증의 **전부**다. **기대값을 코드에 적지 마라.** ⑴ **`text`·`edited_text`에 `@Size`/`@NotBlank` 금지**는 그대로다(F3 · 다섯 필드 중 남은 둘). ⑵ **반대로 `limit`/`offset`은 Bean Validation이다**(지침 3) — 두 축이 서로의 대비 사례이고 **같은 커밋에 함께** 들어간다(§3-1·§6). ⑶ `spring-boot-starter-validation`이 들어오는 순간 **F3 음성 대조의 1차 방벽이 사라진다** — `handleHandlerMethodValidationException` 명시 처리(지침 8)와 DL-5의 `detail` **타입** 단언이 같은 커밋에 있어야 한다. ⑷ **업로드·다운로드 케이스를 MockMvc로 쓰지 마라**(§5-1) — 컨테이너가 만드는 응답과 헤더 값 인코딩은 목으로 재현되지 않으면서 **통과한다**. ⑸ **계약에 조항 하나가 늘었다** — `x-filename-charset`(§7-1 ⑵). 내보내기 파일명 정제는 그 집합을 **계약에서 읽어** 재고, 구현 상수와 계약이 갈리면 CE-5가 빨강이다. ⑹ **`x-open-asymmetry`는 현행 조항이 기준이다**(§7-2) — 두 필드의 측정 축을 **한 곳으로 합치지 마라.** 합치면 DC-11이 빨강이고, 그것이 그 케이스의 일이다 |
| `parity-verifier` | 이 범위에서 **반드시 같아야 하는 것**: 응답 필드 이름 집합(`DocumentCreatedResponse`·`DocumentListResponse`·`DocumentListItem`·`ConversionResponse`·`MaskedItemResponse`), `detail`의 **타입**(문자열/배열 — 특히 `limit`/`offset`은 **배열**이고 본문 필드는 **문자열**이다), 계약이 예시로 **명시한** `detail` 문구 전문, 상태 enum·형식 enum·마스킹 범주 enum·자리표시자 pattern, 미디어 타입 문자열(문자셋 표기 포함), `Location`과 `Content-Disposition`의 **형식**. **새로 범위에 드는 것**: 내보내기 파일명의 **금지 문자 집합**(계약 `x-filename-charset`) — 종전에는 계약에 없어 판정 오라클이 없던 자리다. **오라클이 없는 자리**: `withheld`·`ambiguous`의 처분(O-17) — 계약이 말하지 않으므로 불일치로 올릴 근거가 없다. **의도적으로 다른 조항**: 오류 응답·204의 사적 헤더 — 불일치로 올리지 않는다 |
| `privacy-gate` | 이 배치가 **가려졌던 실제 개인정보를 처음 응답에 싣는다**(CR-5의 `MaskedItemResponse.original`)와 **자리표시자를 원문으로 복원한 산출물을 처음 내보낸다**(CE-12). 계약 축의 대응 단언은 CR-1·CU-1·CE-2의 사적 헤더 **개별** 단언(X-D1 하한선의 남은 4곳 중 셋)이다. **CE-5는 개인정보가 아니라 파일명 안전성**을 재지만, 제목이 사용자 입력이라는 점에서 같은 입력 경로를 공유한다 |
| 리더 | ⑴ **§7-2 `x-open-asymmetry` — 판정 요청.** 선택지 셋과 근거, 각 안의 와이어 변화·React 영향·걸리는 검증을 갖췄다. **게이트 15 X13과 같은 갈래이므로 함께 판정하기를 요청한다.** 계약 소유자는 현행 조항을 유지한 채 검증만 채웠다 ⑵ **O-17 — X-J1·X-J3의 조항 미확정.** 확정 Phase가 4인데 조항이 없어 케이스를 쓸 수 없다. X-J1은 사용자 가시 동작 변경이라 소유자 단독 범위 밖이다 ⑶ **§7-1 ⑵ 계약 개정 1건 보고** — `x-filename-charset` 신설(G2, 비파괴, React 영향 0). L348의 오라클을 세운 것이고 **L348 판정 자체는 그대로 리더의 것이다**. 되돌리려면 YAML 한 절 삭제 + CE-5·CE-6·N-20 철회다 ⑷ **O-14 — `/health` description의 자기모순.** G1이 명백한 한 문장이나 이 단위 밖이라 손대지 않았다. 처리 단위를 지정해 주기를 요청한다 ⑸ **O-20** — 노출 헤더 두 원소가 실물이 됐으나 브라우저 축은 Phase 6이다. 이 배치를 근거로 X-H4/X-H5를 닫지 않기를 |

---

## 9. 2단계에서 확인할 것 (구현 커밋 후)

산출물은 `04_contract-keeper_documents-verification.md`.

1. **파서 실재 · 하드코딩 0** — P-23~P-37이 실제로 파싱되고 **단언에 쓰이는지**, §4-2의 성질 7
   (읽은 집합으로 케이스를 유도한다)이 서는지.
2. **X-D1 하한선 10곳이 닫혔는지** — 남은 4곳(DL-1·CR-1·CU-1·CE-2)의 실행 소스 확인.
3. **X-F9의 `edited_text` 케이스** — F3 다섯 필드의 **마지막** 마감 판정.
4. **지침 3·8의 첫 실행 경로** — DL-5가 `detail` **타입**까지 재는지, F3 1차 방벽 소멸과
   대체 강제자가 **같은 커밋**인지.
5. **L348 CE-5·CE-6** — 금지 집합을 계약에서 읽어 케이스를 **유도**하는지(손열거가 아닌지).
6. **음성 대조 N-20~N-30 실측** — 일회용 worktree, 규칙 5 복원, **N-20·N-25의 「깨지지 않아야
   하는 짝」 확인**, 과잉 결합 확인, 해시 대조 기록.
7. **업로드·다운로드가 실제로 C-R인지** — MockMvc로 내려온 케이스가 있으면 §5-1의 근거로 되돌린다.
8. **실행 상태 열 갱신** — test-plan §5의 2026-08-19 표에 Phase 4 행을 더한다. **값은 옮겨 적지 않는다.**
